# Cycle 68 — Step 7 Implementation Plan

**Date:** 2026-05-08
**Cycle:** 68 (Tier 2, 15 ACs — 9 carry-overs + 6 NEW)
**Discipline:** TDD red→green; ruff format BEFORE test writes per cycle-23 L1
**Owner mix:** MiMo Coding subagent for non-security src; Opus primary for AC01 Popen + AC03 yaml.safe_load per `project_cycle61_mimo_failure`
**Plan author:** primary Opus (mimo Step 07 dispatch returned a 37-line skeleton; resolved inline per cycle-21 L1 with comprehensive per-task body below)

---

## Executive summary

**17 implementation tasks → 18 commits** ordered per FW-6 (workflow/deps before security-class src) and FW-10 (AC15 test BEFORE AC10 delete for revert detection).

Commit DAG:
1. **AC15a** test_cycle68_httpx_pin_drift.py NEW
2. **AC15b** test_cycle68_backlog_cleanup_lockin.py NEW
3. **AC10** BACKLOG.md cleanup (deletes locked-in by step 1-2)
4. **AC09** pyproject.toml httpx ceiling
5. **AC07** evolve/analyzer.py 1 site (line 127 only — pages-supplying calls at 28+358 stay)
6. **AC08a** graph/export.py 1 site
7. **AC08b** mcp/browse.py 1 site
8. **AC08c** query/engine.py inspect-only (pages-supplying call at 408 stays per cycle-64 R1-F5)
9. **AC02** config.py MAX_CLI_STDERR_BYTES
10. **AC11** test_cycle68_cli_backend_popen.py NEW
11. **AC01** cli_backend.py Popen refactor (Opus primary)
12. **AC12** test_cycle68_lint_yaml.py NEW
13. **AC03** lint/_lint_yaml.py NEW (Opus primary)
14. **AC04** lint/checks/duplicate_slug.py allowlist overlay
15. **AC13** test_cycle68_audit_docstrings.py NEW
16. **AC05** scripts/audit_docstrings.py NEW
17. **AC06** .github/workflows/ci.yml docstring-audit step
18. **AC14** test_cycle68_graph_cache_caller_migrations.py NEW

Total: 6 src files modified + 2 NEW src files + 5 NEW test files + 1 BACKLOG cleanup + 1 ci.yml step + 1 config constant.

---

## Forward-looking risks (FW-1 through FW-10, verbatim per Step 5)

**FW-1:** AC01 Popen MUST split stdin write from stdout/stderr reads — never `proc.communicate(input=...)` with daemon readers.

**FW-2:** AC03 MUST use `yaml.safe_load` ONLY; never `yaml.load` (RCE class T7).

**FW-3:** N/A (cycle-67-only — `sk-ant-dummy` grep already shipped).

**FW-4:** AC05 audit_docstrings — generators (with `yield`) that contain `raise` need `Raises:` section.

**FW-5:** N/A (cycle-67-only — MODEL_TIERS already shipped).

**FW-6:** Step-7 ordering: workflow + dep changes commit BEFORE security-class src per cycle-67 R1-C10.

**FW-7:** AC14 AST guard MUST use the `pages_supplied` predicate (`kw.value.value is None` literal-check), not blanket-forbid. Pages-supplying call sites explicitly allowed.

**FW-8:** AC09 pin test MUST add `pip install --dry-run` resolver-compatibility check (or equivalent constraint-satisfaction probe).

**FW-9:** AC10 BACKLOG cleanup MUST defer the 3 cycle-68 self-reference entries (cycle-67 carry-over markers AC03/AC07/AC12 at BACKLOG lines 55-59) to Step 17. Do NOT delete them at AC10.

**FW-10:** AC15 test commits BEFORE AC10 delete commit. AC10 revert alone leaves AC15 RED → revert-detection works.

---

## Per-task plan

### TASK 01 — AC15a: tests/test_cycle68_httpx_pin_drift.py NEW
- **Owner:** primary
- **Files:** `tests/test_cycle68_httpx_pin_drift.py` (new)
- **Sub-conditions:** `C-AC09-resolver-compat` (FW-8), `C-AC15-tomllib-parse` (R1-F7)
- **Test functions:**
  - `test_pyproject_httpx_pin_has_explicit_ceiling()` — parses `pyproject.toml` via `tomllib`; finds httpx in `[project].dependencies`; asserts constraint string `"httpx>=0.28,<0.29"` (exact).
  - `test_pyproject_httpx_resolver_compat_dry_run()` — runs `pip install --dry-run httpx>=0.28,<0.29` (or equivalent) in subprocess; asserts exit code 0 (resolver finds satisfiable version).
- **Commit:** `test(cycle-68): AC15a httpx pin drift regression`

### TASK 02 — AC15b: tests/test_cycle68_backlog_cleanup_lockin.py NEW
- **Owner:** primary
- **Files:** `tests/test_cycle68_backlog_cleanup_lockin.py` (new)
- **Sub-conditions:** `C-AC10-delete-list` (R1-F5)
- **Test functions:**
  - `test_backlog_does_not_contain_shipped_phase_4_5_high_entries()` — parses BACKLOG.md line-by-line; asserts these substrings ABSENT: `"GitPython>=3.1.47"` (unpinned form), `"_autouse_kb_path_sandbox no-drop guard"`, `"hardcoded lru_cache clear list"`, `"_DEFAULT_MODEL_TIERS dual mechanism"`, `"_check_no_secrets_on_argv self-DoS"`, `"graph/cache.py 6th-caller drift"`, `"docs/reference/ lacks an INDEX.md"`. Asserts cycle-68 self-reference entries (AC03 cli_backend.py:241, AC07 wiki/_lint.yml, AC12 audit_docstrings.py) ARE STILL PRESENT (Step 17 deletes post-merge per FW-9).
- **Commit:** `test(cycle-68): AC15b BACKLOG cleanup lock-in regression`

### TASK 03 — AC10: BACKLOG.md cleanup
- **Owner:** primary
- **Files:** `BACKLOG.md`
- **Sub-conditions:** `C-AC10-defer-self-refs` (FW-9), `C-AC10-delete-list` (R1-F5)
- **Pre-edit checks:** re-confirm each deletion target IS present; re-confirm cycle-68 self-reference entries (lines 55-59) ARE PRESERVED.
- **Edits:** delete entries listed in AC10 of `2026-05-07-cycle-68-requirements.md`. Do NOT delete the 3 cycle-68 self-reference entries (FW-9).
- **Verification:** `python -m pytest tests/test_cycle68_backlog_cleanup_lockin.py -xvs` → green.
- **Commit:** `docs(cycle-68): AC10 BACKLOG cleanup — delete stale Phase 4.5/6 R2 entries (defer cycle-68 self-refs to Step 17 per FW-9)`

### TASK 04 — AC09: pyproject.toml httpx ceiling
- **Owner:** primary (one-line edit)
- **Files:** `pyproject.toml`
- **Sub-conditions:** `C-AC09-pin-tightness`, `C-AC09-resolver-compat` (FW-8)
- **Edit:** line 29 `"httpx>=0.27"` → `"httpx>=0.28,<0.29"`.
- **Verification:** `python -m pytest tests/test_cycle68_httpx_pin_drift.py -xvs` → green; runtime assertion at `lint/fetcher.py:51-59` must still pass on installed `httpx==0.28.1`.
- **Commit:** `fix(deps): AC09 tighten httpx pin to >=0.28,<0.29 (matches lint/fetcher.py:51 runtime guard)`

### TASK 05 — AC07: evolve/analyzer.py migrate line 127
- **Owner:** primary
- **Files:** `src/kb/evolve/analyzer.py`
- **Sub-conditions:** `C-AC07-sites` (FW-7), `C-AC07-attribute-lookup` (cycle-18 L1)
- **Edits:**
  - Add `import kb.graph.cache` (module-form per cycle-18 L1).
  - Line 28 `_orphan_via_graph_resolver`: KEEP `build_graph(wiki_dir, pages=pages)` — pages-supplying caller bypasses cache by design.
  - Line 127: change `graph = build_graph(wiki_dir)` → `graph = kb.graph.cache.get_graph(wiki_dir)`.
  - Line 358 `compute_stats_report`: KEEP `build_graph(wiki_dir, pages=pages_dicts)` — pages-supplying.
  - Keep `from kb.graph.builder import build_graph, graph_stats` (still needed).
- **Verification:** `python -m pytest tests/test_evolve.py -xvs` → green.
- **Commit:** `refactor(cycle-68): AC07 migrate evolve/analyzer.py:127 pages-None build_graph → kb.graph.cache.get_graph`

### TASK 06 — AC08a: graph/export.py 1 site
- **Owner:** primary
- **Files:** `src/kb/graph/export.py`
- **Sub-conditions:** `C-AC07-sites` (FW-7), `C-AC07-attribute-lookup`
- **Edits:** add `import kb.graph.cache`; line 83 change `build_graph(wiki_dir)` → `kb.graph.cache.get_graph(wiki_dir)`. If `from kb.graph.builder import build_graph` becomes unused, remove via grep verification.
- **Verification:** `python -m pytest tests/test_graph.py -xvs` → green.
- **Commit:** `refactor(cycle-68): AC08 migrate graph/export.py:83 to kb.graph.cache.get_graph`

### TASK 07 — AC08b: mcp/browse.py 1 site
- **Owner:** primary
- **Files:** `src/kb/mcp/browse.py`
- **Sub-conditions:** `C-AC07-sites` (FW-7), `C-AC07-attribute-lookup`
- **Edits:** add `import kb.graph.cache`; line 345 change `build_graph(wiki_dir=wiki_path)` → `kb.graph.cache.get_graph(wiki_path)`.
- **Verification:** `python -m pytest tests/test_mcp_core.py tests/test_mcp_browse.py -xvs` → green.
- **Commit:** `refactor(cycle-68): AC08 migrate mcp/browse.py:345 to kb.graph.cache.get_graph`

### TASK 08 — AC08c: query/engine.py inspect-only
- **Owner:** primary (verification-only)
- **Files:** none (read query/engine.py:408; pages-supplying call is correct per cycle-64 R1-F5)
- **Action:** confirm line 408 is `build_graph(wiki_dir, pages=preloaded_pages)`. Optionally add a comment explaining cache-bypass rationale. No commit unless comment added.

### TASK 09 — AC02: config.py MAX_CLI_STDERR_BYTES constant
- **Owner:** primary
- **Files:** `src/kb/config.py`
- **Sub-conditions:** `C-AC03-stderr` from cycle-67 design
- **Edit:** insert after line 410:
  ```python
  # Hard cap on stderr bytes read from CLI subprocess (cycle-67 R1-F6).
  MAX_CLI_STDERR_BYTES: int = 64 * 1024
  ```
- **Verification:** `python -c "from kb.config import MAX_CLI_STDERR_BYTES; assert MAX_CLI_STDERR_BYTES == 65536"`.
- **Commit:** `feat(config): AC02 add MAX_CLI_STDERR_BYTES constant for cli_backend stderr cap`

### TASK 10 — AC11: tests/test_cycle68_cli_backend_popen.py NEW
- **Owner:** primary
- **Files:** `tests/test_cycle68_cli_backend_popen.py` (new)
- **Sub-conditions:** `C-AC03-stdin`, `C-AC03-platform`, `C-AC03-stderr`, `C-AC03-error-kinds`
- **Test functions:**
  - `test_cli_backend_popen_large_stdin_plus_large_stdout` — backend stub writes `MAX_CLI_STDOUT_BYTES * 3` to stdout; assert returned length ≤ cap; 4 MB stdin; assert no deadlock (FW-1 stdin/reader split).
  - `test_cli_backend_popen_platform_kill_branch` — monkeypatch `sys.platform`; assert wait timeout 2.0s POSIX vs 0.5s Windows.
  - `test_cli_backend_popen_stderr_capped` — backend writes `MAX_CLI_STDERR_BYTES * 4` to stderr; assert truncation at cap.
  - `test_cli_backend_popen_preserves_error_kinds` — three sub-cases: `kind="not_installed"`, `kind="timeout"`, generic exit-code-non-zero.
- **Commit:** `test(cycle-68): AC11 cli_backend Popen refactor regression pins (4 functions)`

### TASK 11 — AC01: cli_backend.py Popen refactor (Opus primary)
- **Owner:** **primary Opus** (security-class src per `project_cycle61_mimo_failure`)
- **Files:** `src/kb/utils/cli_backend.py`
- **Sub-conditions:** `C-AC03-stdin` (FW-1), `C-AC03-platform`, `C-AC03-stderr`, `C-AC03-error-kinds`
- **Edits:**
  - Replace `subprocess.run(...)` (lines 213-220) with `Popen(stdout=PIPE, stderr=PIPE, stdin=PIPE)` + 2 daemon reader threads + separate stdin write thread.
  - Reader threads accumulate in 64 KB chunks; cap stdout at `MAX_CLI_STDOUT_BYTES`, stderr at `MAX_CLI_STDERR_BYTES`.
  - On reaching cap: signal threads stop; `proc.terminate()` + `proc.wait(timeout=2.0 if POSIX else 0.5)`; on still-alive `proc.kill()`.
  - Stdin write SEPARATE thread (NOT `proc.communicate(input=...)`) per FW-1.
  - On timeout: `proc.kill()` + `LLMError(kind="timeout")`.
  - Preserve `LLMError(kind="not_installed")` at unchanged location.
  - Preserve all post-process logic.
- **Verification:** `python -m pytest tests/test_cycle68_cli_backend_popen.py tests/test_cli_backend.py -xvs` → green.
- **Commit:** `refactor(cli_backend): AC01 replace subprocess.run with Popen + chunked stdout/stderr cap + platform-aware kill grace (FW-1 stdin/reader split)`

### TASK 12 — AC12: tests/test_cycle68_lint_yaml.py NEW
- **Owner:** primary (RCE class T7)
- **Files:** `tests/test_cycle68_lint_yaml.py` (new)
- **Sub-conditions:** `C-AC07-safe`, `C-AC07-fallback`, `C-AC07-schema`
- **Test functions:**
  - `test_lint_yaml_rejects_malicious_payload(tmp_kb_env, monkeypatch)` — write `WIKI_DIR/_lint.yml` with `!!python/object/new:os.system [...]`; spy `os.system` (assert NOT invoked); assert returns `{}`.
  - `test_lint_yaml_file_missing_returns_empty(tmp_kb_env)` — no file; returns `{}`.
  - `test_lint_yaml_parse_error_returns_empty(tmp_kb_env, caplog)` — malformed YAML; returns `{}` + warning.
  - `test_lint_yaml_io_permission_returns_empty(tmp_kb_env)` — POSIX skipif; chmod 000; returns `{}` + warning.
  - `test_lint_yaml_schema_mixed_type_warning(tmp_kb_env, caplog)` — `duplicate_slug_allowlist: "not-a-list"`; warning + key falls through.
  - `test_lint_yaml_call_time_read(tmp_kb_env)` — write A; read; rewrite B; read; assert second read returns B (cycle-19 L2 no-cache).
- **Commit:** `test(cycle-68): AC12 lint_yaml loader safety + fallback + call-time read pins`

### TASK 13 — AC03: lint/_lint_yaml.py NEW (Opus primary)
- **Owner:** **primary Opus** (FW-2 RCE class T7)
- **Files:** `src/kb/lint/_lint_yaml.py` (new)
- **Sub-conditions:** `C-AC07-safe` (FW-2), `C-AC07-fallback`, `C-AC07-schema`
- **Implementation:**
  ```python
  """Lazy YAML loader for wiki/_lint.yml allowlist overrides.

  Cycle 68 AC03 — yaml.safe_load ONLY (RCE class T7 mitigation).
  """

  import logging
  from pathlib import Path
  from typing import Any

  import yaml

  from kb.config import WIKI_DIR

  logger = logging.getLogger(__name__)


  def load_lint_config(wiki_dir: Path | None = None) -> dict[str, Any]:
      target = (wiki_dir or WIKI_DIR) / "_lint.yml"
      if not target.exists():
          return {}
      try:
          text = target.read_text(encoding="utf-8")
      except OSError as exc:
          logger.warning("kb.lint._lint_yaml: read error %s: %s", target, exc)
          return {}
      try:
          data = yaml.safe_load(text)  # FW-2: NEVER yaml.load
      except yaml.YAMLError as exc:
          logger.warning("kb.lint._lint_yaml: YAML parse error: %s", exc)
          return {}
      if not isinstance(data, dict):
          logger.warning("kb.lint._lint_yaml: top-level must be mapping; got %s", type(data).__name__)
          return {}
      if "duplicate_slug_allowlist" in data:
          val = data["duplicate_slug_allowlist"]
          if not (isinstance(val, list) and all(isinstance(p, list) and len(p) == 2 for p in val)):
              logger.warning(
                  "kb.lint._lint_yaml: duplicate_slug_allowlist must be list-of-pairs; got %r", val,
              )
              data.pop("duplicate_slug_allowlist", None)
      return data
  ```
- **Verification:** `python -m pytest tests/test_cycle68_lint_yaml.py -xvs` → green.
- **Commit:** `feat(lint): AC03 NEW lint/_lint_yaml.py with yaml.safe_load lazy loader (FW-2 RCE-class T7 mitigation)`

### TASK 14 — AC04: lint/checks/duplicate_slug.py allowlist overlay
- **Owner:** primary
- **Files:** `src/kb/lint/checks/duplicate_slug.py`
- **Sub-conditions:** `C-AC07-fallback`, `C-AC07-schema`
- **Edit:** modify `_is_allowlisted_pair`:
  ```python
  def _is_allowlisted_pair(slug_a: str, slug_b: str) -> bool:
      pair = frozenset((slug_a, slug_b))
      if pair in DUPLICATE_SLUG_ALLOWLIST:
          return True
      from kb.lint._lint_yaml import load_lint_config
      extra = load_lint_config().get("duplicate_slug_allowlist", [])
      for entry in extra:
          if frozenset(entry) == pair:
              return True
      return False
  ```
- **Verification:** `python -m pytest tests/test_lint.py tests/test_cycle68_lint_yaml.py -xvs`.
- **Commit:** `feat(lint): AC04 overlay wiki/_lint.yml on DUPLICATE_SLUG_ALLOWLIST defaults`

### TASK 15 — AC13: tests/test_cycle68_audit_docstrings.py NEW
- **Owner:** primary
- **Files:** `tests/test_cycle68_audit_docstrings.py` (new)
- **Sub-conditions:** `C-AC12-generator` (FW-4)
- **Test functions:**
  - `test_audit_docstrings_normal_func_with_args_returns(tmp_path)` — fixture function with params + return → flag missing `Args:`/`Returns:`.
  - `test_audit_docstrings_func_with_raise_requires_raises_section(tmp_path)` — body has `raise X(...)` → `Raises:` required.
  - `test_audit_docstrings_generator_with_yield_and_raise(tmp_path)` — body has `yield` AND `raise` → `Raises:` required (FW-4).
  - `test_audit_docstrings_warn_only_exit_zero(tmp_path)` — even with violations, `--warn-only` exits 0.
- **Commit:** `test(cycle-68): AC13 audit_docstrings generator+raise + warn-only pins`

### TASK 16 — AC05: scripts/audit_docstrings.py NEW
- **Owner:** primary
- **Files:** `scripts/audit_docstrings.py` (new)
- **Sub-conditions:** `C-AC12-generator` (FW-4)
- **Implementation outline:** argparse `--warn-only` (default); walks `kb.__all__` via `inspect.unwrap`; parses `__doc__` via `ast.get_docstring`; regex sections (`Args:`, `Returns:`, `Raises:`); conditional Raises via `ast.walk` for `ast.Raise` (incl. inside generators); JSON output to stdout, warnings to stderr; exit 0 in warn-only.
- **Verification:** `python scripts/audit_docstrings.py --warn-only` → exit 0; `python -m pytest tests/test_cycle68_audit_docstrings.py -xvs`.
- **Commit:** `feat(scripts): AC05 NEW audit_docstrings.py (warn-only mode, FW-4 generator+raise rule)`

### TASK 17 — AC06: ci.yml docstring-audit step
- **Owner:** primary
- **Files:** `.github/workflows/ci.yml`
- **Sub-conditions:** R1-F4 transition path
- **Edit:** insert step after pytest:
  ```yaml
  - name: docstring audit (warn-only, cycle 68 transition)
    run: python scripts/audit_docstrings.py --warn-only
  ```
- **Verification:** Step 12 CI run shows new step.
- **Commit:** `ci(cycle-68): AC06 docstring audit step (warn-only mode)`

### TASK 18 — AC14: tests/test_cycle68_graph_cache_caller_migrations.py NEW
- **Owner:** primary (FW-7 predicate is load-bearing)
- **Files:** `tests/test_cycle68_graph_cache_caller_migrations.py` (new)
- **Sub-conditions:** `C-AC07-ast-guard-shape` (FW-7), `C-AC14-cache-hit-spy`, `C-AC14-negative-control` (R2 BLOCKER F3)
- **Test functions:**
  - `test_no_pages_none_build_graph_calls_in_migrated_files()` — AST walk over the 4 migrated files; for each `ast.Call` to `build_graph`: compute `pages_supplied = any(kw.arg == "pages" and not (isinstance(kw.value, ast.Constant) and kw.value.value is None) for kw in node.keywords)`. Assert every match has `pages_supplied=True`. Predicate per FW-7.
  - `test_get_graph_cache_hit_on_repeat_call(tmp_kb_env, monkeypatch)` — spy on `kb.graph.builder.build_graph`; first `kb.graph.cache.get_graph(tmp)` calls real builder (count=1); second hits cache (count=1).
  - `test_get_graph_negative_control_no_caching_diverges(tmp_kb_env, monkeypatch)` — replace `get_graph` with `lambda wiki_dir, **kw: build_graph(wiki_dir)` (no caching); assert second call increments count → divergent-fail proof. Reset state via `kb.graph.cache._reset_for_tests()`.
- **Commit:** `test(cycle-68): AC14 graph cache caller migrations AST guard + cache-hit spy with negative-control`

---

## Step 8 plan-gate readiness

Plan gate (Step 08, mimocoding-rescue) checks:
1. **Every design CONDITION pinned to a test** — ✅ 34 conditions (15 NEW + 19 INHERITED) cross-referenced in TASK Sub-conditions fields.
2. **Every threat in threat-model has a Step-14 verifier line** — ✅ inherited from cycle-67 + cycle-68 threat-model.
3. **Every file path verified-existing** — ✅ source files all read at Step 03 + Step 04; new files marked NEW.
4. **FW-1..FW-10 each named in plan body** — ✅ FW-1 (TASK 11), FW-2 (TASK 13), FW-4 (TASK 15+16), FW-6 (commit DAG), FW-7 (TASK 18), FW-8 (TASK 01), FW-9 (TASK 03), FW-10 (TASK 02 before 03). FW-3+FW-5 N/A.

## Predicted Step 09 friction

1. **TASK 11 (AC01 Popen):** complex thread-based reader; FW-1 stdin/reader split is non-obvious. Primary Opus owner.
2. **TASK 13 (AC03 yaml.safe_load):** RCE-class — strict adherence to `yaml.safe_load` only. Primary Opus owner.
3. **TASK 18 (AC14 AST guard):** FW-7 predicate (`kw.value.value is None`) is load-bearing. Revert-test at Step 14 must confirm divergent-fail.
4. **TASK 06 (AC08a graph/export.py):** verify `from kb.graph.builder import build_graph` line is removable post-migration, OR keep if used elsewhere — check via grep.
5. **TASK 03 (AC10 BACKLOG cleanup):** FW-9 self-reference defer is highest-risk hand-edit. Re-confirm 3 cycle-68 self-reference entries (lines 55-59) ARE PRESERVED before commit.
