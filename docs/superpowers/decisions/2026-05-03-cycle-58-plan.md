# Cycle 58 — Implementation Plan

**Owner:** Opus 4.7 primary session per C14-L1 + C37-L5 (5 small folds, primary holds context).
**Format:** one task per AC, each = one commit per Step 9 contract.

## TASK 1 — AC1 Fold: test_cycle17_mcp_tool_coverage.py → test_mcp_core.py

**Files:**
- DELETE: `tests/test_cycle17_mcp_tool_coverage.py` (110 LOC)
- EDIT:   `tests/test_mcp_core.py` (cross-module hosting per cycle-50 precedent)

**Source classes (5):** `TestKbStats` (3 tests), `TestKbGraphViz` (3), `TestKbVerdictTrends` (2), `TestKbDetectDrift` (2), `TestKbCompileScan` (3) — total 13 tests.

**Production targets:** `kb.mcp.browse.kb_stats`, `kb.mcp.core.kb_compile_scan` (re-export), `kb.mcp.health.{kb_detect_drift, kb_graph_viz, kb_verdict_trends}` — all grep-verified at Step 4.

**Import merge:**
- Existing receiver lines 9-10:
  ```python
  from kb.mcp.core import kb_compile_scan, kb_ingest_content, kb_save_source
  from kb.mcp.health import kb_evolve, kb_lint
  ```
- ADD before line 9 (alphabetical):
  ```python
  from kb.mcp.browse import kb_stats
  ```
- EXTEND line 10 alphabetically:
  ```python
  from kb.mcp.health import kb_detect_drift, kb_evolve, kb_graph_viz, kb_lint, kb_verdict_trends
  ```
- `from pathlib import Path` already present.

**Insertion point:** end-of-file. New section header:
```
# ── MCP tool coverage probes (cycle 58 fold) ──────────────────────
```
Followed by 5 classes verbatim from source.

**Test:** `pytest tests/test_mcp_core.py -q` — count must increase by 13.

**Revert-verify per C40-L3:** flip `assert isinstance(result, str)` → `assert False` on `TestKbStats.test_happy_path_returns_string`; confirm pytest -x FAIL; revert.

**Commit:** `test(cycle 58): fold test_cycle17_mcp_tool_coverage into test_mcp_core (1/5)`

---

## TASK 2 — AC2 Fold: test_cycle18_sanitize.py → test_utils_text.py

**Files:**
- DELETE: `tests/test_cycle18_sanitize.py` (116 LOC)
- EDIT:   `tests/test_utils_text.py` (class-wrap to disambiguate from yaml_sanitize tests)

**Source bare functions (16):** all `test_sanitize_text_*` (12) + `test_sanitize_error_text_*` (2) + `test_abs_path_patterns_regex_compiled` (1) + `test_sanitize_text_empty_string` (1).

**Production targets:** `kb.utils.sanitize.{sanitize_text, sanitize_error_text, _ABS_PATH_PATTERNS}` — distinct module from `kb.utils.text` (the existing receiver target).

**Import merge:**
- Existing receiver imports `from tests.fixtures.injection_payloads import ...` + a function-local `from kb.utils.text import sanitize_extraction_field` per-test.
- ADD top-level import:
  ```python
  from kb.utils.sanitize import _ABS_PATH_PATTERNS, sanitize_error_text, sanitize_text
  ```

**Class wrap:** wrap all 16 functions as methods of:
```python
class TestSanitizePathRedaction:
    """Cycle 18 AC13 — kb.utils.sanitize.sanitize_text path-redaction coverage.

    Distinct from yaml_sanitize tests above (kb.utils.text) — class wraps the
    16 path-redaction tests for namespace clarity (cycle-50 cross-feature
    hosting precedent; folded from test_cycle18_sanitize.py).
    """
```
Method names: keep verbatim (`test_sanitize_text_windows_backslash`, etc.) — class scope already disambiguates from receiver's bare `test_sanitize_strips_*` (yaml_sanitize). Each function gets `self` parameter.

**Function-local imports preserved:** the source has function-local `from pathlib import Path` (line 92) + `import re` (line 109) — keep them as method-local to match source.

**Insertion point:** end-of-file. New section header before the class:
```
# ── kb.utils.sanitize path redaction (cycle 58 fold) ──────────────
```

**Test:** `pytest tests/test_utils_text.py::TestSanitizePathRedaction -q` — 16 tests pass.

**Revert-verify per C40-L3:** flip `assert sanitize_text("") == ""` → `assert False` on `TestSanitizePathRedaction.test_sanitize_text_empty_string`; confirm pytest -x FAIL; revert.

**Commit:** `test(cycle 58): fold test_cycle18_sanitize into test_utils_text (2/5)`

---

## TASK 3 — AC3 Fold: test_cycle18_wiki_log.py → test_utils.py

**Files:**
- DELETE: `tests/test_cycle18_wiki_log.py` (126 LOC)
- EDIT:   `tests/test_utils.py` (existing wiki_log section — extend rotation coverage)

**Source bare functions (5):** `test_rotate_inside_lock`, `test_rotate_if_oversized_generic`, `test_rotate_if_oversized_under_threshold`, `test_rotate_if_oversized_missing_file`, `test_rotate_if_oversized_ordinal_collision`.

**Production targets:** `kb.utils.wiki_log.{rotate_if_oversized, append_wiki_log, _rotate_log_if_oversized, file_lock}` + `kb.utils.wiki_log.LOG_SIZE_WARNING_BYTES`.

**Import merge:**
- Existing receiver line 11: `from kb.utils.wiki_log import append_wiki_log`
- Add module-level import (one shape works for all 5 tests):
  ```python
  from kb.utils import wiki_log  # cycle 58 fold — module-level access for spy patches
  ```
- Add `import logging` and `from contextlib import contextmanager` at the top (receiver currently lacks these — needed by `test_rotate_inside_lock`).
- `from pathlib import Path` already in receiver (verify — looking at the file head at line 4 it has `from pathlib import Path`).

**Insertion point:** end-of-file. New section header:
```
# ── wiki_log rotation (cycle 58 fold) ─────────────────────────────
```
Followed by 5 bare functions verbatim from source.

**Function-local imports preserved:** source has function-local `from datetime import UTC, datetime` (line 110) — keep as method-local.

**Test:** `pytest tests/test_utils.py -q -k rotate` — all 5 tests pass.

**Revert-verify per C40-L3:** flip the `assert not path.exists()` → `assert False` on `test_rotate_if_oversized_missing_file`; confirm pytest -x FAIL; revert.

**Commit:** `test(cycle 58): fold test_cycle18_wiki_log into test_utils (3/5)`

---

## TASK 4 — AC4 Fold: test_cycle15_lint_status_mature.py → test_lint.py

**Files:**
- DELETE: `tests/test_cycle15_lint_status_mature.py` (112 LOC)
- EDIT:   `tests/test_lint.py`

**Source classes (3):** `TestMatureStale` (3 tests), `TestOtherStatusesIgnored` (4), `TestTodayOverride` (1) — total 8 tests.
**Source helper (1):** `_write_page` → RENAME to `_write_status_mature_page` (per C52-L4; receiver's existing `_create_page` is a different function).

**Production targets:** `kb.lint.checks.check_status_mature_stale` (grep-verified at `src/kb/lint/checks/frontmatter.py`).

**Import merge:**
- Existing receiver line 9-15:
  ```python
  from kb.lint.checks import (
      check_dead_links,
      check_frontmatter,
      check_orphan_pages,
      check_source_coverage,
      check_staleness,
  )
  ```
- EXTEND alphabetically: ADD `check_status_mature_stale,` between `check_staleness,` and the closing paren.
- `from datetime import date, timedelta` and `from pathlib import Path` already in receiver.

**Helper rename:** all 8 method bodies' calls to `_write_page(...)` → `_write_status_mature_page(...)`.

**Insertion point:** end-of-file. New section header:
```
# ── status_mature staleness check (cycle 58 fold) ─────────────────
```
Followed by:
1. `def _write_status_mature_page(...)` (renamed helper) — same body as source `_write_page`
2. `class TestMatureStale: ...` (3 methods, calls renamed helper)
3. `class TestOtherStatusesIgnored: ...` (4 methods)
4. `class TestTodayOverride: ...` (1 method)

**Test:** `pytest tests/test_lint.py -q -k "MatureStale or OtherStatuses or TodayOverride"` — 8 tests pass.

**Revert-verify per C40-L3:** flip `assert len(issues) == 1` → `assert False` on `TestMatureStale.test_mature_91d_flagged`; confirm pytest -x FAIL; revert.

**Commit:** `test(cycle 58): fold test_cycle15_lint_status_mature into test_lint (4/5)`

---

## TASK 5 — AC5 Fold: test_cycle17_capture_two_pass.py → test_capture.py

**Files:**
- DELETE: `tests/test_cycle17_capture_two_pass.py` (220 LOC)
- EDIT:   `tests/test_capture.py`

**Source classes (2):** `TestAC10TwoPassWrite` (7 tests), `TestAC10RollbackHelpers` (3) — total 10 tests.
**Source helper (1):** `_make_items` → RENAME to `_make_two_pass_items` (per C52-L4).

**Production targets:** `kb.capture.{_rollback_finalized, _rollback_reservations, _write_item_files, CaptureItem}` — `_write_item_files` and `CaptureItem` already imported in receiver line 24-40 block.

**Import merge:**
- Existing receiver block at lines 24-40:
  ```python
  from kb.capture import (
      _CAPTURE_SCHEMA,
      CaptureItem,
      CaptureResult,
      ...
      _write_item_files,
      capture_items,
  )
  ```
- EXTEND the `from kb.capture import (...)` block alphabetically: ADD `_rollback_finalized,` and `_rollback_reservations,`.
- ADD `from unittest.mock import patch` (receiver already has `from unittest.mock import Mock` at line 18 — extend this import: `from unittest.mock import Mock, patch`).
- `from pathlib import Path` already at receiver line 19.

**Helper rename:** all method bodies' calls to `_make_items(...)` → `_make_two_pass_items(...)`.

**Insertion point:** end-of-file (after the `yield` fixture at line 220+). New section header:
```
# ── capture two-pass write all-or-nothing (cycle 58 fold) ─────────
```
Followed by:
1. `def _make_two_pass_items(n: int) -> list[dict]:` (renamed helper)
2. `class TestAC10TwoPassWrite: ...` (7 methods, calls renamed helper; `from kb.capture import CaptureItem` is function-local in `test_rollback_finalized_unlinks_md` — keep verbatim since receiver already has top-level `CaptureItem`)
3. `class TestAC10RollbackHelpers: ...` (3 methods)

**Note on the existing `from kb.capture import CaptureItem` in source line 203:** the receiver already imports `CaptureItem` at module top, so the source's function-local import is redundant. Keep verbatim to match source intent (cycle 17's defensive pattern), since removing it would be a Step 9 scope-add.

**Test:** `pytest tests/test_capture.py -q -k "AC10TwoPass or AC10Rollback"` — 10 tests pass.

**Revert-verify per C40-L3:** flip `assert err is None` → `assert False` on `TestAC10TwoPassWrite.test_success_path_writes_all_md_files`; confirm pytest -x FAIL; revert.

**Commit:** `test(cycle 58): fold test_cycle17_capture_two_pass into test_capture (5/5)`

---

## Per-task workflow (applies to all 5)

1. Read source file (already done at Step 7 reconnaissance).
2. Open receiver, add new imports at top (alphabetical merge).
3. Append section header + content at end-of-file.
4. Apply helper rename (TASK 4 + TASK 5 only) and class wrap (TASK 2 only) inline.
5. Delete source file (`Bash` `rm`).
6. Run `ruff format <receiver>` then `ruff check <receiver>` per `feedback_ruff_edit_ordering`.
7. Run isolated pytest on receiver: `pytest tests/test_<receiver>.py -q`.
8. Revert-verify per C40-L3 (flip ONE assertion in moved test → `assert False` → pytest -x FAIL → revert).
9. `git add tests/test_<source>.py tests/test_<receiver>.py docs/superpowers/decisions/...` (if applicable).
10. `git commit -m "test(cycle 58): fold test_<source-stem> into <receiver-stem> (N/5)"`.

## Step 9 sequencing (parallel-safe order)

Folds are pairwise-independent (5 distinct receivers, 5 distinct source files). Sequential execution:
1. TASK 1 (test_mcp_core.py)
2. TASK 2 (test_utils_text.py)
3. TASK 3 (test_utils.py)
4. TASK 4 (test_lint.py)
5. TASK 5 (test_capture.py)

After each task, commit + run isolated pytest. After TASK 5, run full suite (`pytest -q`) per C22-L3 BEFORE Step 12.

## Test count audit (Step 17 prep)

| Pre-fold | After T1 | After T2 | After T3 | After T4 | After T5 |
|---------:|---------:|---------:|---------:|---------:|---------:|
| 3021 | 3021 | 3021 | 3021 | 3021 | 3021 |

Test count preserved (5 sources DELETED, all tests moved into receivers, no new tests added).

## File count audit (Step 17 prep)

| Pre-fold | After T1 | After T2 | After T3 | After T4 | After T5 |
|---------:|---------:|---------:|---------:|---------:|---------:|
| 208 | 207 | 206 | 205 | 204 | 203 |

(All numbers at branch HEAD; cycles 53/54 may merge first — Step 21 rebase will refresh CHANGELOG numbers.)

## Plan-gate hand-off to Step 8

Step 8 plan-gate (MiMo Coding subagent) verifies this plan covers:
- Every AC1-AC5 receiver edit is named with grep-verified production symbols
- Every helper rename is justified (TASK 4 + TASK 5 cite C52-L4)
- Every section-header convention is consistent across folds
- File-count and test-count math reconciles
- No fold touches a parallel-cycle receiver (test_query.py, test_config.py, test_compile.py, test_models.py)
