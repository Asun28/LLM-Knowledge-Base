# Cycle 43 — Step 7 Implementation Plan

**Date:** 2026-04-27
**Scope:** 11 folds + 4 BACKLOG entries per Step 5 design gate.

## Pre-Step-9 protocol

- Verify `git branch --show-current == cycle-43-test-folds` BEFORE every commit (Q10 / cycle-42 hazard)
- Run `pytest tests/<canonical>.py -q` after every fold (per-AC verification)
- Run full `pytest -q` once after AC12 (Step 10 CI hard gate)
- One commit per AC per `feedback_batch_by_file`
- BACKLOG entries committed in the SAME commit as the fold per AC8/AC9/AC10 — keeps audit trail tight

## Per-AC task definitions

### TASK 1 — AC1 fold

**Source:** `tests/test_cycle10_browse.py` (1 test, 34 LoC)
**Canonical:** `tests/test_mcp_browse_health.py`
**Operation:**
1. Read source `test_kb_read_page_rejects_case_insensitive_ambiguity_regression_pin`
2. Read host file structure — find `_sanitize_error_str` import block + class organization
3. Append the test as a bare function in `test_mcp_browse_health.py` (host shape: bare functions before class at line 360)
4. Add comment at function header: `# Cycle 43 fold from test_cycle10_browse.py`
5. Delete `tests/test_cycle10_browse.py`
6. Run `pytest tests/test_mcp_browse_health.py::test_kb_read_page_rejects_case_insensitive_ambiguity_regression_pin -q`
7. Commit: `test(cycle 43 AC1): fold test_cycle10_browse into test_mcp_browse_health`

**Test:** new test asserts ambiguous case-insensitive page_id returns Error containing "ambiguous page_id"
**Criteria:** Cycle-43 AC1 (requirements doc)
**Threat:** N/A (fold-only)

### TASK 2 — AC2 fold

**Source:** `tests/test_cycle10_extraction_validation.py` (4 tests, 97 LoC)
**Canonical:** `tests/test_ingest.py`
**Operation:**
1. Read source — 4 tests on `_coerce_str_field`, `ingest_source`, `_build_summary_content`
2. Read host — confirm bare-function host shape
3. Append as bare functions; preserve `@pytest.mark.parametrize` decorator on `test_coerce_str_field_accepts_string_missing_none_and_rejects_non_strings`
4. Delete source
5. Run `pytest tests/test_ingest.py -q -k "coerce_str_field or _build_summary_content or rejects_non_string_extraction or accepts_valid_string_extraction"`
6. Commit: `test(cycle 43 AC2): fold test_cycle10_extraction_validation into test_ingest`

### TASK 3 — AC3 fold

**Source:** `tests/test_cycle10_vector_min_sim.py` (2 tests, 73 LoC)
**Canonical:** `tests/test_query.py`
**Operation:**
1. Read source — 2 tests + `FakeVectorIndex` helper class + `_enable_fake_vector_index` helper
2. Read host — find `class TestSearchPages` or similar; if bare-function shape, fold the helper class as `_FakeVectorIndex_Cycle10` and helper as `_enable_fake_vector_index_cycle10` (avoid name collisions with similar helpers)
3. Append both helpers + 2 tests
4. Delete source
5. Run `pytest tests/test_query.py -q -k "search_pages_filters_low_cosine_vector_hits or search_pages_returns_empty_when_bm25_empty"`
6. Commit: `test(cycle 43 AC3): fold test_cycle10_vector_min_sim into test_query`

### TASK 4 — AC4 fold (NEW class to test_paths.py)

**Source:** `tests/test_cycle11_conftest_fixture.py` (1 test, 33 LoC)
**Canonical:** `tests/test_paths.py`
**Operation:**
1. Read source — 1 test asserting `tmp_project` fixture creates wiki/index.md, _sources.md, log.md with expected frontmatter+content
2. Read host — confirm shape (test_paths.py is bare-function)
3. Wrap the moved test in `class TestTmpProjectFixtureContract:` to keep the fixture-contract scope clearly delineated from path-utility tests
4. Add `EXPECTED_INDEX` and `EXPECTED_SOURCES` module-level constants at top of test_paths.py with a section comment `# ── tmp_project fixture-contract pins (cycle 43 AC4 fold) ───────`
5. Delete source
6. Run `pytest tests/test_paths.py::TestTmpProjectFixtureContract -q`
7. Commit: `test(cycle 43 AC4): fold test_cycle11_conftest_fixture into test_paths`

### TASK 5 — AC5 fold

**Source:** `tests/test_cycle11_ingest_coerce.py` (11 tests, 122 LoC)
**Canonical:** `tests/test_ingest.py`
**Operation:**
1. Read source — 11 tests including parametrized `test_ingest_source_rejects_comparison_and_synthesis_with_kb_create_page_message` and `test_kb_ingest_content_rejects_page_types_without_raw_file`
2. Append all as bare functions; preserve `_valid_extraction(**overrides)` helper as module-level `_valid_extraction_cycle11`
3. Delete source
4. Run `pytest tests/test_ingest.py -q -k "coerce_str_field or extract_entity_context or rejects_non_string_summary_callee or rejects_comparison_and_synthesis or kb_ingest_content_rejects_page_types"`
5. Commit: `test(cycle 43 AC5): fold test_cycle11_ingest_coerce into test_ingest`

### TASK 6 — AC6 fold

**Source:** `tests/test_cycle11_utils_pages.py` (8 tests, 167 LoC)
**Canonical:** `tests/test_utils.py`
**Operation:**
1. Read source — 8 tests on `page_id` / `scan_wiki_pages` from `kb.utils.pages`
2. Read host — bare-function shape; tests already cover `kb.utils.pages.normalize_sources`
3. Append all 8 tests as bare functions; preserve the `@pytest.mark.skipif(sys.platform != "win32", ...)` decorator on `test_page_id_normalizes_backslashes_to_posix_id`
4. Delete source
5. Run `pytest tests/test_utils.py -q -k "page_id or scan_wiki_pages or detect_source_drift_function_local"`
6. Commit: `test(cycle 43 AC6): fold test_cycle11_utils_pages into test_utils`

### TASK 7 — AC7 fold-with-isolation

**Source:** `tests/test_cycle12_config_project_root.py` (5 tests, 100 LoC)
**Canonical:** `tests/test_paths.py`
**Operation:**
1. Wrap moved tests in `class TestProjectRootResolution:` with a class-scoped `_reload_config()` helper as `@staticmethod`
2. Add `@pytest.fixture(autouse=True)` inside the class that yields, then re-reloads `kb.config` to restore PROJECT_ROOT/RAW_DIR/WIKI_DIR snapshots after each test
3. Add section comment: `# ── PROJECT_ROOT resolution pins (cycle 43 AC7 fold; reload-isolated per cycle-19 L2 / cycle-20 L1) ───────`
4. Delete source
5. Run `pytest tests/test_paths.py::TestProjectRootResolution -q`
6. ALSO run full `pytest tests/test_paths.py -q` to confirm no leak into `test_make_source_ref_*` tests
7. Commit: `test(cycle 43 AC7): fold test_cycle12_config_project_root into test_paths with reload-isolation`

### TASK 8 — AC8 fold + 1 BACKLOG entry

**Source:** `tests/test_cycle12_frontmatter_cache.py` (7 tests, 187 LoC)
**Canonical:** `tests/test_models.py`
**Vacuous test identified:** Line 180-186 `test_graph_builder_documents_case_sensitivity_caveat` (asserts `gb.__doc__` contains specific strings)
**Operation:**
1. Append all 7 tests as bare functions
2. Move the `_write_page` helper as `_write_page_cycle12` to avoid collision
3. Move the `_write_valid_lint_page` helper as `_write_valid_lint_page_cycle12`
4. Add BACKLOG.md entry under Phase 4.5 HIGH #4 progress note: "**Vacuous-test upgrade candidate (C40-L3, cycle-43 AC8):** `tests/test_models.py::test_graph_builder_documents_case_sensitivity_caveat` (folded from `test_cycle12_frontmatter_cache.py:180-186`) is a docstring-introspection test asserting `gb.__doc__` contains 'case-sensitiv' / 'path' / 'id'. Upgrade approach: extract a behavioral test that verifies path-id case-sensitivity behavior in `kb.graph.builder.page_id` directly, asserting on the function's BEHAVIOR not its docstring."
5. Delete source
6. Run `pytest tests/test_models.py -q -k "cache_hit_call_count or mtime_invalidation or parse_error_reraise or load_all_pages_regression or batch_lint_frontmatter_load or check_frontmatter_reports_malformed or graph_builder_documents_case_sensitivity"`
7. Commit (single): `test(cycle 43 AC8): fold test_cycle12_frontmatter_cache into test_models + BACKLOG vacuous-upgrade entry`

### TASK 9 — AC9 fold + 2 BACKLOG entries

**Source:** `tests/test_cycle12_io_sweep.py` (7 tests, 117 LoC)
**Canonical:** `tests/test_utils_io.py`
**Vacuous tests identified:**
- Line 91-102 `test_load_page_frontmatter_docstring_documents_mtime_caveat`
- Line 105-116 `test_cycle12_io_doc_caveats_are_present`
**Operation:**
1. Append all 7 tests as bare functions
2. Add 2 BACKLOG.md entries under Phase 4.5 HIGH #4 progress note describing the vacuous tests + upgrade approach (extract behavioral assertions on the actual cache-key collision and lock-recycling logic)
3. Delete source
4. Run `pytest tests/test_utils_io.py -q -k "sweep_orphan_tmp or load_page_frontmatter_docstring or io_doc_caveats"`
5. Commit (single): `test(cycle 43 AC9): fold test_cycle12_io_sweep into test_utils_io + 2 BACKLOG vacuous-upgrade entries`

### TASK 10 — AC10 deferral BACKLOG entry

**Operation:**
1. Add BACKLOG.md entry under Phase 4.5 HIGH #4 progress note: "**Deferred from cycle 43 (AC10):** `tests/test_cycle12_sanitize_context.py → tests/test_mcp_core.py` postponed due to active cycle-42 mcp/* dedup interference. The `_sanitize_conversation_context` symbol survives cycle-42, but co-location with cycle-42's surrounding mcp/core.py edits creates merge surface. Revisit in cycle 44+ once cycle-42 lands. Current file count baseline reflects AC10 still as a separate test_cycle12_*.py file."
2. **Note:** No fold operation. Source file stays in tests/.
3. Commit (single, no test changes): `docs(cycle 43 AC10): defer test_cycle12_sanitize_context fold to cycle 44`

### TASK 11 — AC11 fold-as-class

**Source:** `tests/test_cycle13_augment_raw_dir.py` (5 tests in TestRawDirDerivation class, 94 LoC)
**Canonical:** `tests/test_lint.py`
**Operation:**
1. Read host shape — confirm bare-function predominance
2. Append the entire `class TestRawDirDerivation:` block at end of test_lint.py with a section comment: `# ── augment._resolve_raw_dir branch coverage (cycle 43 AC11 fold) ───────`
3. Delete source
4. Run `pytest tests/test_lint.py::TestRawDirDerivation -q`
5. Commit: `test(cycle 43 AC11): fold test_cycle13_augment_raw_dir into test_lint as class`

### TASK 12 — AC12 fold-as-class

**Source:** `tests/test_cycle13_sweep_wiring.py` (3 tests in TestCliBootSweep class, 112 LoC)
**Canonical:** `tests/test_cli.py`
**Operation:**
1. Read host shape — confirm bare-function predominance
2. Append the entire `class TestCliBootSweep:` block at end of test_cli.py with a section comment: `# ── CLI boot sweep_orphan_tmp wiring (cycle 43 AC12 fold) ───────`
3. Delete source
4. Run `pytest tests/test_cli.py::TestCliBootSweep -q`
5. Commit: `test(cycle 43 AC12): fold test_cycle13_sweep_wiring into test_cli as class`

## Step 10 CI hard gate

After Task 12 commits:
1. `python -m pytest -q` — full suite must show 3014 passed (or 3014-N skipped on Windows local)
2. `ruff check src/ tests/` — clean
3. `ruff format --check src/ tests/` — clean

If any fails: re-investigate the most recent fold commit; fix and amend.

## Step 12 doc update plan

Files to grep + update at Step 12 (per C26-L2 + C39-L3 + C41-L2 multi-site):
1. `CLAUDE.md` — Quick Reference test count + file count narrative
2. `docs/reference/testing.md` — line 13 narrative ("3014 tests / 251 files...")
3. `docs/reference/implementation-status.md` — cycle-43 entry
4. `README.md` — tree block "tests/  # N tests across M files" + Phase X stats prose
5. `CHANGELOG.md` — compact `[Unreleased]` Quick Reference entry
6. `CHANGELOG-history.md` — full per-cycle detail
7. `BACKLOG.md` — Phase 4.5 HIGH #4 progress note refresh + 4 new vacuous-test entries

Numerical commitments:
- Tests: 3014 (preserved)
- Files: 253 → 242 (-11)

## Acceptance criteria coverage map

| Step-1 AC | Plan Task |
|-----------|-----------|
| AC1 | TASK 1 |
| AC2 | TASK 2 |
| AC3 | TASK 3 |
| AC4 | TASK 4 |
| AC5 | TASK 5 |
| AC6 | TASK 6 |
| AC7 | TASK 7 |
| AC8 | TASK 8 |
| AC9 | TASK 9 |
| AC10 | TASK 10 (deferral entry) |
| AC11 | TASK 11 |
| AC12 | TASK 12 |
| AC13 (vacuous-BACKLOG) | TASK 8 + TASK 9 + TASK 10 (4 entries total: 1+2+1) |
| AC14 (count drift) | Step 12 doc update plan above |

## Threat-model coverage map

(Step 2 captured 5 threat-model items; all addressed:)

| T# | Mitigation |
|----|------------|
| T1 (test ordering) | Step 10 full-suite run; Task 7 explicit reload-isolation |
| T2 (vacuous fold) | Tasks 8, 9 file BACKLOG entries per C40-L3 |
| T3 (doc count drift) | Step 12 multi-site doc grep |
| T4 (cycle-42 collision) | Q10 verify-branch-before-commit; Task 10 deferral |
| T5 (reload-leak via host) | Task 7 autouse-fixture reload-teardown; Step 10 full-suite gate |

## Estimated wall-clock

- Tasks 1, 4, 11, 12 (single tests / small): ~5 min each = 20 min
- Tasks 2, 3, 5, 6, 8, 9 (multi-test): ~10 min each = 60 min
- Task 7 (reload-isolation): ~15 min
- Task 10 (BACKLOG-only): ~5 min
- Step 10 CI: ~3 min
- Step 11 + 11.5: ~10 min
- Step 12 docs: ~15 min
- Step 13 + 14 + 15 + 16: ~45 min
- **Total: ~3 hours**
