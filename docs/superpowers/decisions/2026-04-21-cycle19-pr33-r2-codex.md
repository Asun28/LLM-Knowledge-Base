# Cycle 19 PR #33 R2 Review (Codex)

Date: 2026-04-21
Role: R2 reviewer
Scope: Verify R1 fix commit `bdf32ea`
Verdict: APPROVE

## Evidence Reviewed

- `git show bdf32ea --stat` reports commit `bdf32eac47d3f9332327817b00b4fd6ca710f8e8`, dated Tue Apr 21 16:11:16 2026 +1200, with 5 files changed, 483 insertions, and 8 deletions.
- `git show bdf32ea` was read in full for all changed hunks. The source changes are limited to `src/kb/compile/linker.py` and `src/kb/ingest/pipeline.py`; the other changed files in the commit are review/security decision documents, as shown by `git show bdf32ea --stat`.
- Current source line checks were performed against `src/kb/compile/linker.py:356`, `src/kb/compile/linker.py:420`, `src/kb/compile/linker.py:431`, `src/kb/compile/linker.py:448`, and `src/kb/ingest/pipeline.py:964`.

## Per-Finding Verification

### R1 Codex MAJOR - under-lock loop used stale candidates snapshot

Status: RESOLVED.

The R1 gap was that the under-lock winner selection iterated the stale pre-lock `candidates` snapshot. The fix is present: inside `_process_inject_chunk`, `sorted_chunk` is computed from the function's `chunk` argument at `src/kb/compile/linker.py:448-461`, and the under-lock winner loop now iterates `for title, target_pid in sorted_chunk:` at `src/kb/compile/linker.py:520-529`.

The required fresh filters are also re-applied under the lock. The loop rejects self-links with `if pid == target_pid` at `src/kb/compile/linker.py:521-523`, rejects already-linked fresh targets with `if target_pid in fresh_links` at `src/kb/compile/linker.py:524-525`, and checks the fresh masked body with `title_patterns[title].search(body_masked)` at `src/kb/compile/linker.py:526-527`. The fresh body and `fresh_links` are derived after acquiring `file_lock(page_path, timeout=_INJECT_LOCK_TIMEOUT)` at `src/kb/compile/linker.py:495-511`, so the winner is selected from the re-read page content rather than from the pre-lock body.

### R1 Codex NIT - docstring contradicted implementation

Status: RESOLVED.

The return-contract docstring now says the function returns a dict where every sanitized/processed `target_page_id` appears as a key with a list value, empty when no injections occur, and skipped invalid/overlength titles are absent. This is at `src/kb/compile/linker.py:356-360`. That matches the implementation, which initializes `result` for all sanitized pages at `src/kb/compile/linker.py:424-426`.

### R1 Sonnet NIT - empty `manifest_key=""` passed validation

Status: RESOLVED.

The validation block remains gated by `if manifest_key is not None:` at `src/kb/ingest/pipeline.py:1017`, and the first validation predicate is now `not manifest_key` at `src/kb/ingest/pipeline.py:1018-1020`. This rejects the empty string before the existing traversal checks for `..`, leading slash/backslash, NUL, and length over 512 at `src/kb/ingest/pipeline.py:1020-1024`. The function signature documents `manifest_key: str | None = None` at `src/kb/ingest/pipeline.py:964-973`, and the docstring describes `None` as the legacy default at `src/kb/ingest/pipeline.py:988-996`.

## New Issues Check

### Chunk-boundary regression

Result: NO NEW ISSUE.

The new under-lock loop does not leak across chunks. `inject_wikilinks_batch` slices `sanitized_pages` into `chunk = sanitized_pages[chunk_idx * chunk_size : (chunk_idx + 1) * chunk_size]` at `src/kb/compile/linker.py:428-432`, passes only that chunk into `_process_inject_chunk` at `src/kb/compile/linker.py:433-434`, and `_process_inject_chunk` creates `sorted_chunk = sorted(chunk, ...)` at `src/kb/compile/linker.py:448-461`. The under-lock loop uses that local `sorted_chunk` at `src/kb/compile/linker.py:520-529`; it does not iterate `sanitized_pages` or any cross-chunk collection. `title_patterns` is global to all sanitized titles at `src/kb/compile/linker.py:420-422`, but it is only indexed by titles yielded from the local `sorted_chunk`, so it does not expand the winner-search universe beyond the current chunk.

### `not manifest_key` falsy-value regression

Result: NO NEW BLOCKER.

The guard newly rejects empty strings while preserving `None` as the legacy path because the falsy check is inside `if manifest_key is not None:` at `src/kb/ingest/pipeline.py:1017-1025`. The public type annotation is `manifest_key: str | None = None` at `src/kb/ingest/pipeline.py:964-973`, so other non-string falsy values are outside the declared contract. Within the declared contract, the only newly rejected value is `""`; non-empty strings continue to be evaluated by the existing traversal/length checks at `src/kb/ingest/pipeline.py:1020-1024`.

## Rationale

Commit `bdf32ea` cleanly addresses all three R1 findings. The linker change converts the pre-lock candidate list into a lock-skipping gate only, while selecting the under-lock winner from the current chunk's full sorted title set and re-checking self-link, fresh-link, and fresh-body match conditions. The docstring now matches the initialized result shape. The ingest validation now rejects the empty manifest key without changing the documented `None` fallback behavior.

No new blocker was found in the requested regression checks. Tests were not run in this R2 pass; verification was performed from `git show bdf32ea` plus targeted current-source line reads, per the requested steps and the one-file write safety constraint.
