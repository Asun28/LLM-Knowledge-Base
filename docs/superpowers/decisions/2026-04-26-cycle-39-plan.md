# Cycle 39 — Implementation Plan

**Date:** 2026-04-26
**Owner:** Opus 4.7 primary session (per cycle-14 L1 + C37-L5)

## Task summary

| Task | Files | AC ref | Threat ref | LoC est |
|---|---|---|---|---|
| TASK 1 | `BACKLOG.md` | AC1-AC7, AC10 | N/A | ~14 lines edited (markers) + ~3 lines deleted (fold entry) |
| TASK 2 | `tests/test_capture.py` (add) + `tests/test_cycle38_mock_scan_llm_reload_safe.py` (delete) | AC8 | T3 | +~155 / -193 |
| TASK 3 | (verify only — no files) | AC9 | T2 | 0 |
| TASK 4 | `CHANGELOG.md` + `CHANGELOG-history.md` + `CLAUDE.md` | AC11 | N/A | ~12 / ~30 / ~0 |

Total: ~210 lines added, ~210 lines removed (net ~0). Zero `src/kb/` changes (D11).

---

## TASK 1 — BACKLOG.md re-confirmation markers + fold-entry deletion

**Files:** `BACKLOG.md` (confirmed exists at repo root, 604 lines).

**Change:** Append the marker `*(cycle-39 re-confirmed 2026-04-26)*` to each of the 7 cycle-39+ tagged entries:

1. `requirements.txt` `diskcache==5.6.3` (line ~125, MEDIUM)
2. `requirements.txt` `ragas==0.4.3` (line ~128, MEDIUM)
3. `requirements.txt` `litellm==1.83.0` (line ~131, MEDIUM)
4. `requirements.txt` `pip==26.0.1` (line ~134, MEDIUM)
5. `requirements.txt` resolver conflicts (cycle-34 AC52 follow-up) (line ~157, MEDIUM)
6. `Dependabot pip-audit drift on litellm GHSA-r75f-5x8p-qvmc` (cycle-39+) (line ~171, MEDIUM)
7. `Dependabot pip-audit drift on litellm GHSA-v4p8-mg3p-g94g` (cycle-39+) (line ~173, MEDIUM)

DELETE the entry: `tests/test_cycle38_mock_scan_llm_reload_safe.py fold-into-canonical (cycle-39+)` (line ~169, MEDIUM) — this entry is RESOLVED by TASK 2.

**Test expectation:** 
- `grep -c "cycle-39 re-confirmed 2026-04-26" BACKLOG.md` returns 7
- `grep "test_cycle38_mock_scan_llm_reload_safe" BACKLOG.md` returns empty

**Criteria:** AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC10
**Threat:** N/A (BACKLOG-only edit)

---

## TASK 2 — Cycle-38 test fold

**Files:**
- `tests/test_capture.py` (1683 lines existing, add ~155 lines at end + 1 import)
- `tests/test_cycle38_mock_scan_llm_reload_safe.py` (193 lines, DELETE)

**Change:**

(a) In `tests/test_capture.py`:
- Add `import importlib` to imports (alphabetical: between `import base64` (line 10) and `import re` (line 11))
- At end of file (after `class TestAdversarialAuditFixes`), append:
  - 1-line comment noting that kb.capture pre-import is satisfied by `from kb.capture import (...)` at line 20 of this file
  - 4 module-level helpers (`_CANONICAL_BODY`, `_CANONICAL_CONTENT`, `_make_canned_response()`, `_make_fake_call()`) verbatim from cycle-38 file
  - `class TestMockScanLlmReloadSafety` with 2 test methods verbatim
  - `@pytest.fixture(autouse=True)` `_restore_kb_capture` verbatim

(b) Delete `tests/test_cycle38_mock_scan_llm_reload_safe.py`.

**Test expectation:**
- `python -m pytest tests/test_capture.py::TestMockScanLlmReloadSafety -v` shows both test methods PASS
- `python -m pytest --collect-only 2>&1 | tail -1` shows `3014 tests collected` (unchanged)
- `git diff --stat tests/` shows test_capture.py +~155 / test_cycle38_*.py deleted (-193)

**Criteria:** AC8
**Threat:** T3 (cycle-38 fold preserves behavior)
**Skill citations:** cycle-4 L4 (freeze-and-fold), cycle-22 L3 (full-suite verify), cycle-23 L3 (preserve restore fixture)

---

## TASK 3 — Full CI gate verify

**Files:** NONE (verification step only).

**Change:** Run sequentially:
- `.venv\Scripts\python -m pytest -q` — expect `3003 passed, 11 skipped`
- `.venv\Scripts\python -m ruff check src/ tests/` — expect clean exit
- `.venv\Scripts\python -m ruff format --check src/ tests/` — expect clean exit
- `git diff origin/main -- src/` — expect EMPTY (T2)

**Test expectation:** All four pass; capture exit codes for the audit trail.

**Criteria:** AC9
**Threat:** T2 (no src diff)
**Skill citations:** C34-L6 (Step 10 mirrors CI workflow)

---

## TASK 4 — Doc sync

**Files:**
- `CHANGELOG.md` (compact entry under `[Unreleased]` Quick Reference)
- `CHANGELOG-history.md` (full per-cycle detail, newest first)
- `CLAUDE.md` — test count grep check (no change expected)
- `docs/superpowers/decisions/*` already written at Steps 1/2/5/7 (this plan)

**Change:** 

(a) `CHANGELOG.md` add at top of `[Unreleased]` Quick Reference (newest first):
```
- **Cycle 39 — Backlog hygiene + dep-drift re-verification + cycle-38 test fold (2026-04-26)** | Items: 7 dep re-confirmations + 1 test fold + 1 BACKLOG cleanup | Tests: 3014 collected (unchanged) / 3003+11 | Scope: BACKLOG.md / tests / CHANGELOG; zero src/kb edits | Detail: see CHANGELOG-history.md
```

(b) `CHANGELOG-history.md` add full bullet detail (newest first) covering all 11 ACs.

(c) `CLAUDE.md` Quick Reference test count UNCHANGED (3014 collected / 3003+11 skipped). Verify via `grep "3014" CLAUDE.md` before commit.

**Test expectation:**
- `head -30 CHANGELOG.md` shows cycle-39 entry
- `grep "Cycle 39" CHANGELOG-history.md` returns the new entry
- `grep "3014" CLAUDE.md` returns existing line unchanged

**Criteria:** AC11
**Threat:** N/A (doc-only)
**Skill citations:** C26-L2 (grep all narrative sites), C30-L1 (commit-count +TBD), cycle-23 L4 (count drift across fix commits)

---

## Plan-gate self-verify (cycle-21 L1 inline)

Coverage check:

- AC1 ✓ TASK 1 (marker for diskcache)
- AC2 ✓ TASK 1 (marker for ragas)  *NB: ACs in requirements doc map to specific BACKLOG entries — re-mapping below*
- ... (all 11 ACs mapped to TASK 1/2/3/4)

Re-reading requirements ACs:
- AC1: GHSA-r75f-5x8p-qvmc drift → TASK 1 marker entry #6
- AC2: GHSA-v4p8-mg3p-g94g drift → TASK 1 marker entry #7
- AC3: diskcache CVE → TASK 1 marker entry #1
- AC4: ragas CVE → TASK 1 marker entry #2
- AC5: pip CVE → TASK 1 marker entry #4
- AC6: litellm GHSA-xqmj (+ aliased) → TASK 1 marker entry #3
- AC7: resolver conflicts → TASK 1 marker entry #5
- AC8: test fold → TASK 2
- AC9: full pytest pass → TASK 3
- AC10: BACKLOG cleanup → TASK 1 (delete fold entry)
- AC11: CHANGELOG/CLAUDE.md → TASK 4

Threat-model coverage:
- T1 (no PR-introduced CVEs) → Step 11 verify (cycle 39 changes zero deps so the diff is empty by construction)
- T2 (no production code drift) → TASK 3 (`git diff origin/main -- src/`)
- T3 (cycle-38 fold preserves behavior) → TASK 2 + TASK 3
- T4 (no new deferred-promise drift) → TASK 1 deletes a resolved promise; cycle-39 docs add ZERO new deferred-promise prose

PLAN-AMENDS-DESIGN check: NONE — plan adheres to all 11 D# decisions above. No code-exploration gaps surfaced; all ACs are mechanical edits.

**Plan-gate verdict: APPROVE.** Proceed to Step 9.
