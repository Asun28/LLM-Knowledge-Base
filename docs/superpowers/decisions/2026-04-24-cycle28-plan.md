# Cycle 28 — Implementation Plan (Step 7)

**Date:** 2026-04-24
**Step:** feature-dev Step 7 (Primary session per cycle-14 L1 — operator holds Step 1-5 context + grep-verified symbols)
**Inputs:** Requirements (9 ACs), threat model (8 threats T1-T8), design doc (14 CONDITIONS C1-C14, 11 resolved questions Q1-Q11)

## Task-by-task

Each task states: description, files, change, test assertion (TDD-first), AC refs, threat/condition refs.

### TASK 1 — `_ensure_conn` sqlite-vec observability (AC1 + AC2 + AC3)

**Files:**
- `src/kb/query/embeddings.py` (grep-verified: `_ensure_conn` at :427-492, `_conn_lock` at :423 acquired at :453, existing counter precedents at :52 + :64)

**Change:**
1. Add `import time` to module header (after `import threading` at :7). Stdlib, no boot-lean impact per C11 + R2 finding 7.
2. Add module constant `SQLITE_VEC_LOAD_WARN_THRESHOLD_SECS: float = 0.3` next to `VECTOR_COLD_LOAD_WARN_THRESHOLD_SECS` (:56). Comment cross-references cycle-26 AC3 + notes "module-level, no env override per cycle-28 Q4 design decision — matches cycle-26 Q4".
3. Add module counter `_sqlite_vec_loads_seen: int = 0` next to `_vector_model_cold_loads_seen` (:64). Comment documents: "exact-per-instance via `_conn_lock`, approximate across concurrent VectorIndex instances; see `get_sqlite_vec_load_count()` docstring for precedent cross-refs".
4. Add public getter `get_sqlite_vec_load_count() -> int` next to `get_vector_model_cold_load_count()` (:83). Docstring per C8 explicitly names cycle-25 (lock-free) + cycle-26 (locked) precedents.
5. Instrument `_ensure_conn` (:461-491):
   - Inside the `try:` at :461, BEFORE `sqlite3.connect(...)`, add `start = time.perf_counter()`.
   - AFTER `conn.enable_load_extension(False)` at :476, AFTER the (existing) try-block success path, compute `elapsed = time.perf_counter() - start`.
   - AFTER `self._conn = conn` at :491 (post-success ordering per C4), emit:
     ```python
     global _sqlite_vec_loads_seen
     _sqlite_vec_loads_seen += 1
     logger.info("sqlite-vec extension loaded in %.3fs (db=%s)", elapsed, self.db_path)
     if elapsed >= SQLITE_VEC_LOAD_WARN_THRESHOLD_SECS:
         logger.warning(
             "sqlite-vec extension load took %.3fs (threshold=%.2fs); consider warm-loading",
             elapsed,
             SQLITE_VEC_LOAD_WARN_THRESHOLD_SECS,
         )
     ```
   - The counter increment + logs sit BEFORE `return conn` at :492. All three live inside the outer `with self._conn_lock:` span (already held per C1 verification).

**Failing tests (written FIRST before the implementation lands):**
- `test_sqlite_vec_load_emits_info_on_success` — builds a VectorIndex against a minimal DB (via `idx.build([(page_id, vec)])`), calls `_ensure_conn()` on a fresh instance pointing at the same DB, asserts exactly one INFO record matching `"sqlite-vec extension loaded in"` in caplog with `self.db_path` string tail.
- `test_sqlite_vec_load_emits_warning_above_threshold` — monkeypatch `time.perf_counter` via `monkeypatch.setattr("kb.query.embeddings.time.perf_counter", ...)` to return stepped values producing elapsed=0.5s; assert WARNING present + matches `"consider warm-loading"`.
- `test_sqlite_vec_load_no_warning_below_threshold` — stepped perf_counter yielding elapsed=0.1s; assert INFO present, WARNING absent.
- `test_sqlite_vec_load_count_increments_exactly_once` — baseline snapshot via `before = get_sqlite_vec_load_count()`; call `_ensure_conn()`; assert `get_sqlite_vec_load_count() - before == 1` (monotonic-delta per C6).
- `test_sqlite_vec_load_count_stable_on_fast_path` — after first `_ensure_conn()` (cached `self._conn`), snapshot counter; call `_ensure_conn()` again on same instance; assert delta == 0.
- `test_sqlite_vec_load_no_info_on_failure_path` (C3 mandate) — monkeypatch `sqlite_vec.load` to raise `ImportError("forced")`; create fresh VectorIndex; call `_ensure_conn()`; assert NO INFO record matching `"sqlite-vec extension loaded in"`, assert counter delta == 0, assert `idx._disabled is True`, assert existing `"sqlite_vec extension load failed"` WARNING is still emitted.

**AC refs:** AC1, AC2, AC3.
**Threat refs:** T1 (log-injection via `db_path` — using `%s` lazy format, not f-string, mitigates), T2 (path leak accepted as parity), T5 (WARN rate bounded by `_conn is not None` fast path).
**Condition refs:** C1, C4, C5, C6, C7, C8, C9, C10.

### TASK 2 — `BM25Index.__init__` observability (AC4 + AC5)

**Files:**
- `src/kb/query/bm25.py` (grep-verified: `BM25Index.__init__` at :59-94, no existing module counter)

**Change:**
1. Add `import time` after `import re` at :14. Stdlib.
2. Add module counter `_bm25_builds_seen: int = 0` after `logger = logging.getLogger(__name__)` at :19. Comment documents: "lock-free per cycle-25 `_dim_mismatches_seen` precedent (Q2 design decision); approximate under concurrent cache-miss rebuilds".
3. Add public getter `get_bm25_build_count() -> int` immediately below the module counter. Docstring per C8 + C11 — states "constructor executions, NOT distinct cache insertions" + names both call sites `engine.py:110` (wiki) and `engine.py:794` (raw).
4. Instrument `BM25Index.__init__`:
   - After `self.n_docs = len(documents)` at :71, add `start = time.perf_counter()`.
   - After IDF pre-computation block at :93-94 (`self.idf[term] = math.log(...)`), BEFORE `__init__` returns (implicit — end of method), add:
     ```python
     elapsed = time.perf_counter() - start
     global _bm25_builds_seen
     _bm25_builds_seen += 1
     logger.info("BM25 index built in %.3fs (n_docs=%d)", elapsed, self.n_docs)
     ```
   - No WARNING threshold (Q1 decision).

**Failing tests:**
- `test_bm25_build_emits_info_with_n_docs` — build `BM25Index([["foo", "bar"], ["baz"]])`; assert caplog has exactly one INFO matching `"BM25 index built in"` + `"n_docs=2"`. Also sub-test for `BM25Index([])` assertion `"n_docs=0"` still emits (empty-corpus edge case, Q10/R2 finding 6).
- `test_bm25_build_count_monotonic_across_instances` — baseline snapshot via `before = get_bm25_build_count()`; construct 3 BM25Index instances; assert `get_bm25_build_count() - before == 3` (monotonic-delta per C6).

**AC refs:** AC4, AC5.
**Threat refs:** T6 (n_docs disclosure — accepted at single-user scale).
**Condition refs:** C2, C5, C6, C7, C8, C10, C11.

### TASK 3 — Regression-test file scaffolding (AC6)

**Files:**
- `tests/test_cycle28_first_query_observability.py` (new file, written FIRST before TASK 1 + 2 implementation lands)

**Change:**
1. Module docstring documents reload-defense rationale (cycle-20 L1) + monkeypatch discipline (C7).
2. 8 test functions (Task-1: 6 tests, Task-2: 2 tests with 1 sub-test for empty corpus).
3. All counter assertions use monotonic-delta (baseline snapshot + delta) per C6.
4. All `time.perf_counter` patches use module-scoped `monkeypatch.setattr("kb.query.embeddings.time.perf_counter", ...)` / `"kb.query.bm25.time.perf_counter"`; NO raw `time.perf_counter = ...` assignments per C7.
5. Tests use `tmp_path` fixture + minimal DB setup via existing `VectorIndex.build([...])` helper.

**Test expectations:**
- Under AC1-AC5 implementation: all 8 tests pass.
- Under AC1-AC5 reverted to pre-cycle-28 `_ensure_conn` + `BM25Index.__init__`: tests 1, 4, 6, 7 fail on assertion (no INFO in caplog, no counter delta). Test 8 fails (no WARNING for failure path assertions don't apply OK, but the `get_sqlite_vec_load_count` import fails — this is the divergent-fail signal per C3).

**AC refs:** AC6.
**Condition refs:** C3, C6, C7.

### TASK 4 — BACKLOG.md hygiene (AC7)

**Files:**
- `BACKLOG.md`

**Change (three independent edits):**
1. **Narrow HIGH-Deferred entry** (line ~109): Remove "(b) first-query observability — `VectorIndex._ensure_conn` sqlite-vec extension load + BM25 build latency instrumentation (cycle-26 Q16 follow-up ...)" text; keep "(a) dim-mismatch AUTO-rebuild" intact. Update prose to reference cycle 28 AC1-AC5 as the landing point.
2. **Delete MEDIUM line** (line ~137): Delete `src/kb/lint/augment.py::_post_ingest_quality` explanatory AC17-drop line in its entirety — the rationale is duplicated in CHANGELOG-history cycle 13 AC2; keeping it in BACKLOG adds no open work.
3. **Delete LOW entry** (line ~162-163): Delete `CHANGELOG.md cycle-27 quick-reference commit tally` 2-line entry; CHANGELOG currently says "3 commits" which IS the correct value under the +1 self-referential rule — entry is obsolete. When the section becomes empty, replace the LOW body with the `_All items resolved — see CHANGELOG cycle 28._` sentinel to collapse cleanly.

**Test expectations:**
No tests — doc-only edit. Step-11 grep verification per C13: `grep -c "AC17-drop" BACKLOG.md` returns 0; `grep -c "cycle-27 quick-reference commit tally" BACKLOG.md` returns 0.

**AC refs:** AC7.
**Condition refs:** C13.

### TASK 5 — CHANGELOG.md format-guide one-liner + cycle 28 entry (AC8)

**Files:**
- `CHANGELOG.md`

**Change:**
1. Inside the existing entry-rule HTML-comment block (lines 10-17), add a new line codifying the commit-count self-referential rule:
   ```html
   <!-- commits: on feature-branch squash-merge flow, the reported commit count equals pre-doc-update branch commits + 1 for the landing doc-update that contains this changelog line (self-referential per cycle-26 L1 skill patch). -->
   ```
   This is the +1 rule I applied to cycle 27 but never documented in the repo itself (only in `feature-dev` SKILL.md). Codifying it here means future cycles can check the rule without re-reading skill patches.
2. Add cycle 28 Quick Reference entry newest-first under `[Unreleased]`. Fields:
   - Items: 9 AC / 2 src + 1 new test file / 3 commits (pre-doc-update 2 + doc-update 1 per the rule in step (1))
   - Tests: 2801 → 2809 (+8)
   - Scope: `First-query observability — \`VectorIndex._ensure_conn\` sqlite-vec extension load and \`BM25Index.__init__\` corpus indexing (closes HIGH-Deferred (b), cycle-26 Q16 follow-up). AC1+AC2+AC3: \`SQLITE_VEC_LOAD_WARN_THRESHOLD_SECS=0.3\` + \`_sqlite_vec_loads_seen\` counter (locked via \`_conn_lock\`, exact) + \`get_sqlite_vec_load_count()\` getter. AC4+AC5: \`_bm25_builds_seen\` counter (lock-free, approximate — covers wiki + raw BM25 builds) + \`get_bm25_build_count()\` getter. AC6: 8 regression tests including failure-path divergence test (C3). AC7-AC8: BACKLOG hygiene + CHANGELOG commit-count rule. AC9: no-op CVE re-verify, matches cycle-26 baseline.`
   - Detail: pointer to history archive

**Test expectations:**
Step-11 grep per C14: `grep -c "self-referential" CHANGELOG.md` returns exactly one match inside the format-guide comment block.

**AC refs:** AC8.
**Condition refs:** C14.

### TASK 6 — CHANGELOG-history.md full entry

**Files:**
- `CHANGELOG-history.md`

**Change:**
Add full cycle 28 detail section newest-first (immediately above cycle 27 section). Cover:
- AC-by-AC implementation notes
- Commit graph (3 commits expected: feat, fix/tests, docs)
- Test-count accounting: 2801 base + 8 new = 2809
- Cross-references to design doc CONDITIONS C1-C14
- Scope-out list from design doc §Scope-outs preserved

**Test expectations:**
No tests — doc-only.

### TASK 7 — CLAUDE.md + README.md hygiene

**Files:**
- `CLAUDE.md` — update test count line 33 + line 191 (cycle-26 L2: grep ALL narrative sites). Add `_ensure_conn` latency-instrumentation cross-ref + `BM25Index` build-latency cross-ref to "Key APIs" block.
- `README.md` — likely no change (user-facing features unaffected; observability is diagnostic).

**Change:**
1. `CLAUDE.md` line 33: bump "2801 tests across 244 test files" → "2809 tests across 245 test files" (new file adds 1). Adjust "last full run was 2792 passed + 9 skipped" to post-cycle-28 numbers after Step 10 confirms.
2. `CLAUDE.md` line 191: bump same count.
3. `CLAUDE.md` Key APIs block: add one-line entries for `get_sqlite_vec_load_count()` + `get_bm25_build_count()` modeled on cycle-26's `get_vector_model_cold_load_count()` entry.
4. `CLAUDE.md` Implementation Status: mention cycle 28 in the latest-cycle narrative after cycle 27.
5. `README.md`: grep for test-count or observability mentions; update only if needed.

**Test expectations:**
Step-11 grep: `grep -c "2801\|2808" CLAUDE.md` should return 0 after edit (all old counts replaced).

## Commit strategy

Commits ordered to enable step-by-step CI validation. Cycle-26 style (2 primary commits + 1 docs commit):

- **Commit 1** `feat(cycle 28): AC1-AC5 first-query observability — sqlite-vec + BM25 latency + counters`:
  - `src/kb/query/embeddings.py` (TASK 1)
  - `src/kb/query/bm25.py` (TASK 2)
  - `tests/test_cycle28_first_query_observability.py` (TASK 3, all 8 tests)

- **Commit 2** `docs(cycle 28): BACKLOG hygiene + CHANGELOG commit-count rule + CLAUDE refresh`:
  - `BACKLOG.md` (TASK 4)
  - `CHANGELOG.md` (TASK 5, format-guide one-liner + Quick Reference entry)
  - `CHANGELOG-history.md` (TASK 6)
  - `CLAUDE.md` (TASK 7)
  - `.data/cycle-28/cve-baseline.json` — NOT committed (per cycle-22 L1 `.data/cycle-*/` gitignored)

- **Commit 3** (Step 12 doc-sync, self-referential count +1) `docs(cycle 28): post-merge doc sync — final counts`:
  - Deferred to Step 12; may or may not be needed depending on R1/R2 fix commits during PR review.

Commits 1 + 2 land at Step 9; commit 3 lands at Step 12.

**Contingency:** If Step-10 CI fails on a single test, add a fix commit between 1 and 2, updating counts to reflect the added commit. The +1 self-referential rule (C14 / cycle-26 L1) means the CHANGELOG commit-count claim must increment with each added fix commit.

## Plan-gate amendments (2026-04-24, Step 8 REJECT → resolve inline per cycle-21 L1)

Step 8 plan-gate (Codex) returned REJECT with 9 gaps + 1 PLAN-AMENDS-DESIGN. All gaps are documentation clarifications or grep-pattern additions; no new code exploration needed. Resolved inline per cycle-21 L1 heuristic.

**A1 (gaps 1, 2, 3 — AC7/AC8/AC9 revert-failing tests):** DISMISS as overly-strict. AC7 deletes BACKLOG entries, AC8 adds one HTML-comment line to CHANGELOG.md, AC9 is a no-op CVE re-verify. None has behaviour a production revert can regress — they are doc mutations + bookkeeping. Step-11 grep checks at C13/C14 (delete-string greps + self-referential count) are the mechanical verification; adding "revert-failing tests" for doc-edit AC items violates the cycle-11 L2 anti-pattern ("inspect-source / file-grep tests are signature-only in disguise"). Precedent: cycle-26 AC7 (CVE no-op re-verify) + AC8 (BACKLOG narrow) + AC6 (BACKLOG delete) also had "no tests — doc-only" and shipped clean.

**A2 (gap 4 — T7 counter wraparound):** ACCEPT. Add to TASK 1 + TASK 2 explicit one-liner: Python `int` is arbitrary-precision (no overflow risk); counters are monotonically increasing; external serialization edge case (JSON `Number.MAX_SAFE_INTEGER = 2^53 - 1`) is >10^6 years of sustained load at 1 increment/µs — not a cycle-28 concern. Step-11 grep: none needed (Python-language guarantee).

**A3 (gap 5 — C1/C2 bracket grep):** ACCEPT. Upgrade the Risk-checklist C1 + C2 lines from "Step 9 diff shows" prose to a concrete grep command. Full-block bracket verified by: `grep -n "time\.perf_counter" src/kb/query/embeddings.py` returns at least 2 hits inside `_ensure_conn` (start + elapsed); `grep -n "time\.perf_counter" src/kb/query/bm25.py` returns 2 hits inside `BM25Index.__init__`. Combined with C5's single-`+=` grep, the triple (start, elapsed, counter-increment) is structurally pinned.

**A4 (gap 6 — C8 docstring content grep):** ACCEPT. Add explicit grep for docstring content: `grep -c "cycle-25\|cycle-26" src/kb/query/embeddings.py` ≥ 2 (one docstring per getter mentions both precedents); `grep -c "cycle-25\|cycle-26" src/kb/query/bm25.py` ≥ 2. Plus `grep -c "constructor executions" src/kb/query/bm25.py` ≥ 1 (Q11 exact-text mandate) and `grep -c "engine.py:110\|engine.py:794" src/kb/query/bm25.py` ≥ 2 (both call sites named per C8 text).

**A5 (gap 7 — C9 separate from C4):** DISMISS. C9 and C4 test the same invariant from two angles: C4 positive ("log + counter AFTER `self._conn = conn`"), C9 negative ("no `finally:` wraps the triple"). The Step-9 implementation naturally satisfies both; the grep for `finally` exclusion at line-range of the instrumentation IS the C9 distinctness. Keep C9 in the Risk checklist as-is; its Step-11 grep is `grep -n "finally" src/kb/query/embeddings.py` — the BASELINE match count must be preserved (no NEW `finally:` added by cycle 28).

**A6 (gap 8, PLAN-AMENDS-DESIGN — C13 four mutations):** ACCEPT but redefine. The design-gate's "exactly three mutations" counts CONTENT mutations (narrow + delete + delete). The sentinel restoration (`_All items resolved — see CHANGELOG cycle 13._` → `_All items resolved — see CHANGELOG cycle 28._`) is a MECHANICAL CONSEQUENCE of the LOW-section becoming empty — BACKLOG.md format rule (line 28-29) requires resolved-sections collapse to one-liner sentinels. Per cycle-11 L3, this is a cosmetic ordering that preserves the dependency (delete entry → section not left bare). Amend C13 text to: "three content mutations + one mechanical sentinel update required by BACKLOG.md format rule. Grep check: `grep -c "_All items resolved" BACKLOG.md` returns same count as pre-cycle-28 baseline + 0 (sentinel replaces the cycle-27-era entry, net zero)."

Result: this is `PLAN-AMENDS-DESIGN-DISMISSED: the sentinel update is a format-rule consequence of the three content deletions, not an independent fourth edit`. The design's intent (keep BACKLOG.md tidy after delete) is preserved; the plan's sentinel keeps the format rule intact.

**A7 (cycle-18 L2 envelope violation — gap 9 alternative reading):** Also DISMISS. Cycle-18 L2 applies to TELEMETRY EMISSIONS living in helpers when the caller owns the envelope (the `ingest_source` start/success/failure stage JSONL). Cycle-28's perf-log is NOT a caller-envelope telemetry — it is a LOCAL LATENCY observation at the site where the latency accrues. Analogous to cycle-26 `_get_model` which emits INFO from INSIDE the model-load helper, not from `query_wiki`'s envelope. The cycle-18 L2 rule doesn't bind observability at the actual-cost site.

## Risk checklist (Step 11 will re-verify — amended inline per A1-A7 above)

- **C1 bracket scope (amended per A3):** `grep -n "time\.perf_counter" src/kb/query/embeddings.py` returns ≥ 2 hits inside `_ensure_conn` spanning BEFORE `sqlite3.connect` (start) and AFTER `enable_load_extension(False)` (elapsed). Additional: `grep -B3 "start = time\.perf_counter" src/kb/query/embeddings.py` shows the line appears inside the `with self._conn_lock:` block, not at module scope.
- **C2 bracket scope (amended per A3):** `grep -n "time\.perf_counter" src/kb/query/bm25.py` returns exactly 2 hits inside `BM25Index.__init__` (start after `self.n_docs = len(documents)`, elapsed after IDF pre-computation).
- **C3 8-test count:** `.venv/Scripts/python -m pytest tests/test_cycle28_first_query_observability.py --collect-only -q | grep "collected"` shows 8 items.
- **C4 post-success ordering:** Step-11 verifies no `finally:` wraps the perf/log/counter triple in `_ensure_conn`.
- **C5 single-site increments:** Exactly one `_sqlite_vec_loads_seen += 1` and one `_bm25_builds_seen += 1` in the two src files respectively.
- **C6 monotonic-delta:** Every counter read in the test file paired with a preceding baseline snapshot.
- **C7 module-scoped monkeypatch:** Zero `time.perf_counter\s*=` raw-assignment patterns in the test file.
- **C8 docstring precedents (amended per A4):** `grep -c "cycle-25\|cycle-26" src/kb/query/embeddings.py` ≥ 2, `grep -c "cycle-25\|cycle-26" src/kb/query/bm25.py` ≥ 2, `grep -c "constructor executions" src/kb/query/bm25.py` ≥ 1, `grep -c "engine.py:110\|engine.py:794" src/kb/query/bm25.py` ≥ 2.
- **C9 no-finally (retained per A5 — distinctness from C4):** `grep -n "finally" src/kb/query/embeddings.py | wc -l` equals pre-cycle-28 baseline count; no NEW `finally:` introduced around cycle-28 instrumentation.
- **C13 BACKLOG three content mutations + one sentinel (amended per A6):** Three content deletions plus one sentinel update (LOW section) — the sentinel is a BACKLOG.md format-rule consequence. Grep verification: `grep -c "AC17-drop" BACKLOG.md` returns 0; `grep -c "cycle-27 quick-reference commit tally" BACKLOG.md` returns 0; `grep -n "first-query observability" BACKLOG.md` returns 0 (landed in cycle 28, removed from HIGH-Deferred); `grep -c "see CHANGELOG cycle" BACKLOG.md` unchanged (sentinel count preserved).
- **T7 wraparound (added per A2):** Documented in TASK 1 + TASK 2 comments that Python `int` has no overflow; no runtime test needed.
- **C8 getter docstrings:** Both getter docstrings name-check cycle-25 + cycle-26 precedents with explicit lock semantics.
- **C10 no MCP/CLI exposure:** Zero matches in `src/kb/mcp/` and `src/kb/cli.py`.
- **C11 boot-lean preserved:** No new `kb.query.embeddings` / `kb.query.bm25` imports in `kb.mcp.__init__`.
- **C12 CVE no-op:** pip-audit baseline matches cycle-26; CHANGELOG entry includes literal "no-op CVE re-verify, matches cycle-26 baseline".
- **C13 BACKLOG three edits:** Exactly three mutations in `git diff BACKLOG.md`.
- **C14 format-guide:** Exactly one "self-referential" match in `CHANGELOG.md` inside the entry-rule comment block.

## Test-count accounting

| Source | Count |
|---|---|
| Pre-cycle-28 (main) | 2801 |
| TASK 3 new file | +8 |
| **Cycle-28 landing** | **2809** |

If full-suite `pytest --collect-only -q | tail -2` at Step 10 reports anything other than 2809 post-implementation, Step 10 fails and Step 9 re-runs.

## ESCALATE

None. All 14 conditions map to mechanical verification.
