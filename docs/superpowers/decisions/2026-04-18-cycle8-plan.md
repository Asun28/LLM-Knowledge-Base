# Cycle 8 — Step 7 Implementation Plan

**Date:** 2026-04-18  
**Branch:** `feat/backlog-by-file-cycle8`  
**Authority:** `2026-04-18-cycle8-design.md` gate amendments override the base requirements where they differ.

Implement as small file-grouped commits. Keep public signatures additive, reject path traversal at MCP boundaries, and preserve existing default behavior when new keyword arguments are omitted.

## TASK 1 — Config Consistency Caps

**Files:** `src/kb/config.py`

**Change:** Add `MAX_CONSISTENCY_GROUPS = 20` and `MAX_CONSISTENCY_PAGE_CONTENT_CHARS = 4096` near the other lint/context caps. Do not move existing constants or split the config module.

**Test:** Covered by `tests/test_cycle8_consistency_caps.py::test_config_constants`, importing both constants and asserting exact values.

**AC:** AC13.

**Threat:** Supports T5/T8-style transport discipline indirectly by bounding MCP output size; no new trust boundary.

**Grep evidence:** `grep -n "MAX_NOTES_LEN\|PAGE_TYPES\|CONFIDENCE_LEVELS\|VALID_SOURCE_TYPES" src/kb/config.py` → existing validation constants found at lines 82, 123, 124, and 221; new consistency constants are not present yet.

## TASK 2 — Validated Page Models

**Files:** `src/kb/models/page.py`, `src/kb/models/__init__.py`

**Change:** Add `__post_init__` validators: `WikiPage.page_type in PAGE_TYPES`, `WikiPage.confidence in CONFIDENCE_LEVELS`, and `RawSource.source_type in VALID_SOURCE_TYPES`, raising `ValueError` with field-specific messages. Strip control characters and BIDI marks from `WikiPage.title` during initialization and `from_post`. Add `WikiPage.to_dict()` with the exact JSON wire shape and ISO date formatting. Add `WikiPage.from_post(post, path)` that reads only known metadata keys, requires title/type/confidence where applicable, routes `metadata.get("source")` through `normalize_sources`, ignores unknown metadata, and returns a validated instance. Re-export `WikiPage` and `RawSource` from `models/__init__.py` with `__all__`.

**Test:** `tests/test_cycle8_models_validation.py` covers invalid page type, invalid confidence, invalid raw source type, JSON serializable `to_dict`, `from_post` roundtrip, traversal source removal, and title control-char stripping. Add Step-11 grep checks for existing model instantiations so legacy tests use valid enum values or explicit `pytest.raises`.

**AC:** AC3, AC4, AC5, AC6, AC6a, AC20, AC21, AC22, AC23.

**Threat:** T6 and T7. `from_post` must not trust LLM-authored frontmatter; source routing through `normalize_sources` prevents traversal-like refs, and enum validation fails closed.

**Grep evidence:** `grep -rn "class WikiPage\|class RawSource" src/kb/models/page.py` → dataclasses exist at lines 9 and 18 with default kwargs but currently have no validators, serializer, or `from_post`.

## TASK 3 — Top-Level Package Re-Exports

**Files:** `src/kb/__init__.py`

**Change:** Re-export `ingest_source`, `compile_wiki`, `query_wiki`, `build_graph`, `WikiPage`, `RawSource`, `LLMError`, and `__version__`. Define `__all__` with exactly those public names. Avoid importing private helpers or names with leading underscores.

**Test:** `tests/test_cycle8_package_exports.py::test_kb_top_level_exports` runs a fresh subprocess with `python -c "from kb import ..."` to catch import cycles and missing public symbols.

**AC:** AC1, AC17.

**Threat:** T10. Curated `__all__` prevents accidental public exposure while keeping imports additive.

**Grep evidence:** `grep -rn "def ingest_source\|def compile_wiki\|def query_wiki\|class LLMError\|__version__" src/kb` → target exports found in `ingest/pipeline.py:778`, `compile/compiler.py:337`, `query/engine.py:607`, `utils/llm.py:349`, and `kb/__init__.py:3`.

## TASK 4 — Utils Package Re-Exports

**Files:** `src/kb/utils/__init__.py`

**Change:** Re-export `slugify`, `yaml_escape`, `yaml_sanitize`, `STOPWORDS`, `atomic_json_write`, `atomic_text_write`, `file_lock`, `content_hash`, `extract_wikilinks`, `extract_raw_refs`, `FRONTMATTER_RE`, `append_wiki_log`, `load_all_pages`, `normalize_sources`, and `make_source_ref`; define matching `__all__`. Import from the existing source modules only.

**Test:** `tests/test_cycle8_package_exports.py::test_utils_exports` uses a subprocess import for representative names and can include the full `__all__` equality check in-process.

**AC:** AC2, AC18.

**Threat:** T10. Exports only existing public utilities needed by downstream callers.

**Grep evidence:** `grep -rn "def append_wiki_log\|def file_lock\|def atomic_text_write" src/kb/utils/io.py src/kb/utils/wiki_log.py` → `atomic_text_write` at `io.py:86`, `file_lock` at `io.py:160`, and `append_wiki_log` at `wiki_log.py:52`.

## TASK 5 — LLM Success Telemetry

**Files:** `src/kb/utils/llm.py`

**Change:** In `_make_api_call`, store `start = time.monotonic()` per attempt, assign `response = client.messages.create(**kwargs)`, then log exactly one INFO record before returning. Use nil-safe usage extraction: `usage = getattr(response, "usage", None)`, `tokens_in = getattr(usage, "input_tokens", 0)`, `tokens_out = getattr(usage, "output_tokens", 0)`. Log only `model`, `attempt`, `tokens_in`, `tokens_out`, and integer `latency_ms`. Do not include `kwargs`, `messages`, `system`, response text, or content. Existing warning/retry behavior remains unchanged.

**Test:** `tests/test_cycle8_llm_telemetry.py` monkeypatches the Anthropic client, asserts one INFO success record contains required keys, and asserts the rendered log message contains no substring from `kwargs["messages"][0]["content"][:30]` or `kwargs.get("system", "")[:30]`.

**AC:** AC7, AC24.

**Threat:** T8. The new sink must not leak prompt, system, or response body text.

**Grep evidence:** `grep -n "def _make_api_call\|logger\.warning\|return response\|client.messages.create" src/kb/utils/llm.py` → `_make_api_call` starts at line 67 and currently returns `client.messages.create(**kwargs)` directly at line 77 with warning-only telemetry.

## TASK 6 — Consistency Context Caps

**Files:** `src/kb/lint/semantic.py`, `src/kb/mcp/quality.py`

**Change:** Import the new config constants. Update `kb_lint_consistency` docstring to document auto-mode total group cap and per-page truncation. In `build_consistency_context(page_ids=None)` auto mode, dedupe groups, chunk by `MAX_CONSISTENCY_GROUP_SIZE`, then cap emitted chunks to `MAX_CONSISTENCY_GROUPS`; log INFO with dropped count if truncating. Explicit `page_ids` mode remains unchanged. When rendering auto-mode page bodies, strip frontmatter first, then truncate each body over `MAX_CONSISTENCY_PAGE_CONTENT_CHARS` and append the exact marker `\n\n[Truncated at {N} chars — run kb_lint_deep for full body]`. Do not truncate explicit mode.

**Test:** `tests/test_cycle8_consistency_caps.py` checks docstring text, group count `<= MAX_CONSISTENCY_GROUPS`, marker presence after frontmatter stripping, and unchanged explicit mode where practical.

**AC:** AC10, AC11, AC12, AC27, AC28.

**Threat:** Bounds MCP transport payload and prevents large-wiki output DoS while preserving targeted explicit checks.

**Grep evidence:** `grep -n "def build_consistency_context" src/kb/lint/semantic.py` plus read of lines 266-337 → current auto mode chunks groups but has no total cap and appends full page content including frontmatter.

## TASK 7 — `kb_stats` Wiki Directory Plumbing

**Files:** `src/kb/mcp/browse.py`

**Change:** Change signature to `kb_stats(wiki_dir: str | None = None)`. For non-None input, resolve `Path(wiki_dir).resolve()` and require it to be under `PROJECT_ROOT.resolve()` via `relative_to`; return `Error: ...` using `_sanitize_error_str` on rejection. Pass the resolved path to `analyze_coverage(wiki_dir=wiki_path)` and `build_graph(wiki_dir=wiki_path)`. Preserve existing default behavior with `wiki_dir=None`.

**Test:** `tests/test_cycle8_health_wiki_dir.py::test_kb_stats_wiki_dir` builds a temp wiki under the project and asserts total page count scopes to that wiki. Add traversal tests for `wiki_dir="../.."` and an absolute outside-project path returning an `Error:` string.

**AC:** AC8, AC25 plus design V27.

**Threat:** T1. MCP client controls `wiki_dir`; must reject traversal and outside-root absolute paths.

**Grep evidence:** `grep -n "def kb_stats" src/kb/mcp/browse.py` → `kb_stats` is at line 317 with no `wiki_dir` parameter; read shows it currently calls `analyze_coverage()` and `build_graph()` without overrides.

## TASK 8 — `kb_verdict_trends` Wiki Directory Plumbing

**Files:** `src/kb/mcp/health.py`

**Change:** Change signature to `kb_verdict_trends(wiki_dir: str | None = None)`. Validate non-None paths using the same `Path.resolve().relative_to(PROJECT_ROOT.resolve())` pattern as Task 7. Derive the verdict path as `wiki_path.parent / ".data" / "verdicts.json"` when `wiki_dir` is provided; otherwise pass `None` so `compute_verdict_trends(path=None)` uses `VERDICTS_PATH`. Sanitize any rejection/error via `_sanitize_error_str`.

**Test:** `tests/test_cycle8_health_wiki_dir.py::test_kb_verdict_trends_wiki_dir` writes a temp `.data/verdicts.json`, calls `kb_verdict_trends(wiki_dir=tmp_wiki)`, and asserts the temp data is used. Add outside-root traversal rejection tests.

**AC:** AC9, AC26 plus design V27.

**Threat:** T1. Prevents MCP path traversal into arbitrary verdict files.

**Grep evidence:** `grep -n "def kb_verdict_trends" src/kb/mcp/health.py` → function is at line 176 with no `wiki_dir`; read shows it currently calls `compute_verdict_trends()` without a path override.

## TASK 9 — Shared Notes Validation Helper

**Files:** `src/kb/mcp/app.py`, `src/kb/mcp/core.py`, `src/kb/mcp/quality.py`

**Change:** Add `_validate_notes(notes: str, field_name: str) -> str | None` in `mcp/app.py`. It strips control chars and BIDI marks before measuring length against `MAX_NOTES_LEN`; return `None` when valid or `f"Error: {field_name} too long ({n} chars; max {MAX_NOTES_LEN})."` when over cap. Import and call it once at the MCP boundary for `kb_refine_page(... revision_notes=...)` and `kb_query_feedback(... notes=...)`, passing literal field names only. Remove duplicated inline length checks for these two paths. Audit `mcp/core.py`: current grep shows no notes call site there, so no code change is expected unless an implementation-time read finds an indirect wrapper.

**Test:** `tests/test_cycle8_consistency_caps.py` or a focused helper test calls `_validate_notes` for valid, over-limit, and control/BIDI-stripped inputs. Existing/refined MCP tests assert `kb_refine_page` and `kb_query_feedback` return the helper’s exact error string and do not log note bodies.

**AC:** AC16.

**Threat:** T2 and T3. Cap must remain `MAX_NOTES_LEN`; `field_name` is a compile-time literal to avoid CRLF injection in the error response.

**Grep evidence:** `grep -rn "kb_query_feedback\|revision_notes\|MAX_NOTES_LEN\|_validate_notes" src/kb/mcp/core.py src/kb/mcp/quality.py src/kb/mcp/app.py` → inline checks exist in `quality.py:79` and `quality.py:348`, `kb_query_feedback` is in `quality.py:178`, and no `_validate_notes` helper exists yet.

## TASK 10 — Idempotent Contradiction Persistence

**Files:** `src/kb/ingest/pipeline.py`

**Change:** In `_persist_contradictions`, keep the read/check/write inside the existing `with file_lock(contradictions_path):` block. Build the sanitized block exactly as it would be appended: full header plus claim lines. If that exact block is already present in `existing`, log DEBUG with `safe_ref` and date only and return without rewriting. If the same header exists but claim lines differ, append the new block as usual. Preserve first-ingest behavior and existing exception handling.

**Test:** `tests/test_cycle8_contradictions_idempotent.py` calls `_persist_contradictions` twice with identical `(contradictions, source_ref, wiki_dir)` and asserts one exact header/block. A second test calls with the same `source_ref` and same day but distinct claims and asserts a second block is appended.

**AC:** AC15, AC30.

**Threat:** T4 and T5. Exact block matching inside `file_lock` avoids races and avoids suppressing legitimate distinct claims.

**Grep evidence:** `grep -rn "def _persist_contradictions" src/kb/ingest/pipeline.py` plus read of lines 152-183 → function already wraps read and `atomic_text_write` in `file_lock`, but currently always appends and has no exact-block skip.

## TASK 11 — PageRank as RRF Rank List

**Files:** `src/kb/query/engine.py`

**Change:** Replace post-fusion PageRank score multiplication in `search_pages`. Build BM25 and vector candidate lists as today, dedup them, compute PageRank only when `PAGERANK_SEARCH_WEIGHT > 0`, and create a separate PageRank rank list from the union of BM25 and vector candidate IDs only. Key candidates on `id.lower()` to match `_compute_pagerank_scores`, sort by PageRank descending, cap by the existing `limit * 2` budget, and pass exactly `[bm25, vector, pagerank]` to `rrf_fusion` when vector is available or `[bm25, pagerank]` when not. Keep `PAGERANK_SEARCH_WEIGHT = 0` behavior unchanged. A small wrapper around `rrf_fusion(lists)` is acceptable if it preserves the public `rrf_fusion(lists, k=RRF_K)` signature.

**Test:** `tests/test_cycle8_pagerank_prefusion.py` spies on `rrf_fusion`, injects fake BM25/vector candidates and PageRank scores, asserts one input list orders IDs by descending PageRank over the union of candidates, and pins the top-5 fused output for a fixed fixture.

**AC:** AC14, AC29.

**Threat:** T9. Candidate union prevents unrelated graph nodes from entering search results; lower-case IDs prevent collisions with existing PageRank keys.

**Grep evidence:** `grep -n "def search_pages\|rrf_fusion\|pagerank" src/kb/query/engine.py` → current implementation calls hybrid search, dedups, then multiplies fused scores by PageRank at lines 162-174 instead of passing PageRank to `rrf_fusion`.

## TASK 12 — Package Export Tests

**Files:** `tests/test_cycle8_package_exports.py`

**Change:** Add subprocess-based import tests for top-level `kb`, `kb.utils`, and `kb.models`. Use `sys.executable`, set cwd to repo root, and assert return code plus stderr for diagnostics. Include a direct `__all__` equality check for each package if imports are stable.

**Test:** This file is the test. Run `pytest tests/test_cycle8_package_exports.py`.

**AC:** AC17, AC18, AC19.

**Threat:** T10. Ensures only curated public names are exposed and importable in a fresh interpreter.

**Grep evidence:** `grep -rn "def ingest_source\|def compile_wiki\|def query_wiki\|class LLMError\|__version__" src/kb` → all top-level export targets exist, while `src/kb/__init__.py` currently only defines `__version__`.

## TASK 13 — Model Validation Tests

**Files:** `tests/test_cycle8_models_validation.py`

**Change:** Add tests for invalid `WikiPage.page_type`, invalid `WikiPage.confidence`, invalid `RawSource.source_type`, `to_dict()` JSON serialization and exact keys, `from_post(post, path)` roundtrip, missing required metadata raising `ValueError`, and AC6a traversal/control-char sanitization. Use a small `frontmatter.Post` or compatible object rather than disk I/O where possible.

**Test:** Run `pytest tests/test_cycle8_models_validation.py`.

**AC:** AC20, AC21, AC22, AC23, AC6a.

**Threat:** T6 and T7. Tests exercise LLM-frontmatter defensive parsing and validation failures.

**Grep evidence:** `grep -rn "normalize_sources" src/kb/utils src/kb/ingest/pipeline.py` → `normalize_sources` exists in `utils/pages.py:34`, is used by page loading at `utils/pages.py:86`, and is already imported by `pipeline.py:33`.

## TASK 14 — LLM Telemetry Tests

**Files:** `tests/test_cycle8_llm_telemetry.py`

**Change:** Add a fake Anthropic response with `usage.input_tokens` and `usage.output_tokens`, monkeypatch `get_client`, call the success path, and assert one INFO record contains `model=`, `attempt=`, `tokens_in=`, `tokens_out=`, and `latency_ms=`. Add a second success case with missing `usage` to assert nil-safe `0` fallback. Include no-leak substring assertions for prompt and system text.

**Test:** Run `pytest tests/test_cycle8_llm_telemetry.py`.

**AC:** AC24 and AC7 verification.

**Threat:** T8. Tests make prompt leakage executable rather than relying on review.

**Grep evidence:** `Get-Content src/kb/utils/llm.py | Select-Object -Skip 55 -First 35` → `_make_api_call` currently returns the client response immediately inside the try block, so telemetry must be inserted before that return.

## TASK 15 — Health and Browse Wiki-Dir Tests

**Files:** `tests/test_cycle8_health_wiki_dir.py`

**Change:** Create temp wiki fixtures under the repo workspace so project-root validation accepts them. For `kb_stats`, create a known number of page files and assert returned total count and graph stats reflect the temp wiki. For `kb_verdict_trends`, write `tmp_wiki.parent / ".data" / "verdicts.json"` and assert formatted output reflects that file. Add traversal rejection tests for relative `../..` and absolute outside-root paths.

**Test:** Run `pytest tests/test_cycle8_health_wiki_dir.py`.

**AC:** AC25, AC26, plus V27.

**Threat:** T1. Confirms both tools reject attacker-controlled filesystem escapes.

**Grep evidence:** `grep -rn "def analyze_coverage" src/kb/evolve/analyzer.py` and `grep -rn "def compute_verdict_trends" src/kb/lint/trends.py` → `analyze_coverage(wiki_dir: Path | None = None, ...)` and `compute_verdict_trends(path: Path | None = None)` already accept the needed plumbing kwargs.

## TASK 16 — Consistency Caps and Notes Helper Tests

**Files:** `tests/test_cycle8_consistency_caps.py`

**Change:** Build a temp wiki with at least `MAX_CONSISTENCY_GROUPS + 5` natural groups and assert auto mode emits at most the configured number of `## Group N` headers. Add a page body longer than `MAX_CONSISTENCY_PAGE_CONTENT_CHARS`, with frontmatter, and assert the truncation marker appears after body-only truncation. Check config constants and direct `_validate_notes` behavior for valid input, over-limit input, and control/BIDI stripping before length check.

**Test:** Run `pytest tests/test_cycle8_consistency_caps.py`.

**AC:** AC27, AC28, AC13, AC16 helper verification.

**Threat:** T2, T3, and MCP output DoS controls. Tests validate exact helper error text and transport caps.

**Grep evidence:** `grep -rn "def _sanitize_error_str\|def _validate_page_id" src/kb/mcp/app.py` → shared MCP validators live in `app.py` at lines 91 and 177; `_validate_notes` should be added alongside them.

## TASK 17 — PageRank Pre-Fusion Tests

**Files:** `tests/test_cycle8_pagerank_prefusion.py`

**Change:** Monkeypatch search internals so BM25, vector, and PageRank are deterministic. Spy on `kb.query.engine.rrf_fusion` and assert the PageRank list is a separate list ordered by `sorted(candidates, key=pagerank_scores.get, reverse=True)`, with candidates equal to BM25 plus vector IDs only. Pin the final top-5 output order for the fixture. Include a `PAGERANK_SEARCH_WEIGHT = 0` regression assertion that no PageRank list is passed.

**Test:** Run `pytest tests/test_cycle8_pagerank_prefusion.py`.

**AC:** AC29, AC14 verification.

**Threat:** T9 and regression checklist item 24.

**Grep evidence:** `grep -rn "def rrf_fusion" src/kb/query/hybrid.py` → signature is `rrf_fusion(lists: list[list[dict]], k: int = RRF_K)`, so the implementation should pass PageRank as another rank-list, not change this public helper.

## TASK 18 — Contradiction Idempotency Tests

**Files:** `tests/test_cycle8_contradictions_idempotent.py`

**Change:** Use a temp wiki directory and call `_persist_contradictions` twice with identical contradictions and source ref; assert exactly one `## {source_ref} — {today}` header and one claim block. Add the design amendment test: call again with same source ref/date but different claims and assert the second distinct block is present. Include a source ref containing newline/header characters to verify existing `safe_ref` behavior still strips injection.

**Test:** Run `pytest tests/test_cycle8_contradictions_idempotent.py`.

**AC:** AC30 and AC15 verification.

**Threat:** T4 and T5. Confirms race-safe exact-block semantics and avoids broad header-only suppression.

**Grep evidence:** `grep -rn "def append_wiki_log\|def file_lock\|def atomic_text_write" src/kb/utils/io.py src/kb/utils/wiki_log.py` plus `_persist_contradictions` read → the needed `file_lock` and `atomic_text_write` primitives already exist, and `_persist_contradictions` currently uses them inside one lock block.

## Final Verification

Run targeted tests first:

```powershell
pytest tests/test_cycle8_package_exports.py tests/test_cycle8_models_validation.py tests/test_cycle8_llm_telemetry.py tests/test_cycle8_health_wiki_dir.py tests/test_cycle8_consistency_caps.py tests/test_cycle8_pagerank_prefusion.py tests/test_cycle8_contradictions_idempotent.py
```

Then run the Step-11 caller greps:

```powershell
grep -rn -E "WikiPage\(|RawSource\(" tests src/kb
grep -rn -E "from_post\(" src/kb
```

Finally run the broader existing suite subset touched by this cycle: MCP browse/health/quality tests, query engine tests, ingest pipeline tests, and lint semantic tests.
