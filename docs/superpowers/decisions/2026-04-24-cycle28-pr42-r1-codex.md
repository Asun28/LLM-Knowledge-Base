# PR #42 R1 Codex Review — Cycle 28

Review focus: architecture, contracts, integration, correctness. Verdict mode: **PARTIAL-REPORT** for one cosmetic documentation/verification nit only.

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

- **PARTIAL-REPORT:** `docs/superpowers/decisions/2026-04-24-cycle28-security-verify.md:39` records C14 as failed because `grep -n "self-referential" CHANGELOG.md` returns two hits: the format-guide rule at `CHANGELOG.md:18-23` and the cycle-28 Quick Reference phrase at `CHANGELOG.md:54-55`. The rule itself is semantically correct, so this is not a runtime or contract issue; it is only a mechanical grep/documentation drift. Either narrowing the verification grep to the HTML-comment block or rewording the Quick Reference would remove the false failure.

## Checklist

1. **PASS — _ensure_conn post-success ordering.** `self._conn = conn` is assigned before elapsed/counter/log at `src/kb/query/embeddings.py:542-546`, and the failure branch returns at `src/kb/query/embeddings.py:524-537` before that block; no `finally` wraps the instrumentation.

2. **PASS — counter-lock asymmetry documented.** `_sqlite_vec_loads_seen += 1` sits inside the existing `_conn_lock` span at `src/kb/query/embeddings.py:494-545`, while `_bm25_builds_seen += 1` is lock-free at `src/kb/query/bm25.py:143-146`; the getter docstrings document exact/approximate semantics and cycle-25/cycle-26 precedent at `src/kb/query/embeddings.py:113-135` and `src/kb/query/bm25.py:34-53`.

3. **PASS — BM25 full-body bracket.** `self.n_docs = len(documents)` precedes `start = time.perf_counter()` at `src/kb/query/bm25.py:111-114`, and elapsed/logging occur after IDF pre-computation at `src/kb/query/bm25.py:134-146`.

4. **PASS — empty-corpus edge.** `BM25Index([])` still emits the INFO log with `n_docs=0`; test coverage is explicit at `tests/test_cycle28_first_query_observability.py:262-275`.

5. **PASS — revert-divergent failure-path test.** `test_sqlite_vec_load_no_info_on_failure_path` monkeypatches `sqlite_vec.load` to raise, then asserts no success INFO and zero counter delta at `tests/test_cycle28_first_query_observability.py:189-235`; moving the log/counter into a failure-reaching `finally` would flip this test.

6. **PASS — monotonic-delta counter tests.** Counter assertions snapshot a baseline before the action and assert a delta afterward at `tests/test_cycle28_first_query_observability.py:166-170`, `:182-186`, `:209-235`, and `:286-293`; there are no direct `assert get_X() == 1` style assertions.

7. **PASS — boot-lean preserved.** `src/kb/mcp/__init__.py` has no new module-scope `kb.query.embeddings` or `kb.query.bm25` import; the only query import remains function-local inside `main()` at `src/kb/mcp/__init__.py:67-79`.

8. **PASS with cosmetic partial noted above — CHANGELOG commit-count rule.** The HTML-comment rule at `CHANGELOG.md:18-23` accurately states the cycle-26 L1 self-referential `+1` convention and says to increment `<K>` with later PR-review fix commits.

## Additional Concerns

- **VectorIndex.query integration: PASS.** `VectorIndex.query()` calls `_ensure_conn()` once at `src/kb/query/embeddings.py:636-648`; the cached-connection fast path returns before instrumentation at `src/kb/query/embeddings.py:485-499`, and `test_sqlite_vec_load_count_stable_on_fast_path` pins the second-call no-increment behavior at `tests/test_cycle28_first_query_observability.py:173-186`.

- **`time` import placement: PASS.** `_ensure_conn` uses function-local `import time` at `src/kb/query/embeddings.py:502-507`, matching the cycle-26 `_get_model` pattern at `src/kb/query/embeddings.py:401-407`. `bm25.py` uses module-top stdlib `import time` at `src/kb/query/bm25.py:12-16`; this has no boot-lean impact because `kb.mcp.__init__` does not import `kb.query.bm25` at module scope.

- **BM25 serialization/pickling: PASS.** The new counter is module-level, not instance state, and the constructor still only populates ordinary instance fields at `src/kb/query/bm25.py:111-146`. Current BM25 use is in-memory cache storage at `src/kb/query/engine.py:93-122` and `src/kb/query/engine.py:746-803`; no source serialization/pickling path for `BM25Index` was found.

## Verification

Ran focused regression suite:

```text
.venv\Scripts\python -m pytest tests\test_cycle28_first_query_observability.py -q
8 passed in 0.60s
```

Overall verdict: **APPROVE-WITH-PARTIALS**.
