# Cycle 15 — Implementation Plan (Step 7)

**Date:** 2026-04-20
**Author:** Primary session (per cycle-14 L1 heuristic: 26 ACs ≥ 15 AC threshold + operator holds full context).
**Input:** `docs/superpowers/decisions/2026-04-20-cycle15-design-gate.md` (26 surviving ACs).
**Baseline:** 2245 tests.
**Branch:** `feat/backlog-by-file-cycle15` (to be created in TASK 0).

## Grep evidence (Red Flag "Plan uses function X")

All symbols cited below exist in src/ today. Verified before writing plan:

| Symbol | Location |
|---|---|
| `_flag_stale_results` | `src/kb/query/engine.py:366` |
| `_build_query_context` | `src/kb/query/engine.py:636` |
| `_apply_status_boost` | `src/kb/query/engine.py:277` |
| `_PostLike` | `src/kb/query/engine.py:258` |
| `validate_frontmatter` | imported engine.py:27 |
| `normalize_sources` | `src/kb/utils/pages.py:94` |
| `check_staleness` | `src/kb/lint/checks.py:299` |
| `check_frontmatter_staleness` | `src/kb/lint/checks.py:376` |
| `run_all_checks` | `src/kb/lint/runner.py:25` |
| `build_llms_txt` | `src/kb/compile/publish.py:93` |
| `build_llms_full_txt` | `src/kb/compile/publish.py:140` |
| `build_graph_jsonld` | `src/kb/compile/publish.py:216` |
| `atomic_text_write` | `src/kb/utils/io.py:93` |
| `scan_wiki_pages` | `src/kb/utils/pages.py` |
| `load_all_pages` | `src/kb/utils/pages.py:113` |
| `decay_days_for` | `src/kb/config.py:382` |
| `tier1_budget_for` | `src/kb/config.py:350` |
| `STATUS_RANKING_BOOST` | `src/kb/config.py:425` |
| `SOURCE_DECAY_DEFAULT_DAYS` | `src/kb/config.py:379` |
| `kb publish` CLI | `src/kb/cli.py:340` |
| `_is_excluded` (publish epistemic filter) | `src/kb/compile/publish.py:42` |
| Evidence Trail anchor | `src/kb/ingest/evidence.py:96` (`^## Evidence Trail\r?\n`) |

## Task breakdown (7 tasks, 6 source commits + 1 docs commit)

Tasks group by file per `feedback_batch_by_file`. Each TASK ships its regression test(s) in the SAME commit (TDD-pairing per cycle-13 L2 heuristic; all tasks ≤30 LoC source + ≤100 LoC test = inline-in-primary).

### TASK 0 — Branch setup

```
git checkout -b feat/backlog-by-file-cycle15
```

### TASK 1 — `src/kb/config.py` (AC3-constant + AC14 + AC15 + AC16)

**Files:** `src/kb/config.py` + `tests/test_cycle15_config_volatility.py`
**Changes:**
1. Add `AUTHORED_BY_BOOST = 0.02` adjacent to `STATUS_RANKING_BOOST` (line ~425).
2. Add `SOURCE_VOLATILITY_TOPICS: Mapping[str, float]` via `MappingProxyType({k.casefold(): v for k, v in _RAW.items()})` after `SOURCE_DECAY_DAYS` block (line ~380). Requires `from types import MappingProxyType` + `from typing import Mapping` imports (check if present).
3. Add `volatility_multiplier_for(text: str | None) -> float` helper: guard `text is None/empty → 1.0`; truncate `text[:4096]`; iterate keys with `re.search(rf"\b{re.escape(k)}\b", text, re.IGNORECASE)`; return `max(hits, default=1.0)`.
4. Extend `decay_days_for(ref: str, topics: str | None = None) -> int`: when `topics is not None`, compute `mult = volatility_multiplier_for(topics)`; `if not math.isfinite(mult) or mult <= 0: mult = 1.0`; `return max(1, min(int(base_days * mult), SOURCE_DECAY_DEFAULT_DAYS * 50))`. Default `topics=None` preserves backward-compat (no multiplier).

**Imports to add:** `import math`, `from types import MappingProxyType`, `from typing import Mapping` (check).

**Tests (test_cycle15_config_volatility.py):**
- `test_authored_by_boost_constant` — asserts `AUTHORED_BY_BOOST == 0.02`.
- `test_volatility_topics_frozen_mapping` — `isinstance(SOURCE_VOLATILITY_TOPICS, Mapping)` AND `with pytest.raises(TypeError): SOURCE_VOLATILITY_TOPICS["foo"] = 1.5`.
- `test_volatility_topics_casefolded_at_definition` — asserts lookups are case-insensitive (e.g., `"llm" in SOURCE_VOLATILITY_TOPICS`).
- `test_volatility_multiplier_for_basic` — `"LLM agents" → 1.1`; `"Rust systems" → 1.0`; `None → 1.0`; `"" → 1.0`.
- `test_volatility_multiplier_for_word_boundary` — `"reactor"` does NOT fire `react` key (boundary match); `"react-native"` DOES fire.
- `test_volatility_multiplier_for_length_cap_fast` — `volatility_multiplier_for("a"*10_000_000)` returns in < 0.2s AND returns 1.0. Uses `time.monotonic()`.
- `test_volatility_multiplier_for_max_across_keys` — text with both `llm` + `docker` → 1.1 (max).
- `test_decay_days_for_topics_basic` — `decay_days_for("arxiv.org", topics="LLM")` returns `int(1095*1.1) = 1204`.
- `test_decay_days_for_topics_none_backward_compat` — `decay_days_for("arxiv.org")` unchanged (1095).
- `test_decay_days_for_topics_nan_fallback` — monkeypatch `volatility_multiplier_for` to return `float("nan")`; `decay_days_for("arxiv.org", topics="x")` falls back to base (1095).
- `test_decay_days_for_topics_inf_fallback` — same pattern with `inf` → clamped to ceiling or fallback.
- `test_decay_days_for_topics_zero_negative_fallback` — `-1.0` / `0.0` multiplier → fallback to 1.0.
- `test_decay_days_for_topics_clamp_ceiling` — hostile monkeypatched multiplier `1e10` → clamped to `SOURCE_DECAY_DEFAULT_DAYS * 50 = 4500`.

**Commit message:** `feat(cycle15): TASK 1 config — AUTHORED_BY_BOOST + SOURCE_VOLATILITY_TOPICS + volatility_multiplier_for + decay_days_for topics kwarg (AC3/AC14/AC15/AC16)`

### TASK 2 — `src/kb/query/engine.py` (AC1 + AC2 + AC3)

**Files:** `src/kb/query/engine.py` + `tests/test_cycle15_query_decay_wiring.py` + `tests/test_cycle15_query_tier1_wiring.py` + `tests/test_cycle15_authored_by_boost.py`

**Changes:**
1. **AC1 — `_flag_stale_results`:** compose mtime check with decay check. After line 397 (`if newest_source_mtime and newest_source_mtime > page_date:`), add second branch:
   ```python
   if not r["stale"] and sources:
       # AC1 — decay gate alongside mtime gate; stale if EITHER fires
       topics = _compose_topics(r)
       max_decay = max(
           (decay_days_for(str(src), topics=topics) for src in sources),
           default=SOURCE_DECAY_DEFAULT_DAYS,
       )
       if (date.today() - page_date).days > max_decay:
           r["stale"] = True
   ```
   Add module-private helper `_compose_topics(page: dict) -> str` that joins `tags` (list→space-joined, str→pass) + `title`.
   Import `from kb.config import decay_days_for, SOURCE_DECAY_DEFAULT_DAYS` (check — already imported).
2. **AC2 — `_build_query_context`:** replace `CONTEXT_TIER1_BUDGET` references on lines 650+ with `tier1_budget_for("summaries") + tier1_budget_for("details")` OR thread through helper call per tier. Minimal change: `tier1_budget = tier1_budget_for("summaries")` at top of tier-1 loop; reuse for remaining math. Import `from kb.config import tier1_budget_for` (check).
3. **AC3 — `_apply_authored_by_boost`:** after `_apply_status_boost` (line 277), add mirror helper that reconstructs `_PostLike` + calls `validate_frontmatter` + applies `score *= (1 + AUTHORED_BY_BOOST)` when `authored_by in {"human", "hybrid"}` AND validate passes. Pipeline (line 248): insert after `_apply_status_boost` call. Import `from kb.config import AUTHORED_BY_BOOST`.

**Tests:**
- `test_cycle15_query_decay_wiring.py`:
  - `test_flag_stale_results_decay_gate_fires_on_old_github` — 200d-old page, github source mtime synced → flagged via decay (180d window).
  - `test_flag_stale_results_decay_gate_ignores_old_arxiv` — 1000d-old page, arxiv source mtime synced → NOT flagged (1095d window).
  - `test_flag_stale_results_multi_source_lenient_max` — page with both arxiv + github sources 300d old → NOT flagged (lenient max).
  - `test_flag_stale_results_mtime_still_fires` — mtime-vs-page-date fixture unchanged.
  - `test_flag_stale_results_no_sources_skips_decay` — page with empty sources list: no decay flag.
- `test_cycle15_query_tier1_wiring.py`:
  - `test_tier1_budget_for_summaries_called` — monkeypatch `CONTEXT_TIER1_SPLIT["summaries"]` to 10 (of 100), call `_build_query_context` with many summary pages, assert budget shrinks by ~6x vs default split.
- `test_cycle15_authored_by_boost.py`:
  - `test_boost_applied_to_human` — page with valid frontmatter + `authored_by: human` → score multiplied by `1 + AUTHORED_BY_BOOST`.
  - `test_boost_applied_to_hybrid` — same.
  - `test_boost_skipped_on_llm` — `authored_by: llm` → no multiplication.
  - `test_boost_skipped_on_absent` — no `authored_by` → no multiplication.
  - `test_invalid_authored_by_value_no_boost` — `authored_by: robot` → no raise, no boost.
  - `test_invalid_frontmatter_no_boost_T7` — `authored_by: human` + MISSING `source` field (validate_frontmatter error) → no boost.

**Commit message:** `feat(cycle15): TASK 2 query engine — decay gate + tier1 helper + authored_by boost (AC1/AC2/AC3)`

### TASK 3 — `src/kb/lint/checks.py` + `src/kb/lint/runner.py` (AC4 + AC5 + AC6 + AC7)

**Files:** `src/kb/lint/checks.py`, `src/kb/lint/runner.py` + `tests/test_cycle15_lint_decay_wiring.py` + `tests/test_cycle15_lint_status_mature.py` + `tests/test_cycle15_lint_authored_drift.py`

**Changes:**
1. **AC4 — `check_staleness`:** signature change to `max_days: int | None = None`. Body: `if max_days is None: per_page_max = max(decay_days_for(str(s), topics=_compose_topics(page)) for s in page_sources) or SOURCE_DECAY_DEFAULT_DAYS`. Else per_page_max = max_days (caller override). Ensure default-path test compat.
2. **AC5 — `check_status_mature_stale(pages, today=None)`:** new function near `check_frontmatter_staleness` (line 376). `today or date.today()`. Iterate pages with `status == "mature"`, compute `delta = (today - date.fromisoformat(page["updated"])).days`, flag `delta > 90` as `warning`.
3. **AC6 — `check_authored_by_drift(pages)`:** new function. For each page with `authored_by == "human"`: load page body via `load_page_frontmatter(page["path"])` → `post.content`. Locate Evidence Trail span via `re.search(r"^## Evidence Trail\r?\n", body, re.MULTILINE)` — skip if None (no-trail pages never flagged). Find next `^## ` header in `body[span.end():]` (or EOF). Scan that span for `re.search(r"action:\s*ingest", span_text)`. Emit `warning` level issue.
4. **AC7 — `run_all_checks`:** wire both new checks into the `lint/runner.py` check list after `check_frontmatter_staleness`. Plumb `shared_pages` through; append issues to `all_issues`; append check name to `checks_run`.

**Tests:**
- `test_cycle15_lint_decay_wiring.py`:
  - `test_check_staleness_per_source_decay` — arxiv-sourced 1000d page NOT flagged; github-sourced 200d flagged.
  - `test_check_staleness_multi_source_lenient_max` — page with arxiv + github sources 300d → NOT flagged.
  - `test_check_staleness_explicit_max_days_overrides` — `max_days=30` forces all 60d pages flagged regardless of source.
  - `test_check_staleness_no_sources_falls_back_to_default` — page with empty sources falls back to `SOURCE_DECAY_DEFAULT_DAYS`.
- `test_cycle15_lint_status_mature.py`:
  - `test_mature_91d_flagged` — mature page updated 91d ago → `warning` with `check == "status_mature_stale"`.
  - `test_mature_89d_not_flagged`.
  - `test_seed_91d_not_flagged` — status filter.
  - `test_developing_91d_not_flagged`.
  - `test_evergreen_91d_not_flagged` — evergreen pages NOT in scope (test intended behavior).
  - `test_missing_status_not_flagged` — pages without `status` field ignored.
- `test_cycle15_lint_authored_drift.py`:
  - `test_human_page_with_in_scope_ingest_flagged`.
  - `test_hybrid_page_with_in_scope_ingest_not_flagged`.
  - `test_human_page_with_only_edit_action_not_flagged`.
  - `test_human_page_without_evidence_trail_not_flagged` (T5).
  - `test_ingest_action_in_code_fence_above_trail_not_flagged` (T5) — seed a page with `action: ingest` in ```yaml code fence ABOVE the `## Evidence Trail` header; assert NOT flagged.
  - `test_crlf_line_endings_match` — seed Evidence Trail section with `\r\n` endings; anchor still matches.
  - `test_no_trailing_header_span_extends_to_eof` — Evidence Trail followed by EOF (no second `##`); scan still catches in-scope `action: ingest`.
- `test_cycle15_lint_runner_wired`:
  - Integration test: run `run_all_checks` over a tmp_wiki containing (a) a mature-91d page AND (b) a human-drift page; assert both issues surface in the report.

**Commit message:** `feat(cycle15): TASK 3 lint — decay wiring + status_mature_stale + authored_by_drift + runner wire (AC4/AC5/AC6/AC7)`

### TASK 4 — `src/kb/compile/publish.py` (AC9 + AC10 + AC11 + AC12)

**Files:** `src/kb/compile/publish.py` + `tests/test_cycle15_publish_atomic.py` + `tests/test_cycle15_publish_incremental.py`

**Changes:**
1. **AC9 — `build_llms_txt`:** replace `out_path.write_text(...)` with `atomic_text_write(content, out_path)`. Import `from kb.utils.io import atomic_text_write`.
2. **AC10 — `build_llms_full_txt`:** same substitution on final-write line.
3. **AC11 — `build_graph_jsonld`:** replace `json.dump(obj, f, ...)` direct write with `atomic_text_write(json.dumps(obj, ensure_ascii=False, indent=2, sort_keys=True) + "\n", out_path)`. Preserve existing T3/T8 behaviour (dict fully constructed, url is POSIX-relative).
4. **AC12 — incremental + ordering:**
   - New helper `_publish_skip_if_unchanged(wiki_dir: Path, out_path: Path) -> bool` returning True when skip should fire. Implementation: guard `if not out_path.exists(): return False`; compute `max_page_mtime_ns = max((p.stat().st_mtime_ns for p in scan_wiki_pages(wiki_dir)), default=0)`; return `max_page_mtime_ns <= out_path.stat().st_mtime_ns`.
   - Docstring notes single-writer assumption + points to `--no-incremental` escape hatch.
   - Each builder gains `incremental: bool = False` kwarg. Body shape:
     ```python
     def build_llms_txt(wiki_dir, out_path, *, incremental: bool = False):
         # Epistemic filter (T2) + containment already enforced by caller
         pages = load_all_pages(wiki_dir)
         kept, _ = _partition_pages(pages)  # T2 runs BEFORE skip
         if incremental and _publish_skip_if_unchanged(wiki_dir, out_path):
             return out_path
         # ... existing content build + atomic_text_write ...
     ```
   - Ordering invariant: T2 filter happens BEFORE `_publish_skip_if_unchanged` (so a retracted page whose mtime advanced still regenerates with filter applied).

**Tests:**
- `test_cycle15_publish_atomic.py`:
  - `test_build_llms_txt_uses_atomic_text_write` — monkeypatch `kb.compile.publish.atomic_text_write` with spy; assert called exactly once per build.
  - `test_build_llms_full_txt_uses_atomic_text_write` — same.
  - `test_build_graph_jsonld_uses_atomic_text_write` — same (not json.dump direct).
  - `test_build_graph_jsonld_no_tmp_residue_on_replace_failure` (T4) — monkeypatch `kb.utils.io.atomic_text_write` to simulate post-temp-write raise; assert no orphan `.tmp` in `out_path.parent`.
- `test_cycle15_publish_incremental.py`:
  - `test_incremental_true_skips_when_unchanged` — build once; call again with `incremental=True`; spy asserts `atomic_text_write` NOT called second time.
  - `test_incremental_false_regenerates` — build once; call again with `incremental=False`; spy asserts called.
  - `test_incremental_true_mtime_freshen_retriggers` — build once; `os.utime(page, ns=(now_ns, now_ns))`; re-run with `incremental=True`; spy asserts called.
  - `test_incremental_skips_index_file_mutations` — auto-maintained `_sources.md` / `index.md` mtime change does NOT re-trigger build (scan_wiki_pages excludes).
  - `test_incremental_T10c_retracted_page_still_regenerates` — build once; freshen a page AND set `belief_state: retracted`; re-run with `incremental=True`; spy asserts called AND output does NOT contain retracted page (T2 filter ordering).
  - `test_incremental_docstring_mentions_single_writer` — asserts `"single-writer"` or `"single writer"` substring in `_publish_skip_if_unchanged.__doc__`.

**Commit message:** `feat(cycle15): TASK 4 publish — atomic writes + incremental skip helper + ordering invariant (AC9/AC10/AC11/AC12)`

### TASK 5 — `src/kb/cli.py` (AC13)

**Files:** `src/kb/cli.py` + `tests/test_cycle15_cli_incremental.py`

**Changes:**
- `kb publish` command: add Click option `--incremental/--no-incremental` default `True`.
- Plumb `incremental=incremental` into each builder call inside existing `try:` block at lines 391-402. **No change to path-validation code at lines 369-388**.
- Grep-verify source-order: `[369: containment check] → [389: mkdir] → [391: try: ... builders(incremental=)]`.

**Tests (test_cycle15_cli_incremental.py):**
- `test_publish_default_is_incremental` — `runner.invoke(publish, ["--out-dir", tmp])` → builders called with `incremental=True` (spy monkeypatch on module-level builders).
- `test_publish_no_incremental_disables` — `["--no-incremental"]` → builders called with `incremental=False`.
- `test_no_incremental_containment_preserved` (T8) — `["--out-dir", "/tmp/outside-project", "--no-incremental"]` → `UsageError` raised BEFORE any `mkdir`; tmp outside-project dir not created.
- Use `result.output` instead of `capsys` (R2 flakiness guard).

**Commit message:** `feat(cycle15): TASK 5 CLI — kb publish --incremental/--no-incremental flag + containment regression (AC13)`

### TASK 6 — cycle-14 contract regression test (AC32)

**Files:** `tests/test_cycle15_load_all_pages_fields.py`

**Changes:** test-only. Asserts `load_all_pages` returns both `authored_by` AND `belief_state` keys with empty-string default when frontmatter omits them, and with correct value when present.

- `test_load_all_pages_emits_authored_by_when_present`
- `test_load_all_pages_emits_belief_state_when_present`
- `test_load_all_pages_emits_empty_string_when_absent`
- `test_load_all_pages_preserves_cycle14_status_key` — regression guard that cycle-14's `status` key isn't regressed.

**Commit message:** `test(cycle15): TASK 6 cycle-14 contract regression test for load_all_pages additive keys (AC32)`

### TASK 7 — Docs (Step 12 output, landed at cycle close)

**Files:** `CHANGELOG.md`, `BACKLOG.md`, `CLAUDE.md`
Handled by Codex subagent at Step 12 per pipeline. Includes T10c operator note + AC8-carry BACKLOG entry + deleted closed items.

## Threat-mitigation cross-check

| Threat | Mitigating AC(s) | Verification anchor |
|---|---|---|
| T1 (volatility ReDoS / metachar) | AC15 | `test_volatility_multiplier_for_length_cap_fast`, `test_volatility_multiplier_for_word_boundary` |
| T2 (decay overflow/NaN) | AC16 | `test_decay_days_for_topics_{nan,inf,zero,clamp}_fallback` |
| T3 (incremental TOCTOU) | AC12 | docstring assertion test |
| T4 (JSON-LD cross-volume) | AC11 | `test_build_graph_jsonld_no_tmp_residue_on_replace_failure` |
| T5 (Evidence Trail false positive) | AC6 | `test_ingest_action_in_code_fence_above_trail_not_flagged` + `test_human_page_without_evidence_trail_not_flagged` |
| T6 (cache_clear over-invalidation) | AC17 DROPPED | closed by Q3 |
| T7 (authored_by boost gate) | AC3 | `test_invalid_frontmatter_no_boost_T7` |
| T8 (CLI flag bypass) | AC13 | `test_no_incremental_containment_preserved` |
| T9 (additive keys leak) | AC32 regression | `test_load_all_pages_emits_*` |
| T10a-d (residual) | closed-via-design | no new surface |

## Test count projection

Baseline 2245 → +38 new tests (~38 = 12 config + 6 query-decay + 1 tier1 + 6 authored-by-boost + 4 lint-decay + 6 lint-mature + 7 lint-drift + 1 runner-wire + 4 publish-atomic + 6 publish-incremental + 3 CLI + 4 contract-regression — some overlap via parametrize; actual delta ≈ 35-45). Expected total: **~2280 ± 5**.

## Dependency ordering (critical for Step 9)

- TASK 1 (config.py) MUST land before TASK 2 (engine.py uses `AUTHORED_BY_BOOST`) and TASK 3 (checks.py uses `decay_days_for(topics=)`).
- TASK 2 + TASK 3 parallel-safe (no shared code path).
- TASK 4 (publish.py) independent of TASK 2/3.
- TASK 5 (cli.py) depends on TASK 4 (builders accept `incremental=` kwarg).
- TASK 6 (regression test) independent.
- TASK 7 (docs) last.

Linear order: 1 → 2 → 3 → 4 → 5 → 6 → 7.

## Rollback strategy

Each TASK commit is atomic (source + tests + any needed import). A revert of any single TASK commit leaves main in a consistent state. No cross-task monkeypatches or shared mutable state.

## PLAN-AMENDS-DESIGN check

Plan does not amend the design gate. All 26 surviving ACs from design-gate flow through tasks above. No reordering of constraints; TASK 1 → TASK 2 → TASK 3 dependency is implicit in the AC grep evidence (TASK 2 imports `AUTHORED_BY_BOOST` defined in TASK 1) — not a design change.
