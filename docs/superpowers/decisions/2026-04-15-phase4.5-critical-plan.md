# Step 2.5 Opus Plan Gate — Phase 4.5 CRITICAL fixes

**Date:** 2026-04-15
**Feature:** Phase 4.5 CRITICAL bug-fix bundle (Themes 1–4, 16 items)
**Gate:** feature-dev Step 2.5 Opus plan decision & argumentation
**Sub-agent model:** Opus
**Verdict:** `APPROVE-WITH-CONDITIONS`
**PLAN-AMENDS-DESIGN:** no (design remains stable)

## Summary

Plan is structurally sound; 7 amendments required to correct ordering (docs AFTER verification, not before), per-task Codex fan-out (all 6 tasks, not just Task 1), security subagent type identifier, Task 7 commit big-bang replaced by incremental per-task commits, PR draft-first default workflow, and error-chain audit decision trail.

## Decisions

### Q1 — Commit timing: `INCREMENTAL per-task commits` (HIGH) — OVERRIDES big-bang Task 7
`git add <file>` stages cumulative delta when multiple themes touch same file (`pipeline.py` edited by items 6 + 14 + 18). Big-bang Task 7 creates genuine correctness bug: impossible to separate themes by file. Incremental commits are lower blast radius and enforce scope-gate C1 ("Theme 1 ships FIRST commit") mechanically.

### Q2 — Docs commit placement: `AFTER verification` (HIGH) — REORDERING
feature-dev Step 4c canonical ordering is docs AFTER security AFTER Codex. Current plan had docs BEFORE verification, so Codex/security blockers require amending docs commit. Move docs to post-verification.

### Q3 — PR draft-first specificity: `TIGHTEN — draft is default` (MEDIUM)
feature-dev Step 5 has detailed 6-step draft-first procedure. Current plan's "if still pending: --draft" is a one-liner fallback. Invert: `--draft` default, flip to ready after all gates pass.

### Q4 — Subagent parallelism: `KEEP SERIAL` (HIGH) — confirmed from design
Design Q11 already decided serial due to pipeline.py triple-touch. No re-litigation.

### Q5 — Test-file discovery guidance: `KEEP "grep first" as-is` (MEDIUM)
Plan's candidate list + OR alternatives + grep-first instruction is adequate. Subagent-driven-dev handles uncertainty via TDD cycle.

### Q6 — Slugify fallback: `KEEP `untitled-<hash6>`` (HIGH)
Silent graceful degradation preserves upstream extraction work. Hash on `text_lower` (content-specific) gives 16M-collision safety. Security review checklist item 1 covers collision audit.

### Q7 — Audit guardrails (Task 6): `TIGHTEN — require per-site decision log in commit body` (MEDIUM)
Current "ONLY add where chained" is good guidance; adding explicit per-site decision (SKIPPED/ADDED + reason) in commit body gives audit traceability at `git log` time.

### Q8 — Item 8 test verifies correct fix approach: `ADEQUATE` (HIGH)
Test asserts the INVARIANT (`before_nodes == after_nodes`), not the implementation. Both `graph.copy()` and "return-augmentations-separately" fixes satisfy the contract. Test is correct to stay implementation-agnostic.

### Q9 — Per-task Codex for all 6 tasks: `MANDATORY — ADD` (HIGH) — PLAN BUG
feature-dev Step 4a + design Q7 both specify per-task Codex. Plan has Codex only after Task 1. Outright omission — add to Tasks 2, 3, 4, 5, 6.

### Q10 — Security subagent type: `everything-claude-code:security-review` (HIGH) — PLAN BUG
Plan used `security-scanning:security-auditor` which is NOT a valid subagent type. Canonical per feature-dev Step 4b is `everything-claude-code:security-review`.

### Q11 (surfaced) — Task 7 file-add granularity: `REJECT big-bang; incremental` (HIGH)
Reinforces Q1. `git add src/kb/ingest/pipeline.py` in Task 7.2 would stage items 14 and 18 under Theme 2's commit message. Fixed by incremental task-scoped commits.

### Q12 (surfaced) — BACKLOG HIGH tracker: `No amendment needed` (HIGH)
Phase 4.5 HIGH `_is_duplicate_content` race is already tracked; item 14 TODO is an inline callout, not a new tracker.

### Q13 (surfaced) — item 16 `anthropic.APIStatusError` constructor: `subagent-verifies` (MEDIUM)
Plan already notes "subagent should verify anthropic import path." Guardrail exists.

### Q14 (surfaced) — item 13 `contextmanager` import placement: `MINOR — subagent handles` (LOW)
Plan comment `# at top of file` embedded in function is reminder text; subagent reads it via TDD red-green.

## Conditions

- **C1:** Task 7 DELETED. Each theme commits INCREMENTALLY at end of its subagent task. Task 1 → Theme 1 commit; Task 3 → Theme 2 commit; Task 5 → Theme 3 commit; Task 6 → Theme 4 commit. Task 2 and Task 4 stage only (for upcoming theme commits).
- **C2:** Docs commit (CHANGELOG + BACKLOG) moves AFTER verification. New ordering: Tasks 1-6 (each with commits + Codex) → Task 7 Verification → Task 8 Docs → Task 9 PR.
- **C3:** Add per-task Codex review steps to Tasks 2, 3, 4, 5, 6. All use `Agent(run_in_background=true, ...)` pattern matching existing Task 1 Step 1.9. Each task waits for its Codex to return clean before advancing.
- **C4:** Task 7 (verification) security review must use `subagent_type="everything-claude-code:security-review"` (not `security-scanning:security-auditor`).
- **C5:** Task 9 (PR open) defaults to `gh pr create --draft`. Flip to `gh pr ready <PR>` only after Codex (branch-level) AND security review return clean.
- **C6:** Task 6 Step 6.2.4 audit MUST produce per-site decision log in Theme 4 commit body (SKIPPED/ADDED + reason per site).
- **C7:** Split-bundle item placement:
  - item 8 (`lint/checks.py`) → Theme 1 commit at end of Task 1
  - item 9 (`lint/runner.py`) → Theme 2 commit at end of Task 3
  - item 10 (`review/refiner.py` line 111) → Theme 2 commit at end of Task 3
  - item 13 (`review/refiner.py` line 13 + 120-133) → Theme 3 commit at end of Task 5

## Escalations

None.

## Amendments

7 amendments applied to plan file. See `docs/superpowers/plans/2026-04-15-phase4.5-critical-fixes.md` — full rewrite with all amendments integrated.
