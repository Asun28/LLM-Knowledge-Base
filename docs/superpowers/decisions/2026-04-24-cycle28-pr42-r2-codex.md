## R1 fix verification (M1 + M2 status with IMPLEMENTED/PARTIAL/MISSING)

Item 1 - IMPLEMENTED. In `src/kb/query/embeddings.py`, the success path now computes `elapsed = time.perf_counter() - start` before `self._conn = conn`, then increments `_sqlite_vec_loads_seen` and emits `logger.info(...)` after `self._conn = conn`. This matches design C1 while preserving C4.

Item 2 - IMPLEMENTED. `test_sqlite_vec_load_count_stable_on_fast_path` now snapshots `baseline` before the first `_ensure_conn()` call, asserts `after_first == 1`, then asserts the second cached call returns the same connection and leaves `after_second == 1`. Removing `_sqlite_vec_loads_seen += 1` would make `after_first` equal 0 and fail.

Item 4 - IMPLEMENTED. `git rev-list --count main..HEAD` and `origin/main..HEAD` both report 5 commits before this R2 doc. Commit d9053c4 changes the CHANGELOG count from 4 to 6, accounting for the R1 fix commit plus the expected R2 verification-doc commit, so `6 commits` is correct.

Item 7 - IMPLEMENTED. With `.venv\Scripts` prepended to PATH, `pytest --collect-only -q tests/test_cycle28_first_query_observability.py` reports 8 collected tests, and `ruff check src/ tests/ --select E,F,I,W,UP` reports `All checks passed!`. The unqualified commands were not initially on PATH in this PowerShell session; the same command names pass once the project venv is on PATH.

## Regression check (items 3, 5)

Item 3 - IMPLEMENTED. The M2 restructure is local to test 5 and does not introduce sibling shared-state or fixture-order hazards. Tests 1, 2, 3, 4, and 6 each build their own `tmp_path` sqlite-vec DB and use scoped `caplog` / `monkeypatch` fixtures. Counter checks remain monotonic-delta based, so prior test increments do not affect assertions. Tests 7 and 8 use independent BM25 caplog/baseline state. No `importlib.reload(...)` call appears in the file.

Item 5 - IMPLEMENTED. The `_ensure_conn` instrumentation remains a straight success-path sequence after the `try/except` block. There is no `finally:` wrapping the perf/log/counter triple; the only real `finally` in `embeddings.py` is in `build()`, outside `_ensure_conn`. The C1 elapsed-before-assignment swap does not weaken C4 or C9.

## Carry-forward NITs acknowledgement (item 6)

Item 6 - IMPLEMENTED. `git show d9053c4` documents R1 Sonnet N1-N5 explicitly under "Carried as cosmetic NITs not fixed in-cycle". The review trail also preserves them in `docs/superpowers/decisions/2026-04-24-cycle28-pr42-r1-sonnet.md` under the NIT section. They were acknowledged, not silently dropped.

## Overall verdict (APPROVE or REQUEST-CHANGES with brief rationale)

APPROVE. Both R1 major findings are implemented, sibling regression risk is clean, C1/C4/C9 ordering remains intact, CHANGELOG arithmetic is correct for the expected R2 doc commit, carry-forward NITs are documented, and the focused collect-only plus ruff checks pass.
