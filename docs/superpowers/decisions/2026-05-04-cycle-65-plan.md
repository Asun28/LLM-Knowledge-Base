# Cycle 65 — Implementation Plan (Step 7 — Opus expanded per HANDOFF Option C)

**Cycle:** 65
**Step:** 7 — Implementation Plan
**Date:** 2026-05-04
**Branch:** `feat/cycle-65`
**Pipeline:** `dev-mimo-opus` (May 2026 MiMo trial)
**Authoring history:** v1 was a 90-line summary written by `mimocoding-rescue` in 8.2 min (committed `c0a8020`). It mis-batched AC2 with the config commit (AC2 lives in `kb/utils/llm.py`, not `config.py`) and collapsed design.md's 21 dependency-ordered commits to 12. Per HANDOFF Option C and Red Flag "Plan gate REJECTed — must be real gaps" (cycle-6 L4), Opus expanded the plan inline against `2026-05-04-cycle-65-design.md` so the Step 8 plan gate ingests the full body, not a paraphrase.

---

## Inputs (source-of-truth)

| Doc | Authority | Used for |
|-----|-----------|----------|
| `2026-05-04-cycle-65-design.md` | LOCKED — Step 5 Opus subagent verdict APPROVE | All AC contracts, Q2.1-Q2.10 decisions, signature-drift watchlist, same-class peer scan, commit ordering |
| `2026-05-04-cycle-65-threat-model.md` | LOCKED — Step 2 Opus subagent | T1-T21 threats, C1-C23 conditions (each is a load-bearing test requirement per cycle-22 L5) |
| `2026-05-04-cycle-65-requirements.md` | LOCKED — Step 1 (with BACKLOG drift corrections AC2 + AC12 — requirements doc wins over threat model where they conflict) | 23 ACs, Tier 2 classification, OOS-1..10 |
| `CLAUDE.md` | LIVE | Project conventions, fixture rules, Quick Reference numbers |

The plan does NOT duplicate AC contracts from `design.md` — the gate reads both files as a pair. Each commit row below references the design AC and adds the operational detail the design intentionally leaves to Step 7 (commit ordering, named test files, sub-AC split per cycle-9 L1 dual-mechanism rule).

---

## Decision summary

- **Tier:** 2 (standard feature; auto-approve gates per `feedback_auto_approve`).
- **Commits:** **21** (foundation + 20 ACs grouped per design.md lines 432-453). The 12-commit batching from the v1 summary is REJECTED — AC2's `kb/utils/llm.py` location forbids the AC1+AC2+AC3 bundle, and dependency edges (AC1→AC9, AC9→AC10, AC9→AC23, AC14→AC21, AC3→AC12) require the granular ordering.
- **ACs:** 23 (AC1-AC23). All 23 mapped 1:1 to ≥1 named test function (30 named tests total, some conditions split into structural+behavioural per cycle-9 L1).
- **Conditions:** C1-C23 (+ C4-bis, C22-bis split per design.md lines 297-326). Plan-gate verifies the 1:1 condition-to-test traceability matrix.
- **Signature-drift checkpoints:** **8** explicit caller-grep checkpoints inserted AFTER specific commits (see § Step 11 caller-grep table).
- **NEW underscore-private functions:** 12 (per design.md § Signature drift watchlist NEW functions list). Public API surface NOT expanded.
- **Test count estimate:** 30 named test functions across 12 new test files + 1 augmented (`tests/test_cycle64_snapshots.py` for AC19 negative controls). Cycle-22 L3: full pytest suite must remain green; touched-file coverage ≥90%.
- **Foundation commit:** `tests/_helpers/ast_walk.py` lands FIRST per design.md line 432. No commit may import `ast_walk` until it exists.

---

## Foundation commit (commit #1)

**Title:** `test(cycle 65): foundation — tests/_helpers/ast_walk.py for AST meta-tests`

**Files added:**
- `tests/_helpers/ast_walk.py` (NEW) — exports four helpers consumed by AC4, AC17, AC18, AC20, AC23.
- `tests/_helpers/__init__.py` — already exists; no change needed if it's already a package marker (verified via Glob: `tests/_helpers/__init__.py` and `api_key.py` already present).

**Helper API (from design.md § NEW functions):**
```python
# tests/_helpers/ast_walk.py
def find_imports_from(module: str, name: str) -> list[Path]: ...
def find_function_def(file_path: Path, name: str) -> ast.FunctionDef | None: ...
def find_calls_of(file_paths: list[Path], qualified_name: str) -> list[tuple[Path, int]]: ...
def assert_decorator_present(func_def: ast.FunctionDef, expected_decorator: str, source_path: Path) -> None: ...
```

`assert_decorator_present` (per R2 D3 ADOPT) prints expected vs. actual + line number on failure — used by AC4 + AC23 for legible AST failure messages.

**Tests in this commit:** unit tests for each helper in `tests/_helpers/test_ast_walk.py` (≥6 functions: 1 happy-path + 1 missing-symbol + 1 multi-file scan per helper). Self-tests prevent the helpers from being silently broken in later commits.

**No production import of these helpers exists yet.** The commit is safe to land in isolation; it's only consumed starting at commit #2 (AC18).

---

## 21-commit walkthrough

Each row: **(idx) AC — file(s) — action — named tests added — sig-drift checkpoint?**

| # | AC | File(s) | Action | Named tests | Sig-drift? |
|---|----|---------|--------|-------------|------------|
| 1 | (foundation) | `tests/_helpers/ast_walk.py` | NEW helpers + self-tests | `tests/_helpers/test_ast_walk.py::test_*` (≥6) | — |
| 2 | AC18 | NEW `tests/test_security_cve_greps.py` | Python-`re` scan over `src/kb/**/*.py` for `diskcache`, `litellm`, `pip`, `ragas` imports → assert zero hits per package. Failure message names file path + matched line. | `test_diskcache_zero_imports`, `test_litellm_zero_imports`, `test_pip_zero_imports`, `test_ragas_zero_imports` (4 named, NOT parametrized) | — |
| 3 | AC11 | `requirements.txt` (line 82); NEW `tests/test_cycle65_dep_pinning.py` | Edit `GitPython>=3.1.47` → `GitPython>=3.1.47,<3.2`. Test reads `requirements.txt` and asserts both floor + ceiling pattern. | `test_gitpython_has_floor_and_ceiling` | — |
| 4 | AC15 | `src/kb/lint/fetcher.py` (module top); NEW `tests/test_cycle65_trafilatura_cache_disabled.py` | Add `os.environ.setdefault("TRAFILATURA_DOWNLOAD_NO_CACHE", "1")` ABOVE the trafilatura import. Two-stage test (env present + spy observation). | `test_no_cache_env_set`, `test_fetch_url_observes_no_cache` | — |
| 5 | AC1 | `src/kb/config.py` (lines 48-180 internal use stays; new accessor + PEP 562 shim); NEW `tests/test_cycle65_config_call_time.py` | Add `def get_project_root() -> Path` (call-time `os.environ.get("KB_PROJECT_ROOT")` + heuristic fallback). Add `def _reset_project_root() -> None` test helper. Add `def __getattr__(name)` PEP 562 module shim re-routing `kb.config.PROJECT_ROOT` → `get_project_root()`. KEEP module-level `PROJECT_ROOT = _resolve_project_root()` as initial value (the shim takes over on attribute access). | `test_kb_project_root_call_time` (C1) | **YES** — `kb.config.PROJECT_ROOT` is the load-bearing constant (200+ test refs). Step 11 grep AFTER this commit. |
| 6 | AC2 | `src/kb/utils/llm.py` (lines 17, 69-71); NEW `tests/test_config_no_direct_model_tiers.py` | Edit line 17: `from kb.config import MODEL_TIERS` → `from kb.config import get_model_tier`. Edit lines 69-71: replace `if tier not in MODEL_TIERS: ... return MODEL_TIERS[tier]` with `try: return get_model_tier(tier) except ValueError as exc: raise ValueError(...) from exc`. AST-walk test (using `find_imports_from`) over `src/kb/**/*.py` excluding `config.py` + the two versioned test files (`test_v099_phase39.py` + `test_v0912_phase393.py`). KEEP `kb.config.MODEL_TIERS` and `_DEFAULT_MODEL_TIERS` (Q2.1 lock). | `test_no_direct_model_tiers_imports` (C3) | **YES** — Step 11 grep `MODEL_TIERS\[` outside config.py. |
| 7 | AC3 | `src/kb/config.py` (lines 478-480 region); migrate `lint/augment/orchestrator.py:214,234` + `lint/augment/proposer.py:78,105` | Add `def get_allowed_domains() -> tuple[str, ...]` reading `os.getenv("KB_AUGMENT_ALLOWED_DOMAINS") or os.getenv("AUGMENT_ALLOWED_DOMAINS") or "en.wikipedia.org,arxiv.org"` (DUAL env-var name per design lock — current source uses unprefixed `AUGMENT_ALLOWED_DOMAINS`; KB_-prefixed alias added for namespace hygiene). Extend the AC1 PEP 562 `__getattr__` shim to back-compat-route `kb.config.AUGMENT_ALLOWED_DOMAINS` → `get_allowed_domains()`. Migrate four call sites. | `test_allowed_domains_call_time` parametrized over both env-var names (C2) | **YES** — Step 11 grep `kb.config.AUGMENT_ALLOWED_DOMAINS` zero outside config.py + the test. Verified callers: 4 sites. |
| 8 | AC4 + AC5 | `tests/conftest.py` (lines 333-347 region); NEW `tests/test_conftest_sandbox_guard.py` | AC4: NO production change (the autouse fixture from cycle 64 AC1 already exists per CLAUDE.md Quick Reference; AC4 ADDS the meta-test asserting it stays autouse). AC5: replace hardcoded `[load_purpose, _load_template_cached, _build_schema_cached]` list at conftest.py:333-347 with `sys.modules` walk filter `name.startswith("kb.")` + iterate `vars(mod)` + `cache_clear` if callable; wrap each attribute in `try/except` for exotic objects. | `test_autouse_decorator_present_structural` (C4) — uses `find_function_def` + `assert_decorator_present`; `test_autouse_decorator_redirects_paths_behavioural` (C4-bis) — uses `pytester` fixture; `test_lru_cache_walk_clears_kb_modules` (C5) — stubs `kb._test_path_sensitive_module` with `@lru_cache`, runs sandbox teardown via fixture finalizer, asserts `cache_info().currsize == 0`. | — |
| 9 | AC6 + AC7 + AC8 | `src/kb/mcp/app.py` (line 230 `_validate_page_id`); NEW `tests/test_cycle65_validate_page_id.py` | All three rules added inside `_validate_page_id` body, BEFORE the existing `resolve()` call (so rejections fire before OS normalization can mask). AC6 — segment-by-segment `seg != seg.rstrip(". ")` check. AC7 — `_WINDOWS_ILLEGAL_CHARS_RE = re.compile(r'[:<>"|?*]')` near `_CTRL_CHARS_RE`; reject any match. AC8 — replace substring `".." in page_id` with `any(seg == ".." for seg in page_id.replace("\\", "/").split("/"))`. KEEP existing `resolve().relative_to(WIKI_DIR)` containment net. Docstring update naming each rejected pattern + Windows rationale (per R2 D4 ADOPT). | `test_ac6_rejects_trailing_dot_or_space` parametrized 4 cases (C6); `test_ac7_rejects_windows_illegal_chars` parametrized 7 cases (C7); `test_ac8_segment_aware_dotdot_match` 1 negative-control + 2 positive (`"notes..draft"` ACCEPTED, `"foo/../bar"` REJECTED) — the AC8 containment behaviour is also covered by AC23's same-class peer test (C8). | **YES** — `_validate_page_id` keeps signature; Step 11 grep `_validate_page_id(` SAME count pre+post (9 sites). |
| 10 | AC9 | NEW `src/kb/utils/path_safety.py`; migrate `mcp/app.py:121 _validate_wiki_dir`, `mcp/app.py:230 _validate_page_id` containment, `compile/compiler.py:645 _validate_path_under_project_root` | Create `src/kb/utils/path_safety.py` exporting `_assert_under_project_root(path: Path, field_name: str, *, require_exists: bool = False, require_dir: bool = False, dual_anchor: bool = True, allow_symlinks: bool = False) -> None`. **Hard cap of 4 keyword-only params per Q2.2** — adding a 5th requires a Step 5 follow-up gate. Body: dual literal-and-resolved anchor under `kb.config.get_project_root()` (call-time, depends on commit #5 AC1); optional existence + isdir + symlink checks. Migrate three sites: `_validate_wiki_dir` body becomes `_assert_under_project_root(wiki_dir, "wiki_dir", require_exists=True, require_dir=True); return wiki_dir.resolve(), None`. `_validate_path_under_project_root` body becomes `_assert_under_project_root(path, field_name)`. `_validate_page_id` containment portion delegates (char rules from AC6/AC7/AC8 stay inline above). All three migrations preserve their EXISTING signatures per Step 11 sig-drift watchlist. | (no NEW test file in this commit — AC23's `test_validator_contract_consolidation.py` lands in commit #12) | **YES** — three signature-keep migrations. Step 11 greps for each: `_validate_wiki_dir(`, `_validate_path_under_project_root(`, `_validate_page_id(` SAME pre+post counts. |
| 11 | AC10 | `src/kb/compile/compiler.py` (lines 720-770 region inside `rebuild_indexes`); `src/kb/utils/path_safety.py` (extend with `_open_no_follow`); NEW `tests/test_cycle65_rebuild_indexes_toctou.py` | Add `_open_no_follow(path: Path) -> int` to `path_safety.py` — primary path uses `os.open(path, os.O_NOFOLLOW \| os.O_RDONLY)` (POSIX) / Windows `FILE_FLAG_OPEN_REPARSE_POINT` via `ctypes.windll.kernel32.CreateFileW` for parity. Fallback path (Q2.3 Option B) re-resolves immediately before mutate + ONE-TIME per-process `logger.warning("AC10: O_NOFOLLOW unsupported on platform %s; falling back to re-resolve TOCTOU mitigation", sys.platform)`. Replace direct `manifest_path.unlink()` etc. inside `rebuild_indexes` with helper-mediated open+unlink. Docstring documents fallback contract (R2 D1 deferred to Step 17 doc adds `docs/reference/error-handling.md` "Path safety" section). | `test_symlink_swap_rejected_primary` (C9 primary path), `test_symlink_swap_rejected_fallback` (Q2.3 fallback via `monkeypatch` simulating `O_NOFOLLOW` unavailability) | **YES** — Step 11 grep that all `unlink(` calls inside `rebuild_indexes` now route through `_open_no_follow` or its fallback (≥3 mutator sites at lines 720-770). |
| 12 | AC23 | NEW `tests/test_validator_contract_consolidation.py` | AST-walk using `find_calls_of` for `Call(func=Name(id="_assert_under_project_root"))` AND `Call(func=Attribute(attr="_assert_under_project_root"))`. Assert THREE historical sites PRESENT: (1) `mcp/app.py` `_validate_wiki_dir`, (2) `mcp/app.py` `_validate_page_id` containment portion, (3) `compile/compiler.py` `_validate_path_under_project_root`. Q2.9 Option B: additional callers ALLOWED. Failure message: `"AC9 historical site missing from migration: %s"`. | `test_three_historical_sites_present` (C8) | — |
| 13 | AC12 | `src/kb/lint/fetcher.py` (line 232 region); NEW `tests/test_cycle65_url_scheme.py` | Add `def _url_scheme_allowed(url: str) -> bool: return urlparse(url).scheme in {"http", "https"}` next to `_url_is_allowed`. Modify `_url_is_allowed` to call `_url_scheme_allowed(url)` first and return False on failure — KEEP `_url_is_allowed` signature. Reuse existing `SafeBackend` for RFC1918/loopback/link-local + DNS-rebind. Tests: parametrized 5-case scheme rejection (`file://`, `gopher://`, `data://`, `javascript://`, `ftp://`), DNS-rebind acceptance test confirming existing SafeBackend still catches rebind via `monkeypatch.setattr("socket.gethostbyname", ...)`. | `test_ac12_rejects_non_http_schemes` parametrized 5 cases (C11); `test_ac12_rejects_private_loopback_link_local` parametrized 8 cases (C10); `test_ac12_dns_rebind_rejected_via_safebackend` (C12) | **YES** — Step 11 grep `_url_is_allowed(` SAME count (5 sites). |
| 14 | AC13 | `src/kb/query/embeddings.py` (`VectorIndex.build`); NEW `tests/test_cycle65_vector_build_multiprocess.py` | Wrap DROP→CREATE→INSERT→COMMIT block in `VectorIndex.build` with `kb.utils.io.file_lock(db_path.with_suffix(".db.lock"))`. Existing in-process `_rebuild_lock = threading.Lock()` (line 29) STAYS — file lock is the multi-process layer, threading lock is the in-process layer. Document acquire order in module docstring. | `test_concurrent_build_serialised_via_file_lock` (C20) — spawn two `multiprocessing.Process` instances calling `VectorIndex.build`, assert `time.monotonic()` shows second waits on first AND post-condition dim consistency. | — |
| 15 | AC14 | `src/kb/query/embeddings.py` (`VectorIndex._ensure_conn`); NEW `tests/test_cycle65_sqlite_vec_error_sanitised.py` | Wrap `sqlite_vec.load(conn)` in `try/except sqlite3.OperationalError as exc: raise RuntimeError("sqlite-vec extension failed to load; reinstall the sqlite-vec wheel") from exc`. AC21 boundary in MCP further sanitizes via `sanitize_error_text` from `kb.utils.sanitize`. | `test_sqlite_vec_load_error_no_path` — `monkeypatch.setattr(sqlite_vec, "load", lambda c: ...raise OperationalError("/home/user/.../vec0.so: ...")...); assert "/" not in caught_message and ".so" not in caught_message and "site-packages" not in caught_message` (C16). | — |
| 16 | AC16 | `src/kb/utils/cli_backend.py` (line 128 region); NEW `tests/test_cycle65_check_no_secrets.py` | DELETE the existing regex-based detection. Iterate over six env vars — `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `FIRECRAWL_API_KEY`, `DEEPSEEK_API_KEY`, `MIMOCODING_API_KEY`, `MIMOCHAT_API_KEY` — for each non-empty value, iterate argv, if any element equals the value via `secrets.compare_digest` raise the existing `LLMError`. KEEP `_check_no_secrets_on_argv` signature. | `test_legitimate_token_format_discussion_allowed` (positive prong: argv contains a valid-looking but FAKE token `"sk-ant-api03-AAAA..."` → no error); `test_actual_env_value_blocked` (negative prong: `monkeypatch.setenv("ANTHROPIC_API_KEY", "test-fake-value-not-real")` then argv contains that exact string → LLMError raised). Q2.10 lock: BOTH tests use `monkeypatch.setenv` exclusively, NEVER `os.environ.get(...)`. (C18) | **YES** — Step 11 grep `_check_no_secrets_on_argv\b` SAME count (2 sites at cli_backend.py:178,185). |
| 17 | AC17 | `src/kb/graph/cache.py` (top of module); NEW `tests/test_graph_cache_no_direct_imports.py` | Add `__all__ = []` at top of `kb/graph/cache.py`. AST-walk using `find_imports_from(module="kb.graph.cache", name="get_graph")` over `src/kb/**/*.py`; assert ZERO matches. Note: per CLAUDE.md cycle-18 L1, callers should already use `kb.graph.cache.get_graph(...)` attribute-lookup form — AC17 codifies that discipline. | `test_all_callers_use_attribute_lookup_form` (C21) | **YES** — Step 11 grep `from kb.graph.cache import get_graph` returns ZERO. |
| 18 | AC21 | NEW `src/kb/mcp/_error_boundary.py`; modify `src/kb/mcp/core.py`, `src/kb/mcp/ingest.py`, `src/kb/mcp/quality.py`; NEW `tests/test_cycle65_mcp_error_boundary.py` | New module `src/kb/mcp/_error_boundary.py` exporting `_mcp_error_boundary` decorator. Body: `try/except Exception as exc: logging.getLogger("kb.mcp").exception(...); return f"Error: {sanitize_error_text(exc)}"`. Import `sanitize_error_text` from `kb.utils.sanitize` (NOT `kb.utils.text` — drift in threat model corrected at design lock). Apply `@_mcp_error_boundary` BELOW each `@mcp.tool()` in the three target files. NOT applied to `mcp/browse.py` (already uses sanitize_error_text per OOS-3), NOT to `mcp/compile.py` or `mcp/health.py` (OOS-3). Naming kept `_mcp_error_boundary` (R2 D10 REJECT — naming consistency with cycle 18 AC13 vocabulary). | `test_<tool_name>_error_sanitised` parametrized over each `@mcp.tool` in core/ingest/quality (C17). Force exception inside body via `monkeypatch`; assert response is `"Error: <sanitized>"`. | **YES** — Step 11 dual-grep checkpoint: `Grep "@mcp.tool" mcp/{core,ingest,quality}.py` count == `Grep "@_mcp_error_boundary" mcp/{core,ingest,quality}.py` count. |
| 19 | AC19 | `tests/test_cycle64_snapshots.py`; `.github/workflows/ci.yml`; NEW `tests/test_cycle65_ci_snapshot_flags.py` | For each cycle-64 snapshot subject (evidence-trail, Mermaid export, lint-report-structure), ADD a paired `_neg_control` test mutating one input field of the captured-from path AND asserting `snapshot != actual`. Update CI yml: REMOVE any `--snapshot-update` flag from pytest invocations; ADD `--snapshot-warn-unused`. Test asserts CI yml contents. | `test_<subject>_neg_control` (3 named — evidence_trail/mermaid_export/lint_report_structure) (C22); `test_ci_yml_no_snapshot_update` + `test_ci_yml_has_snapshot_warn_unused` (C22-bis) | — |
| 20 | AC20 | NEW `docs/reference/INDEX.md`; NEW `tests/test_docs_reference_index_complete.py` | Hand-author `docs/reference/INDEX.md` listing every `docs/reference/*.md` file's first H1 + frontmatter. Meta-test asserts every `docs/reference/*.md` (excluding INDEX.md, README.md) appears in INDEX.md AND in the CLAUDE.md "Detailed Documentation" table. | `test_index_md_includes_all_reference_files`, `test_claude_md_table_includes_all_reference_files` (C23) | — |
| 21 | AC22 | `.github/workflows/ci.yml`; NEW `tests/test_cycle65_ci_dummy_key_guard.py` | Add CI step after `pip-audit` job: `name: dummy-key-leak-guard\n  run: \"git ls-files \| xargs grep -l 'sk-ant-dummy' \| grep -v '.github/workflows/ci.yml' \| (! read)\"`. Step fails if `sk-ant-dummy` appears anywhere except `ci.yml` itself. Regression test ast-parses `ci.yml` (using PyYAML) and asserts the step exists with the expected `run:` block. | `test_grep_step_present_in_ci_yml` (C19) | — |

**Total commits:** 21. **Total NEW test files:** 12 (`test_cycle65_*.py` × 11 + `test_security_cve_greps.py`) + 1 augmented (`test_cycle64_snapshots.py`) + 1 foundation self-test (`tests/_helpers/test_ast_walk.py`).

**Test count:** 30+ named test functions. (Some commits add multiple parametrized cases under a single named function — the named function is the unit Plan-gate counts; parametrize cases are the unit Step 9 implementer expands.)

---

## Step 11 caller-grep checkpoints (8 explicit)

Per `feedback_signature_drift_verify` and design.md § Signature drift watchlist. Run AFTER each listed commit lands; grep MUST show same counts pre/post unless the commit explicitly migrates callers in the same commit:

| After commit | Grep | Expected |
|--------------|------|----------|
| #5 (AC1) | `from kb.config import PROJECT_ROOT` in `src/kb/` `tests/` | UNCHANGED count (PEP 562 `__getattr__` shim handles back-compat). 200+ test refs MUST stay green. |
| #6 (AC2) | `from kb.config import MODEL_TIERS` in `src/kb/` excluding `config.py` | ZERO. `MODEL_TIERS\[` outside `config.py` + 2 versioned tests: ZERO. |
| #7 (AC3) | `kb.config.AUGMENT_ALLOWED_DOMAINS` attribute access in `src/kb/lint/augment/` | ZERO (4 sites migrated to `get_allowed_domains()`). PEP 562 shim covers any stragglers. |
| #9 (AC6+7+8) | `_validate_page_id(` in `src/kb/` `tests/` | SAME count (9 caller sites at `mcp/quality.py:48,84,141,179,208,280,367,439`, `mcp/browse.py:93`). |
| #10 (AC9) | `_validate_wiki_dir(` SAME (8+ sites); `_validate_path_under_project_root(` SAME (7 sites at `compile/compiler.py:724,732,737`, `compile/publish.py:684,691`, `query/embeddings.py:766,769`); `_assert_under_project_root` ≥3 (the three migrated sites) | SAME counts on the keep-signatures; AC23 in commit #12 codifies the ≥3 assertion. |
| #11 (AC10) | `_open_no_follow` callers + `unlink(` direct calls inside `rebuild_indexes` | All `unlink` mutators inside `rebuild_indexes` route through `_open_no_follow` or its fallback. |
| #13 (AC12) | `_url_is_allowed(` in `src/kb/` `tests/` | SAME count (5 sites at `lint/fetcher.py:361,414,455`, `lint/augment/orchestrator.py:234`, `lint/augment/proposer.py:105`). |
| #16 (AC16) | `_check_no_secrets_on_argv\b` in `src/kb/` `tests/` | SAME count (2 caller sites at `cli_backend.py:178,185`). |
| #17 (AC17) | `from kb.graph.cache import get_graph` in `src/kb/` | ZERO (entire point of AC17). Test files exempt only if they explicitly test import-time-snapshot — verify each test file individually. |
| #18 (AC21) | `@mcp.tool` count in `mcp/{core,ingest,quality}.py` == `@_mcp_error_boundary` count in same files | EQUAL counts. Use `grep -c "@mcp.tool()"` (with parens) per cycle-20 L2 — bare `@mcp.tool` over-counts comment lines. |

(Counts above use cycle-20 L2 disciplined grep — `@mcp.tool()` not `@mcp.tool`.)

---

## Lessons applied (explicit citations to design.md § Step 9 implementation guardrails)

- **Cycle-19 L2 reload-leak:** AC1 / AC3 / AC15 read env at CALL TIME, never cached at import. AC1 internal cache (if any) cleared by `_reset_project_root()`.
- **Cycle-18 L1 attribute-lookup:** AC9 callers use `kb.utils.path_safety._assert_under_project_root(...)` form (NOT `from ... import`). AC17 enforces same discipline for `kb.graph.cache.get_graph`. CLAUDE.md Quick Reference already documents this for cycle-64 AC9 graph cache.
- **Cycle-23 L4 same-class peer scan:** AC23 codifies for AC9; design.md § Same-class peer scan section enumerates deliberate OOS peers per AC.
- **Cycle-22 L5 conditions-as-tests:** every C1-C23 has ≥1 named test function; some split (C4 + C4-bis, C22 + C22-bis) per cycle-9 L1 dual-mechanism rule.
- **Cycle-7 (test behavior over signature):** every test exercises the production code path. NO `inspect.getsource(module) + "X" in src` — banned by `feedback_inspect_source_tests`. AC4 structural test uses `ast.parse` + decorator-list assertion (NOT `inspect.getsource`).
- **Cycle-21 (CLI value-scrub):** AC16 migrates from regex-match to value-based scrub via `secrets.compare_digest` against the six env vars.
- **Cycle-3 L4 grep-verify:** all symbols in design verified at named line numbers via Grep at design lock time. Two drifts surfaced + corrected: `sanitize_error_text` location is `kb.utils.sanitize` (NOT `kb.utils.text` per threat-model wording); `AUGMENT_ALLOWED_DOMAINS` env var has no `KB_` prefix in current source — AC3 reads BOTH names with `KB_` first.
- **Cycle-20 L2 disciplined grep:** Step 11 uses `@mcp.tool()` (with parens) NOT `@mcp.tool` (matches comment lines).
- **`feedback_no_secrets_in_code`:** AC16 + AC22 use `monkeypatch.setenv("ANTHROPIC_API_KEY", "test-fake-value-not-real")` exclusively. Real env values NEVER read in test scope.
- **`feedback_batch_by_file`:** commits group HIGH+MED+LOW from the SAME file (AC6+AC7+AC8 share `mcp/app.py`; AC4+AC5 share `tests/conftest.py`). Dependency edges OVERRIDE alphabetical-by-file ordering (foundation → AC1 → AC9 → AC10 etc., per design.md line 455).
- **`feedback_taskcreate_zero_pad`:** TaskCreate subjects use `Step 08` not `Step 8` so display order matches step order.

---

## Plan-gate (Step 8) checklist

The Step 8 gate (`mimocoding-rescue` @ `mimo-v2.5-pro`) MUST verify:

- [ ] **21 commits** match the design.md § Commit ordering (line 432-453); reject if v1 summary's 12-commit collapsing reappears.
- [ ] **AC2 separated** from AC1+AC3 — AC2 is `kb/utils/llm.py`, not `config.py`.
- [ ] **30 named test functions** itemised; parametrize-collapsing rejected per design.md § CONDITIONS section line 327.
- [ ] **1:1 condition-to-test traceability matrix** — every C1-C23 (+ C4-bis, C22-bis splits) maps to a named test.
- [ ] **Foundation commit** (`tests/_helpers/ast_walk.py`) lands FIRST and self-tests are present.
- [ ] **Dependency edges respected:** AC1 → AC9 (commits #5 → #10), AC9 → AC10 (#10 → #11), AC9 → AC23 (#10 → #12), AC3 → AC12 (#7 → #13), AC14 → AC21 (#15 → #18).
- [ ] **8 Step-11 caller-grep checkpoints** documented with explicit grep + expected count (table above).
- [ ] **NEW underscore-private functions** = 12 (per design.md § NEW functions). No public-API expansion in cycle 65.
- [ ] **Drift corrections:** `sanitize_error_text` import from `kb.utils.sanitize`; `AUGMENT_ALLOWED_DOMAINS` dual env-var read with `KB_` prefix priority.
- [ ] **Q2.2 kwargs cap = 4** for `_assert_under_project_root` (design lock — adding 5th requires Step 5 follow-up gate).
- [ ] **Q2.3 fallback** on `O_NOFOLLOW` unsupported — `logger.warning` once per process; document in `docs/reference/error-handling.md` (Step 17 doc).
- [ ] **Q2.10 fixture-set env values exclusively** for AC16 tests — never `os.environ.get` real keys.
- [ ] **R2 D7 + D9 cycle-66 deferrals** documented in CHANGELOG.md compact entry.

If the gate REJECTs with documentation/design-clarification gaps, resolve INLINE per cycle-21 Step-8 lesson — most gaps will already be answered in design.md or this expanded plan. Reserve re-dispatch for gaps requiring exploration of unfamiliar code (none expected — all symbols grep-verified at design lock).

---

## Risk acknowledgements

1. **Plan summary phantom-gap risk (cycle-6 L4)** — neutralised by this expansion. The original 90-line summary at `c0a8020` is superseded by this file (same path); the gate ingests this v2 body.
2. **Signature drift (cycle-7 L4)** — 8 explicit caller-grep checkpoints. Step 9 must run grep AFTER each signature-touching commit, not at end.
3. **Reload-leak (cycle-19 L2 / cycle-20 L1)** — every NEW config accessor reads env at CALL TIME. AC1 PEP 562 `__getattr__` shim is the load-bearing mechanism; AC2 + AC3 + AC17 + AC21 all depend on the shim being correct.
4. **Cross-file dependency (AC9 spans 3 files)** — design.md line 459 permits split into 4 sub-commits within AC9 logical unit; current plan keeps AC9 as commit #10 (single commit) to keep the dependency graph simple. If Step 9 implementer needs to split for clarity, they MAY split `path_safety.py` creation as a separate commit before the three migrations.
5. **Full pytest suite regression risk (cycle-22 L3)** — 200+ test files reference `kb.config.PROJECT_ROOT`. AC1's PEP 562 `__getattr__` shim MUST keep them all green. Step 12 CI hard gate runs FULL suite, not isolated tests.
6. **Late-arrival CVE risk (cycle-22 L4)** — Step 02 baseline captured 4 known dev-eval CVEs; Step 11 PR-CVE diff and Step 15 Dependabot patch verify no new arrivals during the cycle's wall-clock span.
7. **MiMo Coding plan summary regression (cycle-65 telemetry)** — this v2 expansion was authored by Opus inline. Step 24 captures: `mimocoding-rescue` Step 7 produced a 90-line summary that mis-batched AC2 (real failure mode for the trial writeup, not a hallucinated one).

---

**Commits:** 21 · **Conditions:** 23 (C1-C23, all mapped) · **Named tests:** 30 · **Sig-drift checkpoints:** 8 · **NEW underscore-private functions:** 12 · **Public API surface change:** 0
