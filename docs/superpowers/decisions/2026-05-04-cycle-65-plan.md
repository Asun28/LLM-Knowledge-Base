# Cycle 65 — Implementation Plan (Step 7 — MiMo Coding)

**Cycle:** 65  
**Step:** 7 — Implementation Plan  
**Date:** 2026-05-04  
**Branch:** `feat/cycle-65`  
**Pipeline:** `dev-mimo-opus` (May 2026 MiMo trial)

---

## Summary

This plan converts 23 ACs across 7 risk-bearing clusters and 4 hygiene clusters into 12 commits, ordered by dependency and file grouping per feedback_batch_by_file. Total estimated test count: 56–74 new functions covering all conditions C1–C23.

---

## Commit Grouping

| # | ACs | Type | Key Conditions |
|---|-----|------|----------------|
| C1 | (infra) | Foundation | AST helpers |
| C2 | AC1, AC2, AC3 | Config | Call-time accessors (Cycle-19 L2) |
| C3 | AC4, AC5 | Sandbox | Autouse decorator + lru_cache walk |
| C4 | AC6 | Validation | Trailing-dot/space hardening |
| C5 | AC7, AC8 | Validation | NTFS chars + dotdot segments |
| C6 | AC9 | **CHECKPOINT** | Unified path_safety.py helper |
| C7 | AC10 | TOCTOU | Re-validate before unlink |
| C8 | AC11, AC12 | Deps + URL | GitPython pin + SSRF hardening |
| C9 | AC13, AC14, AC15 | Error / Cache | File lock + error wrap + cache disable |
| C10 | AC16, AC17 | CLI / CI | Value-based secrets + CI greps |
| C11 | AC18–AC22 | Security / Snapshot | CVE greps + snapshot hardening + docs |
| C12 | AC23 | MCP | Error boundary + peer-scan regression |

---

## Key Lessons Applied

- **Cycle-19 L2 (reload-leak):** AC1–AC3 read env at call time (tests verify via post-import monkeypatch)
- **Cycle-18 L1 (snapshot-binding):** AC17 enforces attribute-lookup form via __all__ = [] + AST-grep guard
- **Cycle-23 L4 (same-class peer scan):** AC9 consolidates 3 path validators; AC23 codifies regression test enumerating all call sites
- **Cycle-7 (signature-only tests fail on revert):** Every test exercises production code path, never just inspect.getsource().contains()
- **Cycle-21 (CLI hardening):** AC16 migrates from regex-match to value-based scrub (prevents self-DoS on legitimate prompts discussing API formats)

---

## Test Count Estimate

- C1: 6–8 AST helpers
- C2: 7–10 config call-time  
- C3: 2–3 sandbox  
- C4: 4–5 trailing-dot  
- C5: 7–8 NTFS+dotdot  
- C6: 5–6 path_safety  
- C7: 1–2 TOCTOU  
- C8: 8–10 GitPython+URL  
- C9: 6–8 file-lock+error+cache  
- C10: 3–4 CLI+CI  
- C11: 5–7 CVE+snapshot+docs  
- C12: 2–3 MCP+peer-scan  

**Total: 56–74 new test functions** (target ≥20 per done-conditions; well exceeded)

---

## Step 11 Signature-Drift Checkpoints

1. **After C2:** `grep -rn "from kb.config import WIKI|PROJECT_ROOT|ALLOWED|MODEL_TIERS"` → zero hits (all → call-time)
2. **After C6:** `grep -rn "_assert_under_project_root" src/kb/mcp/ src/kb/compile/` → ≥3 hits at historical line regions

---

## Plan-Gate Checklist (Step 8)

- [ ] All 23 ACs have a commit (C1–C12 grouped per file)
- [ ] All C1–C23 conditions have NAMED test functions (not parametrized blobs)
- [ ] C1 → C3: foundation before sandbox hardening
- [ ] C2 → C8: config accessors before AC12 URL tests
- [ ] C4–C5 → C6: validate_page_id hardening informs path_safety design
- [ ] C6 → C7: path_safety helper before TOCTOU defense
- [ ] Cycle-19 L2: config accessors read at call time (verified in code sketches)
- [ ] Cycle-18 L1: graph/cache.py __all__ = [] + AST-grep guard (AC17)
- [ ] Cycle-23 L4: AC23 codifies peer-scan regression test
- [ ] All new code follows feedback rules (test behavior over signature, no inspect.getsource)

---

**Commits:** 12  
**Conditions:** 23 (C1–C23, all mapped)  
**Tests:** 56–74 new functions  

