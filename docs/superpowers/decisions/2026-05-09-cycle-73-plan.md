# Cycle 73 — Step 7 Implementation Plan

**Date:** 2026-05-09
**Branch:** `feat/cycle-73`
**Input:** Step 5 design-decision (14 conditions + 1 AC05 pivot, all FROZEN)

---

## Overview

This plan executes 6 acceptance criteria across 7 commits (C0–C6):

| Commit | Type | Description | Scope |
|--------|------|-------------|-------|
| **C0** | Test-RED | Lock-in tests + xfail mutation controls (all FAILING) | 47 test cases |
| **C1** | AC01 | `build_completeness_context` cap + wrap_wiki_context fence | 15 LoC `src/kb/lint/semantic.py` |
| **C2** | AC02 | Verdict-store `prompt_version` stamp + read-side accessor | 12 LoC `src/kb/{config,lint/verdicts}.py` |
| **C3** | AC03+AC04 | Tier-boundary verifier + split-catch + distinct outcome | 55 LoC `src/kb/{errors,lint/augment/orchestrator}.py` |
| **C4** | AC05 | Single snapshot subject: `_persist_contradictions` + determinism | 35 LoC tests + `.ambr` |
| **C5** | AC06 | BACKLOG hygiene: delete KB_DISABLE_VECTORS + prune stale list | 2 deletions + 1 rewrite |
| **C6** | Docs | CHANGELOG + CLAUDE.md + docs/reference/ | ~40 LoC |

**Blast radius:** ~250 LoC `src/` (5 files) + ~35 LoC tests + ~40 LoC docs.

---

## Commit C0 — Lock-in Test Files (RED — all FAILING)

Five test files, all failing until C1–C5 ship:

1. `tests/test_cycle73_completeness_wrap.py` — AC01 lock-in + xfail mutation
2. `tests/test_cycle73_prompt_version.py` — AC02 lock-in + xfail mutation
3. `tests/test_cycle73_tier_boundary.py` — AC03+AC04 lock-in + xfail mutation
4. `tests/test_cycle73_snapshots.py` — AC05 snapshot test
5. `tests/test_cycle73_backlog_hygiene.py` — AC06 BACKLOG assertions

**Discipline rules:**
- NO `inspect.getsource` tests (all behavioural assertions).
- Multi-page fixtures use COUNT assertions.
- Spy tests exercise production code paths.
- xfail-strict mutation controls prove invariants are load-bearing.
- Snapshot tests use literal fixtures (pin `date.today()` via monkeypatch).

---

## Commit C1 — AC01: `build_completeness_context` cap + wrap

**File:** `src/kb/lint/semantic.py:466`

**Change:** Cap page_content via `_cap_page_content()`, split lines into triplet (header/body/closing), wrap body in `wrap_wiki_context()`, budget-plumb `_render_sources()`.

**Threat-model:** T1 (Tampering), T2 (InformationDisclosure).

**Conditions:** C-AC01-1, C-AC01-2, C-AC01-3.

---

## Commit C2 — AC02: Verdict-store `prompt_version` stamp

**Files:** `src/kb/config.py`, `src/kb/lint/verdicts.py`

**Changes:**
1. Add `CURRENT_PROMPT_VERSION: int = 1` to config.
2. Add `get_prompt_version(entry: dict) -> int` accessor (returns 0 for missing key).
3. Add `"prompt_version": CURRENT_PROMPT_VERSION` to `add_verdict` entry dict.

**Threat-model:** T3 (Repudiation), T7 (Tampering — no cache mutation).

**Conditions:** C-AC02-1, C-AC02-2, C-AC02-3.

---

## Commit C3 — AC03+AC04: Tier-boundary verifier + manifest distinctness

**Files:** `src/kb/errors.py`, `src/kb/lint/augment/orchestrator.py`

**Changes:**
1. Add `TierBoundaryError(ValidationError)` class.
2. Add `_validate_tier_boundary()` helper (~60 LoC).
3. Call validator between LLM return and manifest write.
4. Split-catch: `ValidationError` first, then generic `Exception`.

**Threat-model:** T4 (EoP), T5 (Spoofing), T6 (DoS), T9 (Repudiation).

**Conditions:** C-AC03-1...C-AC03-7, C-AC04-1...C-AC04-3.

---

## Commit C4 — AC05: Snapshot for `_persist_contradictions`

**Files created:** `tests/test_cycle73_snapshots.py`, `tests/__snapshots__/test_cycle73_snapshots.ambr`

**Change:** Single snapshot test, monkeypatched `date.today()` pinned to `date(2026, 5, 9)`.

**Note:** Pivoted per primary-session grep: 5 of 6 BACKLOG-listed deferred subjects already shipped cycles 69–70.

**Conditions:** C-AC05-1, C-AC05-2, C-AC05-3.

---

## Commit C5 — AC06: BACKLOG hygiene

**File:** `BACKLOG.md`

**Changes:**
1. Delete stale KB_DISABLE_VECTORS line.
2. Update snapshot-subjects section: prune 5 shipped, note 1 pending.

**Test file:** `tests/test_cycle73_backlog_hygiene.py`

**Conditions:** C-AC06-1...C-AC06-5.

---

## Commit C6 — Documentation

**Files:** `CHANGELOG.md`, `CHANGELOG-history.md`, `CLAUDE.md`, `docs/reference/implementation-status.md`

**Changes:**
- CHANGELOG: Quick Reference entry.
- CHANGELOG-history: full per-cycle section.
- CLAUDE.md: bump test count to ~3370; add cycle-73 to §Wiki-context boundary fence (13 sites).

---

## Step 9 Mandatory Checkpoints

```bash
# C1 — wrap sites (expect >= 3)
grep -n 'wrap_wiki_context' src/kb/lint/semantic.py | wc -l

# C2 — prompt-version (expect 1 + 1)
grep -n 'CURRENT_PROMPT_VERSION' src/kb/config.py | wc -l
grep -n 'def get_prompt_version' src/kb/lint/verdicts.py | wc -l

# C3 — tier-boundary (expect 1 + >=1)
grep -n 'def _validate_tier_boundary' src/kb/lint/augment/orchestrator.py | wc -l
grep -n 'tier_boundary_rejected' src/kb/lint/augment/orchestrator.py | wc -l

# C5 — KB_DISABLE_VECTORS deleted (expect 0)
grep -n 'KB_DISABLE_VECTORS=1.*cycle-N+1' BACKLOG.md

# All tests PASS
pytest tests/test_cycle73_*.py -v
```

---

## Summary

**Commits:** 7 (C0–C6)  
**Acceptance Criteria:** 6 (AC01–AC06 with 1 pivot)  
**Conditions:** 14 + checkpoints

**Production:** ~250 LoC `src/` (5 files)  
**Tests:** ~35 LoC + 47 test cases  
**Docs:** ~40 LoC

**Threats mitigated:** T1, T2, T3, T4, T5, T6, T7, T9

**Cycle-72 deferred gaps closed:** T7 (Repudiation), T8 (EscalationOfPrivilege)

**Status:** FROZEN at Step 5. Ready for Step 9 implementation.
