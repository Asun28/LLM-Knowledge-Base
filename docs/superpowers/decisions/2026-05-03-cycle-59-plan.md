# Cycle 59 Dev-Codex-Gate Freeze-And-Fold Plan

Date: 2026-05-03
Branch: `feat/cycle-59`
Worktree: `.claude/worktrees/cycle-59`
Requested literal branch: `feat(cycle 59)` (invalid Git ref, mapped to `feat/cycle-59`)

## Goal

Fold 20 selected versioned backlog tests into canonical receiver files while preserving test count and behavior. Keep the cycle pure tests/docs: no production code and no dependency changes.

## Selected Items

1. `tests/test_v0917_rewriter.py` -> `tests/test_query.py`
2. `tests/test_v0917_raw_fallback.py` -> `tests/test_query.py`
3. `tests/test_v0917_layered_context.py` -> `tests/test_query.py`
4. `tests/test_v0917_hybrid.py` -> `tests/test_query.py`
5. `tests/test_v0917_embeddings.py` -> `tests/test_query.py`
6. `tests/test_v01004_query_correctness.py` -> `tests/test_query.py`
7. `tests/test_v01005_query_perf_docs.py` -> `tests/test_query.py`
8. `tests/test_v0917_stale_query.py` -> `tests/test_query.py`
9. `tests/test_v0917_dedup.py` -> `tests/test_query.py`
10. `tests/test_v4_11_markdown.py` -> `tests/test_query.py`
11. `tests/test_v5_augment_config.py` -> `tests/test_config.py`
12. `tests/test_v01002_consolidated_constants.py` -> `tests/test_config.py`
13. `tests/test_v5_verdict_augment_type.py` -> `tests/test_lint.py`
14. `tests/test_v5_autogen_prefixes.py` -> `tests/test_lint.py`
15. `tests/test_v01010_lint_fixes.py` -> `tests/test_lint.py`
16. `tests/test_v0917_contradiction.py` -> `tests/test_ingest.py`
17. `tests/test_v0917_evidence_trail.py` -> `tests/test_ingest.py`
18. `tests/test_v01006_compile_fixes.py` -> `tests/test_compile.py`
19. `tests/test_v4_11_cli.py` -> `tests/test_cli.py`
20. `tests/test_v4_11_mcp.py` -> `tests/test_mcp_core.py`

## Acceptance Criteria

AC1: All 20 selected source files are deleted only after their tests are hosted in canonical receivers.

AC2: No unselected test files are folded or deleted. `tests/test_v4_11_formats_dispatch.py` and `tests/test_cycle20_windows_tilde_path.py` are explicitly out of scope and remain present.

AC3: Test count remains 3021 after the fold, with no production-code or dependency changes.

AC4: Receiver tests, full pytest, collect-only, and ruff pass locally.

AC5: Related docs reflect cycle-59 counts, receiver mapping, and delegate/gate evidence.

## Delegate And Gate Notes

Laplace folded compile/ingest receiver work, then the primary session restored the accidental `test_cycle20_windows_tilde_path.py` drift.

Hypatia folded CLI/config/lint/MCP receiver work. The evidence-trail coverage was initially placed in `test_utils.py`; Avicenna's review flagged that the correct owner is `kb.ingest.evidence`, so the primary session moved it into `test_ingest.py`.

Avicenna reviewed the aggregate diff and caught unselected-source drift, receiver mismatch, and import-order/lint risk.

Direct MiMo CLI produced a generic first review without enough file/test evidence; it failed the `dev-codex-gate` delegate quality bar. Direct DeepSeek CLI returned conditional-pass gates: collision scan, count preservation, full tests, and doc sync. Primary Codex retained final design, integration, security, and closeout authority.

## Verification

- Selected source baseline before folding: `97 passed in 5.17s`
- Consolidated receiver set after folding: `384 passed in 11.22s`
- Full suite after removing root scratch files: `3009 passed, 12 skipped, 48 warnings in 137.50s`
- Collect-only: `3021 tests collected`
- Ruff: `ruff check src tests` passed

## Commits

- `154e41c` - `test(cycle 59): fold 20 versioned tests`
- `5471336` - `test(cycle 59): clean folded receiver whitespace`

## Security And Dependencies

No `src/kb/` files changed. No dependency files changed. PR-introduced dependency risk is empty by diff. The cycle only moves existing tests into canonical receivers and updates docs.
