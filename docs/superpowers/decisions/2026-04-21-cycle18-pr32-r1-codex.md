## Verdict — REQUEST-CHANGES

## Blockers — severity, file:line, fix

BLOCKER: `src/kb/ingest/pipeline.py:1067` emits `stage="start"`, but extraction/validation at `src/kb/ingest/pipeline.py:1072-1074` runs before the `try` that emits failure (`src/kb/ingest/pipeline.py:1102-1124`). If `extract_from_source` or `_pre_validate_extraction` raises, JSONL gets an orphan `start` with no terminal `failure`. Fix: start the failure-emission `try` immediately after `start`, keeping duplicate-skip terminal handling intact.

## Majors — severity, file:line, fix

MAJOR: `stage="success"` is emitted inside `_run_ingest_body` at `src/kb/ingest/pipeline.py:1398-1408`, but the integration contract requires all 4 `_emit_ingest_jsonl` stage calls to be wired only in `ingest_source`, not in `_run_ingest_body`. This makes the extracted body capable of emitting `success` without the `ingest_source` start/failure envelope. Fix: have `_run_ingest_body` return the result, then emit success in `ingest_source` after the body returns.

## Minors / NITs — low-severity findings

None.

## Test-contract assessment — are regression tests non-vacuous?

Partially. The rotation and `_write_index_files` tests are non-vacuous because source order is pinned by spies and production call sites delegate through module attributes (`src/kb/utils/wiki_log.py:149-151`, `src/kb/ingest/pipeline.py:741-748`, `src/kb/ingest/pipeline.py:1269-1274`). However, the observability tests do not protect the missing terminal row for extraction/validation failures because that path is outside the `try` (`src/kb/ingest/pipeline.py:1072-1074`, `src/kb/ingest/pipeline.py:1102`).

## Summary

Most architecture contracts are correctly wired: `_emit_ingest_jsonl` reads `PROJECT_ROOT` dynamically (`src/kb/ingest/pipeline.py:826-832`), rotation preserves suffix/ordinal and runs under lock at both call sites (`src/kb/utils/wiki_log.py:43-58`, `src/kb/ingest/pipeline.py:835-840`), `_write_index_files` is sources-before-index (`src/kb/ingest/pipeline.py:741-748`), and `append_wiki_log` signature is unchanged (`src/kb/utils/wiki_log.py:71`). The remaining issue is the ingest telemetry boundary.
