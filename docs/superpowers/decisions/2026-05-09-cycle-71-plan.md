# Cycle 71 — Implementation Plan

**Date:** 2026-05-09
**Tier:** 2
**Step:** 07 (implementation plan)
**Source of truth:** `2026-05-09-cycle-71-design.md` (Step 5 lock)

> Note on authorship: Step 7 was first dispatched to `mimocoding-rescue`; the subagent created the file shell but did not write content (cycle-12 L2 "described but not implemented" pattern). Per cycle-13 sizing heuristic, primary session (Opus 4.7 main) implemented directly. Step 24 self-review captures the dispatch failure for trial telemetry.

## Plan structure

Tasks are GROUPED BY FILE (per `feedback_batch_by_file` memory). Each task = one commit. TDD discipline: write the lock-in test FIRST (red), then implementation (green), then refactor if needed.

Per design.md Step 5 lock: 12 ACs locked, 17 binding conditions. R1 + R2 amendments are field-level extensions (no new ACs). Cycle-72+ deferrals (H1/H2/H6/R2-cumulative + page-content overshoot) ship as BACKLOG entries under AC09 only.

## Tasks (13 commits, file-grouped)

### Task 01 — `tests/test_cycle71_wrap_extensions.py` scaffold + AC05 lock-in (kb_search per-snippet + title wrap)

- **File:** `tests/test_cycle71_wrap_extensions.py` (NEW)
- **Commit:** `test(cycle-71): AC05 kb_search per-snippet + title wrap lock-in`
- **Tests first (RED expected):**
  - Module-level helper `_make_attacker_payload(prefix="A"*100, suffix="B"*100)` returning `f"{prefix}</wiki_context>{suffix}"`.
  - Class `TestAC01_KbSearchSnippetWrap`:
    - `test_per_snippet_wrap_count` — stub `kb.query.engine.search_pages` to return 2 deterministic results; invoke `_format_search_results(results)`; assert `output.count("<wiki_context>") == 4` (2 content + 2 title), `output.count("</wiki_context>") == 4` (R2-F4 fence-balance), `output.count("Found 2 matching page(s)") == 1`, `output.count("- **stub-id-A**") == 1` (scaffolding outside fence).
    - `test_attacker_substring_rewritten_in_both_fields` — fixture with attacker payload in BOTH `r["content"]` and `r["title"]`; assert `"</wiki_context>" not in output and output.count("</wiki-context>") >= 2`.
    - `test_assertion_sentence_present` — assert at least 4 occurrences of the assertion sentence text.
  - Paired `TestAC01_Mutation::test_xfail_under_identity_wrap` decorated `@pytest.mark.xfail(strict=True)`: monkeypatch `kb.mcp.browse.wrap_wiki_context` to `lambda x: x`, re-run lock-in body, expect failure.
- **Production change:** None (test file only).
- **Self-check:** `grep -nE "inspect\.getsource|read_text.*\.py|splitlines.*\.py" tests/test_cycle71_wrap_extensions.py` returns ZERO (cycle-4 L1 / cycle-11 L1 anti-source-scan).

### Task 02 — `src/kb/mcp/browse.py` AC01 implementation (kb_search per-snippet + title wrap)

- **File:** `src/kb/mcp/browse.py`
- **Commit:** `feat(cycle-71): AC01 wrap kb_search snippets + title via wrap_wiki_context`
- **Tests first (GREEN expected):** Re-run `tests/test_cycle71_wrap_extensions.py::TestAC01*`.
- **Production change:**
  - Add `from kb.utils.text import wrap_wiki_context` to imports.
  - At line 47, change `snippet = r["content"][:200].replace("\n", " ").strip()` to `snippet = wrap_wiki_context(r["content"][:200].replace("\n", " ").strip())`.
  - At line 53, change f-string interpolation from `Title: {r['title']}` to `Title: {wrap_wiki_context(r['title'])}`.
- **Self-check:**
  - `grep -n "wrap_wiki_context" src/kb/mcp/browse.py` returns >=3 hits (1 import + 2 calls).
  - `python -m pytest tests/test_cycle71_wrap_extensions.py::TestAC01_KbSearchSnippetWrap -x` → all green.

### Task 03 — `tests/test_cycle71_wrap_extensions.py` AC06 lock-in (kb_read_page body wrap)

- **File:** `tests/test_cycle71_wrap_extensions.py` (extend)
- **Commit:** `test(cycle-71): AC06 kb_read_page body char-cap + wrap lock-in`
- **Tests first (RED expected):**
  - Class `TestAC02_KbReadPageBodyWrap`:
    - `test_body_wrap_with_char_cap` — fixture wiki page body length = `QUERY_CONTEXT_MAX_CHARS + 1000` containing attacker `</wiki_context>` substring; call `kb_read_page("entities/test")`; assert (a) `len(response) <= QUERY_CONTEXT_MAX_CHARS` SHARP, (b) `<wiki_context>` opening tag present, (c) attacker substring rewritten to `</wiki-context>`, (d) truncation footer `[Truncated:` present, (e) `response.find("[Truncated:") > response.find("<wiki_context>")` AND `response.find("[Truncated:") < response.find("</wiki_context>")` (footer-inside-fence T8), (f) fence-balance `count("<wiki_context>") == count("</wiki_context>")` (R2-F4).
  - Paired `TestAC02_Mutation::test_xfail_under_identity_wrap` (xfail-strict) — monkeypatch `kb.mcp.browse.wrap_wiki_context` to `lambda x: x`, expect SHARP `len(response) <= QUERY_CONTEXT_MAX_CHARS` to FAIL (cap-not-reduced means total > cap when fence overhead added).
- **Production change:** None.
- **Self-check:** `grep -n "len(response) <= QUERY_CONTEXT_MAX_CHARS" tests/test_cycle71_wrap_extensions.py` returns hit.

### Task 04 — `src/kb/mcp/browse.py` AC02 implementation (kb_read_page char-cap reduction + wrap)

- **File:** `src/kb/mcp/browse.py` (extend)
- **Commit:** `feat(cycle-71): AC02 kb_read_page char-cap reservation + wrap_wiki_context body`
- **Tests first (GREEN expected):** Re-run `tests/test_cycle71_wrap_extensions.py::TestAC02*`.
- **Production change:**
  - Update import to `from kb.utils.text import _FENCE_OVERHEAD, wrap_wiki_context`.
  - Line 133: `cap_bytes = QUERY_CONTEXT_MAX_CHARS * 4 + 4096` UNCHANGED (byte-cap independent of fence).
  - Line 151: change `len(body) > QUERY_CONTEXT_MAX_CHARS` → `len(body) > QUERY_CONTEXT_MAX_CHARS - _FENCE_OVERHEAD`.
  - Line 154: change `len(body) - QUERY_CONTEXT_MAX_CHARS` → `len(body) - (QUERY_CONTEXT_MAX_CHARS - _FENCE_OVERHEAD)`.
  - Line 157: change `body[:QUERY_CONTEXT_MAX_CHARS]` → `body[:QUERY_CONTEXT_MAX_CHARS - _FENCE_OVERHEAD]`.
  - Line 159: change `cap={QUERY_CONTEXT_MAX_CHARS}` → `cap={QUERY_CONTEXT_MAX_CHARS - _FENCE_OVERHEAD}` (user-visible effective cap).
  - Line 162: change `return body` → `return wrap_wiki_context(body)`. **WRAP IS LAST OPERATION** (footer constructed at lines 157-161 BEFORE wrap).
- **Self-check:**
  - `grep -nE "QUERY_CONTEXT_MAX_CHARS - _FENCE_OVERHEAD" src/kb/mcp/browse.py` returns >=3 hits (lines 151, 154, 157, 159).
  - `grep -n "return wrap_wiki_context(body)" src/kb/mcp/browse.py` returns 1 hit at line ~162.
  - `grep -nE "cap_bytes = QUERY_CONTEXT_MAX_CHARS \\* 4" src/kb/mcp/browse.py` returns 1 hit at line 133 (byte-cap unchanged).
  - `python -m pytest tests/test_cycle71_wrap_extensions.py::TestAC02_KbReadPageBodyWrap -x` → all green.

### Task 05 — `tests/test_cycle71_wrap_extensions.py` AC07 lock-in (build_fidelity_context single-fence + path-sanitization + invariant)

- **File:** `tests/test_cycle71_wrap_extensions.py` (extend)
- **Commit:** `test(cycle-71): AC07 build_fidelity_context wrap + path-sanitization + invariant lock-in`
- **Tests first (RED expected):**
  - Class `TestAC03_FidelityContextWrap`:
    - `test_single_fence_around_page_and_sources` — patch `kb.lint.semantic.pair_page_with_sources` to return fixed dict with `page_content` (attacker `</wiki_context>` substring) + 1 source whose `content` carries the substring. Call `build_fidelity_context("entities/test")`. Assert (a) `output.count("<wiki_context>") == 1`, (b) `output.find("# Source Fidelity Check:") < output.find("<wiki_context>")` (heading OUTSIDE fence), (c) `output.find("For each factual claim, identify whether it is:") > output.find("</wiki_context>")` (closing instructions OUTSIDE fence), (d) `output.find("## Wiki Page") > output.find("<wiki_context>")` AND `output.find("## Wiki Page") < output.find("</wiki_context>")` (section markers INSIDE fence), (e) `"</wiki_context>" not in output and "</wiki-context>" in output` (T3 escape rewrite), (f) fence-balance (R2-F4).
    - `test_path_sanitization` — fixture sets `source["path"] = "raw/x.md\n## Wiki Page\n...injection..."`. Call `build_fidelity_context`. Assert `"raw/x.md\n## Wiki Page" not in output` (newline+`##` injection blocked by `sanitize_extraction_field`).
    - `test_fence_overhead_invariant` — assert `_FENCE_OVERHEAD == len(wrap_wiki_context("X")) - len("X")` (R2-F5 runtime invariant).
    - `test_budget_respected_with_short_page` — fixture page body short (~500 chars) + 2 sources with controlled lengths; assert `len(returned_text) <= QUERY_CONTEXT_MAX_CHARS + LARGE_PAGE_SLACK` where `LARGE_PAGE_SLACK = 0` for short fixture (sharp).
  - Paired `TestAC03_Mutation::test_xfail_under_identity_wrap` (xfail-strict) — monkeypatch `kb.lint.semantic.wrap_wiki_context` to `lambda x: x`; expect single-fence count assertion to fail.
- **Production change:** None.
- **Self-check:** `grep -nE "sanitize_extraction_field|_FENCE_OVERHEAD ==" tests/test_cycle71_wrap_extensions.py` returns hits for path-sanitization + invariant tests.

### Task 06 — `src/kb/lint/semantic.py` AC03 implementation (single-fence wrap + budget plumb + path sanitization)

- **File:** `src/kb/lint/semantic.py`
- **Commit:** `feat(cycle-71): AC03 build_fidelity_context single-fence wrap + _render_sources budget arg + path sanitization`
- **Tests first (GREEN expected):** Re-run `tests/test_cycle71_wrap_extensions.py::TestAC03*`.
- **Production change (3 sub-edits):**
  1. **Imports:** Add `from kb.utils.text import _FENCE_OVERHEAD, sanitize_extraction_field, wrap_wiki_context` after `import yaml`.
  2. **`_render_sources` signature + body** at lines 36-60:
     - Change signature to `def _render_sources(sources: list[dict], lines: list[str], *, budget: int = QUERY_CONTEXT_MAX_CHARS) -> None:`.
     - Line 46: `if used >= QUERY_CONTEXT_MAX_CHARS:` → `if used >= budget:`.
     - Line 48: `header = f"## Source {i}: {source['path']}\n"` → `header = f"## Source {i}: {sanitize_extraction_field(source['path'], max_len=500)}\n"`.
     - Line 52: `QUERY_CONTEXT_MAX_CHARS - used - len(header) - 20` → `budget - used - len(header) - 20`.
  3. **`build_fidelity_context` body** at lines 63-95: restructure into 3 segments (header_lines outside fence, body_lines inside fence, closing outside fence) per design.md §Q2 A1 lock.
- **Self-check:**
  - `grep -nE "wrap_wiki_context|_FENCE_OVERHEAD|sanitize_extraction_field|budget=" src/kb/lint/semantic.py` returns hits at: import line, `_render_sources` signature, line 48 sanitize, line 46/52 budget refs, return-statement wrap call, caller's `budget=` kwarg.
  - **Caller-grep** for `_render_sources` (cycle-7 L3 / cycle-11 L3 same-class peer): `grep -rn "_render_sources" src/kb/ tests/` — verify all callers use the new keyword-only `budget` arg correctly. Expected: 1 production caller (`build_fidelity_context`) + any tests.
  - `python -m pytest tests/test_cycle71_wrap_extensions.py::TestAC03_FidelityContextWrap -x` → all green.

### Task 07 — `tests/test_cycle71_wrap_extensions.py` AC08 lock-in (proposer empty-input early-return + wrapped-prompt spy)

- **File:** `tests/test_cycle71_wrap_extensions.py` (extend)
- **Commit:** `test(cycle-71): AC08 _relevance_score wrap + empty-input early-return lock-in`
- **Tests first (RED expected):**
  - Class `TestAC04_RelevanceScoreWrap`:
    - `test_wrapped_prompt_via_spy` — spy on `_call_llm_json` via `monkeypatch.setattr("kb.lint.augment.proposer._call_llm_json", spy)` returning `{"score": 0.5}`; invoke `_relevance_score(stub_title="X", extracted_text="A"*100 + "</wiki_context>" + "B"*100)`; assert (a) spy called once, (b) captured `prompt` contains `<wiki_context>` and the assertion sentence, (c) attacker `</wiki_context>` substring rewritten to `</wiki-context>` in captured prompt, (d) returned float == 0.5, (e) fence-balance equality on captured prompt.
    - `test_empty_extracted_text_skips_llm_call` — spy on `_call_llm_json`; invoke `_relevance_score(stub_title="X", extracted_text="")`; assert (a) returned float == 0.0, (b) spy `_call_llm_json` was NEVER called (R2-F3 early-return).
    - `test_whitespace_only_extracted_text_skips_llm_call` — same pattern with `extracted_text="   \n  \t"`; spy not called, returns 0.0.
  - Paired `TestAC04_Mutation::test_xfail_under_identity_wrap` (xfail-strict) — monkeypatch `kb.lint.augment.proposer.wrap_wiki_context` to `lambda x: x`; expect captured-prompt assertion to fail.
- **Production change:** None.
- **Self-check:** `grep -n "spy" tests/test_cycle71_wrap_extensions.py` returns hits in TestAC04.

### Task 08 — `src/kb/lint/augment/proposer.py` AC04 implementation (early-return + wrap)

- **File:** `src/kb/lint/augment/proposer.py`
- **Commit:** `feat(cycle-71): AC04 _relevance_score early-return guard + wrap_wiki_context extracted_text`
- **Tests first (GREEN expected):** Re-run `tests/test_cycle71_wrap_extensions.py::TestAC04*`.
- **Production change:** Edit `_relevance_score` at lines 136-148:
  - Add `wrap_wiki_context` to existing `from kb.utils.text import wrap_purpose` import: `from kb.utils.text import wrap_purpose, wrap_wiki_context`.
  - Insert at the top of the function body, immediately AFTER the docstring closing `"""` (cycle-23 L1 ordering rule):
    ```python
    if not extracted_text or not extracted_text.strip():
        return 0.0
    ```
  - Replace lines 138-143 with:
    ```python
    wrapped_text = wrap_wiki_context(extracted_text[:2000])
    prompt = (
        f"Score how relevant the following extracted text is to the topic "
        f"{stub_title!r}.\n"
        f'Return JSON: {{"score": <0.0-1.0>}}.\n\n'
        f"Extracted text (first 2000 chars):{wrapped_text}"
    )
    ```
    (`wrap_wiki_context` already prepends `\n` to the assertion line — no extra newline.)
- **Self-check:**
  - `grep -nE "if not extracted_text or not extracted_text\\.strip\\(\\)" src/kb/lint/augment/proposer.py` returns 1 hit.
  - `grep -n "wrap_wiki_context" src/kb/lint/augment/proposer.py` returns >=2 hits (import + call).
  - `python -m pytest tests/test_cycle71_wrap_extensions.py::TestAC04_RelevanceScoreWrap -x` → all green.

### Task 09 — `tests/test_cycle68_backlog_cleanup_lockin.py` extension (Q7 fold)

- **File:** `tests/test_cycle68_backlog_cleanup_lockin.py`
- **Commit:** `test(cycle-71): extend cycle-68 backlog lock-in with 4 deletion strings`
- **Tests first:** Order of operations — Task 09 lands the lock-in test FIRST; Task 10 then deletes the BACKLOG entries; running the suite after Task 10 sees the lock-in test go GREEN. To avoid temporary RED in CI, sequence as Task 09 → Task 10 in the SAME PR (CI runs after both land).
- **Production change:** Append to the existing `DELETED_ENTRIES` tuple under a new comment block:
  ```
      # Cycle 71 cleanup (4 prompt-injection wrap surfaces shipped this cycle)
      "_format_search_results` snippets prompt-injection wrap",   # AC01
      "kb_read_page` body return prompt-injection wrap",           # AC02
      "build_fidelity_context` prompt-injection wrap",             # AC03
      "_relevance_score` prompt-injection wrap",                   # AC04
  ```
- **Self-check:** `grep -nE "Cycle 71 cleanup" tests/test_cycle68_backlog_cleanup_lockin.py` returns 1 hit.

### Task 10 — `BACKLOG.md` hygiene (4 deletions + 5 new LOW + CVE refresh)

- **File:** `BACKLOG.md`
- **Commit:** `docs(cycle-71): AC09 BACKLOG hygiene — delete 4 cycle-71+ entries, add 5 cycle-72+ peers, refresh CVE timestamp`
- **Production change (per design.md AC09 expansion §A-§D):**
  - **§A (4 deletions):** Delete the 4 bullet entries at lines 152, 154, 156, 158 (each spans 1-3 lines including rationale + Fix sub-bullet — delete WHOLE bullet block per existing convention).
  - **§B:** Already covered by Task 09.
  - **§C (5 new LOW entries):** Append to the `### LOW` section after the existing `mutmut` entry (current line 162):
    1. Page-content overshoot (Q3 carry).
    2. H1 — `build_review_context` migration.
    3. H2 — `orchestrator.py` pre-extract migration.
    4. H6 — `build_consistency_context` migration.
    5. R2-cumulative — `_relevance_score` stub_title field.
    Verbatim text per design.md §AC09-C.
  - **§D (CVE refresh):** Edit line 64 — change `as of 2026-05-08` to `as of 2026-05-09` for `diskcache 5.6.3 / CVE-2025-69872`.
- **Self-check:**
  - `grep -nE "(_format_search_results|kb_read_page.*prompt-injection|build_fidelity_context.*prompt-injection|_relevance_score.*prompt-injection)" BACKLOG.md` returns ZERO hits in the entries body (cycle-71+ entries are gone).
  - `grep -nE "(cycle-72\\+)" BACKLOG.md` returns >=5 hits (the 5 new entries all tagged `cycle-72+`).
  - `grep -n "as of 2026-05-09" BACKLOG.md` returns 1 hit (CVE timestamp).
  - `python -m pytest tests/test_cycle68_backlog_cleanup_lockin.py -x` → green (Task 09 substrings now absent from BACKLOG.md).

### Task 11 — `CLAUDE.md` Quick Reference sync (AC12)

- **File:** `CLAUDE.md`
- **Commit:** `docs(cycle-71): AC12 CLAUDE.md Quick Reference sync (test count, scope, dedupe boundary-fence bullet)`
- **Production change:**
  - Update test count from `3288 passed` to actual post-cycle-71 count (run `python -m pytest --collect-only` post-Task-09 to get exact number, expected ~3296 = +4 lock-in + 4 xfail + invariant/budget tests).
  - Update scope language to mention cycle-71.
  - **REMOVE** the duplicate `Wiki-context boundary fence` bullet at lines 31 + 32 (currently identical text appears twice — keep only one).
  - Update the surviving bullet to reflect 6 in-scope sites (was 2): cycle-70 sites (`engine.py:1063` synthesis, `mcp/core.py:417-432` Claude Code mode response) plus cycle-71 4 new sites (`mcp/browse.py:_format_search_results` snippet+title, `mcp/browse.py:kb_read_page`, `lint/semantic.py:build_fidelity_context`, `lint/augment/proposer.py:_relevance_score`).
  - Add note about R2-F2 path sanitization addition.
- **Self-check:** `grep -c "Wiki-context boundary fence" CLAUDE.md` returns 1 (was 2 pre-cycle-71).

### Task 12 — `CHANGELOG.md` + `CHANGELOG-history.md` cycle-71 entries (AC11)

- **File:** `CHANGELOG.md`, `CHANGELOG-history.md`
- **Commit:** `docs(cycle-71): AC11 CHANGELOG entries (compact + per-AC detail)`
- **Production change:**
  - **`CHANGELOG.md`** — add compact `[Unreleased]` Quick Reference entry under "Cycle 71" with newest-first ordering: Items / Tests / Files / Detail (links to docs/superpowers/decisions/2026-05-09-cycle-71-*.md).
  - **`CHANGELOG-history.md`** — add full per-AC bullet-level archive entry for cycle 71 (newest-first):
    - AC01-04: 4 wrap_wiki_context extensions across mcp/browse.py / lint/semantic.py / lint/augment/proposer.py.
    - AC05-08: 4 lock-in tests + 4 xfail-mutation controls in tests/test_cycle71_wrap_extensions.py.
    - AC09: BACKLOG hygiene (4 deletions + 5 new LOW deferrals + CVE refresh).
    - AC10-12: doc artifacts.
    - R2 cross-family value: caught title (R2-F1) + path (R2-F2) injection vectors R1 missed; folded into existing AC envelope without scope expansion.
    - Step 6 SKIP rationale (pure-internal, no new lib refs).
- **Self-check:** `grep -n "Cycle 71" CHANGELOG.md CHANGELOG-history.md` returns hits in both files.

### Task 13 — Decision artifacts already committed (AC10) + Step 8 plan-gate doc

- **File:** None (verification task) + `docs/superpowers/decisions/2026-05-09-cycle-71-plan-gate.md` (NEW, written by Step 8 subagent)
- **Commit:** Step 8 subagent commits the plan-gate doc.
- **Production change:** None for this task; design artifacts already committed at `c11a9c9`. This task is documentation: confirming `2026-05-09-cycle-71-plan.md` (this file) lands in the next commit batch.
- **Self-check:** `ls docs/superpowers/decisions/2026-05-09-cycle-71-*.md` returns 8 files post-Step-8 (requirements, threat-model, brainstorm, design-eval-R1, design-eval-R2, design, plan, plan-gate).

## TDD discipline reminders (per `superpowers:test-driven-development`)

For each AC NN that pairs an impl task with a lock-in test task, the order is:
1. Write the lock-in test (Task 0N — test scaffolding) → run pytest → expect RED (production code unchanged).
2. Write the implementation (Task 0N+1 — impl) → run pytest → expect GREEN.
3. Step 11 SAST + Step 12 full-suite gate.

The pairs are: Task 01→02 (AC01/05), Task 03→04 (AC02/06), Task 05→06 (AC03/07), Task 07→08 (AC04/08).

## Same-class peer self-check (cycle-7 L3 / cycle-11 L3)

No NEW public functions added by cycle 71. All changes are call-site additions to existing functions. Specifically:
- AC01: `_format_search_results` already public-ish (called from `kb_search` MCP tool). No new symbol.
- AC02: `kb_read_page` already public MCP tool. No new symbol.
- AC03: `build_fidelity_context` already public MCP tool entry (`kb_lint_deep` → wraps it). `_render_sources` keyword-only `budget` arg added — caller-grep at Task 06 self-check confirms only `build_fidelity_context` calls it in production; no test fixtures break.
- AC04: `_relevance_score` private helper, called from `kb.lint.augment.proposer` orchestrator. No new symbol.

→ NO orchestrator-wiring required. Step 14 security-verify will confirm via grep.

## Anti-pattern self-checks (cycle-N L_M defenses)

Each task's self-check section explicitly addresses the relevant cycle-N L_M lesson:

- **No `inspect.getsource` / `read_text` source-grep tests** (cycle 4 L1, cycle 6 L3, cycle 11 L1) — every test uses spy/monkeypatch + production call site invocation.
- **Late-bind `wrap_wiki_context` import in test fixtures** (cycle 18 L1, cycle 20 L1) — xfail-mutation tests monkeypatch the IMPORTED BINDING in the call-site module's namespace (`kb.mcp.browse.wrap_wiki_context`, `kb.lint.semantic.wrap_wiki_context`, `kb.lint.augment.proposer.wrap_wiki_context`), NOT `kb.utils.text.wrap_wiki_context`.
- **Position assertions, not content assertions** (cycle 24 L1) — AC05/06/07 lock-ins use `output.find(...) < output.find(...)` style assertions for boundary checks.
- **Non-vacuous lock-ins** (cycle 9 L1, cycle 16 R2 N1, cycle 22 L5) — every primary assertion runs UNCONDITIONALLY; xfail-strict mutation control tests verify the lock-in actually fails when wrap is identity.
- **Ruff format BEFORE Edit** (cycle 4 L1 follow-up) — Tasks 02/04/06/08 self-checks include "run `ruff format` BEFORE writing test code OR re-Read between."
- **`Edit(replace_all=true)` discipline** (cycle 24 L1) — all multi-line edits in Tasks 02/04/06/08 use single-target `Edit` calls (no `replace_all`).
- **Function-local imports AFTER docstring** (cycle 23 L1) — AC04 (Task 08) places early-return guard immediately after docstring closing `"""`; no function-local imports added before docstrings.
- **Snapshot-binding hazard** (cycle 18 L1) — Tests use the imported binding (`kb.lint.augment.proposer.wrap_wiki_context`) for mutation control to match the production module's snapshot.
- **Telemetry envelope rule** (cycle 18 L2) — N/A (no telemetry added in cycle 71).
- **Ordering after extract** (cycle 10 L2) — `_render_sources` body unchanged in cycle 71; only `budget` plumbed through. Iteration order preserved.
- **CommonMark fence-masking** (cycle 24 L2) — N/A (cycle 71 reuses cycle-70 helper which uses regex-based escape, not fence-masking).
- **Cache-eviction lock-ordering** (cycle 24 L4) — N/A (no cache eviction in cycle 71).
- **Plan gate REJECT inline-resolution** (cycle 21 L1) — if Step 8 REJECTs with documentation gaps already addressed at Step 5, resolve inline; reserve re-dispatch for code-exploration gaps.

## Plan budget

- **Tasks:** 13 (one per file/commit unit).
- **Commits:** ~13 (one per task; some optional consolidation if Task 11+12 doc-only changes share scope).
- **Test count delta:** +4 positive lock-ins (AC05-08) + 4 paired xfail-mutation tests + 2 invariant/budget tests (AC07 invariant, AC04 empty-input + whitespace) ≈ **+10 net tests passed; +4 net xfail**. Final: 3288 → ~3296 passed, 24 skipped unchanged.
- **src/kb/ files modified:** 3 (mcp/browse.py, lint/semantic.py, lint/augment/proposer.py).
- **New test files:** 1 (tests/test_cycle71_wrap_extensions.py).
- **Doc files modified:** 5 (CLAUDE.md, CHANGELOG.md, CHANGELOG-history.md, BACKLOG.md, plus the 8 decision-doc commits in cycle-71 series).

## Risk callouts (cycle-71 specific)

- **R-Plan-1 — `_render_sources` signature change:** Task 06 changes `_render_sources(sources, lines)` to `_render_sources(sources, lines, *, budget=...)` (keyword-only). All 1 production caller updated in same commit. Test fixtures using `_render_sources` directly (if any in legacy tests) need verification via Task 06 caller-grep.
- **R-Plan-2 — Empty-input behavior change in AC04:** Task 08 adds early-return guard. If any test asserted that `_relevance_score("")` previously called LLM and returned 0.0 via the try/except path, that test breaks. Mitigation: Task 07's empty-input test ASSERTS the new contract (spy not called); legacy tests that asserted call-then-fail need updating in same commit.
- **R-Plan-3 — Title field user-controllability assumption:** R2-F1 wrap of `r["title"]` assumes `title` is the page frontmatter title and user-controllable. Task 02 self-check confirms via tracing: `kb.query.engine.search_pages` returns dicts with `title` from `frontmatter.load(page).metadata.get("title")` — confirmed user-controllable.
- **R-Plan-4 — `sanitize_extraction_field` semantics:** R2-F2 calls existing helper at `utils/text.py:247`. Task 06 self-check verifies sanitization removes the specific newline-plus-`##` injection pattern. The helper strips control chars, frontmatter fences, HTML comments, and level-2+ markdown headers — fits the threat model.
- **R-Plan-5 — Ordering between Task 09 and Task 10:** If Task 09 lands in CI WITHOUT Task 10 also landing (e.g. partial PR), the cycle-68 lock-in test FAILS (asserting absence of strings still present in BACKLOG). Mitigation: same PR, both tasks ship together; CI runs once on merged batch.

## Verdict

**PLAN READY** — submit to Step 8 plan gate.
