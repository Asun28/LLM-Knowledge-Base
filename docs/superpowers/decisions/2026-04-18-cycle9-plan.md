# Cycle 9 Implementation Plan (2026-04-18)

Preflight performed before writing tasks: read `2026-04-18-cycle9-design.md` and
`2026-04-18-cycle9-threat-model.md` cover-to-cover; ran `rg -n "^def ..."` for
all referenced functions and `rg -n "^class ..."` for class references. Confirmed
functions include `search_pages`, `_flag_stale_results`, `query_wiki`,
`search_raw_sources`, `_vec_db_path`, `get_flagged_pages`, `get_coverage_gaps`,
`kb_compile_scan`, `kb_ingest`, `kb_ingest_content`, `_validate_file_inputs`,
`find_changed_sources`, `scan_raw_sources`, `load_manifest`, `run_augment`,
`check_source_coverage`, `analyze_coverage`, `build_graph`, `build_backlinks`,
capture helpers, `_make_api_call`, `truncate`, and `_sanitize_error_str`.
Confirmed classes include `LLMError` and `CaptureError`. Verification hard gates:

```bash
python -m pytest
ruff check
ruff format --check
```

TASK 1: Lazy Ingest Re-Export
  AC: AC29 — "`src/kb/ingest/__init__.py:3 full replacement`: replace eager `from kb.ingest.pipeline import ingest_source` with PEP 562 lazy `__getattr__` pattern mirroring `src/kb/__init__.py:17-43`. Keep `__all__ = ["ingest_source"]`."
  Threat: T5
  Files: `src/kb/ingest/__init__.py`; new `tests/test_cycle9_package_exports.py`
  Scope-out: `kb.compile.__init__`, `kb.query.__init__`, `kb.graph.__init__` remain out of scope; design says cycle 9 addresses only the `kb.ingest` fast-path blocker.
  Change: Replace the eager import with lazy `__getattr__(name: str)` that imports `kb.ingest.pipeline.ingest_source` only when requested and raises `AttributeError` for unknown names. No public signature change to `ingest_source` (`src/kb/ingest/pipeline.py:785` confirmed).
  Test file: `tests/test_cycle9_package_exports.py`
  Test assertion: subprocess imports `kb.ingest` with explicit `PYTHONPATH`, asserts `kb.ingest.pipeline` is absent from `sys.modules`, then imports `ingest_source` and asserts the pipeline module appears.
  Commit message: `fix(cycle9): lazy-load ingest export (AC-29)`

TASK 2: Query Override Isolation
  AC: AC1 — "replace `Path(PROJECT_ROOT) / VECTOR_INDEX_PATH_SUFFIX` with `_vec_db_path(wiki_dir or WIKI_DIR)`"; AC2 — "`_flag_stale_results(scored[:max_results], project_root=(wiki_dir.parent if wiki_dir else None))`"; AC3 — "replace `Path(PROJECT_ROOT) / VECTOR_INDEX_PATH_SUFFIX` with `_vec_db_path(wiki_dir or WIKI_DIR)`"; AC4 — "new test `test_query_wiki_raw_fallback_uses_override_raw_dir` calls `query_wiki(wiki_dir=tmp_wiki)` ... Zero signature changes to `search_raw_sources`."
  Threat: T2
  Files: `src/kb/query/engine.py`; `tests/test_query_raw_sandbox.py` or new `tests/test_cycle9_query_engine.py`
  Scope-out: `rebuild_vector_index` already uses `_vec_db_path(wiki_dir)`; no other `_flag_stale_results` callers exist; `search_raw_sources(raw_dir: Path | None)` already parameterized and is not changed.
  Change:
  - Import `_vec_db_path` inside vector paths and use it at the `search_pages` vector search gate and `query_wiki` hybrid-configured gate.
  - Thread `project_root=wiki_dir.parent` into `_flag_stale_results` when `wiki_dir` is supplied.
  - Leave `query_wiki` and `search_raw_sources` signatures unchanged.
  Test file: `tests/test_cycle9_query_engine.py`
  Test assertion: failing regressions prove tmp vector DB, tmp stale raw source, and tmp raw fallback are used instead of `PROJECT_ROOT`; raw fallback contains tmp-only content and not production raw content.
  Commit message: `fix(cycle9): isolate query override paths (AC-1,AC-2,AC-3,AC-4)`

TASK 3: Health Feedback Path Isolation
  AC: AC5 — "derive `feedback_path = (wiki_path.parent if wiki_path else PROJECT_ROOT) / ".data" / "feedback.json"` and pass `path=feedback_path` ... to `get_flagged_pages(path=...)`"; AC6 — "derive `feedback_path` same way as AC5 and pass `path=feedback_path` to `get_coverage_gaps(path=...)`."
  Threat: T2
  Files: `src/kb/mcp/health.py`; `tests/test_mcp_browse_health.py` or new `tests/test_cycle9_mcp_health.py`
  Scope-out: `kb_reliability_map` and `kb_query_feedback` remain out of scope because their MCP tools have no `wiki_dir` kwarg in this cycle.
  Change: In `kb_lint` and `kb_evolve`, compute `wiki_path = Path(wiki_dir) if wiki_dir else None` once and pass the derived `.data/feedback.json` path to confirmed functions `get_flagged_pages(path=...)` and `get_coverage_gaps(path=...)`.
  Test file: `tests/test_cycle9_mcp_health.py`
  Test assertion: with production `.data/feedback.json` populated and tmp project feedback absent, `kb_lint(wiki_dir=tmp_wiki)` shows no low-trust section and `kb_evolve(wiki_dir=tmp_wiki)` shows no coverage gaps.
  Commit message: `fix(cycle9): scope health feedback to wiki_dir (AC-5,AC-6)`

TASK 4: MCP Core Compile And Ingest Caps
  AC: AC7 — "`kb_compile_scan(incremental: bool = True, wiki_dir: str | None = None)` ... thread both to `find_changed_sources(raw_dir, manifest_path)` and `scan_raw_sources(raw_dir)`"; AC8 — "replace the `logger.warning` + `content = content[:QUERY_CONTEXT_MAX_CHARS]` sequence with `return f"Error: source too long ...`"; AC9 — "add regression test asserting `kb_ingest_content` with content > `MAX_INGEST_CONTENT_CHARS` returns Error and leaves `raw/articles/` empty."
  Threat: T1, T2
  Files: `src/kb/mcp/core.py`; `tests/test_mcp_core.py` or new `tests/test_cycle9_mcp_core.py`
  Scope-out: `kb_compile` still lacks `wiki_dir`; `kb_save_source` already uses `_validate_file_inputs`; `kb_ingest_content` already validates and only needs parity coverage.
  Change: Signature change: add optional `wiki_dir` to `kb_compile_scan`; migrate no callers because existing calls use `incremental` by name or default. Derive `raw_dir` and `manifest_path` from `wiki_path.parent` when provided. In `kb_ingest`, reject decoded content above `QUERY_CONTEXT_MAX_CHARS` instead of truncating.
  Test file: `tests/test_cycle9_mcp_core.py`
  Test assertion: `kb_compile_scan(wiki_dir=tmp/wiki)` calls confirmed compiler functions with tmp raw/hash paths; `kb_ingest` returns `Error: source too long` for char overflow; `kb_ingest_content` rejects oversize before writing any raw file.
  Commit message: `fix(cycle9): scope compile scan and reject long ingest prompts (AC-7,AC-8,AC-9)`

TASK 5: Manifest OSError Resilience
  AC: AC10 — "widen `except (json.JSONDecodeError, UnicodeDecodeError)` to `except (json.JSONDecodeError, UnicodeDecodeError, OSError)` (append-only, never replace); log warning; return `{}`."
  Threat: T3
  Files: `src/kb/compile/compiler.py`; `tests/test_compiler_mcp_v093.py` or new `tests/test_cycle9_compiler.py`
  Scope-out: `load_feedback`, `load_verdicts`, and `load_review_history` are already widened; augment manifest stores are a different atomic state machine.
  Change: Add `OSError` to the confirmed `load_manifest(manifest_path: Path | None = None)` catch tuple and keep the warning/reset behavior.
  Test file: `tests/test_cycle9_compiler.py`
  Test assertion: monkeypatch `Path.read_text` for the manifest to raise `OSError`; `load_manifest()` returns `{}` and emits a warning without raising.
  Commit message: `fix(cycle9): tolerate manifest read os errors (AC-10)`

TASK 6: Augment Summary Per Stub
  AC: AC11 — "compute summary from FINAL per-stub state recorded in `ingests` / `verdicts` only. Stub state in `ingests` / `verdicts` is the ONLY source of truth for Saved/Skipped/Failed counts. URL-attempt entries in `fetches` must NEVER be used to infer summary counts. Per-URL detail preserved in the returned `fetches` list."
  Threat: T1
  Files: `src/kb/lint/augment.py`; `tests/test_v5_lint_augment_orchestrator.py` or new `tests/test_cycle9_lint_augment.py`
  Scope-out: `fetches` remains per-URL for audit; manifest URL rows remain unchanged.
  Change: In confirmed `run_augment`, replace summary counts derived from `fetches` with final per-stub outcomes from `ingests` / `verdicts`; do not collapse or otherwise inspect URL-attempt entries for summary totals.
  Test file: `tests/test_cycle9_lint_augment.py`
  Test assertion: one stub with first URL failure and second URL success reports `Saved: 1, Skipped: 0, Failed: 0` while returned `fetches` still contains both URL attempts.
  Commit message: `fix(cycle9): summarize augment by stub outcome (AC-11)`

TASK 7: Single Parse Source Coverage
  AC: AC12 — "keep signature `check_source_coverage(wiki_dir, raw_dir, pages: list[Path] | None = None)`. Inside the for-loop ... call `post = frontmatter.loads(content)` ONCE per page and pass `post.metadata.get("source")` to `normalize_sources` without a second parse."
  Threat: T6
  Files: `src/kb/lint/checks.py`; `tests/test_cycle9_lint_checks.py`
  Scope-out: `check_frontmatter`, `check_staleness`, and `check_frontmatter_staleness` already parse once; no public parameter shape change.
  Change: Preserve the confirmed function signature. For pages with an opening fence, parse frontmatter once and reuse `post.metadata` plus `extract_raw_refs(content)` for the raw-ref merge.
  Test file: `tests/test_cycle9_lint_checks.py`
  Test assertion: spy on `yaml.safe_load` while running `check_source_coverage` over N pages; call count equals N, not 2N, and raw source references still suppress orphan-source warnings.
  Commit message: `fix(cycle9): parse source coverage frontmatter once (AC-12)`

TASK 8: Evolve Orphan Resolver Parity
  AC: AC13 — "in the orphan-concept report ... replace the bare `pid not in backlinks` check with lookup against a `build_graph(wiki_dir)` node set ... Extract a small helper `_orphan_via_graph_resolver(pages, wiki_dir)`."
  Threat: T6
  Files: `src/kb/evolve/analyzer.py`; `tests/test_v0915_task08.py` or new `tests/test_cycle9_evolve.py`
  Scope-out: `build_backlinks` signature stays; `find_connection_opportunities` and `generate_evolution_report` backlink consumers are not orphan-detection sites.
  Change: Add a private helper that uses confirmed `build_graph(wiki_dir, pages=pages_dicts)` semantics to decide whether a concept has inbound graph edges; use it from `analyze_coverage` for `orphan_concepts`.
  Test file: `tests/test_cycle9_evolve.py`
  Test assertion: page A links `[[b]]` and page B is `concepts/b.md`; pre-fix `analyze_coverage` reports B orphan, post-fix it does not.
  Commit message: `fix(cycle9): align evolve orphan resolver with graph (AC-13)`

TASK 9: Capture Hardening Batch
  AC: AC14 — "broaden `except ...` to `except Exception as exc: logger.debug(...)`; continue"; AC15 — "append to `_check_rate_limit` docstring"; AC16 — "add module-level const `_SLUG_COLLISION_CEILING = 10000`"; AC17 — "rename `_path_within_captures` → `_is_path_within_captures`"; AC18 — "split `_normalize_for_scan` to return `list[tuple[str, str]]`"; AC19 — "refactor `_CAPTURE_SECRET_PATTERNS` ... into `list[_SecretPattern]`"; AC20 — "set `item["body"] = body_stripped`"; AC21 — "`MAX_PROMPT_CHARS = 600_000` ... raise `LLMError(...)`."
  Threat: T4, T1, Doc
  Files: `src/kb/capture.py`
  Scope-out: `_scan_for_secrets` main loop stays narrow; `kb_ingest_content`/`kb_save_source` slug paths use different `slugify` + exclusive-create pattern; no callers outside `kb.capture` for private normalize/pattern helpers.
  Change: Apply all capture.py ACs in one atomic capture commit touching ONLY `src/kb/capture.py`: logging broadened decode skips, docstring clarification, bounded slug loop, private helper rename, encoding-label scan results, `_SecretPattern` NamedTuple, stripped body propagation, and prompt preflight guard. Signature change: `_normalize_for_scan` and `_scan_for_secrets` private return contracts change together; grep-confirmed production call sites are inside `capture.py`.
  Test file: covered by TASK 10, not modified in this commit.
  Test assertion: covered by TASK 10; this commit touches ONLY `src/kb/capture.py`.
  Commit message: `fix(cycle9): harden capture scanner and prompt bounds (AC-14,AC-15,AC-16,AC-17,AC-18,AC-19,AC-20,AC-21)`

TASK 10: Capture Test Cleanup
  AC: AC22 — "change `from kb.capture import CAPTURE_KINDS` → `from kb.config import CAPTURE_KINDS`. Update `_path_within_captures` → `_is_path_within_captures`"; AC23 — "two tests — `test_title_with_backslash_round_trips` ... `test_title_with_double_quote_round_trips`"; AC24 — "remove duplicate `import re as _test_re`; fix the byte-comment."
  Threat: T4, Test hygiene
  Files: `tests/test_capture.py`
  Scope-out: no other tests import `CAPTURE_KINDS` or `_path_within_captures` per grep; `yaml_escape` implementation remains characterization-only.
  Change: Commit touches ONLY `tests/test_capture.py`: update imports/assert sites, add YAML round-trip coverage, add capture hardening regressions from TASK 9, remove duplicate import, and correct the byte arithmetic comment.
  Test file: `tests/test_capture.py`
  Test assertion:
  1. AC14 (`_normalize_for_scan` broaden+log): test invokes `_normalize_for_scan` with a segment that triggers `TypeError` (for example, monkeypatch `base64.urlsafe_b64decode` to raise `TypeError`); assert the function returns the rest of the text unchanged and `caplog.records` contains a DEBUG-level log with "normalize" in the message. No inspect-source.
  2. AC15 (docstring invariant): import `kb.capture._check_rate_limit` and assert `"per-process" in _check_rate_limit.__doc__.lower()` and `"TODO(v2)" in _check_rate_limit.__doc__`.
  3. AC16 (`_build_slug` collision cap): inject 10001 colliding slugs in the `existing` set, invoke `_build_slug(kind="note", title="x", existing=existing)`, and assert `RuntimeError` is raised with "collision" in the message.
  4. AC17 (`_path_within_captures` rename): import the NEW name `_is_path_within_captures` from `kb.capture`; assert the OLD name `_path_within_captures` is NOT a module attribute (`hasattr(capture_mod, "_path_within_captures") is False`).
  5. AC18 (`_scan_for_secrets` encoding label): parametrised test with raw-b64-secret and raw-url-secret; assert returned `location` contains "base64" for the first case and "URL" or "URL-encoded" for the second.
  6. AC19 (`_SecretPattern` NamedTuple): import `kb.capture._SecretPattern`, assert it is a subclass of `tuple` with `.label` and `.pattern` fields using `_fields`, and assert every element of `_CAPTURE_SECRET_PATTERNS` is a `_SecretPattern` instance.
  7. AC20 (`_verify_body_is_verbatim` strip): invoke capture end-to-end with an input body containing leading/trailing whitespace; read the written `.md` file; assert frontmatter parse round-trips body without the leading/trailing whitespace.
  8. AC21 (`MAX_PROMPT_CHARS` prompt cap): test asserts `CAPTURE_MAX_BYTES <= MAX_PROMPT_CHARS` at module import time; alternatively, a pytest fixture temporarily raises `kb.capture.CAPTURE_MAX_BYTES` above the cap and reloads the module, asserting `AssertionError`.
  Existing AC22/AC23/AC24 block: frontmatter round-trips exact backslash and quote titles; renamed path guard tests still pass behaviorally; ruff no longer sees the duplicate import.
  Commit message: `fix(cycle9): clean capture tests and imports (AC-22,AC-23,AC-24)`

TASK 11: Test Fixture Source Dirs
  AC: AC25 — "inside `tmp_captures_dir`, after `captures.mkdir(...)`, add `assert captures.resolve().is_relative_to(tmp_project.resolve())`"; AC26 — "replace `RAW_SUBDIRS = ...` with `RAW_SUBDIRS = tuple(sorted(d.name for d in SOURCE_TYPE_DIRS.values()))`."
  Threat: Test hygiene
  Files: `tests/conftest.py`
  Scope-out: `tmp_project` and `tmp_wiki` derive from `tmp_path`; `WIKI_SUBDIRS` stays hardcoded because wiki convention coupling is different from config-derived raw source dirs.
  Change: Commit touches ONLY `tests/conftest.py`: import confirmed `SOURCE_TYPE_DIRS` from `kb.config`, derive `RAW_SUBDIRS`, and assert the capture fixture stays inside `tmp_project`.
  Test file: `tests/conftest.py`
  Test assertion: fixture construction creates every configured raw source subdir and fails loudly if captures escapes the tmp project.
  Commit message: `fix(cycle9): derive raw fixture dirs from config (AC-25,AC-26)`

TASK 12: LLM Error Secret Redaction
  AC: AC27 — "define `_LLM_ERROR_REDACT_PATTERNS` ... Add `_redact_secrets(msg: str) -> str` helper. Call `truncate(_redact_secrets(...), limit=500)` at all 4 redaction sites in `src/kb/utils/llm.py`: line 127 non-retryable caller-bug 4xx classes (`BadRequestError` / `AuthenticationError` / `PermissionDeniedError`), line 171 non-retryable `APIStatusError` status not in `(500, 502, 503, 529)`, line 238 retry-exhausted retryable `APIStatusError`, and line 244 retry-exhausted generic `last_error` path."
  Threat: T4
  Files: `src/kb/utils/llm.py`; `tests/test_cycle9_llm_redaction.py`
  Scope-out: `_sanitize_error_str` in `src/kb/mcp/app.py` handles filesystem paths only; no app-layer path redaction rewrite.
  Change: Add `re` import, co-located redaction constants/helper, and redact before truncate in non-retryable 4xx, non-retryable status, retry-exhausted status, and generic retry-exhausted paths. Preserve confirmed `LLMError` class signature.
  Test file: `tests/test_cycle9_llm_redaction.py`
  Test assertion: use `pytest.mark.parametrize` with exactly 4 monkeypatched anthropic-client failures shaped as `(exception_class, kwargs, expected_redaction_label, assert_not_present_substring)`: line 127 raises `anthropic.BadRequestError(message="...sk-ant-abc123...")` on the first attempt and asserts `[REDACTED:ANTHROPIC_KEY]` is present and `sk-ant-abc123` absent; line 171 raises `anthropic.APIStatusError` with `status_code=418` and a message containing a `Bearer abc123...` token and asserts `[REDACTED:BEARER_TOKEN]` is present and the token absent; line 238 raises retryable `anthropic.APIStatusError` with `status_code=503` on every retry, simulating `MAX_RETRIES + 1` attempts, with message containing `sk-leaked123` and asserts redacted; line 244 raises `anthropic.APITimeoutError` on every retry using `str(last_error)` via a fake `__str__` secret-bearing payload and asserts redacted. Also include a redaction-before-truncation assertion using a secret longer than 500 chars split across the truncation boundary, proving `truncate(_redact_secrets(...), limit=500)` ordering rather than accidental omission from the truncated head.
  Commit message: `fix(cycle9): redact llm error secrets before truncation (AC-27)`

TASK 13: MCP Instructions Sort
  AC: AC28 — "rewrite `mcp.instructions` to sort tool names alphabetically within each thematic block (Browse / Ingest / Quality / Health). Generated at module load — use a tuple-of-tuples source structure + `sorted()` calls so drift is impossible."
  Threat: T7
  Files: `src/kb/mcp/app.py`; `tests/test_cycle9_mcp_app.py`
  Scope-out: README.md and CLAUDE.md tool lists are handwritten and not affected; no downstream client parses the instructions block.
  Change: Replace the literal workflow bullet list with grouped tuples rendered through `sorted()` per block while preserving the current user-facing descriptions.
  Test file: `tests/test_cycle9_mcp_app.py`
  Test assertion: after import, each thematic block in `mcp.instructions` has tool-name bullets sorted alphabetically.
  Commit message: `fix(cycle9): sort mcp instruction tool blocks (AC-28)`

TASK 14: Document Optional Anthropic Key
  AC: AC30 — "change `# Required — Claude API for compile/query/lint operations` to `# Optional — Claude API. NOT needed when using Claude Code ...` `ANTHROPIC_API_KEY=sk-ant-...` line unchanged. Add an active deterministic test in `tests/test_env_example.py` that reads `.env.example` and asserts: (a) the line containing `ANTHROPIC_API_KEY` matches prefix `ANTHROPIC_API_KEY=sk-ant-`; (b) the immediately preceding line contains `optional` case-insensitively and does NOT contain `required`; (c) a clarifying comment in the same block mentions at least one of `Claude Code`, `MCP`, or `use_api=True`."
  Threat: T7
  Files: `.env.example`; `tests/test_env_example.py`
  Scope-out: README.md wording is left to the Step 12 doc-updater parity check; CLAUDE.md already reflects the intended MCP-default positioning.
  Change: Update only the comment block above `ANTHROPIC_API_KEY`; keep the placeholder line unchanged. Add the deterministic `.env.example` assertion in `tests/test_env_example.py`.
  Test file: `tests/test_env_example.py`
  Test assertion: `.env.example` has an `ANTHROPIC_API_KEY=sk-ant-` placeholder, the immediately preceding comment marks it optional and not required, and the same comment block mentions `Claude Code`, `MCP`, or `use_api=True`; then run final hard gates:
```bash
python -m pytest
ruff check
ruff format --check
```
  Commit message: `fix(cycle9): mark anthropic key optional for mcp mode (AC-30)`
