# Cycle 72 — Implementation Plan (Step 07)

**Date:** 2026-05-09
**Branch:** feat/cycle-72
**Pipeline:** dev-mimo-opus
**Owner:** mimocoding-rescue (Step 09 implementation)

---

## Overview

- **Final AC count:** 17
- **Binding conditions:** 14 (all from design-decision.md)
- **Commit count:** 7 (C1–C7)
- **Test file:** `tests/test_cycle72_wrap_extensions.py` (NEW)
- **Lines of scope:** ~180 LoC code + ~680 LoC test + ~15 LoC doc/changelog

---

## Per-commit task plan

### C1 — `src/kb/lint/semantic.py` + `src/kb/config.py` (AC01 + AC04)

**AC01 — `build_fidelity_context` page-content cap**

Source: `src/kb/lint/semantic.py:115` (SINGLE SITE ONLY per condition 1).

- Add helper `_cap_page_content(text: str, max_chars: int) -> str` that truncates text to max_chars with marker `"\n…[truncated for context budget]"` (condition 2).
- Call `_cap_page_content(paired["page_content"], QUERY_CONTEXT_MAX_CHARS - _FENCE_OVERHEAD)` at L115 BEFORE appending.
- Add import: `from kb.utils.text import _FENCE_OVERHEAD`.
- Do NOT modify L428 (completeness) — deferred per threat-model §T1.
- AC06 lock-in verifies truncation (condition 7) + overhead measurement (condition 8).

**AC04 — `build_consistency_context` per-page wrap + constant**

- Modify `src/kb/config.py:467`: `MAX_CONSISTENCY_PAGE_CONTENT_CHARS = 4096 - _FENCE_OVERHEAD` (condition 5).
- Inside `build_consistency_context` loop: wrap each page with `wrap_wiki_context(page_body)` (condition 6 — Approach A).
- Cap each page to `MAX_CONSISTENCY_PAGE_CONTENT_CHARS`.
- Add import: `from kb.utils.text import wrap_wiki_context` (direct form per condition 13).
- AC09 lock-in verifies N=4 tags for 4 pages (condition 10).

### C2 — `src/kb/review/context.py` + `tests/test_phase45_theme3_sanitizers.py` (AC02 + AC02a)

**Atomic commit (single diff covering both regions per condition 3):**

**Assembly migration (L195 + L207):**
- Replace `<wiki_page_body>...</wiki_page_body>` with `wrap_wiki_context(combined_body)`.
- Replace `<raw_source_N>...</raw_source_N>` with section headers INSIDE outer fence.
- Import: `from kb.utils.text import wrap_wiki_context` (direct form per condition 13).

**Checklist text update (L151 + L153) — atomic:**
- Replace `<wiki_page_body>` / `<raw_source_N>` references with `<wiki_context>` (condition 4).

**Pre-existing test update (atomic in same commit):**
- File: `tests/test_phase45_theme3_sanitizers.py` (L352–377).
- Update assertions for new `<wiki_context>` sentinels.

### C3 — `src/kb/lint/augment/orchestrator.py` (AC03)

Source: `src/kb/lint/augment/orchestrator.py:368` (single-line drop-in).

- Locate: `f"<untrusted_source>\n{raw_content}\n</untrusted_source>"` at L368.
- Replace with: `wrap_wiki_context(raw_content)`.
- Add import: `from kb.utils.text import wrap_wiki_context` (direct form per condition 13).

### C4 — `src/kb/lint/augment/proposer.py` (AC05)

Source: `src/kb/lint/augment/proposer.py:155`.

- Locate: `f"{stub_title!r}.\n"` at L155.
- Replace with: `f"{sanitize_extraction_field(stub_title)!r}.\n"` (keep !r per Approach B).
- Add import: `from kb.utils.text import sanitize_extraction_field` (direct form per condition 13).

### C5 — `tests/test_cycle72_wrap_extensions.py` (AC06–AC15: 10 test classes)

**NEW test file (mirror cycle-71 pattern):**

**5 Lock-in tests (AC06–AC10):**
- **AC06:** `TestAC01PageContentCap` — truncation with endswith assertion (condition 7) + runtime `_FENCE_OVERHEAD` check (condition 8).
- **AC07:** `TestAC02ReviewContextMigration` — 2 open tags; `<wiki_page_body>` and `<raw_source_1>` NOT present; checklist references `<wiki_context>` (condition 9).
- **AC08:** `TestAC03OrchestratorPreExtractMigration` — `<wiki_context>` in prompt; `<untrusted_source>` NOT present.
- **AC09:** `TestAC04ConsistencyContextMigration` — N=4 fixture (50k chars/page); exactly 4 open tags; post-cap pages <= cap (condition 10).
- **AC10:** `TestAC05RelevanceScoreStubTitle` — combined-attack fixture (header + frontmatter + >2k chars); header/fence NOT in prompt; length capped (condition 11).

**5 Mutation controls (AC11–AC15):**
- All use `@pytest.mark.xfail(strict=True, reason="cycle-72 AC<N> divergence pin — passing means revert")`.
- Monkeypatch IMPORTED BINDING in call-site module (condition 12):
  - AC11: Patch `_cap_page_content` to identity → assert NOT capped.
  - AC12: Patch `wrap_wiki_context` in context module → assert old sentinels ARE in output.
  - AC13: Patch `wrap_wiki_context` in orchestrator module → assert `<untrusted_source>` IS present.
  - AC14: Patch `wrap_wiki_context` in semantic module → assert fewer tags or pages NOT capped.
  - AC15: Patch `sanitize_extraction_field` → assert `## ATTACKER` IS in prompt.

**All production modules use direct-import form (condition 13):**
```
from kb.utils.text import wrap_wiki_context, sanitize_extraction_field
```

### C6 — `CLAUDE.md` (AC16)

- Update test count: +680 LoC, 10 classes. Update "~3345 collected tests" to new total.
- Update wrap_wiki_context site count: cycle-70 (2) + cycle-71 (4) + cycle-72 (5) = **11 total**.
- Remove "cycle-72+" tag from deferred-peer list (all 5 resolved).
- Inline cycle-72 AC list: "5 code + atomic test coupling + 5 lock-in + 5 mutations".

### C7 — `CHANGELOG.md` + `CHANGELOG-history.md` + `BACKLOG.md` (AC17)

**CHANGELOG.md `[Unreleased]`:**
```
Items: 17 ACs. Tests: +680 LoC, 10 classes. Scope: semantic.py (fidelity cap + consistency), review/context.py (review migrate + atomic checklist), orchestrator/proposer.py (pre-extract + stub_title), config.py, test_cycle72_wrap_extensions.py (NEW). Detail: Extends cycle-70/71 fence family to 4 residual sites. All 14 binding conditions encoded as Step-07 sub-ACs. 3 cycle-73+ deferred entries in BACKLOG.md per lifecycle.
```

**BACKLOG.md (condition 14):**
- DELETE all 5 cycle-72+ LOW entries.
- ADD 3 deferred entries with literal token `deferred — file BACKLOG entry post-cycle-72`:
  1. `build_completeness_context` cap + wrap pair (cycle-73+)
  2. `kb.lint.verdict_db` `prompt_version` schema (cycle-73+)
  3. Tier-boundary verifier (cycle-73+)

---

## Step-9 implementation order (TDD)

1. **C5 first (RED):** Create test file. Run — expect lock-ins FAIL, mutations xfail as expected.
2. **C1–C4 (GREEN):** Implement AC01–AC05. Run — expect lock-ins PASS, mutations XPASS (strict).
3. **C6–C7 (DOCS):** Update docs + changelog + BACKLOG.
4. **Step-12 full-suite:** `pytest --strict-xfail` passes.

---

## Risks (cycle-lessons applied)

- **cycle-9 L1:** AC02+AC02a atomic commit prevents assembly/checklist gap.
- **cycle-11 L2:** AC01 single-site per threat-model §T1; completeness deferred.
- **cycle-15 L1:** All mutations `xfail(strict=True)` to catch reverts.
- **cycle-16 L1:** AC06–AC10 reach production call sites.
- **cycle-20 L1:** Mutations monkeypatch IMPORTED BINDING.
- **cycle-22 L5:** All 14 conditions → Step-7 sub-ACs.
- **cycle-23 R1:** 3 deferred entries contain literal token for cycle-73+ grep.
- **cycle-24 L1:** AC06 endswith for marker; AC09 count for fences.
- **cycle-71 L1+L2:** Mutations patch IMPORTED BINDING in call-site modules.

---

## Final checklist before Step-9

- [ ] All 14 binding conditions encoded as per-commit sub-ACs
- [ ] C1 introduces `_cap_page_content` + AC04 constant + imports
- [ ] C2 is atomic (assembly + checklist + test update in single diff)
- [ ] C3 is single-line drop-in
- [ ] C4 adds `sanitize_extraction_field`, keeps !r
- [ ] C5 creates 10 test classes with monkeypatch-imported-binding
- [ ] C6 updates CLAUDE.md counts
- [ ] C7 deletes 5 cycle-72+ entries + adds 3 deferred with literal token
- [ ] Cycle-lessons applied per risk section

**STATUS:** READY FOR STEP-09 IMPLEMENTATION
