PLAN-AMENDS-DESIGN: user-mandated commit order moves `tests/conftest.py` before `src/kb/utils/pages.py`; FINAL DECIDED DESIGN's per-file plan listed `src/kb/utils/pages.py` first and `tests/conftest.py` sixth. This plan follows the user's required 13-commit order.

# Cycle 11 Implementation Plan

Source verification completed before task authoring:
- `grep -rn` confirmed all requested function definitions in `src/kb/ingest/pipeline.py`, `src/kb/graph/builder.py`, `src/kb/utils/pages.py`, `src/kb/compile/compiler.py`, `src/kb/cli.py`, `src/kb/query/engine.py`, `src/kb/mcp/core.py`, and `tests/conftest.py`.
- `grep -n` for class references in the relevant `src/` files found no class declarations or custom class references to call/modify; only comments containing the word "class" in `src/kb/utils/pages.py` and `src/kb/lint/checks.py`.
- Read the required decision docs, `CLAUDE.md`, `tests/conftest.py`, and relevant source/test files to confirm signatures and imports.

TASK 1: Enhance tmp_project Fixture
  Files: `tests/conftest.py`
  Change: Extend confirmed `tmp_project(tmp_path: Path) -> Path` fixture to create `wiki/index.md` and `wiki/_sources.md` with minimal canonical headers alongside existing wiki/raw subdirs and `log.md`.
  Test: Existing and later AC12 tests can use `tmp_project/"wiki"/"index.md"` and `tmp_project/"wiki"/"_sources.md"` without manual scaffolding; failing assertion before change is file existence/read of those two paths.
  Criteria: AC12
  Threat: T4

TASK 2: Move Canonical Page Helpers To utils.pages
  Files: `src/kb/utils/pages.py`
  Change: Add confirmed public `page_id(page_path, wiki_dir=None)` using the current `kb.graph.builder.page_id` semantics, add confirmed `scan_wiki_pages(wiki_dir=None)` using `WIKI_SUBDIRS`, update `load_all_pages` to call `page_id`, and keep confirmed `_page_id` as `_page_id = page_id` compatibility alias with deprecation comment.
  Test: New `tests/test_cycle11_utils_pages.py` will fail before change because `from kb.utils.pages import page_id, scan_wiki_pages` cannot import the public helpers.
  Criteria: AC4, AC5, AC6
  Threat: T3

TASK 3: Convert graph.builder Helpers To Re-export Shim
  Files: `src/kb/graph/builder.py`
  Change: Replace confirmed local `page_id` and `scan_wiki_pages` bodies with re-exports from `kb.utils.pages`; preserve confirmed `build_graph` and `graph_stats` behavior and imports needed for graph construction.
  Test: Existing `tests/test_graph.py` imports from `kb.graph.builder` must still pass; new AC6 tests assert canonical behavior through `kb.utils.pages`.
  Criteria: AC4, AC5
  Threat: T3, T8

TASK 4: Switch Internal Graph Helper Imports Atomically
  Files: `src/kb/compile/linker.py`, `src/kb/evolve/analyzer.py`, `src/kb/lint/checks.py`, `src/kb/lint/runner.py`, `src/kb/lint/semantic.py`, `src/kb/compile/compiler.py`
  Change: Switch confirmed internal `page_id` and/or `scan_wiki_pages` imports from `kb.graph.builder` to `kb.utils.pages`. In confirmed `detect_source_drift`, preserve function-local import placement and import `page_id as get_page_id` plus `scan_wiki_pages` from `kb.utils.pages`.
  Test: Existing graph/lint/evolve/compile tests still pass; grep should show no internal `from kb.graph.builder import page_id` or `scan_wiki_pages` in these six modules.
  Criteria: AC5
  Threat: T3

TASK 5: Harden Ingest Coercion And Reject comparison/synthesis
  Files: `src/kb/ingest/pipeline.py`
  Change: Use confirmed `_coerce_str_field` at string-consuming extraction reads: authors/title path, key_claims/key_points/key_arguments reads, `_extract_entity_context` overview fields, entity/concept summary reads, final contradiction key-claim read where applicable. Add early confirmed `ingest_source` guard rejecting `source_type in {"comparison", "synthesis"}` with `ValueError` naming `kb_create_page` before `source_path.read_bytes()`.
  Test: New `tests/test_cycle11_ingest_coerce.py` asserts malformed non-str extraction fields raise clean `ValueError`, not `AttributeError`/`TypeError`; direct `ingest_source(..., source_type="comparison"|"synthesis")` raises before writing files.
  Criteria: AC1, AC2, AC3
  Threat: T1, T2

TASK 6: Add MCP Ingest Special-case Guidance
  Files: `src/kb/mcp/core.py`
  Change: In confirmed `kb_ingest` and `kb_ingest_content`, add `source_type in {"comparison", "synthesis"}` checks before the generic `SOURCE_TYPE_DIRS` unknown-type rejection, returning sanitized `Error:` text naming `kb_create_page`.
  Test: New `tests/test_cycle11_ingest_coerce.py` asserts MCP wrapper calls for comparison/synthesis return guidance mentioning `kb_create_page` and do not write raw/wiki files.
  Criteria: AC2
  Threat: T2

TASK 7: Simplify test_ingest Scaffolding
  Files: `tests/test_ingest.py`
  Change: Update confirmed `test_ingest_source` to use enhanced `tmp_project`, create only the source file and contradictions file if still needed, and call confirmed `ingest_source` with explicit `wiki_dir=tmp_project/"wiki"` and `raw_dir=tmp_project/"raw"` instead of multi-patching wiki/raw bindings.
  Test: `tests/test_ingest.py::test_ingest_source` still asserts summary/entity/concept pages, index update, and `_sources.md` update; failure before Task 1 would be missing fixture index/source files.
  Criteria: AC12
  Threat: T4

TASK 8: Add Manifest Content Stability Assertion
  Files: `tests/test_compile.py`
  Change: Extend confirmed `test_compile_loop_does_not_double_write_manifest` without removing the existing `save_manifest` counter: capture parsed manifest JSON after first confirmed `compile_wiki` call, run the same compile again, and assert parsed manifest content remains equal while retaining the one-save expectation.
  Test: Failing assertion targets a compile refactor that writes incorrect manifest content or mutates existing single-source hashes on the second run.
  Criteria: AC13
  Threat: T9

TASK 9: Add Direct utils.pages Tests
  Files: `tests/test_cycle11_utils_pages.py`
  Change: Add direct tests for confirmed `page_id` and `scan_wiki_pages` at canonical `kb.utils.pages`: lowercasing, subdir separator preservation, root sentinel files excluded because scanning only `WIKI_SUBDIRS`, and deterministic sorted order.
  Test: New test file fails before Tasks 2-3 because public `page_id`/`scan_wiki_pages` do not exist in `kb.utils.pages`.
  Criteria: AC6
  Threat: T3

TASK 10: Add CLI Import And Version Short-circuit Tests
  Files: `tests/test_cycle11_cli_imports.py`
  Change: Add `click.testing.CliRunner` coverage for confirmed Click callbacks `cli`, `ingest`, `compile`, `query`, `lint`, `evolve`, and `mcp` with deep workers monkeypatched. Add subprocess tests for `--version` and `-V` that set `sys.argv` before `import kb.cli` and assert `kb.config` is not imported.
  Test: Failing assertions: each command exits 0 without `ImportError`/`ModuleNotFoundError`; subprocess returncode is 0, stdout contains `kb, version`, and `kb.config` is absent from `sys.modules`.
  Criteria: AC7, AC8
  Threat: T5, T6

TASK 11: Add Stale Result Edge-case Tests
  Files: `tests/test_cycle11_stale_results.py`
  Change: Add direct tests for confirmed `_flag_stale_results`: `sources=[]` and missing `sources` keep `stale=False`; malformed `updated` values `["yesterday", "04/19/2026", "", 20260101]` keep `stale=False`; source mtime date equal to page `updated` keeps `stale=False`.
  Test: New test file asserts no exception and `result[0]["stale"] is False` for each branch.
  Criteria: AC9, AC10, AC11
  Threat: T7

TASK 12: Add Ingest Coercion And Dead-path Regression Tests
  Files: `tests/test_cycle11_ingest_coerce.py`
  Change: Add tests for confirmed `_coerce_str_field` fail-fast contract; direct confirmed `ingest_source` rejection for comparison/synthesis; confirmed `kb_ingest` and `kb_ingest_content` MCP guidance; and an extraction regression that exercises pre-validation for non-str fields.
  Test: Assertions: `None` and missing fields coerce to `""`, strings unchanged, int/list/bool/dict/float raise `ValueError`; comparison/synthesis errors mention `kb_create_page`; no files are written for rejected MCP content ingest.
  Criteria: AC1, AC2, AC3
  Threat: T1, T2

TASK 13: Sync Changelog And Backlog
  Files: `CHANGELOG.md`, `BACKLOG.md`
  Change: Add Cycle 11 `[Unreleased]` notes covering AC1-AC13, list new test files, and remove resolved backlog items named in AC14 while preserving deferred items.
  Test: Documentation review assertion: resolved Cycle 11 backlog entries are absent from `BACKLOG.md`; `CHANGELOG.md` documents graph helper relocation, ingest dead-path closure, CLI/stale-result tests, fixture cleanup, and manifest assertion.
  Criteria: AC14
  Threat: T10
