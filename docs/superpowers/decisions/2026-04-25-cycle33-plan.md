# Cycle 33 — Implementation Plan (Step 7)

Drafted in primary session per cycle-14 L1 (operator holds full Steps 1-6 context). Five tasks, one commit per file per `feedback_batch_by_file`.

## Pre-flight grep verification (already done in Step 4)

| Symbol | File:line | Status |
|---|---|---|
| `_sanitize_error_str` | `src/kb/mcp/app.py:136` (imported `core.py:70`) | EXISTS |
| `sanitize_error_text` | `src/kb/utils/sanitize.py:56` | EXISTS |
| `_rel` (mcp.app) | `src/kb/mcp/app.py:116` | EXISTS |
| `_update_sources_mapping` | `src/kb/ingest/pipeline.py:761` | EXISTS, signature `(source_ref, wiki_pages, wiki_dir=None) -> None` |
| `_update_index_batch` | `src/kb/ingest/pipeline.py:801` | EXISTS, signature `(entries: list[tuple[str, str, str]], wiki_dir=None) -> None` |
| `atomic_text_write` | `src/kb/utils/io.py` (imported `pipeline.py`) | EXISTS |
| `tmp_kb_env` fixture | `tests/conftest.py:128` | EXISTS, mirror-rebinds SOURCE_TYPE_DIRS into `kb.*` modules at conftest:248-255 |
| `tmp_wiki` fixture | `tests/conftest.py:81` | EXISTS |
| `caplog.set_level(level, logger=name)` | pytest stdlib (Context7 verified) | EXISTS |
| `pytest.mark.xfail(strict=True)` | pytest stdlib (Context7 verified) | EXISTS |
| `mock.MagicMock(wraps=fn)` | unittest.mock (stdlib) | EXISTS |

---

## TASK 1: `src/kb/mcp/core.py` — path-leak fixes (AC1 + AC2 + AC3 + AC4)

**Files:** `src/kb/mcp/core.py` only.

**Change:**

1. **AC1 + AC3 (kb_ingest_content @ lines 748-764).** In the `except OSError as write_err:` block, immediately after `file_path.unlink(missing_ok=True)`, add a single binding:

   ```python
   sanitized_err = _sanitize_error_str(write_err, file_path)
   ```

   Replace `write_err` in `logger.warning(...)` with `sanitized_err`. Replace `({write_err})` in the return f-string with `({sanitized_err})`.

2. **AC2 + AC3 (kb_save_source @ lines 868-883).** Same pattern — bind `sanitized_err = _sanitize_error_str(write_err, file_path)`, swap both call sites.

3. **AC4 (kb_query.save_as @ lines 279-281).** Replace `logger.warning("save_as write failed for slug=%r: %s", slug, exc)` with `logger.warning("save_as write failed for slug=%r: %s", slug, _sanitize_error_str(exc, target))`. Replace `return f"\n[warn] save_as failed: {_sanitize_error_str(exc)}"` with `return f"\n[warn] save_as failed: {_sanitize_error_str(exc, target)}"`. Per Q2 + Codex R1-02, this passes `target` (a `Path` already in scope at line 269: `target = synthesis_dir / f"{slug}.md"`) for symmetric depth.

**Test (TDD):** None in this task — TASK 2 lands the tests first if going strict-TDD; otherwise this task lands together with TASK 2 since they are tightly coupled (the test file calls into `mcp/core.py`). Convention: one commit per file means we land the production change first, then the test file in TASK 2 — but the cycle-22 L5 self-check requires the spy/revert-fail chain to fire on the same PR, which it does either way.

**Criteria refs:** AC1, AC2, AC3, AC4.
**Threat refs:** T1, T2, T3, T5.
**CONDITIONS satisfied (per design Step 5):** Pre-compute once (Q1), multi-arg logger shape preserved (Q3), AC4 both log and return get `target` (Q2).

**Acceptance grep:**
- `grep -n "_sanitize_error_str(write_err, file_path)" src/kb/mcp/core.py` → 2 matches (one per OSError block).
- `grep -n "_sanitize_error_str(exc, target)" src/kb/mcp/core.py` → 2 matches (line 280 logger AND line 281 return).
- `grep -nE "logger\.warning.*write_err\)" src/kb/mcp/core.py` → 0 matches in OSError blocks (sanitized_err only).
- `grep -nE "logger\.warning.*, exc\)" src/kb/mcp/core.py` → 0 matches in kb_query.save_as block.

---

## TASK 2: `tests/test_cycle33_mcp_core_path_leak.py` — AC5 + R1-03 + Q8 xfail-strict

**File:** new `tests/test_cycle33_mcp_core_path_leak.py`.

**Change:** Create new test file with these test classes:

### `class TestPathLeakRedaction` (integration tests against MCP tools)

Uses `tmp_kb_env` fixture (which mirror-rebinds `SOURCE_TYPE_DIRS`). Each test:
1. `caplog.set_level(logging.WARNING, logger="kb.mcp.core")` (Q7).
2. Construct expected leaky filename under tmp via the patched SOURCE_TYPE_DIRS resolution.
3. Monkeypatch `os.fdopen` to raise an `OSError(errno, msg, leaky_path_str)` where `leaky_path_str` is a literal absolute path that should NOT appear in the output.
4. Invoke the MCP tool (`kb_ingest_content`, `kb_save_source`, or `kb_query` with `save_as`).
5. Assert returned-string + `caplog.text` contain neither the raw `leaky_path_str` (in either Windows `D:\\` or forward-slash `D:/` form) nor any drive-letter / POSIX-root literal.
6. Defensive assertion (Q9): the `file_path` derived in the test resolves under `tmp_path`.

Test methods (8 total):
- `test_kb_ingest_content_windows_path_redacted` — leaky_path = `"D:\\Projects\\test\\fake.md"`.
- `test_kb_ingest_content_posix_path_redacted` — leaky_path = `"/tmp/test/fake.md"`.
- `test_kb_save_source_windows_path_redacted`
- `test_kb_save_source_posix_path_redacted`
- `test_kb_query_save_as_windows_path_redacted` — uses `kb_query(save_as="..."` path; force OSError on `save_page_frontmatter` via monkeypatch.
- `test_kb_query_save_as_posix_path_redacted`
- `test_kb_query_save_as_return_string_redacted` — explicitly asserts the return string also has no path leak (covers AC4 return-side fix per Q2).
- `test_kb_query_save_as_log_record_redacted` — explicitly asserts caplog records have no path leak (covers AC4 log-side fix per Q2).

### `class TestSanitizeErrorTextOSErrorShapes` (R1-03 IN-CYCLE unit suite)

5 parametrised cases against `kb.utils.sanitize.sanitize_error_text` directly:
- 3-arg form `OSError(13, "Access is denied", "/home/user/secret.md")` — assert `/home/` not in output.
- No-filename `OSError("Access is denied")` — assert string returned unchanged (no path to leak).
- `filename=None` 3-arg — `OSError(13, "Access is denied", None)` — assert no error and no spurious `<path>`.
- `filename2=` 5-arg `OSError(errno.EACCES, "MoveFile failed", "C:\\\\src.md", None, "C:\\\\dst.md")` — assert neither `C:\\src.md` nor `C:\\dst.md` raw.
- Path in `args[1]` only — `OSError(13, "/home/user/leaked.md")` (errno + free-text args[1] holding a path) — assert `/home/` not in output (regex sweep catches this).

### `class TestSanitizeErrorTextUNCAndLongPath` (Q8 xfail-strict)

3 tests; mark `pytest.mark.xfail(strict=True, reason="sanitize.py UNC/long-path filename slash-normalize bug — see BACKLOG cycle-34 candidate")`. **Important:** if a test currently PASSES against `sanitize_error_text`, leave it un-xfailed; if it currently FAILS, mark xfail-strict so a future helper fix flips it to PASS and forces the marker removal.

- `test_sanitize_error_text_long_path_filename_redacts` — `OSError(13, "Access is denied", "\\\\?\\C:\\\\Projects\\\\foo.md")`.
- `test_sanitize_error_text_unc_filename_redacts` — `OSError(13, "Access is denied", "\\\\server\\share\\secret.md")`.
- `test_sanitize_error_text_unc_long_path_filename_redacts` — `OSError(13, "Access is denied", "\\\\?\\UNC\\server\\share\\foo.md")`.

**Approach for xfail decision:** I will REPL-probe each shape against the current helper before committing the file. Any that PASS get plain assertions; any that FAIL get xfail-strict. (Cycle-16 L3 — reproduce the failure mode first; don't write phantom-failure-mode tests.)

**Criteria refs:** AC5.
**Threat refs:** T1, T2, T3, T4, T5, T10.

**Acceptance grep / runtime:**
- `pytest tests/test_cycle33_mcp_core_path_leak.py -v` → 8 integration tests + 5 unit tests + 3 UNC tests = 16 cases (counting xfails as cases). All non-xfail cases PASS.
- Revert-fail (Step-11): `git stash push src/kb/mcp/core.py && pytest tests/test_cycle33_mcp_core_path_leak.py::TestPathLeakRedaction -v` produces ≥ 4 failures across the 8 integration tests.

---

## TASK 3: `src/kb/ingest/pipeline.py` — Idempotency docstrings (AC6)

**Files:** `src/kb/ingest/pipeline.py` only.

**Change:**

1. Add `## Idempotency` paragraph to `_update_sources_mapping` docstring (currently at lines 764-768):

   ```
   ## Idempotency

   Safe to re-call after a crash that aborted the ingest before manifest-save.
   The first call's effect is preserved; the second call is a no-op for already-
   present entries (identical source-ref + identical pages → existing line
   unchanged; new pages → MERGED into the existing line via the dedup branch).

   **Idempotency holds for serial re-calls only.** Concurrent invocations
   (e.g. two parallel ingests writing the same source-ref simultaneously) are
   NOT synchronised — the read-modify-write window is unguarded. See BACKLOG
   `IndexWriter` abstraction entry for the open concurrency surface.
   ```

2. Add equivalent `## Idempotency` paragraph to `_update_index_batch` docstring (currently at line 802) — parameterised for index entries (`if [[subdir/slug]] already in section: skip`).

**Test:** None — TASK 4 covers behaviour.

**Criteria refs:** AC6.
**Threat refs:** T7, T8 (documentation arm; behaviour arm in TASK 4).

**Acceptance grep:**
- `grep -nc "Idempotency" src/kb/ingest/pipeline.py` → ≥ 2.
- `grep -n "concurrent calls" src/kb/ingest/pipeline.py` → ≥ 2 (per docstring).
- `grep -n "IndexWriter" src/kb/ingest/pipeline.py` → ≥ 2 (per docstring back-ref).

---

## TASK 4: `tests/test_cycle33_ingest_index_idempotency.py` — AC7 + AC8 + R1-09

**File:** new `tests/test_cycle33_ingest_index_idempotency.py`.

**Change:** Create new test file with these tests:

### `class TestSourcesMappingIdempotency` (AC7)

1. `test_repeat_call_no_duplicate_and_no_write` (AC7 dedup contract + Q11 spy):
   - Use `tmp_wiki` fixture; seed `_sources.md` (created via the fixture's parent `tmp_path`-scoped helper or written explicitly).
   - Spy: `spy = mock.MagicMock(side_effect=kb.ingest.pipeline.atomic_text_write)`.
   - `monkeypatch.setattr("kb.ingest.pipeline.atomic_text_write", spy)`.
   - Call `_update_sources_mapping("raw/articles/x.md", ["entities/foo", "concepts/bar"], wiki_dir=tmp_wiki)` TWICE.
   - Assert `_sources.md` text contains exactly ONE `raw/articles/x.md` line.
   - Assert `spy.call_count == 1` (Q11 — second call short-circuits without writing per dedup branch).

2. `test_missing_sources_file_warns_and_skips` (R1-09 IN-CYCLE):
   - Use `tmp_wiki` minus `_sources.md` (do NOT seed the file).
   - Call `_update_sources_mapping("raw/articles/x.md", ["entities/foo"], wiki_dir=tmp_wiki)`.
   - Assert `caplog.text` contains `"_sources.md not found — skipping source mapping"` (matches `pipeline.py:774`).
   - Assert `_sources.md` does NOT exist after the call.

### `class TestIndexBatchIdempotency` (AC7)

1. `test_repeat_call_no_duplicate_and_no_write`:
   - Seed empty `index.md` with the section headers (`## Entities`, `## Concepts`, etc.) and `*No pages yet.*` placeholders (use the same template `tmp_project` fixture builds at conftest:98-118, but only need the index file).
   - Spy `atomic_text_write` as above.
   - Call `_update_index_batch([("entity", "foo", "Foo Title"), ("concept", "bar", "Bar Title")], wiki_dir=tmp_wiki)` TWICE.
   - Assert `index.md` contains exactly ONE `[[entities/foo|Foo Title]]` and exactly ONE `[[concepts/bar|Bar Title]]`.
   - Assert `spy.call_count == 1`.

### `class TestSourcesMappingCrashRecovery` (AC8)

1. `test_dedup_then_merge_branch`:
   - Seed `_sources.md` empty.
   - Spy `atomic_text_write`.
   - Step (a)+(b)+(c)+(d): call `_update_sources_mapping("raw/articles/x.md", ["entities/foo", "concepts/bar"], wiki_dir=tmp_wiki)` once → assert `_sources.md` contains line; call AGAIN with same args → assert content unchanged AND `spy.call_count == 1` (no second write — Q11 no-op contract).
   - Step (e)+(f) merge branch: call `_update_sources_mapping("raw/articles/x.md", ["entities/foo", "concepts/bar", "concepts/baz"], wiki_dir=tmp_wiki)` (added `concepts/baz`) → assert the existing `raw/articles/x.md` line MERGES (single line, contains all 3 page IDs) AND `spy.call_count == 2` (Q11 merge-branch DOES write a second time).

   Test docstring: `"""manifest never updated; this scenario is functionally equivalent to a crash before manifest-save (function is manifest-agnostic by construction, Q5)."""`.

**Criteria refs:** AC7, AC8.
**Threat refs:** T7, T8.

**Acceptance grep / runtime:**
- `pytest tests/test_cycle33_ingest_index_idempotency.py -v` → 4 tests, all PASS.
- `grep -n "spy.call_count" tests/test_cycle33_ingest_index_idempotency.py` → ≥ 4 matches (the 1+1+1+2 assertions across tests).

---

## TASK 5: `BACKLOG.md` + `CHANGELOG.md` + `CHANGELOG-history.md` + `CLAUDE.md` (AC9 + AC10 + AC11)

**Files:** `BACKLOG.md`, `CHANGELOG.md`, `CHANGELOG-history.md`, `CLAUDE.md`.

**Change:**

### `BACKLOG.md`

1. **DELETE** the `mcp/core.py:762,881` MEDIUM entry (currently at lines 146-147 of BACKLOG.md per cycle-32 file state) — failure mode closed by AC1+AC2+AC3+AC4 (Q12).

2. **REWRITE** the `ingest/pipeline.py` index-file write order MEDIUM entry (currently at lines 143-144) to focus ONLY on the residual concurrency-RMW surface (Q10 + Q12). New text:

   ```markdown
   - `ingest/pipeline.py:761-791` `_update_sources_mapping` and `pipeline.py:801-829` `_update_index_batch` perform RMW (read full file → mutate in memory → atomic-write) without holding `file_lock(target_path)`. Concurrent ingests may produce lost-update races even though serial re-calls dedup correctly (cycle 33 pinned the serial dedup contract via docstring + spy regression). Fix-recipe: introduce `IndexWriter` helper wrapping all four index-file writes (`_sources.md`, `index.md`, `_categories.md`, `log.md`) with a documented order and per-file `file_lock`; until then, callers MUST serialise ingest manually. (R2; cycle 33 narrowed from "duplicate entries on re-ingest" to concurrency residual)
   ```

3. **ADD** new MEDIUM `BACKLOG.md` entry under §"Phase 4.5 MEDIUM" for the sanitize.py UNC/long-path gap (Q8 spawn):

   ```markdown
   - `utils/sanitize.py:30-31,68-75` `_rel(Path(fn_str))` slash-normalises Windows path separators (`\\` → `/`) BEFORE the `_ABS_PATH_PATTERNS` regex sweep. The regex covers backslash UNC + long-path forms only (`sanitize.py:13-17`); after `_rel` runs on an outside-PROJECT_ROOT filename attribute, paths in shapes `\\?\C:\...`, `\\server\share\...`, and `\\?\UNC\server\share\...` slip through redaction. Three xfail-strict tests in `tests/test_cycle33_mcp_core_path_leak.py::TestSanitizeErrorTextUNCAndLongPath` document the gap. (R1; surfaced 2026-04-25 cycle 33 R1 Codex; spawn entry per Q8)
     (fix: union `_ABS_PATH_PATTERNS` with forward-slash UNC/long-path variants OR pre-normalize-check before `_rel(Path(fn_str))` substitution)
   ```

4. **ADD** three new MINOR entries under §"Phase 4.5 MEDIUM" (or a new MINOR section if needed) for AC11 deferred items:

   - `ingest/pipeline.py:770-779` `_update_sources_mapping` empty-list semantics — when `wiki_pages=[]`, the function emits `- \`raw/path\` → \n` (a line with no page refs). Behaviour is undocumented; design call needed (skip-empty? error? emit empty?). (R1-08; surfaced cycle 33 R1 Codex)
   - `ingest/pipeline.py:771-784` `_update_sources_mapping` backtick-in-`source_ref` lookup mismatch — `escaped_ref = source_ref.replace(\`\`\`, r"\\\`")` written but raw `source_ref` searched at lines 777/784. Repeat calls with backtick-bearing source_refs could duplicate. Low-risk for normal slugified MCP inputs which exclude backticks. (R1-10; surfaced cycle 33 R1 Codex)
   - `mcp/core.py:167-178,681-686,812-817` `kb_ingest_content` and `kb_save_source` filename validation is weaker than `kb_query.save_as` (line 188-216) — no ASCII / Windows-reserved-name / homoglyph checks; just empty + length + content size. Slugify preserves Unicode word chars (`utils/text.py:173`). (R1-11; surfaced cycle 33 R1 Codex)

### `CHANGELOG.md`

ADD compact Quick Reference row to `[Unreleased]` (newest first):

```markdown
- **Cycle 33 — backlog-by-file (mcp.core path-leak + ingest.pipeline idempotency-pin):**
  - **Items:** AC1+AC2+AC3 wrap raw `OSError write_err` interpolation in `_sanitize_error_str(write_err, file_path)` at `mcp/core.py` `kb_ingest_content` (lines 756-764) + `kb_save_source` (lines 875-883) post-create-OSError emitters (closes mcp/core.py:762,881 BACKLOG MEDIUM, threat T11 from cycle 32). AC4 same-class peer at `mcp/core.py:279-281` `kb_query` `save_as` write-failure path — both the `logger.warning` AND the return string upgraded to `_sanitize_error_str(exc, target)` for symmetric depth. AC6+AC7+AC8 add idempotency docstring to `ingest/pipeline.py:761,801` and pin the serial dedup contract via `atomic_text_write` spy regressions. AC9 deletes the closed BACKLOG entry. AC10 narrows the index-write BACKLOG entry from "duplicate-on-reingest" (closed) to "RMW-concurrency residual" (still open). AC11 files three MINOR BACKLOG entries (empty wiki_pages, backtick source_ref, weaker filename validation). New BACKLOG MEDIUM: `sanitize.py` UNC/long-path filename slash-normalize gap (Q8 spawn).
  - **Tests:** +N new tests across 2 new files (test_cycle33_mcp_core_path_leak.py + test_cycle33_ingest_index_idempotency.py). Total now {2901 + N} tests.
  - **Scope:** `src/kb/mcp/core.py` (5 line-level swaps), `src/kb/ingest/pipeline.py` (docstrings only, zero behaviour change), 2 new test files.
  - **Detail:** see `CHANGELOG-history.md` cycle 33.
```

(Replace `+N` with the actual collected-count delta after Step 9 ships per cycle-15 L4. Replace commit count with `+TBD` per cycle-30 L1.)

### `CHANGELOG-history.md`

ADD per-cycle detail block at TOP (newest first) — full prose covering each AC, the threat references, the symbol grep table, and the design-gate question outcomes.

### `CLAUDE.md`

Update test count in "Quick Reference" line — 2901 → new count.
Update "Latest full-suite" line in §Implementation Status.
No module-map update needed (no new modules).

**Test:** None (doc-only).

**Criteria refs:** AC9, AC10, AC11.
**Threat refs:** T11 (closed in CHANGELOG narrative).

**Acceptance grep:**
- `grep -c "mcp/core.py:762,881" BACKLOG.md` → 0 (deleted).
- `grep -c "_update_sources_mapping" BACKLOG.md` → 1 (rewritten entry).
- `grep -c "sanitize.py" BACKLOG.md` → ≥ 1 (new entry).
- `grep -c "Cycle 33" CHANGELOG.md` → ≥ 1 (Quick Reference row).
- `grep -nc "Cycle 33" CHANGELOG-history.md` → ≥ 1 (detail block).

---

## Order of execution

1. TASK 3 (docstrings only — least invasive, no test dep).
2. TASK 4 (test file for TASK 3 — pins the docstring contract behaviourally).
3. TASK 1 (production code change in mcp/core.py).
4. TASK 2 (test file for TASK 1 — pins the path-leak fix; revert-fail check fires on this file).
5. TASK 5 (doc updates — depends on TASK 1/2/3/4 commits being known for `git log` count).

Each task = one commit (per `feedback_batch_by_file`).

After all 5 tasks: full pytest + ruff at Step 10. Step-11 security verify against threat model. Step-12 doc-count-backfill on TASK 5's `+N` placeholders.

## Risk + sizing

- Per cycle-13 L2: each task is < 30 LOC code + < 100 LOC tests + uses only stdlib + pytest + already-imported helpers. Primary-session implementation appropriate; Codex dispatch unnecessary.
- Per cycle-22 L3: full suite green is the gate, not isolation passes. The `tmp_kb_env` mirror-rebind handles the snapshot-binding hazard (cycle-18 L1).
- Per cycle-20 L1: the new tests use `pytest.raises(...)` only minimally; if any pop, late-bind via the production module attribute.
- Per cycle-22 L5: every CONDITION from the Step 5 design gate maps to a specific assertion in the test files (spy call counts, caplog assertions, missing-file warning, xfail markers).
