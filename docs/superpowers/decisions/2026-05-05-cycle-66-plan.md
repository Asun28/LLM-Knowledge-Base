# Cycle 66 — Implementation Plan

**Date:** 2026-05-05
**Branch:** feat/cycle-66
**Step:** 7 of 24
**Owner:** MiMo Coding v2.5-pro (plan generation) + Step 9 implementer
**Inputs:** requirements.md, threat-model.md, brainstorm.md, design-eval-R1-opus.md, design.md

---

## Plan summary

Commit order (IR-6): **AC5 → AC1 → AC4 → AC3 → AC2**

Five atomic file-grouped commits. Scope: 5 code ACs + test-infra AC6 + doc AC7.

Total: ~165 LOC production + ~460 LOC tests ≈ 155 minutes (~2.5 hours).

---

## TASK-AC5 — Drop allow_symlinks kwarg

**Commit:** fix(path-safety): drop unused allow_symlinks kwarg (AC5)

**Production:** src/kb/utils/path_safety.py lines 13,37,50,99

**Changes:**
- Line 13: "Hard cap: 4" → "Hard cap: 3 (cycle-66 AC5)"
- Line 37: Remove allow_symlinks parameter
- Line 50: Remove docstring entry
- Line 99: if path.is_symlink(): (unconditional)

**Tests:** tests/test_cycle66_path_safety_symlink.py (new, 3 tests)
- test_signature_excludes_allow_symlinks
- test_symlink_rejected_under_tmp_path
- test_compiler_validates_symlink_rejection

**Verify:** python -m pytest tests/test_cycle66_path_safety_symlink.py -v

**Effort:** 20 minutes | **Closes:** T7 (symlink opt-out structural closure)

---

## TASK-AC1 — Remove dead __getattr__ branch

**Commit:** fix(config): remove dead __getattr__ branch, clarify comment (AC1)

**Production:** src/kb/config.py lines 760-776

**Changes:**
- Delete lines 772-773 (dead branch)
- Rewrite comment: PROJECT_ROOT bound at line 107, only AUGMENT_ALLOWED_DOMAINS uses PEP 562

**Tests:** tests/test_cycle66_config_pep562.py (new, 3 tests)
- test_get_project_root_uses_module_binding_not_dead_branch (IR-2 monkeypatch pattern)
- test_project_root_monkeypatch_propagates
- test_kb_project_root_env_override

**Verify:** python -m pytest tests/test_cycle66_config_pep562.py -v

**Effort:** 25 minutes | **Closes:** T1 (test divergent-fail on branch revert)

---

## TASK-AC4 — Consolidate CVE-import walks + helper

**Commit:** refactor(test-security): consolidate CVE walks, add find_module_imports (AC4)

**Production (new helper):** tests/_helpers/ast_walk.py

Helper signature:
  def find_module_imports(module: str, *, src_root: Path) -> dict[str, list[Path]]
Returns {"import": [...], "from": [...]}
Detects ast.Import + ast.ImportFrom, handles namespace prefixes

**Tests (MANDATORY real coverage, not stubs):**

1. tests/_helpers/test_ast_walk.py (append, 8+ cases for find_module_imports):
   - test_find_module_imports_bare_form
   - test_find_module_imports_from_form
   - test_find_module_imports_both_forms
   - test_find_module_imports_namespace_prefix_bare
   - test_find_module_imports_namespace_prefix_from
   - test_find_module_imports_unrelated_module
   - test_find_module_imports_syntax_error_file
   - test_find_module_imports_missing_src_root

2. tests/test_security_cve_greps.py (modify, replace 4 methods):
   - test_module_zero_imports (parametrized over 4 modules)

3. tests/test_cycle66_cve_greps_consolidated.py (new):
   - `test_both_import_forms_detected` — parametrized over `["diskcache", "litellm", "pip", "ragas"]`. **Per banned module**, the fixture writes BOTH `tmp_path/_test_only_bare_{module}.py` containing `import {module}` AND `tmp_path/_test_only_from_{module}.py` containing `from {module} import X`. Then runs `find_module_imports({module}, src_root=tmp_path)` and asserts `result["import"]` contains the bare-form file AND `result["from"]` contains the from-form file. This is the load-bearing T6 closure: if the walker silently misses one form, the per-module fixture fires RED for that module's "import" or "from" key.

**Verify:**
  python -m pytest tests/_helpers/test_ast_walk.py::TestFindModuleImports -v
  python -m pytest tests/test_security_cve_greps.py::TestCVEBannedImports -v
  python -m pytest tests/test_cycle66_cve_greps_consolidated.py -v

**Effort:** 45 minutes | **Closes:** T6 (silent-pass on both-import-forms)

---

## TASK-AC3 — Add heuristic walk-up cache

**Commit:** perf(config): add lru_cache to _resolve_project_root (AC3)

**Production:** src/kb/config.py lines 1, 15-82, 76

**Changes:**
- Add import functools
- Add @functools.lru_cache(maxsize=8) on new _heuristic_walk_up_cached(cwd_str: str)
- Refactor _resolve_project_root to call cached inner
- Update _reset_project_root to call cache_clear()

**Tests:** tests/test_cycle66_project_root_cache.py (new, 5 tests)
- test_cache_hit_on_second_call (Q-7.3: spy cache_info().hits)
- test_reset_clears_cache
- test_env_override_bypasses_cache
- test_module_binding_override_bypasses_cache
- test_cwd_change_invalidates_cache

**Verify:** python -m pytest tests/test_cycle66_project_root_cache.py -v

**Effort:** 35 minutes | **Closes:** T5 (cache key includes cwd)

---

## TASK-AC2 — Expand scrub-key set from CLI_BACKEND_ENV_INJECT

**Commit:** security(cli-backend): expand scrub-key set (AC2)

**Production:** src/kb/utils/cli_backend.py lines 138-159

**Changes:**
Add _SCRUB_KEYS frozenset (11 keys: 4 standalone + 7 from CLI_BACKEND_ENV_INJECT)
Add design comment (module-literal derivation, cycle-19 L2 safe, no re-hardcoding)
Update _check_no_secrets_on_argv loop

Post-AC2 keys (11 total, 5 net-new):
  Standalone: ANTHROPIC, FIRECRAWL, MIMOCODING, MIMOCHAT
  From CLI_BACKEND_ENV_INJECT: GEMINI, OPENAI, KIMI, QWEN, DEEPSEEK, ZAI, ZHIPUAI
  Net-new: GEMINI, KIMI, QWEN, ZAI, ZHIPUAI

**Critical:** Parametrize source MUST be the LIVE canonical map `kb.config.CLI_BACKEND_ENV_INJECT.values()` flattened, NOT a literal mirror of the post-AC2 `_SCRUB_KEYS` frozenset. This is what closes T4 (future-revert hazard).

**Concrete parametrize implementation:**
```python
import kb.config
from kb.utils.cli_backend import _check_no_secrets_on_argv
from kb.utils.llm import LLMError

# Source-of-truth: the canonical map + 4 standalone keys. NEVER hardcode.
_CANONICAL_SCRUB_KEYS = sorted(
    {"ANTHROPIC_API_KEY", "FIRECRAWL_API_KEY", "MIMOCODING_API_KEY", "MIMOCHAT_API_KEY"}
    | {k for v in kb.config.CLI_BACKEND_ENV_INJECT.values() for k in v}
)


@pytest.mark.parametrize("key", _CANONICAL_SCRUB_KEYS)
def test_scrub_blocks_argv_with_env_value(key, monkeypatch):
    sentinel = f"sentinel-value-for-{key}-12345"
    monkeypatch.setenv(key, sentinel)
    with pytest.raises(LLMError, match=r"(?i)refusing to place env secret"):
        _check_no_secrets_on_argv(["kb", "--header", f"Authorization: Bearer {sentinel}"])
```

**Comment block to add to `cli_backend.py` immediately above `_SCRUB_KEYS`** (verbatim from design.md AC2 diff):
- Document that `_SCRUB_KEYS` derives at IMPORT TIME from `CLI_BACKEND_ENV_INJECT` (canonical, module-literal dict at config.py:329-338).
- Explicitly state "DO NOT re-hardcode this list" — adding a 9th backend automatically gains scrub coverage.
- Document why import-time capture is safe under cycle-19 L2: L2 targets env-DERIVED constants. `CLI_BACKEND_ENV_INJECT` is a module-literal dict (NOT env-derived), so capturing it at import time is fine. (IR-1 clarification.)

**Tests:** tests/test_cycle66_secret_scrub.py (new)
- `test_scrub_blocks_argv_with_env_value` (parametrized over `_CANONICAL_SCRUB_KEYS` — auto-expands when CLI_BACKEND_ENV_INJECT gains a new backend).
- `test_scrub_allows_argv_without_env_value` (negative control — closes T3: substring-scrub self-DoS intended behavior preserved; sentinel placed in argv WITHOUT being set as env value, asserts no exception).
- `test_scrub_empty_env_value` (edge case — empty env value short-circuits scrub even if argv contains the literal env-var name).

**Verify:** python -m pytest tests/test_cycle66_secret_scrub.py -v

**Effort:** 30 minutes | **Closes:** T2 (argv leak of 5 net-new keys), T3 (substring scrub false-positive — intended behavior preserved by negative-control test), T4 (revert hazard CLOSED by parametrize-from-canonical source).

---

## TASK-AC7 — Step 17 doc updates (deferred from Step 9)

**Pipeline step:** Step 17 (doc-update gate, owner: DeepSeek-rescue / primary-session). NOT a Step 9 commit; runs after all 5 AC commits land.

**Files edited at Step 17:**

1. **`BACKLOG.md`** — DELETE the `## Cycle 66 candidates (2026-05-04, surfaced by cycle-65 reviews)` section (current lines 27-46). Per `feedback_batch_by_file` BACKLOG lifecycle.

2. **`CHANGELOG.md` `[Unreleased]` Quick Reference** — Add compact one-liner entry per FORMAT GUIDE. Locked text per IR-4 must frame AC3 as "test-suite + dev-loop perf" NOT "MCP-boundary production perf". Example shape:
   ```
   - **Cycle 66 (carry-over hardening — eighth dev-mimo-opus trial cycle)** — Items: 5 ACs / 4 src files + 1 helper module + 5 new test files. (A) AC1 dead `__getattr__` branch removal in `kb.config`; (B) AC2 `_check_no_secrets_on_argv` derives from `CLI_BACKEND_ENV_INJECT` (5 net-new keys: GEMINI/KIMI/QWEN/ZAI/ZHIPUAI); (C) AC3 `lru_cache(maxsize=8)` on heuristic walk-up (test-suite + dev-loop perf — production typically sets `KB_PROJECT_ROOT` and short-circuits before the cache); (D) AC4 `find_module_imports` AST helper covering both `ast.Import` + `ast.ImportFrom`; (E) AC5 `_assert_under_project_root` `allow_symlinks` kwarg dropped (Q2.2 cap 4→3). | Tests: 3134 → 3134+~20 (~20 new across 6 test files). | Files: ~213 → ~219 (+6: 5 cycle-66 + helper coverage in test_ast_walk.py). | Detail: see CHANGELOG-history.md cycle-66 + docs/superpowers/decisions/2026-05-05-cycle-66-{requirements,threat-model,brainstorm,design-eval-R1-opus,design-eval-R2-deepseek,design,plan,plan-gate}.md
   ```

3. **`CHANGELOG-history.md`** — Add full per-AC bullet detail newest-first (matches cycle-65 narrative depth). Lists this cycle's 8 decision files as Detail.

4. **`CLAUDE.md` Quick Reference** — Update the test-count claim (3134 → post-Step-12 count) and the cycle-history table at line 13.

5. **`docs/reference/error-handling.md`** — Q2.2 entry: "Hard cap: 4 keyword-only parameters" → "Hard cap: 3 keyword-only parameters (cycle-66 AC5 dropped `allow_symlinks`)" per design.md AC7 + audit B-1.

6. **`docs/reference/architecture.md`** — Optional: note the new `find_module_imports` helper in the test-helpers section if such a section exists. (May skip if architecture.md doesn't currently enumerate test helpers.)

**Verify:** read each updated file at Step 17 and confirm the diff matches the locked text above. PR description (Step 18) cites this task as the doc-sync trail.

**Effort:** 20 minutes | **Closes:** AC7 (BACKLOG cleanup, CHANGELOG entry, CLAUDE.md sync, doc-cap update).

**Origin:** Audit B-1 (error-handling.md update missing) + M-1 (CHANGELOG framing absent) inline-resolved here per cycle-21 L1.

---

## Cross-cutting tasks

### CC1 — Background reviewer dispatch (DeepSeek V4 Pro)

Dispatch AFTER AC2 commit lands (per IR-8).
Focus: frozenset derivation soundness + parametrize-from-canonical closes T4.
Expected: ~30 min turnaround in parallel with AC3 impl.

### CC2 — Step-12 CI hard-gate

Command: python -m pytest --tb=short
Gate: 3134+20 passed, 21 skipped (full suite, no regressions)

### CC3 — Step-13 coverage delta

Command: python -m pytest --cov=src/kb --cov-report=term-missing
Gate: touched files >=90%, repo total <=0.5pp regression

---

## Open questions (Q-7.1 through Q-7.5) — All resolved

**Q-7.1:** Commit order?
→ Confirmed: AC5 → AC1 → AC4 → AC3 → AC2 (lowest-risk first, highest-stakes last)

**Q-7.2:** AC1 monkeypatch pattern?
→ Resolved: Try monkeypatch.setattr first, fallback to direct binding per IR-2

**Q-7.3:** AC3 cache spy?
→ Decided: cache_info().hits increment (idiomatic lru_cache spy)

**Q-7.4:** AC4 namespace-prefix matching?
→ Confirmed: Helper includes startswith logic; test cases #4/#5 validate

**Q-7.5:** DeepSeek dispatch timing?
→ Confirmed: AFTER AC2 commit per IR-8

---

## Risk register

Risk 1: AC1 monkeypatch may fail → Mitigation: IR-2 documents fallback
Risk 2: AC4 helper-test obligation forgotten → Mitigation: IR-3 MANDATORY, Step 14 verifies >=8 real tests
Risk 3: AC2 parametrize drifts → Mitigation: Design comment forbids re-hardcoding
Risk 4: AC3 perf claim overstated → Mitigation: CHANGELOG frames as test-suite/dev perf (IR-4)

---

## Effort summary

| AC | Production LOC | Test LOC | Minutes |
|----|---|---|---|
| AC5 | 25 | 60 | 20 |
| AC1 | 30 | 70 | 25 |
| AC4 | 50 | 150 | 45 |
| AC3 | 40 | 100 | 35 |
| AC2 | 20 | 80 | 30 |
| TOTAL | 165 | 460 | 155 (~2.5h) |

---

## Step 7 closure checklist

- [x] 5 ACs → TASK-AC1..AC5
- [x] Files, line ranges, diffs, tests, verify commands, effort per AC
- [x] Cross-cutting tasks (CC1-CC3)
- [x] Open questions Q-7.1..Q-7.5 answered
- [x] Risk register (AC4 helper-test non-optional)
- [x] Commit order locked (IR-6)
- [x] Threat closures per AC (T1-T7)

→ Proceed to Step 8 (plan-gate audit, MiMo v2.5-pro audit role).
