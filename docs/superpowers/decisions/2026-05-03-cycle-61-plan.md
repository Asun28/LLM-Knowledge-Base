=== answer ===
# Cycle 61 — Implementation Plan

> **Design-decision source:** `2026-05-03-cycle-61-design-decision.md`
> **Total ACs:** 22 (AC1–AC22)
> **Total CONDITIONS:** 16 (C1–C16)
> **Total tasks:** 23 (Tasks 01–03 previously completed, Tasks 03–23 elaborated below)

---

## Tasks 01–02 (Previously Completed)

> Tasks 01 and 02 were written in the prior plan file and are already landed.
> They cover AC1–AC4 and AC9 (core plumbing and initial allowlist groundwork).
> No further action required for this cycle.

---

## Task 03 — `src/kb/lint/checks/duplicate_slug.py` (AC6 consumer migration)

**ACs:** AC6

**Action:**
Refactor `duplicate_slug.py` so that the hard-coded `KNOWN_SLUG_EXCEPTIONS` frozenset is removed and replaced with a call to a new helper `load_lint_allowlist("duplicate_slug")` that reads from `config/lint_allowlist.json`. At line 42, where the current code performs `if slug in KNOWN_SLUG_EXCEPTIONS`, replace it with `if slug in allowlist_dup_slugs` where `allowlist_dup_slugs` is loaded once at module scope via the new helper. The helper must cache the result in a `functools.lru_cache(maxsize=1)` so repeated lint invocations do not re-read disk. If the JSON file is missing or malformed, the helper must log a warning via `logging.getLogger("kb.lint")` and return an empty `frozenset` so the linter degrades gracefully rather than crashing.

**File:line target:** `src/kb/lint/checks/duplicate_slug.py` — lines 38–55 (remove `KNOWN_SLUG_EXCEPTIONS`, insert `load_lint_allowlist` call at line 42)

**Tests:** Task 09 (`tests/test_lint.py` — AC8)

**Dependency:** Task 05 (allowlist JSON file must exist before the consumer can load it)

**Verification:** Run `pytest tests/test_lint.py -k duplicate_slug -v`; confirm the 3 allowlist-loading cases pass. Manually verify `grep -r "KNOWN_SLUG_EXCEPTIONS" src/` returns zero hits.

---

## Task 04 — `src/kb/compile/compiler.py` (AC22: extend `rebuild_indexes` signature + thread caller=) [REWRITTEN PER STEP 08 BLOCKER GAP 3]

**ACs:** AC22

**Action:** Two edits in `compiler.py`:
1. **Extend `rebuild_indexes()` signature** at `compiler.py:642`. Add a keyword-only `*, caller: str = "cli"` parameter. The existing positional/keyword arguments are unchanged. Backward-compatible default `"cli"` means existing callers (CLI, library code) continue to work.
2. **Thread `caller=` to `append_wiki_log` call** at `compiler.py:809`. Change from `append_wiki_log("rebuild-indexes", msg, log_path)` to `append_wiki_log("rebuild-indexes", msg, log_path, caller=caller)`.
3. **Existing `append_wiki_log` call at `compiler.py:575`** (inside `compile_wiki()`) is LEFT UNCHANGED — `compile_wiki` is only invoked from CLI / library contexts, the new `caller="cli"` default in Task 01's signature covers it. No edit needed.

**File:line target:** `src/kb/compile/compiler.py` — line 642 (signature), line 809 (call). Total 2 edits in compiler.py.

**Tests:** Task 18 (`tests/test_utils_io.py` — AC22 unit pin on `append_wiki_log` signature) AND Task 11 (`tests/test_mcp_browse_health.py` — AC13(d) audit-log substring `"caller=mcp"` after `kb_rebuild_indexes` invocation).

**Dependency:** Task 01 (append_wiki_log signature must accept `caller=` kwarg first). Blocks Task 08 (MCP tool calls `rebuild_indexes(..., caller="mcp")`).

**Verification:** `grep -nE "def rebuild_indexes" src/kb/compile/compiler.py` shows the new signature; `grep -nE "caller=caller" src/kb/compile/compiler.py` shows the threading at line ~809; `pytest tests/test_compile.py -v` passes.

---

## Task 05 — `config/lint_allowlist.json` (AC7 new file)

**ACs:** AC7

**Action:**
Create the file `config/lint_allowlist.json` with the following top-level structure:
```json
{
  "duplicate_slug": [
    "home",
    "index",
    "readme"
  ],
  "frontmatter_re": []
}
```
The `duplicate_slug` array contains the three slugs that were previously in `KNOWN_SLUG_EXCEPTIONS`. The `frontmatter_re` array is seeded empty for future expansion. The file must be valid JSON (no trailing commas, UTF-8 encoded). Place it under `config/` so that the `load_lint_allowlist` helper in `src/kb/lint/checks/_allowlist.py` can locate it via `importlib.resources` or `Path(__file__).resolve().parents[3] / "config"`.

**File:line target:** `config/lint_allowlist.json` — new file (entire file)

**Tests:** Task 09 (`tests/test_lint.py` — AC8 file-load tests)

**Dependency:** None (leaf file; no code imports it until Task 03)

**Verification:** `python -m json.tool config/lint_allowlist.json` exits 0. `pytest tests/test_lint.py -k allowlist -v` passes.

---

## Task 06 — `src/kb/query/engine.py` (AC10: production short-circuit) [REWRITTEN PER STEP 08 BLOCKER GAP 1]

**ACs:** AC10

**Action:** `engine.py` is module-level functions, NOT a class — there is no `query()` method, no `QueryEngine` class, no `self._settings`. The production search path is the module-level function `search_pages()` (around line 47). At line 204 (the `try:` block guarding `vector_results = vector_search(question, candidate_limit)` at line 205), wrap the existing try/except with a `_kb_disable_vectors()` guard:

```python
# AC10 (cycle 61) — KB_DISABLE_VECTORS=1 short-circuit
if kb.config._kb_disable_vectors():
    logger.info("hybrid_search: KB_DISABLE_VECTORS=1 — vector layer skipped")
    vector_results = []
else:
    try:
        vector_results = vector_search(question, candidate_limit)
    except Exception as exc:
        logger.warning(
            "hybrid_search backend=vector failed: %s (%s); query_tokens=%d",
            exc.__class__.__name__,
            exc,
            query_tokens_count,
        )
        vector_results = []
```

The existing try/except block at lines 204–213 is REPLACED with the if/else above. Required: `import kb.config` added near the top of engine.py (existing imports use `from kb.config import ...` for many constants; either keep both `from` imports + add `import kb.config` for the function call OR change to `kb.config._kb_disable_vectors()` via the existing module reference).

**File:line target:** `src/kb/query/engine.py` — replace lines 204–213 (the `try: vector_results = vector_search(...) except Exception ...` block) with the guarded if/else above.

**Tests:** Task 10 (`tests/test_query.py` — AC11). AC11 must invoke `search_pages()` (or the public `query_wiki()` that wraps it), spy on `kb.query.embeddings.get_vector_index` (the closure's actual entry point) — NOT spy on `vector_fn` directly (which only catches the test-only `hybrid_search` API).

**Dependency:** Task 02 (`_kb_disable_vectors()` helper must exist first).

**Verification:** `grep -nE "_kb_disable_vectors\(\)" src/kb/query/engine.py` returns at least one hit; `grep -nE "from kb.config import.*KB_DISABLE_VECTORS" src/kb/query/engine.py` returns ZERO (cycle-18 L1 snapshot-binding hazard prevention); `pytest tests/test_query.py -k disable_vectors -v` passes 2 cases.

---

## Task 07 — `src/kb/query/hybrid.py` (AC10: mirror guard) [REWRITTEN PER STEP 08 BLOCKER GAP 2]

**ACs:** AC10

**Action:** `hybrid_search()` is defined at line 54 (NOT line 32 — line 32 is inside `rrf_fusion()`). The signature is `hybrid_search(question, bm25_fn, vector_fn, expand_fn=None, *, limit=10)` — there is NO `settings` parameter. Insert the mirror short-circuit at the top of the function body (after the docstring at line 70, before `vector_limit = limit * VECTOR_SEARCH_LIMIT_MULTIPLIER` at line 71):

```python
# AC10 (cycle 61) — KB_DISABLE_VECTORS=1 mirror short-circuit (test-only API; production path is engine.py)
if kb.config._kb_disable_vectors():
    logger.info("hybrid_search: KB_DISABLE_VECTORS=1 — vector layer skipped")
    return bm25_fn(question, limit)
```

When the env-var is set, the function returns the BM25 result directly without RRF fusion. Required: `import kb.config` near the top of hybrid.py (existing imports use `from kb.config import ...` for `BM25_SEARCH_LIMIT_MULTIPLIER` etc.; add `import kb.config` separately for the function call).

**File:line target:** `src/kb/query/hybrid.py` — insert at line 71 (immediately after docstring close at line 70). New `import kb.config` line near top of file (line ~5).

**Tests:** Task 10 (`tests/test_query.py` — AC11, mirror test class). The mirror test invokes `hybrid_search` directly with stub `bm25_fn`/`vector_fn` and asserts `vector_fn` is NOT called when env-var set.

**Dependency:** Task 02 (`_kb_disable_vectors()` helper). Independent of Task 06 — both can run in parallel within the same commit.

**Verification:** `grep -nE "_kb_disable_vectors\(\)" src/kb/query/hybrid.py` returns at least one hit; `grep -nE "from kb.config import.*KB_DISABLE_VECTORS" src/kb/query/hybrid.py` returns ZERO; `pytest tests/test_query.py -k disable_vectors -v` 2 cases pass.

---

## Task 08 — `src/kb/mcp/health.py` (AC12: new `kb_rebuild_indexes` MCP tool) [REWRITTEN PER STEP 08 BLOCKER GAP 4]

**ACs:** AC12 (public MCP tool surface) + relies on AC22 (internal `caller=` threading via Task 04)

**Action:** Add a new `@mcp.tool()`-decorated function to `health.py`. **The public signature has NO `caller` parameter** (the previous plan's `caller: str = "mcp"` exposed an internal-only field on the public API — Step 8 BLOCKER 4). The MCP tool internally calls `rebuild_indexes(wiki_dir=..., caller="mcp")` (Task 04 extended `rebuild_indexes` signature for this). Use the existing `_validate_health_wiki_dir` pattern at `health.py:18` for path validation:

```python
@mcp.tool()
def kb_rebuild_indexes(wiki_dir: str | None = None) -> dict:
    """Rebuild KB search indexes (vector DB + manifest + LRU caches).

    Wraps kb.compile.compiler.rebuild_indexes for MCP clients.
    The wiki_dir is validated via _validate_health_wiki_dir (cycle-7
    dual-anchor path safety pattern). Audit log entry is tagged with
    caller="mcp" per cycle-20 L3 + BACKLOG.md:374 mandate.

    Args:
        wiki_dir: Optional path to wiki directory. Must be inside PROJECT_ROOT.
                  Defaults to kb.config.WIKI_DIR.

    Returns:
        dict with rebuild status. On error, returns
        {"error": "<sanitized message>"} per cycle-23 sanitize_error_text.
    """
    wiki_path, err = _validate_health_wiki_dir(wiki_dir)
    if err is not None:
        return {"error": err}
    try:
        return rebuild_indexes(wiki_dir=wiki_path, caller="mcp")
    except Exception as exc:
        return {"error": sanitize_error_text(str(exc))}
```

Place after the last existing `@mcp.tool()` in `health.py` (currently at line 239). Imports needed: `from kb.compile.compiler import rebuild_indexes`, `from kb.utils.errors import sanitize_error_text` (verify the latter exists; if not, use a basic `str()` truncation per cycle-23 path-disclosure pattern).

**File:line target:** `src/kb/mcp/health.py` — append after line 239 (the last existing `@mcp.tool()` decorator). NO entry needed in any `_TOOL_TABLE` list — FastMCP `@mcp.tool()` registration is decorator-based; there is no separate registry list (the previous plan claim of "register in `_TOOL_TABLE` at line 140" was incorrect — verified by reading the file).

**Tests:** Task 11 (`tests/test_mcp_browse_health.py` — AC13, 4 cases including AC22 audit-log assertion).

**Dependency:** Task 04 (`rebuild_indexes` must accept `caller="mcp"` kwarg first).

**Verification:** `grep -nE "def kb_rebuild_indexes" src/kb/mcp/health.py` returns 1; the function appears under a `@mcp.tool()` decorator; `pytest tests/test_mcp_browse_health.py -k rebuild_indexes -v` passes 4 cases (happy path, invalid wiki_dir, wrapped exception, audit-log substring).

---

## Task 09 — `tests/test_lint.py` (AC8: TestDuplicateSlugAllowlistFileLoad)

**ACs:** AC8

**Action:**
Add a new test class `TestDuplicateSlugAllowlistFileLoad` to `tests/test_lint.py` containing three test methods:

1. `test_allowlist_json_loads_and_contains_expected_slugs` — Import `load_lint_allowlist`, call it with `"duplicate_slug"`, assert the returned frozenset equals `{"home", "index", "readme"}`.
2. `test_allowlist_missing_file_returns_empty_frozenset` — Monkeypatch the config path to a nonexistent directory; assert the return value is `frozenset()` and that a `WARNING`-level log record was emitted.
3. `test_allowlist_malformed_json_returns_empty_frozenset` — Write invalid JSON to a tmp file; monkeypatch the config path; assert graceful degradation.

Each test must import from `src.kb.lint.checks._allowlist` (the helper added in Task 03's refactor).

**File:line target:** `tests/test_lint.py` — append after existing `TestDuplicateSlug` class (approximately line 95)

**Tests:** This task *is* the test.

**Dependency:** Tasks 03 and 05 (consumer code and JSON file must exist)

**Verification:** `pytest tests/test_lint.py -k TestDuplicateSlugAllowlistFileLoad -v` — 3 passed.

---

## Task 10 — `tests/test_query.py` (AC11: TestKBDisableVectors)

**ACs:** AC11

**Action:**
Add a new test class `TestKBDisableVectors` to `tests/test_query.py` containing two test methods:

1. `test_engine_query_short_circuits_when_vectors_disabled` — Create a `KBSettings` with `kb_disable_vectors=True`, instantiate `QueryEngine`, call `query("test")`. Assert the result source is `"keyword"` and that no embedding model mock was called.
2. `test_hybrid_search_short_circuits_when_vectors_disabled` — Call `hybrid_search("test", settings=settings_with_vectors_disabled)`. Assert the result source is `"keyword"` and that no vector similarity mock was invoked.

Both tests use `unittest.mock.patch` to spy on the vector-search code path.

**File:line target:** `tests/test_query.py` — append after existing test classes (approximately line 180)

**Tests:** This task *is* the test.

**Dependency:** Tasks 06 and 07 (both guard sites must be in place)

**Verification:** `pytest tests/test_query.py -k TestKBDisableVectors -v` — 2 passed.

---

## Task 11 — `tests/test_mcp_browse_health.py` (AC13: TestKbRebuildIndexes)

**ACs:** AC13, AC22

**Action:**
Add a new test class `TestKbRebuildIndexes` to `tests/test_mcp_browse_health.py` containing four test methods:

1. `test_rebuild_indexes_success` — Mock `rebuild_all_indexes` to return `{"count": 3}`; assert response is `{"ok": True, "indexes_rebuilt": 3}`.
2. `test_rebuild_indexes_failure_returns_error` — Mock `rebuild_all_indexes` to raise `RuntimeError("boom")`; assert response is `{"ok": False, "error": "boom"}`.
3. `test_rebuild_indexes_registered_in_tool_table` — Import `_TOOL_TABLE` from `health.py`; assert `"kb_rebuild_indexes"` is in the list.
4. `test_rebuild_indexes_default_caller_is_mcp` — Inspect the signature of `kb_rebuild_indexes` via `inspect.signature`; assert the default value of `caller` parameter is `"mcp"`. This pins the AC22 contract for the MCP path.

**File:line target:** `tests/test_mcp_browse_health.py` — append after existing test classes (approximately line 120)

**Tests:** This task *is* the test.

**Dependency:** Task 08 (MCP tool must be implemented)

**Verification:** `pytest tests/test_mcp_browse_health.py -k TestKbRebuildIndexes -v` — 4 passed.

---

## Task 12 — `tests/test_compile.py` (AC18: dual-site prune assertion)

**ACs:** AC18

**Action:**
Extend the existing test `test_prune_base_uses_canonical_rel_path_at_both_sites` in `tests/test_compile.py` to add a second assertion verifying that `compile_wiki(mode="full")` also prunes using the canonical relative path. Create a new fixture `tmp_full_wiki_project` that sets up a temporary project with a stale output file whose path uses the old non-canonical form. Assert that after `compile_wiki(mode="full")` the stale file is removed and the canonical path is written. This covers both the incremental-prune site and the full-prune site, completing the dual-site assertion required by AC18.

**File:line target:** `tests/test_compile.py` — modify existing test at approximately line 200, add new fixture at line 190

**Tests:** This task *is* the test.

**Dependency:** None (compile code already implements canonical paths; this test asserts correctness)

**Verification:** `pytest tests/test_compile.py -k test_prune_base_uses_canonical_rel_path -v` — both variants pass.

---

## Task 13 — `tests/test_lint_query_fixes_v092.py` (AC14: replace `inspect.getsource` with logger spies)

**ACs:** AC14

**Action:**
The file `tests/test_lint_query_fixes_v092.py` currently contains two `inspect.getsource(...)` calls used to verify that specific error-handling code paths exist in production modules. Replace both sites with behavioral tests using logger spies. For the first site (around line 45), patch `logging.getLogger("kb.lint").error` and trigger a lint error by passing a malformed input; assert `logger.error` was called with a substring matching the expected error message. For the second site (around line 78), patch `logging.getLogger("kb.query").exception` and trigger a query failure; assert `logger.exception` was called. Both replacements must import from `unittest.mock` and use `assert_called_once_with(...)`.

**File:line target:** `tests/test_lint_query_fixes_v092.py` — lines 45 and 78

**Tests:** This task *is* the test.

**Dependency:** None (modifying existing test file)

**Verification:** `pytest tests/test_lint_query_fixes_v092.py -v` — all tests pass. `grep -n "inspect.getsource" tests/test_lint_query_fixes_v092.py` returns zero hits.

---

## Task 14 — `tests/test_v0911_phase392.py` (AC15: behavioral threshold-flow test)

**ACs:** AC15

**Action:**
In `tests/test_v0911_phase392.py`, the current test uses `inspect.getsource` to verify the threshold-flow logic exists. Replace it with two complementary checks: (a) `hasattr(module, "apply_threshold_flow") and callable(module.apply_threshold_flow)` to confirm the function exists, and (b) a behavioral test that feeds a known input through `apply_threshold_flow(input_values=[0.1, 0.5, 0.9], threshold=0.4)` and asserts the output is `[False, True, True]`. This ensures the threshold logic is tested by behavior rather than source-code introspection.

**File:line target:** `tests/test_v0911_phase392.py` — replace `inspect.getsource` block at approximately line 30

**Tests:** This task *is* the test.

**Dependency:** None (modifying existing test file)

**Verification:** `pytest tests/test_v0911_phase392.py -v` — all tests pass. `grep -n "inspect.getsource" tests/test_v0911_phase392.py` returns zero hits.

---

## Task 15 — `tests/test_v0915_task01.py` (AC16: drop identity check, add behavioral test)

**ACs:** AC16

**Action:**
The file `tests/test_v0915_task01.py` currently contains an identity assertion `assert builder.WIKI_SUBDIRS is expected_tuple` that compares object identity of the subdirectory list. Drop this check entirely. Replace it with a behavioral test: create a temporary wiki project with subdirectories matching `WIKI_SUBDIRS`, run `builder.discover_wiki_pages(tmp_path)`, and assert that all expected subdirectories appear in the result set. This tests the *function* of `WIKI_SUBDIRS` rather than its identity, making the test resilient to tuple/list type changes.

**File:line target:** `tests/test_v0915_task01.py` — remove identity assertion at approximately line 25, add behavioral test at same location

**Tests:** This task *is* the test.

**Dependency:** None (modifying existing test file)

**Verification:** `pytest tests/test_v0915_task01.py -v` — all tests pass. `grep -n "is expected_tuple\|identity" tests/test_v0915_task01.py` returns zero hits.

---

## Task 16 — `tests/test_v0915_task08.py` (AC17: replace `inspect.getsource` with FRONTMATTER_RE divergencer)

**ACs:** AC17

**Action:**
In `tests/test_v0915_task08.py`, replace the `inspect.getsource(...)` call that checks for the `FRONTMATTER_RE` pattern in `src/kb/lint/frontmatter.py` with a concrete behavioral reproducer. The new test must: (1) construct a markdown string with frontmatter containing a known divergent edge case (e.g., a value with an embedded triple-dash `---` inside a quoted YAML string), (2) run `FRONTMATTER_RE.match(text)`, and (3) assert the match captures the correct group boundaries. This ensures the regex is tested against a real divergent input rather than merely checking that the source code contains a regex literal.

**File:line target:** `tests/test_v0915_task08.py` — replace `inspect.getsource` block at approximately line 20

**Tests:** This task *is* the test.

**Dependency:** None (modifying existing test file)

**Verification:** `pytest tests/test_v0915_task08.py -v` — all tests pass. `grep -n "inspect.getsource" tests/test_v0915_task08.py` returns zero hits.

---

## Task 17 — `tests/test_cycle21_cli_backend.py` (AC5 D5 absorption)

**ACs:** AC5

**Action:**
Extend the existing test in `tests/test_cycle21_cli_backend.py` with two new assertions that pin the sandbox-flag behavior absorbed from the D5 decision. Specifically: (1) Assert that when `--sandbox` is passed to the CLI entry point, the resulting `KBSettings` object has `sandbox_mode == True` — this confirms the flag propagates to the settings layer. (2) Assert that when `--no-sandbox` is passed, the resulting `KBSettings` has `sandbox_mode == False` and that no sandbox-specific sandbox-directory creation occurs. Both assertions should use `click.testing.CliRunner.invoke()` to exercise the CLI surface.

**File:line target:** `tests/test_cycle21_cli_backend.py` — append after existing test at approximately line 60

**Tests:** This task *is* the test.

**Dependency:** None (CLI flags already exist; test pins them)

**Verification:** `pytest tests/test_cycle21_cli_backend.py -v` — all tests pass including the 2 new assertions.

---

## Task 18 — `tests/test_utils.py` / `tests/test_utils_io.py` (AC22: caller= signature unit test)

**ACs:** AC22

**Action:**
Add a new test class `TestAppendWikiLogCaller` to `tests/test_utils_io.py` (or `tests/test_utils.py` if the IO module is tested there) containing three test methods:

1. `test_append_wiki_log_accepts_caller_keyword` — Call `append_wiki_log("test msg", caller="cli")` and assert no exception is raised and the log line contains `"caller=cli"`.
2. `test_append_wiki_log_default_caller_is_unknown` — Call `append_wiki_log("test msg")` without a `caller` kwarg and assert the log line contains `"caller=unknown"`.
3. `test_append_wiki_log_caller_mcp` — Call `append_wiki_log("test msg", caller="mcp")` and assert the log line contains `"caller=mcp"`.

These three cases pin the AC22 contract that `caller=` is a string keyword argument with default `"unknown"` and that `"cli"` and `"mcp"` are both accepted values.

**File:line target:** `tests/test_utils_io.py` — append new class at end of file (approximately line 150)

**Tests:** This task *is* the test.

**Dependency:** Task 04 (the `caller` parameter must be threaded in at least one call-site to prove end-to-end)

**Verification:** `pytest tests/test_utils_io.py -k TestAppendWikiLogCaller -v` — 3 passed.

---

## Task 19 — `BACKLOG.md` (AC19: delete 3 entries)

**ACs:** AC19

**Action:**
Remove the following three stale entries from `BACKLOG.md`:
1. The entry titled "Add allowlist file for lint duplicate-slug exceptions" — this is now completed by Tasks 03/05.
2. The entry titled "Thread caller= through CLI and MCP code paths" — this is now completed by Tasks 04/08.
3. The entry titled "Add MCP tool for rebuilding KB indexes" — this is now completed by Task 08.

After deletion, verify the file still has valid markdown heading structure (no orphaned sub-bullets, no double blank lines).

**File:line target:** `BACKLOG.md` — remove 3 list items (lines referencing the above titles)

**Tests:** No automated test; manual verification by reading the file.

**Dependency:** Tasks 03, 04, 05, 08 (must be completed so the backlog items are genuinely done)

**Verification:** `grep -c "allowlist file for lint\|Thread caller=\|MCP tool for rebuilding" BACKLOG.md` returns 0.

---

## Task 20 — `CHANGELOG.md` (AC20: brief `[Unreleased]` entry)

**ACs:** AC20

**Action:**
Add a brief cycle-61 entry under the `## [Unreleased]` heading in `CHANGELOG.md`. The entry must be a single `### Changed` sub-section containing bullet points for: (a) lint allowlist moved to external JSON config, (b) `kb_disable_vectors` short-circuit added to query engine and hybrid search, (c) new `kb_rebuild_indexes` MCP tool, (d) `caller=` provenance threading across CLI and MCP paths, (e) stale test `inspect.getsource` calls replaced with behavioral tests. Keep the entry terse (one line per bullet) since the full details go in `CHANGELOG-history.md`.

**File:line target:** `CHANGELOG.md` — insert after `## [Unreleased]` line

**Tests:** No automated test; manual review.

**Dependency:** All production tasks (03–08) must be complete so the changelog accurately reflects the cycle.

**Verification:** `grep "cycle-61" CHANGELOG.md` returns a match.

---

## Task 21 — `CHANGELOG-history.md` (AC20: full per-cycle archive)

**ACs:** AC20

**Action:**
Append a new `## Cycle 61` section to the bottom of `CHANGELOG-history.md`. This section must contain a `### Summary` paragraph (2–3 sentences describing the cycle's theme: externalized lint config, vector-disable flag, MCP rebuild tool, test hardening) followed by `### Changed`, `### Added`, and `### Removed` sub-sections with full bullet-point descriptions of every change. Reference the 22 ACs by number where appropriate (e.g., "AC6: duplicate_slug consumer now reads from config/lint_allowlist.json"). The archive must be self-contained so a reader does not need the design-decision doc.

**File:line target:** `CHANGELOG-history.md` — append at end of file

**Tests:** No automated test; manual review.

**Dependency:** Tasks 19 and 20 (ensure BACKLOG and CHANGELOG are consistent before archiving)

**Verification:** `grep "## Cycle 61" CHANGELOG-history.md` returns a match.

---

## Task 22 — `docs/reference/implementation-status.md` (AC21: cycle-61 notes)

**ACs:** AC21

**Action:**
Add a new row or entry for Cycle 61 in `docs/reference/implementation-status.md`. The entry must state: "Cycle 61 — Lint allowlist externalization, kb_disable_vectors short-circuit, kb_rebuild_indexes MCP tool, caller= provenance threading, test hardening (inspect.getsource removal). All 22 ACs satisfied. 16 CONDITIONS verified." Place it at the top of the version history table (most recent first). Update the "Current Cycle" pointer at the top of the file from "Cycle 60" to "Cycle 61".

**File:line target:** `docs/reference/implementation-status.md` — update header row and insert new history entry

**Tests:** No automated test; manual review.

**Dependency:** All tasks must be complete for the status to be accurate.

**Verification:** `grep "Cycle 61" docs/reference/implementation-status.md` returns a match.

---

## Task 23 — `CLAUDE.md` (AC21: State bullet + cycle-61 pointer)

**ACs:** AC21

**Action:**
Update `CLAUDE.md` with two changes: (1) In the "State" section (the bullet list summarizing project stats), update the test count to reflect any new tests added in Tasks 09–18, and update the file count if `config/lint_allowlist.json` bumps the total. (2) Add or update the cycle pointer bullet to read `**Current cycle:** 61 — see 2026-05-03-cycle-61-design-decision.md` so that any contributor (human or AI) landing in the repo can find the authoritative design decision.

**File:line target:** `CLAUDE.md` — "State" section bullet list and cycle pointer bullet

**Tests:** No automated test; manual review.

**Dependency:** All prior tasks (test count and file count must be final)

**Verification:** `grep "Cycle 61" CLAUDE.md` returns a match. Test and file counts match `pytest --collect-only -q | tail -1` and `find . -name '*.py' | wc -l` respectively.

---

## Per-AC Verification Matrix

| AC# | Task | Test file | Step-14 verify item |
|-----|------|-----------|---------------------|
| AC1 | 01 | `tests/test_compile.py` | `compile_wiki` produces correct output structure |
| AC2 | 01 | `tests/test_compile.py` | incremental compile detects unchanged files |
| AC3 | 02 | `tests/test_query.py` | keyword search returns ranked results |
| AC4 | 02 | `tests/test_query.py` | hybrid search merges keyword + vector scores |
| AC5 | 17 | `tests/test_cycle21_cli_backend.py` | `--sandbox` / `--no-sandbox` flags propagate to `KBSettings` |
| AC6 | 03 | `tests/test_lint.py` | `duplicate_slug` loads slugs from JSON, not hard-coded set |
| AC7 | 05 | `tests/test_lint.py` | `config/lint_allowlist.json` exists and is valid JSON |
| AC8 | 09 | `tests/test_lint.py` | 3 allowlist-load test cases pass |
| AC9 | 02 | `tests/test_query.py` | query engine initializes with default settings |
| AC10 | 06, 07 | `tests/test_query.py` | `kb_disable_vectors` short-circuits in engine.py and hybrid.py |
| AC11 | 10 | `tests/test_query.py` | 2 `TestKBDisableVectors` cases pass |
| AC12 | 08 | `tests/test_mcp_browse_health.py` | `kb_rebuild_indexes` tool exists and is registered |
| AC13 | 11 | `tests/test_mcp_browse_health.py` | 4 `TestKbRebuildIndexes` cases pass |
| AC14 | 13 | `tests/test_lint_query_fixes_v092.py` | 0 `inspect.getsource` hits; logger spies pass |
| AC15 | 14 | `tests/test_v0911_phase392.py` | 0 `inspect.getsource` hits; threshold-flow behavioral test passes |
| AC16 | 15 | `tests/test_v0915_task01.py` | identity assertion removed; behavioral `discover_wiki_pages` test passes |
| AC17 | 16 | `tests/test_v0915_task08.py` | 0 `inspect.getsource` hits; FRONTMATTER_RE divergencer test passes |
| AC18 | 12 | `tests/test_compile.py` | dual-site prune assertion passes (incremental + full) |
| AC19 | 19 | *(manual)* | 3 stale entries removed from `BACKLOG.md` |
| AC20 | 20, 21 | *(manual)* | `CHANGELOG.md` and `CHANGELOG-history.md` updated for cycle 61 |
| AC21 | 22, 23 | *(manual)* | `implementation-status.md` and `CLAUDE.md` updated for cycle 61 |
| AC22 | 04, 08, 18 | `tests/test_utils_io.py` | `caller=` keyword threaded; 3 unit-test cases pass |

---

## CONDITIONS Section

> All 16 conditions restated verbatim from the design-decision doc, each with a Step-9 self-check action.

**C1: All tests pass with `pytest -x -q`.**
*Step-9 self-check:* Run `pytest -x -q` in the repo root; confirm exit code 0 and zero failures.

**C2: No new flake8/ruff warnings introduced by this cycle's changes.**
*Step-9 self-check:* Run `ruff check src/ tests/` and confirm zero new warnings beyond the pre-existing baseline.

**C3: `config/lint_allowlist.json` must be valid JSON parseable by `json.load()`.**
*Step-9 self-check:* Run `python -m json.tool config/lint_allowlist.json`; confirm exit code 0.

**C4: `duplicate_slug` lint check loads from `config/lint_allowlist.json`, not from a hard-coded frozenset.**
*Step-9 self-check:* `grep -r "KNOWN_SLUG_EXCEPTIONS" src/` returns zero hits.

**C5: `kb_disable_vectors` flag short-circuits both `engine.py:query()` and `hybrid.py:hybrid_search()`.**
*Step-9 self-check:* `grep -c "kb_disable_vectors" src/kb/query/engine.py src/kb/query/hybrid.py` returns at least 1 match per file.

**C6: `kb_rebuild_indexes` MCP tool exists in `health.py` and is registered in `_TOOL_TABLE`.**
*Step-9 self-check:* `grep "kb_rebuild_indexes" src/kb/mcp/health.py` returns at least 2 matches (function def + registry entry).

**C7: `caller=` keyword argument is threaded through all `append_wiki_log` call-sites in CLI code.**
*Step-9 self-check:* `grep -n 'append_wiki_log' src/kb/compile/compiler.py` shows `caller="cli"` on each line.

**C8: Default value of `caller` in `append_wiki_log` is `"unknown"`.**
*Step-9 self-check:* `grep 'caller.*=.*"unknown"' src/kb/utils/io.py` returns a match.

**C9: All `inspect.getsource` usages in tests listed in AC14, AC15, AC17 are replaced with behavioral or spy-based alternatives.**
*Step-9 self-check:* `grep -rn "inspect.getsource" tests/test_lint_query_fixes_v092.py tests/test_v0911_phase392.py tests/test_v0915_task08.py` returns zero hits.

**C10: The identity check on `builder.WIKI_SUBDIRS` in `test_v0915_task01.py` is removed.**
*Step-9 self-check:* `grep -n "is expected_tuple" tests/test_v0915_task01.py` returns zero hits.

**C11: `compile_wiki(mode="full")` prune path uses canonical relative paths (AC18 dual-site).**
*Step-9 self-check:* `pytest tests/test_compile.py -k test_prune_base_uses_canonical_rel_path -v` — both incremental and full variants pass.

**C12: `BACKLOG.md` no longer contains entries for completed work items from cycle 61.**
*Step-9 self-check:* `grep -c "allowlist file for lint\|Thread caller=\|MCP tool for rebuilding" BACKLOG.md` returns 0.

**C13: `CHANGELOG.md` `[Unreleased]` section contains a cycle-61 entry.**
*Step-9 self-check:* `grep "cycle-61\|Cycle 61" CHANGELOG.md` returns a match.

**C14: `CHANGELOG-history.md` contains a `## Cycle 61` archive section.**
*Step-9 self-check:* `grep "## Cycle 61" CHANGELOG-history.md` returns a match.

**C15: `docs/reference/implementation-status.md` and `CLAUDE.md` both reference Cycle 61.**
*Step-9 self-check:* `grep -l "Cycle 61" docs/reference/implementation-status.md CLAUDE.md` returns both files.

**C16: Test counts in `CLAUDE.md` State section match actual `pytest --collect-only -q` output.**
*Step-9 self-check:* Compare the count in `CLAUDE.md` with `pytest --collect-only -q | tail -1`; numbers must agree.

---

## Dependency Graph

```
Task 05 (config/lint_allowlist.json)
  └── Task 03 (duplicate_slug.py consumer)
        └── Task 09 (test_lint.py — 3 cases)

Task 06 (engine.py short-circuit)
  ├── Task 07 (hybrid.py mirror)
  └── Task 10 (test_query.py — 2 cases)
        [depends on both 06 and 07]

Task 08 (health.py MCP tool)
  └── Task 11 (test_mcp_browse_health.py — 4 cases)

Task 04 (compiler.py caller="cli")
  ├── Task 18 (test_utils_io.py — 3 caller= cases)
  └── [no other dependency; standalone]

Task 12 (test_compile.py dual-site)
  [no dependency; test-only]

Task 13 (test_lint_query_fixes_v092.py — AC14)
  [no dependency; test-only]

Task 14 (test_v0911_phase392.py — AC15)
  [no dependency; test-only]

Task 15 (test_v0915_task01.py — AC16)
  [no dependency; test-only]

Task 16 (test_v0915_task08.py — AC17)
  [no dependency; test-only]

Task 17 (test_cycle21_cli_backend.py — AC5)
  [no dependency; test-only]

Task 19 (BACKLOG.md — AC19)
  └── depends on Tasks 03, 04, 05, 08

Task 20 (CHANGELOG.md — AC20)
  └── depends on all production tasks (03–08)

Task 21 (CHANGELOG-history.md — AC20)
  └── depends on Tasks 19, 20

Task 22 (implementation-status.md — AC21)
  └── depends on all tasks complete

Task 23 (CLAUDE.md — AC21)
  └── depends on all tasks complete (test counts must be final)

─── Critical Path ───
Task 05 → Task 03 → Task 09 → Task 19 → Task 21 → Task 22/23
```

---

## Risk Register

| # | Risk | Likelihood | Impact | Mitigation |
|---|------|-----------|--------|------------|
| R1 | `load_lint_allowlist` caching via `lru_cache` causes stale allowlist if JSON is edited mid-lint-run | Low | Medium | Document that the allowlist is loaded once per process; add a `--no-cache` CLI flag to `kb lint` in a future cycle if needed. For cycle 61, the cache is acceptable because lint runs are short-lived. |
| R2 | `kb_disable_vectors` guard placement misses a code path that still tries to import sentence-transformers | Medium | High | Tasks 06 and 07 cover both entry points (`engine.py` and `hybrid.py`). Task 10's tests mock the embedding import and assert it is never called. Add a `try/except ImportError` fallback in `__init__.py` as defense-in-depth (outside scope of this cycle but logged as a follow-up). |
| R3 | `caller="mcp"` default in `kb_rebuild_indexes` drifts from `"cli"` convention if someone copies the signature | Low | Low | Task 11 case 4 (`test_rebuild_indexes_default_caller_is_mcp`) pins the default via signature introspection. Task 18 case 2 pins the `append_wiki_log` default as `"unknown"`. Both serve as regression anchors. |
| R4 | Removing `inspect.getsource` tests may hide source-level regressions that behavioral tests do not cover | Medium | Medium | Each replacement test is designed to exercise the same code path behaviorally. The behavioral tests are more robust against refactoring. Accept this trade-off; monitor for any gap in the next cycle's test-coverage report. |
| R5 | `BACKLOG.md` deletion of 3 entries may leave orphaned references in linked issues or PR descriptions | Low | Low | Grep the repo for the exact backlog entry titles before deletion. External references (GitHub issues) are out of scope for this plan. |
| R6 | Cycle-61 changelog entries may conflict with concurrent PRs touching `CHANGELOG.md` | Low | Low | Use a clear `[Unreleased]` section separator. Rebase any conflicting branch before merge. |
| R7 | Test count in `CLAUDE.md` may become stale before the cycle branch merges if another branch adds tests | Medium | Low | Task 23 is the last task; update the count immediately before pushing. CI does not enforce CLAUDE.md accuracy, so this is a best-effort documentation sync. |

---

## Task Ordering Rationale

1. **Leaf files first** (Task 05: JSON config) — no code depends on them yet, safe to create.
2. **Production code migration** (Tasks 03, 04, 06, 07, 08) — consumer refactors and new features.
3. **Test additions** (Tasks 09–18) — each test task follows its corresponding production task.
4. **Documentation** (Tasks 19–23) — backlog pruning, changelog, implementation status, and CLAUDE.md updates come last because they depend on the final state of all code and tests.

---

## Step-9 Commit Grouping

| Commit # | Tasks | Message |
|----------|-------|---------|
| 1 | 05 | `feat(lint): add config/lint_allowlist.json (AC7)` |
| 2 | 03, 04, 06, 07, 08 | `feat(core): cycle-61 production changes — allowlist consumer, caller= threading, kb_disable_vectors, kb_rebuild_indexes (AC6, AC10, AC12, AC22)` |
| 3 | 09, 10, 11, 12 | `test: cycle-61 new test classes — allowlist load, disable vectors, rebuild indexes, dual-site prune (AC8, AC11, AC13, AC18)` |
| 4 | 13, 14, 15, 16, 17, 18 | `test: cycle-61 test hardening — remove inspect.getsource, add behavioral tests, pin sandbox flags, pin caller= (AC5, AC14, AC15, AC16, AC17, AC22)` |
| 5 | 19, 20, 21, 22, 23 | `docs: cycle-61 backlog prune, changelog, implementation-status, CLAUDE.md (AC19, AC20, AC21)` |

---

## Self-Check

- [x] **Task ordering rationale preserved** from the prior plan file
- [x] **Step-9 commit grouping preserved** from the prior plan file (5 commits)
- [x] **Tasks 03–23 fully elaborated** — all 21 task bodies include ACs, Action (≥2 sentences with concrete file:line), File:line target, Tests, Dependency, Verification
- [x] **Per-AC verification matrix** — 22-row markdown table with columns AC#, Task, Test file, Step-14 verify item
- [x] **CONDITIONS section** — all 16 conditions restated verbatim, each with Step-9 self-check action
- [x] **Dependency graph** — indented bullet list showing critical path (Task 05 → 03 → 09 → 19 → 21 → 22/23)
- [x] **Risk register** — 7 cycle-specific risks (≥5 required) with likelihood, impact, and mitigation
- [x] **Self-check section** — 8 hard requirements listed with `- [x]` checkmarks
