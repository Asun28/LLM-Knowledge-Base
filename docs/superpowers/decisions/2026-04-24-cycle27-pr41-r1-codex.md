# R1 Codex Review — PR #41 Cycle 27

**Verdict:** APPROVE-WITH-NITS

## Blockers
- None

## Majors
- None

## Minors / Nits
- CHANGELOG.md:27 — cycle 27 says `2 commits`, but `git log --oneline main..HEAD` is already 2 and the requested self-referential `+1` rule would make the doc-update commit count 3.

## Check Results
| Check | Result | Notes |
|---|---|---|
| AC1b semantics | PASS | Compared `main` inline `kb_search` formatter to extracted helper at src/kb/mcp/browse.py:42-55; empty list, `stale=True`, and absent/false `stale` semantics match. |
| Decorator placement | PASS | `@mcp.tool()` is on `kb_search` at src/kb/mcp/browse.py:58-59, not on helper at :30. `git log --oneline` shows fix commit `0751066`. |
| CLI function-local imports | PASS | Cycle-27 imports are function-local in src/kb/cli.py:588-590, 624, 653, 674. Module-scope `from kb.` lines are only src/kb/cli.py:29-31. |
| Error-string exits | PASS | `stats`, `list-pages`, `list-sources` check `startswith("Error:")` and exit 1 at src/kb/cli.py:628-630, 657-659, 678-680. `search` has explicit empty/length guards at :592-600 before `search_pages()` call. |
| wiki_dir pass-through | PASS | `search` converts `wiki_dir` to `Path|None` and passes `wiki_dir=wiki_path` at src/kb/cli.py:603-604; `search_pages` accepts `wiki_dir: Path | None` at src/kb/query/engine.py:48-54. |
| Commit count | FAIL | CHANGELOG.md:27 claims `2 commits`; actual `main..HEAD` count is 2 now, but under the requested self-referential `+1` rule the doc-update commit would make it 3. |
| MCP collateral | PASS | `kb_search` still calls `_format_search_results(results)` at src/kb/mcp/browse.py:78-79. Tests `test_mcp_all_tools_registered` and `test_instructions_tool_names_sorted_within_groups` exist in tests/test_v070.py:405 and tests/test_cycle9_mcp_app.py:23; targeted pytest passed (`2 passed`). |
