# Cycle 70 — Implementation Plan

**Date:** 2026-05-08  
**Tier:** 2  
**Step:** 07  
**Gate verdict:** BINDING (4 amendments + 6 conditions applied)

---

## Task Structure

Per `feedback_batch_by_file`: Tasks group related acceptance criteria by file + module. Each atomic commit follows the task boundary.

- **Bucket A (AC01-AC05)** — BACKLOG hygiene: verify shipped + delete + extend lock-in. Single task, single commit.
- **Bucket B (AC06-AC08)** — Snapshot subjects: 3 distinct test files. Three parallel-ready tasks.
- **Bucket C (AC09)** — Cycle-69 carry-over: date-contingent audit. Single task.
- **Bucket D (AC10)** — Test quality: C41-L1 behavioral spy upgrade. Single task.
- **Bucket E (AC11-AC12)** — MCP prompt-injection boundary: helper + 2 wiring sites + lock-in. Three sequential tasks.
- **Bucket F (AC13-AC16)** — Doc artifacts. Single task.

**Caller-grep checkpoints:** AC11 adds NEW helper `wrap_wiki_context()` in `kb.utils.text` (colocates with `wrap_purpose` per A2-bis). No existing function signatures change; zero caller-grep drift detection needed.

---

## TASK 1 — Bucket A: BACKLOG Hygiene + AC05 Lock-In

**Files touched:**
- `BACKLOG.md`
- `tests/test_cycle68_backlog_cleanup_lockin.py`

**ACs:** AC01, AC02, AC03, AC04, AC05

**Sub-steps:**

1. Verify AC01 shipped: `grep -n "httpx>=" pyproject.toml` → line 30 `"httpx>=0.28,<0.29"` ✓
2. Delete BACKLOG.md entry for AC01 at line 103.
3. Verify AC02 shipped: `grep -n "KB_PROJECT_ROOT" README.md` → lines 137,141,142,148 ✓
4. Delete BACKLOG.md entry for AC02 at line 165.
5. Verify AC03 shipped: `grep -rn "KB_STRICT_PUBLISH" src/kb/` ✓
6. Delete BACKLOG.md entry for AC03 at line 159.
7. Verify AC04 shipped: `grep -rn "inspect\.getsource" tests/test_lint_query_fixes_v092.py ...` → ZERO function-call hits ✓
8. Delete BACKLOG.md entry for AC04 at line 153.
9. **AC05 lock-in extension** (per C1):
   - Extend `tests/test_cycle68_backlog_cleanup_lockin.py` tuple with 6 substrings:
     - `"httpx constraint mismatch"`
     - `"package-install KB_PROJECT_ROOT bootstrap undocumented"`
     - `"auto_publish_after_compile exceptions swallowed"`
     - `"versioned-file 'inspect.getsource' C11-L1 batch-filing"`
     - `"test_compile.py::test_prune_base_uses_canonical_rel_path"`
     - `"prompt-injection boundary gap"`
   - Run: `pytest tests/test_cycle68_backlog_cleanup_lockin.py -v` → pass green.

**Commit message:**
```
chore(cycle-70): AC01-AC05 BACKLOG hygiene + lock-in extension [6 deletions]
```

---

## TASK 2-4 — Bucket B: Snapshots AC06-AC08

[AC06, AC07, AC08 each in separate commits per cycle-69 pattern. Each ships snapshot fixture + negative-control. A1 amendment (key_claims not contradictions) embedded in AC06. A4 canonicalization (re-parse + sort_keys) embedded in AC08.]

---

## TASK 5 — Bucket C: AC09 Date Audit

**Files:** tests/test_cycle69_snapshots.py (extend)  
**ACs:** AC09

[Extend existing test with forward-looking date-string lock-in. No production change.]

---

## TASK 6 — Bucket D: AC10 C41-L1 Spy

**Files:** tests/test_compile.py (rewrite lines 217-239)  
**ACs:** AC10 (C5: Mock(wraps=...))

[Parametrized spy covering both call sites per C5.]

---

## TASK 7-8 — Bucket E: AC11 Helper + Wiring

**Files:** src/kb/utils/text.py, src/kb/query/engine.py, src/kb/mcp/core.py

[A2-bis: colocate in kb.utils.text. A3: reserve overhead at engine.py:1051. A2: wire 2 sites (engine.py:1063, mcp/core.py:417-432).]

---

## TASK 9 — Bucket E: AC12 Lock-In

**Files:** tests/test_cycle70_prompt_safety.py  
**ACs:** AC12

[Unit + integration tests covering helper + both wiring sites.]

---

## TASK 10 — Bucket F: Docs

**Files:** CHANGELOG.md, CHANGELOG-history.md, CLAUDE.md, BACKLOG.md

[AC13-AC16: decision artifacts, CHANGELOG entry, CLAUDE.md sync, BACKLOG close-out + 4 Phase 4.5 LOW forward-looking entries.]

---

## Caller-Grep Checkpoints

**Zero checkpoints required.** AC11 adds NEW helper (no signature drift of existing functions). AC10 modifies test only.

---

## Step-9 Self-Check: TDD Compliance

- AC05: lock-in test extended; assertion fails on missing deletion.
- AC06-08: tests created BEFORE snapshot files (TDD red); fixtures match A1/C3/A4 constraints; snapshots committed.
- AC09: audit + forward-looking lock-in; no production change.
- AC10: spy parametrized 2 sites; revert-test on either site fails the branch.
- AC11: helper unit tests first (fence/escape/empty), wiring tests second (both sites called).
- AC12: covered above (unit + integration).

---

## Step-14 Self-Check: Threat-Model Checklist

1. T1 — `wrap_wiki_context()` returns fence + assertion: test_cycle70_prompt_safety.py::test_wrap_wiki_context_basic
2. T2 — AC12 integration tests assert both sites use helper: test_wrap_wiki_context_integration_engine + test_wrap_wiki_context_integration_mcp
3. T3 — Helper escapes </wiki_context>: test_wrap_wiki_context_escape_closing_tag
4. T4 — Helper short-circuits empty: test_wrap_wiki_context_empty_short_circuit (C6)
5. T5 — Fence-overhead reserved at engine.py:1051: production code budget subtraction verified
6. T6 — Snapshot determinism vectors enumerated in C2 docstrings: all 3 snapshot tests
7. T7 — No hash output in snapshots: verified by R1
8. T8 — Date-coverage audit + forward-looking lock-in: test_contradictions_append_snapshot
9. T9 — Parametrized spy, revert-test per site: test_prune_base_uses_canonical_rel_path[full_mode] + [drift_detect]
10. T10 — BACKLOG lock-in covers 6 substrings: test_backlog_cleanup_extended
11. NEW — Out-of-scope Phase 4.5 LOW entries in BACKLOG.md post-merge: 4 new entries (browse/lint sites)

---

## Risk Callouts

- **R1 — AC11 boundary scope drift:** Design gate (A2) narrowed to 2 in-scope sites (engine.py:1063, mcp/core.py:417-432). T2 lock-in + forward-looking BACKLOG Phase 4.5 LOW (F1 AST guard, cycle-71+).
- **R2 — AC09 uncertainty:** R1 Opus verified module-global patch covers all 4 call sites. Forward-looking lock-in = safety net.
- **R3 — AC11 production code touch:** Step-11 SAST + Step-13 coverage gate required (not deferred).
- **R4 — Snapshot determinism:** AC06-08 fixtures verified deterministic; C2 docstrings enumerate vectors; A4 canonicalization robust to key-order.

---
