REQUEST_CHANGES

B1: VERIFIED-FIXED. `_mask_fenced_blocks` exists at `src/kb/ingest/evidence.py:92` and `append_evidence_trail` masks content before `_EVIDENCE_TRAIL_HEADER_RE.search` at `src/kb/ingest/evidence.py:151-152`. `test_fenced_code_block_header_does_not_hijack` seeds a fenced `## Evidence Trail` before the real section and asserts the new entry is in the real section, not the fence, at `tests/test_cycle24_evidence_sentinel_anchored.py:168-207`; mentally removing masking makes that anti-assert fail.

M1: VERIFIED-FIXED. `_evict_vector_index_cache_entry` holds `_index_cache_lock` across pop, close, and rebind at `src/kb/query/embeddings.py:106-116`. Both `popped._conn.close()` and `popped._conn = None` are inside the `with _index_cache_lock:` block.

M2: VERIFIED-FIXED. `test_sentinel_only_no_header_creates_fresh_section` now asserts a fresh real header/section exists and that the new entry is after the header at `tests/test_cycle24_evidence_sentinel_anchored.py:136-164`. Reverting to whole-file `str.index(SENTINEL)` would insert at the attacker sentinel and fail the fresh-header/count/header-position assertions.

N1: VERIFIED-FIXED. The three polling sleeps call `_backoff_sleep_interval(attempt_count)` at `src/kb/utils/io.py:356`, `:371`, and `:383`; no polling site uses inline `min(LOCK_INITIAL...)`. Equivalent grep output also shows the formula as code only in `_backoff_sleep_interval` at `src/kb/utils/io.py:249` plus an explanatory comment at `:288`.

N2: VERIFIED-FIXED. The test comment now explicitly says `LOCK_INITIAL_POLL_INTERVAL=0.01` remains unchanged and `LOCK_POLL_INTERVAL=0` clamps every sleep to zero at `tests/test_cycle24_lock_backoff.py:172-178`.

New issues:

MAJOR: `_FENCED_CODE_RE` does not correctly handle nested/longer backtick fences. The pattern at `src/kb/ingest/evidence.py:89` always opens on the first three backticks and closes on the first subsequent triple-backtick line, so a valid four-backtick outer fence containing an inner triple-backtick line can stop masking early; a later `## Evidence Trail` still inside the outer fence can hijack header detection. CRLF simple fences do work by inspection and regex simulation.

No deadlock risk found for close-under-lock: `rebuild_vector_index` takes `_rebuild_lock` then calls eviction at `src/kb/query/embeddings.py:167-199`, while `get_vector_index` takes only `_index_cache_lock` at `:233`; I found no inverse lock order.

Numeric claims: `pytest` is unavailable here (`pytest` and `python -m pytest` both fail with no module), so I could not verify the 2767 collected-test claim. `git log --oneline origin/main..HEAD | wc -l` equivalent reports 8 commits.
