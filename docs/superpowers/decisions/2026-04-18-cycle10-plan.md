# Cycle 10 Step 7 Implementation Plan

Mandatory grep/read baseline:
- `src\kb\mcp\app.py:187:def _validate_wiki_dir(wiki_dir: str | None) -> tuple[Path | None, str | None]:`; current body at `app.py:190-200` checks absolute/existing/dir and returns `path.resolve()` without PROJECT_ROOT containment.
- `src\kb\lint\_safe_call.py:20:def _safe_call[T](`; current body at `_safe_call.py:44-46` catches `Exception` and embeds raw `{exc}`.
- `src\kb\mcp\app.py:145:def _sanitize_error_str(exc: BaseException, *paths: "Path | None") -> str:`.
- `src\kb\query\engine.py:120:    def vector_search(query: str, lim: int) -> list[dict]:`; production path uses local `vector_search` then `rank_lists`/RRF at `engine.py:170-183`, not `hybrid.py`.
- `src\kb\ingest\pipeline.py:321:def _build_summary_content(extraction: dict, source_type: str) -> str:`.
- `src\kb\ingest\pipeline.py:225:def _check_and_reserve_manifest(`; reservation is called in `ingest_source` at `pipeline.py:884`, before extraction use and before `_build_summary_content` at `pipeline.py:942`.
- `src\kb\compile\linker.py:166:def inject_wikilinks(`; current wikilink display escaping is reached at `linker.py:242-243` via `wikilink_display_escape`, with pipe-to-space substitution verified at `src\kb\utils\text.py:285-298`.
- `src\kb\capture.py:345:def _extract_items_via_llm(content: str) -> dict:`; current template uses legacy `--- INPUT ---` fences at `capture.py:321-323`, and `_extract_items_via_llm` only calls `_escape_prompt_fences` before formatting at `capture.py:355-356`.

Read before planning: canonical design, requirements, threat model, and `CLAUDE.md`.

TASK 1: Harden wiki_dir validation
  Files: `src/kb/mcp/app.py`, `tests/test_cycle10_validate_wiki_dir.py`
  ACs covered: AC0 + AC15
  Threat refs: T3, T4
  TDD red: `assert err.startswith("wiki_dir must be inside project root")`; `assert "outside" not in str(path_ok)` for outside/symlink escape cases.
  Implementation change: Add PROJECT_ROOT containment to `_validate_wiki_dir` after `resolve()`, using `resolved == root or resolved.is_relative_to(root)`. Standardise helper errors to message-only `wiki_dir ...` strings so callers add one `Error: ` prefix.
  TDD green: `_validate_wiki_dir` accepts an existing project wiki dir and rejects absolute outside, relative traversal, and symlink-to-outside without leaking absolute paths.
  Commit message: `fix(cycle10): harden wiki_dir validation`
  Rollback: Revert this commit before any validator-migration commits; no data migration is involved.

TASK 2: Sanitize safe-call errors
  Files: `src/kb/lint/_safe_call.py`, `tests/test_cycle10_safe_call.py`
  ACs covered: AC1s
  Threat refs: T1, T2, T14
  TDD red: `assert "disk full" in err`; `assert str(secret_path) not in err`.
  Implementation change: Import `_sanitize_error_str` and build `msg = f"{label}_error: {type(exc).__name__}: {_sanitize_error_str(exc)}"`. Preserve the existing `_safe_call` signature and `except Exception` behavior.
  TDD green: Existing callers still receive `(fallback, err)` and path-like exception text is redacted centrally.
  Commit message: `fix(cycle10): sanitize safe-call errors`
  Rollback: Revert helper/test commit; callers continue using the old signature.

TASK 3: Surface quality degradation warnings
  Files: `src/kb/mcp/quality.py`, `tests/test_cycle10_quality.py`
  ACs covered: AC1 + AC1b
  Threat refs: T1, T14
  TDD red: `assert "[warn] backlinks_error:" in response`; `assert "[warn] shared_sources_error:" in response`; `assert response.startswith("Refined:")`.
  Implementation change: Replace the silent backlinks fallback at `quality.py:102-110` and shared-source silent continue at `quality.py:288-301` with `_safe_call(..., label="backlinks")` and `_safe_call(..., label="shared_sources")`. Append warning lines while preserving successful refine/affected-page output.
  TDD green: Backlink or shared-source failures are visible to the MCP client and successful main results still render.
  Commit message: `fix(cycle10): report quality lookup degradation`
  Rollback: Revert quality/test commit; no persisted state changes.

TASK 4: Validate browse paths and ambiguity
  Files: `src/kb/mcp/browse.py`, `tests/test_cycle10_browse.py`, `tests/test_cycle10_validate_wiki_dir.py`
  ACs covered: AC3 + AC16 + AC21 (AC2 **regression-pin**)

  **AC2 is a REGRESSION PIN** (not a new-feature AC). `kb_read_page` ambiguity handling already exists at `browse.py:87-104` (G3 from Phase 4.5 R4 LOW). TASK 4 does NOT re-implement — it only ensures the test in `tests/test_cycle10_browse.py` locks the current behaviour so future refactors cannot regress to insertion-order-picks-first. AC2's TDD-red assertion passes today; the TDD-green assertion IS the same — this is the pin.

  Threat refs: T3, T4, T12
  TDD red:
  - AC3: `assert kb_stats(wiki_dir="../../evil").startswith("Error: wiki_dir ")` (new — fails on main because manual-resolve block's error shape differs).
  - AC2/AC21 (regression pin): `assert result.startswith("Error: ambiguous page_id")` AND `assert "foo-bar" in result AND "Foo-Bar" in result` (passes today on case-sensitive FS; locks G3 behaviour).
  - AC16: `assert ["page-a", "page-b"] ⊂ kb_stats(wiki_dir=str(tmp_project / "wiki"))` (valid override produces a non-error response enumerating the seeded pages).

  Implementation change: Migrate `kb_stats` from manual `Path(wiki_dir).resolve().relative_to(...)` at `browse.py:325-328` to `_validate_wiki_dir`. DO NOT modify the existing `kb_read_page` ambiguity handling at `browse.py:87-104`; the test file's regression pin is sufficient to protect it.
  TDD green: `kb_stats` valid overrides work, traversal rejects share the AC0 error shape, and the AC2 regression pin stays green.
  Commit message: `fix(cycle10): validate browse wiki_dir inputs`
  Rollback: Revert browse/test rows; AC0 remains safe for other callers. AC2 regression pin remains valid even after revert.

TASK 5: Validate health wiki_dir inputs
  Files: `src/kb/mcp/health.py`, `tests/test_cycle10_validate_wiki_dir.py`
  ACs covered: AC4 + AC5 + AC6 + AC17 + AC18 + AC19 + AC20
  Threat refs: T3, T4
  TDD red: `assert result.startswith("Error: wiki_dir ")` for `kb_graph_viz`, `kb_verdict_trends`, `kb_detect_drift`; `assert "1 verdict" in kb_verdict_trends(...)`.
  Implementation change: Replace `Path(wiki_dir) if wiki_dir else None` at `health.py:177` and `health.py:223`, plus the manual resolve/relative block at `health.py:194-198`, with `_validate_wiki_dir` and immediate `if err: return f"Error: {err}"`. Preserve verdict path derivation from the validated wiki dir parent.
  TDD green: All three tools accept valid overrides and reject traversal/absolute-outside paths with consistent sanitized error shape.
  Commit message: `fix(cycle10): validate health wiki_dir inputs`
  Rollback: Revert health/test rows; helper hardening stays in place.

TASK 6: Gate vector similarity in production search
  Files: `src/kb/config.py`, `src/kb/query/engine.py`, `tests/test_cycle10_vector_min_sim.py`
  ACs covered: AC7 + AC8 + AC22 + AC23
  Threat refs: not enumerated in CHECKLIST; correctness/noise hardening
  TDD red: `assert [r["id"] for r in results] == ["high"]`; `assert search_pages("noise query", max_results=10) == []` when BM25 is empty and all vector scores are below threshold.
  Implementation change: Add `VECTOR_MIN_SIMILARITY = 0.3` to config. In the local nested `vector_search` in `engine.py`, filter converted scores after `1.0 / (1.0 + dist)` and before returning to `rank_lists`; log dropped/kept counts. Tests target `search_pages`, not `hybrid_search`.
  TDD green: Low-scoring vector-only hits do not enter RRF fusion; high-scoring hits remain available.
  Commit message: `fix(cycle10): filter low-similarity vector hits`
  Rollback: Revert config/engine/test commit; no index format changes.

TASK 7: Document drift pruning and preserve pipe titles
  Files: `src/kb/compile/compiler.py`, `src/kb/utils/text.py`, `src/kb/compile/linker.py`, `tests/test_cycle10_linker.py`
  ACs covered: AC9 + AC28.5 + AC28a

  **Authoritative AC28.5 target (blocker resolution):** the pipe substitution is at `utils/text.py:wikilink_display_escape` line 297 (currently replaces `|` with single space `" "`). The design doc wording `compile/linker.py:219-220` is stale — `linker.py` reaches `wikilink_display_escape` via `linker.py:242-243`. Fix is applied at `utils/text.py:wikilink_display_escape` directly so ALL callers benefit (there is only one caller: `linker.py` through `inject_wikilinks`). No double-escape risk because no callsite wraps the output again.

  Threat refs: T15; AC28.5 is data-preservation hardening outside the checklist

  TDD red:
  - `assert "deletion-pruning" in detect_source_drift.__doc__`
  - `assert "save_hashes=False" in detect_source_drift.__doc__`
  - `assert "A—B" not in output and "A B" not in output.split("|")` (em-dash absence + current space substitution absence)
  - `assert "A\\|B" in output or "A\\\\|B" in output` (Markdown-escaped pipe present)

  Implementation change:
  1. **AC9** — Expand `detect_source_drift` docstring in `compile/compiler.py` to state deletion-pruning persists despite `save_hashes=False`.
  2. **AC28.5** — In `utils/text.py:wikilink_display_escape`, replace `.replace("|", " ")` at line 297 with `.replace("|", r"\|")` (backslash-escaped pipe — Markdown-safe). Leave other substitutions (`[`, `]`) untouched. This is a ONE-LINE change at the authoritative site.
  3. **AC28a test** in `tests/test_cycle10_linker.py` — integration test: create a wiki page with title `"A|B"`, call `inject_wikilinks(target_page, all_pages)`, assert the rendered output contains the backslash-escaped pipe form and does NOT contain the legacy space-substituted form.

  TDD green: Docstring contract is pinned and pipe-containing titles are linked with Markdown-safe escaping; no data loss.
  Commit message: `fix(cycle10): preserve compile edge-case contracts`
  Rollback: Revert compiler/text/linker/test commit; no manifest mutation occurs. `utils/text.py` has exactly one caller (`linker.py`) so rollback is scoped.

TASK 8: Harden capture boundaries and timestamp semantics
  Files: `src/kb/capture.py`, `CLAUDE.md`, `tests/test_cycle10_capture.py`
  ACs covered: AC10 + AC11 + AC14 + AC24 + AC24b + AC25
  Threat refs: T5, T6, T13

  TDD red (grep-able assertion text, per cycle-4 Red Flag):
  - AC24: `assert "<<<INPUT-0123456789abcdef0123456789abcdef>>>" in prompt and "<<<END-INPUT-0123456789abcdef0123456789abcdef>>>" in prompt` (positive) AND `assert "--- INPUT ---\n" not in prompt_template_body` (negative — asserts the TEMPLATE body does not contain the legacy fence as a literal line; user-escaped content allowed to).
  - AC24b: `with pytest.raises(ValueError, match="boundary collision after 3 retries")` — monkeypatch `_secrets.token_hex` to a colliding constant contained in input; assert the ValueError is raised AND assert `call_llm_json_spy.call_count == 0` (helper never reached LLM).
  - AC11+AC25: `pre_llm = datetime.now(UTC); result = kb_capture("benign", llm_sleep=2); post_llm = datetime.now(UTC)` — then `captured_at_dt = datetime.fromisoformat(result.items[0]["captured_at"].replace("Z","+00:00"))`; assert `captured_at_dt - pre_llm <= timedelta(seconds=1)` AND `post_llm - captured_at_dt >= timedelta(seconds=1)` (demonstrates pre-LLM ordering with seconds-granular format).
  - AC14: `claude_md = (PROJECT_ROOT / "CLAUDE.md").read_text(); assert "except raw/captures/" in claude_md AND "deletion-pruning" in claude_md` (doc-grep assertions; acceptable per R2 major for AC9).

  Implementation change: Generate per-call `_secrets.token_hex(16)` boundaries, retry collisions three times, and retain legacy fence escaping as defense-in-depth. Move `captured_at` from `capture.py:726` to immediately after provenance resolution at `capture.py:668`; update the `CaptureResult` docstring and add the `raw/captures/` plus drift-pruning notes in `CLAUDE.md`.
  TDD green: Prompt fences are UUID-bound, collision exhaustion fails fast, and `captured_at` reflects submission time under a slow LLM mock.
  Commit message: `fix(cycle10): harden capture boundaries`
  Rollback: Revert capture/docs/test commit; generated capture files remain readable.

TASK 9: Validate extraction fields before reservation effects
  Files: `src/kb/ingest/pipeline.py`, `tests/test_cycle10_extraction_validation.py`
  ACs covered: AC12 + AC13a + AC13b + AC26a + AC26b + AC27

  **Dual-mechanism split (per cycle-9 Red Flag):** AC13 splits into AC13a (pre-validation pass) and AC13b (defensive double-check). Each mechanism ships as a distinct deliverable with a distinct assertion.

  Threat refs: T9, T10

  TDD red (3 distinct assertions, one per mechanism):
  - AC13a red: `with pytest.raises(ValueError, match="core_argument.*must be string"): ingest_source(raw_path, extraction={"title":"x","core_argument":{"nested":"dict"}})` — pre-validation fires BEFORE `_check_and_reserve_manifest`; proven by `assert raw_path_hash not in json.loads(manifest_path.read_text())` after exception.
  - AC13b red: monkeypatch `_pre_validate_extraction` (the AC13a pre-pass) to a no-op, then call `ingest_source` with the same malformed extraction; assert the defensive `_coerce_str_field` inside `_build_summary_content` ALSO raises `ValueError` before any `_write_wiki_page` call (use a spy on `_write_wiki_page` and assert `spy.call_count == 0`).
  - AC26a red: `assert list((tmp_wiki / "summaries").iterdir()) == [] and list((tmp_wiki / "entities").iterdir()) == [] and list((tmp_wiki / "concepts").iterdir()) == []` after the AC13a exception path.
  - AC26b red: same empty-dirs assertion after the AC13b exception path (pre-pass disabled).

  Implementation change (two distinct code edits):
  1. **AC12** — Add `_coerce_str_field(extraction, field)` helper near the top of `pipeline.py` returning strings or `""` for missing/None and raising `ValueError(f"extraction field {field!r} must be string, got {type(value).__name__}")` for other types.
  2. **AC13a** — Add `_pre_validate_extraction(extraction)` helper that runs `_coerce_str_field` on every known string field (`title`, `author`, `core_argument`, any other `_build_summary_content` consumer) and raises on the first failure. Call `_pre_validate_extraction(extraction)` inside `ingest_source` IMMEDIATELY BEFORE `_check_and_reserve_manifest` at `pipeline.py:884`.
  3. **AC13b** — Inside `_build_summary_content`, replace direct `extraction["title"]` / `extraction.get("author", ...)` reads with `_coerce_str_field(extraction, "title")` / `_coerce_str_field(extraction, "author")` etc. This is the defensive double-check: even if an unusual code path bypasses `_pre_validate_extraction` (a future refactor, a direct call to `_build_summary_content`), the defensive site still fails fast.

  Both mechanisms must appear in the diff as separate function definitions / separate call sites. No rollback logic — validation precedes reservation.

  TDD green: Malformed extraction fields fail before page writes and before manifest reservation via BOTH the pre-validation pass AND the defensive in-builder check.
  Commit message: `fix(cycle10): validate extraction strings pre-reservation and in-builder`
  Rollback: Revert pipeline/test commit; no cleanup routine is required.

TASK 10: Final cycle documentation pass
  Files: `CHANGELOG.md`, `BACKLOG.md`
  ACs covered: AC28
  Threat refs: T15 plus stale-item audit from CHECKLIST context
  TDD red: `assert "Backlog-by-file cycle 10" in changelog`; `assert "torn-last-line" not in backlog`; `assert "_coerce_str_field remaining" in backlog`.
  Implementation change: Add cycle 10 resolved items and STALE-at-cycle-10 notes to `[Unreleased]`; delete resolved/stale backlog entries and add the cycle-11 `_coerce_str_field` remaining-sites deferral. `CLAUDE.md` edits are already in TASK 8 per Q10.
  TDD green: Documentation reflects all landed ACs, deletes resolved stale lines, and preserves the explicit follow-up.
  Commit message: `docs(cycle10): record backlog-by-file completion`
  Rollback: Revert documentation commit only; code commits remain intact.

## Task-to-AC cross-reference

| AC | TASK |
|---|---|
| AC0 | TASK 1 |
| AC1s | TASK 2 |
| AC1, AC1b | TASK 3 |
| AC2, AC3, AC16, AC21 | TASK 4 |
| AC4, AC5, AC6, AC17, AC18, AC19, AC20 | TASK 5 |
| AC7, AC8, AC22, AC23 | TASK 6 |
| AC9, AC28.5, AC28a | TASK 7 |
| AC10, AC11, AC14, AC24, AC24b, AC25 | TASK 8 |
| AC12, AC13, AC26, AC27 | TASK 9 |
| AC15 | TASK 1 |
| AC28 | TASK 10 |

## Threats explicitly out-of-scope this cycle

- **T7 — Torn-line audit-log boundary.** Out-of-scope because wiki_log.py already implements `newline="\n"` + `file_lock` (verified at `utils/wiki_log.py:114, 126-127`). The requirements doc explicitly dropped the wiki_log ACs. Step 12 BACKLOG pass deletes the stale entry; no code change lands this cycle.
- **T8 — Behavioural test for `newline="\n"`.** Same rationale as T7. AC29 was removed from the AC set; no test lands.
- **T11 — kb_search DoS / perf boundary (query length cap).** Out-of-scope because `mcp/browse.py:43-44` already enforces `MAX_QUESTION_LEN` before any BM25 work. The requirements doc explicitly dropped that AC. No new code; the cap is already regression-protected.

## Plan-gate amendments (post-gate)

This plan was amended inline in response to the Step 8 plan gate PLAN-AMENDS-DESIGN verdict:

1. **AC13 split** — AC13 expanded to AC13a (pre-validation pass) + AC13b (defensive double-check in `_build_summary_content`). TASK 9 now has 4 distinct TDD-red assertions, one per mechanism per test site.
2. **AC28.5 targeting** — authoritative site pinned to `utils/text.py:wikilink_display_escape` line 297 (NOT `compile/linker.py:219-220` as the design wording suggested; the design wording was stale). TASK 7 updated accordingly.
3. **Threat out-of-scope annotation** — T7, T8, T11 enumerated above with rationale.
4. **TASK 8 assertion tightening** — AC24's "legacy fence absence" narrowed to `"--- INPUT ---\n" not in prompt_template_body` (template-only, grep-able).
5. **AC2/AC21 regression-pin label** — TASK 4 now explicitly labels the existing `browse.py:87-104` G3 behaviour as "regression pin" and forbids re-implementation.

## Blockers / open questions

None remain post-amendments. Step 9 TDD implementation proceeds.
