## Verdict — APPROVE

Cycle-18 threat-model checklist verification is complete against `feat/backlog-by-file-cycle18` (`origin/main..HEAD`: 6 commits, TASK 1-6). All T1-T10 items are IMPLEMENTED against the requested evidence points.

## Threat-item verification — table T# | Status | Evidence (file:line)

| T# | Status | Evidence (file:line) |
|---|---|---|
| T1 | IMPLEMENTED | `_emit_ingest_jsonl` validates stages and constructs an explicit row schema, with outcome allowlist at `src/kb/ingest/pipeline.py:758` and `src/kb/ingest/pipeline.py:800`; allowed outcome keys are only counts plus `error_summary` at `src/kb/ingest/pipeline.py:760`; `error_summary` is passed through `sanitize_text` and truncated to 2048 at `src/kb/ingest/pipeline.py:813`; raw content is decoded and used for extraction/body processing at `src/kb/ingest/pipeline.py:1030` and `src/kb/ingest/pipeline.py:1073`, but the JSONL row only serializes `ts/request_id/source_ref/source_hash/stage/outcome` at `src/kb/ingest/pipeline.py:817`. |
| T2 | IMPLEMENTED | `append_wiki_log._write` acquires `file_lock(log_path)` at `src/kb/utils/wiki_log.py:149`; `_rotate_log_if_oversized(log_path)` is inside that lock at `src/kb/utils/wiki_log.py:150`; append open/write follows at `src/kb/utils/wiki_log.py:151`. |
| T3 | IMPLEMENTED | `inject_wikilinks` has zero-lock fast-path reads/checks at `src/kb/compile/linker.py:222`, `src/kb/compile/linker.py:228`, and `src/kb/compile/linker.py:231`; candidates enter `file_lock(page_path, timeout=_INJECT_LOCK_TIMEOUT)` at `src/kb/compile/linker.py:242`; the file is re-read under lock at `src/kb/compile/linker.py:244`; race-lost/already-linked and no-match cases skip write at `src/kb/compile/linker.py:260` and `src/kb/compile/linker.py:271`; `atomic_text_write` occurs only inside the lock at `src/kb/compile/linker.py:296`. |
| T4 | IMPLEMENTED | `ingest_source` generates `request_id = uuid.uuid4().hex[:16]` at entry after `source_ref`/`source_hash` are known at `src/kb/ingest/pipeline.py:1055` and `src/kb/ingest/pipeline.py:1062`; request IDs are pinned to 16 lowercase hex in tests at `tests/test_cycle18_ingest_observability.py:410` and `tests/test_cycle18_ingest_observability.py:418`. |
| T5 | IMPLEMENTED | `_TMP_KB_ENV_PATCHED_NAMES` includes `HASH_MANIFEST` at `tests/conftest.py:23`; the config `getattr` loop remains limited to config-backed `patched` names at `tests/conftest.py:195`; `kb.compile.compiler.HASH_MANIFEST` is patched separately at `tests/conftest.py:233` and `tests/conftest.py:234`; the mirror-rebind dictionaries include `HASH_MANIFEST` at `tests/conftest.py:239` through `tests/conftest.py:242`; the mirror loop consumes that combined map at `tests/conftest.py:253`. |
| T6 | IMPLEMENTED | `rotate_if_oversized` computes ordinal archives with `while archive.exists()` at `src/kb/utils/wiki_log.py:43` through `src/kb/utils/wiki_log.py:49`; wiki-log rotation calls this inside `file_lock` at `src/kb/utils/wiki_log.py:149` and `src/kb/utils/wiki_log.py:150`; JSONL rotation calls it inside `file_lock(jsonl_path)` at `src/kb/ingest/pipeline.py:835` and `src/kb/ingest/pipeline.py:839`. |
| T7 | IMPLEMENTED | JSONL writer uses `file_lock(jsonl_path)` at `src/kb/ingest/pipeline.py:835`, direct append open at `src/kb/ingest/pipeline.py:840`, `write` at `src/kb/ingest/pipeline.py:841`, `flush` at `src/kb/ingest/pipeline.py:842`, and `os.fsync` at `src/kb/ingest/pipeline.py:843`; the helper comment explicitly rejects `atomic_text_write` for append history at `src/kb/ingest/pipeline.py:755` through `src/kb/ingest/pipeline.py:757`. |
| T8 | IMPLEMENTED | No-match/already-linked pages return before lock at `src/kb/compile/linker.py:228` and `src/kb/compile/linker.py:231`; `_INJECT_LOCK_TIMEOUT = 0.25` at `src/kb/compile/linker.py:21`; timeout is caught and converted to skip-with-warning at `src/kb/compile/linker.py:299` through `src/kb/compile/linker.py:307`; fast-path zero-lock and timeout are pinned by tests at `tests/test_cycle18_linker_lock.py:27` and `tests/test_cycle18_linker_lock.py:205`. |
| T9 | IMPLEMENTED | `request_id` is generated as hex-only at `src/kb/ingest/pipeline.py:1062`; the wiki log message prefixes `[req={request_id}]` at `src/kb/ingest/pipeline.py:1299` through `src/kb/ingest/pipeline.py:1302`; `_escape_markdown_prefix` only replaces pipe/control characters, leading markdown markers, and wikilink brackets at `src/kb/utils/wiki_log.py:91` through `src/kb/utils/wiki_log.py:100`, so the hex `[req=...]` prefix flows unchanged; tests pin the `[req=<16 hex>]` pattern at `tests/test_cycle18_ingest_observability.py:24` and `tests/test_cycle18_ingest_observability.py:62`. |
| T10 | IMPLEMENTED | `_write_index_files` delegates to `_update_sources_mapping` first and `_update_index_batch` second with independent try/except blocks at `src/kb/ingest/pipeline.py:741` through `src/kb/ingest/pipeline.py:748`; both helpers remain module-level attributes at `src/kb/ingest/pipeline.py:647` and `src/kb/ingest/pipeline.py:687`; legacy monkeypatch sites still target those symbols at `tests/test_v01008_ingest_pipeline_fixes.py:98` and `tests/test_v01008_ingest_pipeline_fixes.py:99`. |

## Same-class peer scan — per-pattern findings

- `rg "atomic_text_write" src/kb/utils/ src/kb/ingest/ src/kb/compile/`: IMPLEMENTED. The append-only JSONL writer does not use `atomic_text_write`; it uses `file_lock + open("a") + write + flush + os.fsync` at `src/kb/ingest/pipeline.py:835` through `src/kb/ingest/pipeline.py:843`, and the code documents the explicit `atomic_text_write` exclusion at `src/kb/ingest/pipeline.py:755` through `src/kb/ingest/pipeline.py:757`. Other `atomic_text_write` hits are page/index/publish writes, not append-only JSONL; the cycle-18 linker write is under `file_lock` at `src/kb/compile/linker.py:242` and `src/kb/compile/linker.py:296`.
- `rg "def _\w*_for_legacy|bypass|unsafe_" src/`: IMPLEMENTED. No cycle-18 diff hunk introduced a legacy/bypass/unsafe accommodation; the full-tree hits are pre-existing explanatory comments in unrelated modules, while cycle-18 symbols use direct helper names and module attributes (`_update_sources_mapping` at `src/kb/ingest/pipeline.py:647`, `_update_index_batch` at `src/kb/ingest/pipeline.py:687`).
- `rg "with file_lock" src/kb/ingest/pipeline.py src/kb/utils/wiki_log.py src/kb/compile/linker.py`: IMPLEMENTED. New RMW/append/rotate surfaces land inside locks: linker RMW at `src/kb/compile/linker.py:242`, wiki-log rotate+append at `src/kb/utils/wiki_log.py:149` through `src/kb/utils/wiki_log.py:151`, JSONL rotate+append at `src/kb/ingest/pipeline.py:835` through `src/kb/ingest/pipeline.py:843`, and HASH manifest confirmation remains locked at `src/kb/ingest/pipeline.py:1282`.

## Orchestrator wiring check — one-liner per wiring point

- `_write_index_files` is called from `_run_ingest_body` at `src/kb/ingest/pipeline.py:1269`; the helper itself is defined at `src/kb/ingest/pipeline.py:724`.
- `_emit_ingest_jsonl` is called at all four required stages: `start` at `src/kb/ingest/pipeline.py:1067`, `duplicate_skip` at `src/kb/ingest/pipeline.py:1085`, `failure` in the outer `except BaseException` path at `src/kb/ingest/pipeline.py:1116` and `src/kb/ingest/pipeline.py:1118`, and `success` before return at `src/kb/ingest/pipeline.py:1398` through `src/kb/ingest/pipeline.py:1408`.
- Caller-grep compatibility holds: `_update_sources_mapping` and `_update_index_batch` remain reachable as `kb.ingest.pipeline` module attributes at `src/kb/ingest/pipeline.py:647` and `src/kb/ingest/pipeline.py:687`, matching legacy monkeypatch sites at `tests/test_v01008_ingest_pipeline_fixes.py:98` and `tests/test_v01008_ingest_pipeline_fixes.py:99`.

## PR-introduced CVE check — one-line diff result

Branch-vs-baseline advisory IDs are IDENTICAL: current `pip-audit` advisory ID set is `{CVE-2025-69872}`, matching the cycle-18 baseline statement at `docs/superpowers/decisions/2026-04-20-cycle18-threat-model.md:112` and the BACKLOG entry at `BACKLOG.md:163`; no dependency manifest files changed in the branch, with project dependencies still declared at `pyproject.toml:7` through `pyproject.toml:14`.

## PARTIAL / MISSING summary — each gap + suggested in-cycle fix (cycle-12 L3 handling)

- None. No PARTIAL or MISSING threat items found; no in-cycle fix required.

## Final verdict summary

APPROVE. T1-T10 are implemented against the checklist, same-class peer scan found no cycle-18 recurrence of the named anti-patterns, orchestrator wiring remains reachable and monkeypatch-compatible, and the PR introduces no new CVE advisory ID beyond the known diskcache baseline.
