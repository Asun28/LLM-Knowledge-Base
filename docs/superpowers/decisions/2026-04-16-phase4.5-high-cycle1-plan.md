# Phase 4.5 HIGH Cycle 1 — Opus Decision Gate (Plan)

**Date:** 2026-04-16
**Gate:** Step 2.5 Opus decision & argumentation gate (plan)
**Verdict:** APPROVE-WITH-CONDITIONS
**Scope change:** None — preserves 22 items + 4 themes from design.
**PLAN-AMENDS-DESIGN:** None — all amendments are plan-internal.

## Summary

Plan is substantially sound. Three blocker-level correctness bugs must be patched before execution: (1) `_persist_contradictions` is inline code in `ingest/pipeline.py:790-805`, not an existing function — Task 2 must extract it as the first step before H3/H6/H13; (2) `VectorIndex.build(entries)` takes pre-computed `list[tuple[page_id, embedding]]`, not a `wiki_dir` — rebuild must load+embed+pair first; (3) Q_K_a target is `extract_citations()` in `query/citations.py:23`, not `extract_raw_refs()` in `utils/markdown.py`. Seven smaller clarifications (narrow manifest lock, FIRST-match heuristic, mtime `os.utime`, H1 iteration count, 4th golden, CLAUDE.md placement, test-count verification).

## Key Decisions

**Q1 (task ordering):** APPROVE as written. Task 1→2→3a→3b→4a→4b serialization is correct; no hidden dependencies between themes.

**Q18 (BLOCKER — `_persist_contradictions` function doesn't exist):** AMEND plan. Insert Step 2.11.5 to extract inline block at `pipeline.py:790-805` into a new `_persist_contradictions(contradictions, source_ref, effective_wiki_dir)` helper before H3/H6/H13 edits target it.

**Q19 (BLOCKER — `VectorIndex.build` API mismatch):** AMEND plan. `rebuild_vector_index(wiki_dir)` must `load_all_pages(wiki_dir=wiki_dir)` → `embed_texts([p.content])` → pair `(page_id, embedding)` tuples → `idx.build(entries)`.

**Q20 (BLOCKER — Q_K_a wrong target):** AMEND plan. Target is `query/citations.py:23` `extract_citations()`, not `utils/markdown.py` `extract_raw_refs()`. Fix: add `or part.startswith(".")` to existing per-segment check.

**Q5/Q10 (H12 heuristic):** AMEND plan. Use FIRST `## Evidence Trail` match (attacker-planted forgeries land AFTER legitimate history-top); defense-in-depth argument.

**Q9 (Q_A manifest lock scope):** AMEND plan. Acquire manifest lock TWICE — once at start (check + reserve hash), release during ingest work, reacquire at end for idempotent final save. NOT one coarse lock across full ingest.

**Q4/Q21 (mtime test flake):** AMEND plan. Test `test_mtime_gated_rebuild_skips_fresh` must `os.utime(vec_db, (newest + 2.0, newest + 2.0))` after first rebuild to defeat filesystem mtime granularity.

**Q2 (H1 threading reliability):** AMEND plan. Wrap Step 2.5 test body in `for iteration in range(10):` loop.

**Q6 (H10 golden-file coverage):** AMEND plan. Add 4th golden-file test for numbered-list-with-references content.

**Q13 (CLAUDE.md placement):** AMEND plan. Place rebuild-policy bullet under "Architecture → Python Package → Key APIs" (next to `ingest_source` bullet), not in a non-existent "API semantics" section.

**Q3/Q8/Q11/Q12/Q14/Q15/Q16/Q17 (test-strategy, residuals, lazy-import cost, doc delegation, tag audit, force-push risk, partial completion):** APPROVE as written. Plan already handles or design already ratified.

## Conditions (C1-C10)

1. Insert Step 2.11.5: extract `_persist_contradictions` helper before applying H3/H6/H13.
2. `rebuild_vector_index` body: `load_all_pages → embed_texts → (id, emb) tuples → idx.build(entries)`.
3. Q_K_a target: `query/citations.py:23` `extract_citations()`.
4. H12 use FIRST match (not LAST); test reflects FIRST-match semantics.
5. Q_A manifest lock acquired TWICE (check+reserve, then final-save).
6. Mtime test uses `os.utime(vec_db, (newest + 2, newest + 2))`.
7. H1 threading test iterates 10 times.
8. Golden-file tests: 4 total (numbered-list-with-references 4th case).
9. CLAUDE.md H17 bullet under "Key APIs" in Architecture section.
10. Verify actual test count via `python -m pytest --co -q` when updating CLAUDE.md.

## Escalations

**None.** All 21 surfaced questions resolved with HIGH or MEDIUM confidence.

## Next Step

Apply 10 amendments to `docs/superpowers/plans/2026-04-16-phase4.5-high-cycle1.md`. Then Step 3 Context7 verification for anthropic SDK error taxonomy. Then Step 4 subagent-driven-development with per-task Codex review.
