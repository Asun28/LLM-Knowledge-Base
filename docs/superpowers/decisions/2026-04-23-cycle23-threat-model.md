# Cycle 23 — Threat Model + CVE Baseline

**Date:** 2026-04-23
**Cycle:** 23

## CVE Baseline (Step 2 snapshot)

**Tool:** `pip-audit` against installed `.venv` (cycle-22 L1 — no `-r requirements.txt` resolver).

| CVE | Package | Version | Severity | fix_versions | Class |
|---|---|---|---|---|---|
| CVE-2025-69872 / GHSA-w8v5-vhqr-4h9v | diskcache | 5.6.3 | Low | `[]` (no upstream fix) | A — existing on main |
| CVE-2026-6587 / GHSA-95ww-475f-pr4f | ragas | 0.4.3 | Low | `[]` (vendor unresponsive) | A — existing on main |

**Direct-use grep (`src/kb/`):** zero hits for `diskcache`/`Diskcache` (pinned via trafilatura's robots.txt cache only); zero hits for `ragas`/`Ragas` (dev-eval-only dep, listed in `requirements.txt:220`, not imported by any `kb.*` module).

**Action:** Both advisories lack patched releases; Step 11.5 cannot bump them. Keep the diskcache BACKLOG entry; add a sibling BACKLOG line for ragas. If either ships a fix during the cycle, Step 11.5 re-read will pick it up.

## Dependabot Alerts Baseline (GitHub)

Single open alert mirrors pip-audit: `GHSA-95ww-475f-pr4f` (ragas, low severity, `first_patched: null`). No patchable alerts; Step 11.5 skip path.

## Threat Model (for in-cycle surface)

Adapted from the Step-2 subagent output; integrated with Step-1 requirements. Issues I1-I5 are carried forward as design constraints into Step 4.

### Trust boundaries

| Surface | Trust level | Guard |
|---|---|---|
| `rebuild_indexes(wiki_dir=None)` helper | Library (callers: CLI + tests) | **Add `is_relative_to(PROJECT_ROOT)` containment** (I1) |
| `kb rebuild-indexes` CLI | Operator owns filesystem | `--yes` flag + `click.confirm` on TTY (I2) |
| `kb.mcp.core.__getattr__` PEP-562 shim | Module-level attribute hook | Closed `_LAZY_MODULES` allowlist; `AttributeError` fallback (I3) |
| Subprocess spawn in AC5 regression test | Test | `sys.executable` + list-form argv + `os.pathsep` on PYTHONPATH (I4) |

### Data classification

- **Destructive (AC2, AC3):** `HASH_MANIFEST`, `vector_index.db` — both regenerated by next `ingest_source` / `compile_wiki` run. No user content lost; only derived indices.
- **Audit (I5):** `wiki/log.md` gains a `rebuild-indexes` line per call.
- **No secrets / PII / network** in any AC.

### Per-AC threat items → Step-11 verification checklist

| AC | Threat id | Step-11 check |
|---|---|---|
| AC1 | T-docstring | Docstring contains `"manifest"`, `"vector"`, `"LRU"` sentinels + `rebuild_indexes` pointer. |
| AC2 | I1 | `rebuild_indexes(wiki_dir="/etc")` raises `ValidationError`; error string does not leak `/etc` (cycle-20 `StorageError(path=)` redaction pattern). |
| AC2 | I1-peer | Helper uses `Path.resolve().is_relative_to(PROJECT_ROOT.resolve())` — same shape as `kb.capture:839`, `mcp/core.py:218`, `publish.py:424` (cycle-16 L1 same-class peer). |
| AC2 | I5 | One `rebuild-indexes` line appended to `wiki/log.md` per call; audit-write `OSError` logged via `logger.warning`, helper returns normally. |
| AC2 | atomic | Each of `{manifest_cleared, vector_cleared, caches_cleared}` wrapped in `try/except OSError` so one stale lock does not prevent others clearing. |
| AC3 | I2 | `kb rebuild-indexes` without `--yes` on TTY prompts `click.confirm(..., abort=True)`; `--yes` skips; non-TTY defaults to `--yes` required (scripts must explicit). |
| AC3 | cycle-8 L1 | `kb --version` subprocess test: `kb.compile.compiler` is NOT in `sys.modules` (lazy CLI registration preserved). |
| AC4 | I3 | `getattr(kb.mcp.core, "os")` → `AttributeError`; only names in `_LAZY_MODULES` resolve. |
| AC4 | reload-safe | `kb.mcp.core.ingest_pipeline is kb.ingest.pipeline` after first access (cycle-19 AC15 contract preserved). |
| AC5 | I4 | Subprocess command built with list-form argv (no `shell=True`); `PYTHONPATH` uses `os.pathsep`. |
| AC5 | T-sys.modules | Post-`import kb.mcp`, none of `{anthropic, networkx, sentence_transformers, kb.query.engine, kb.ingest.pipeline, kb.feedback.reliability}` in `sys.modules`. |
| AC6 | hermetic | Test has monkeypatched `call_llm` (no network); < 3 s wallclock; uses `tmp_project` fixture only. |
| AC7 | T5 | Start method checked via `mp.get_start_method()`; Windows skip gate explicit; sentinel cleanup in `finally`. |
| AC8 | docs-sync | BACKLOG Phase 4.5 lines deleted (not strikethrough); CLAUDE.md test count bumped; README documents `kb rebuild-indexes`. |

### T6 / T7 / T8 — cleared

- **T6 (loader additive fields):** N/A — rebuild_indexes is a deletion op.
- **T7 (MCP peer `kb_rebuild_indexes`):** deliberately out of scope; flagged in BACKLOG as deferred follow-up.
- **T8 (UTF-8 byte-vs-char):** no new error-string caps introduced.

### Design-constraint carry into Step 4-5

All I1-I5 become AC conditions, NOT separate ACs (cycle-22 L5: design-gate CONDITIONS are load-bearing test coverage). Implementation plan (Step 7) must map each I* to an explicit test assertion.
