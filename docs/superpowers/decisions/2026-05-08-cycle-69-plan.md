# Cycle 69 — Step 7 Implementation Plan

**Author:** mimocoding-rescue @ mimo-v2.5-pro (agentId aa9f724a1b0adfca6) + primary-session file-write proxy (cycle-12 L2 fallback — subagent produced substantive plan structure, file-write tool failed; primary session lands plan to disk preserving subagent authorship).
**Date:** 2026-05-08
**Tier:** 2
**ACs:** 22 (per design.md Step 5 lock)
**Files modified/created:** ~17 distinct paths
**Commit count target:** 13–14 atomic commits
**Test delta target:** 3274 → ~3290 (+~16 net)
**src/kb/ changes:** 0

## Task ordering rationale

Per Step 5 design lock R3 risk + amendment A4/A5: lock-in test extension (AC01) must land in the SAME COMMIT as the BACKLOG deletions (AC02 + AC03 + AC04 + AC22) it protects. Tasks 1 + 2 are therefore a single commit.

After the lock-in commit, snapshots (AC13–AC15) ship before C11-L1 upgrades (AC07–AC12) so a stable snapshot baseline exists before any file with multiple ACs is edited. Folds ship last — large receiver files (`test_query.py`, `test_config.py`) are the most likely surface for fold-induced ordering interactions, and we want C11-L1 upgrades stable first. Doc sync is final.

## Tasks

### TASK 1 — tests/test_cycle68_backlog_cleanup_lockin.py + BACKLOG.md (SAME COMMIT)

**ACs:** AC01 + AC02 + AC03 + AC04 + AC22 (the cycle-68 lock-in retirement + 4 BACKLOG deletions, all in one commit per R3)

**TDD:** RED — current `test_backlog_preserves_cycle68_self_reference_entries` requires `**AC03**` / `**AC07**` / `**AC12**` markers in BACKLOG; if BACKLOG is edited first, test FAILs. GREEN — invert that test + extend `DELETED_ENTRIES` + delete BACKLOG sections in the same edit batch + commit.

**Edits (in order — all staged before commit):**

1. `tests/test_cycle68_backlog_cleanup_lockin.py`:
   - Extend `DELETED_ENTRIES` tuple with three substrings (per A4/A5):
     - `"_validate_page_id` `..` substring match"` — AC03 lock-in
     - `"graph/builder.py` non-lint `build_graph` callers"` — AC04 lock-in
     - `"lint/checks/duplicate_slug.py` `check_duplicate_slugs`"` — AC22 lock-in
   - Rename second test `test_backlog_preserves_cycle68_self_reference_entries` → `test_backlog_does_not_contain_cycle68_self_reference_section` and invert assertions:
     - `assert "CYCLE 68 carry-over" not in backlog_text`
     - For each `marker` in `("AC03", "AC07", "AC12")` (rename `PRESERVED_SELF_REFS` → `DELETED_SELF_REFS`): `assert f"**{marker}** (SHIPPED cycle 68" not in backlog_text` (the SPECIFIC carry-over phrasing — bare `**AC03**` may legitimately appear in CHANGELOG-narrative-derived text in the future).
   - Update file docstring to reflect cycle-69 retirement.

2. `BACKLOG.md`:
   - Delete lines 57–67 inclusive: the entire `### CYCLE 68 carry-over (cycle-67 design-locked, deferred for time/risk)` section + the AC03/AC07/AC12 bullet lines. Includes the surrounding HTML comment block at lines 59–63.
   - Delete lines 92–93 (Phase 6 LOW): `mcp/app.py:254 _validate_page_id` `..` substring match entry + its `(fix:` continuation.
   - Delete lines 110–111 (Phase 4.5 HIGH): `graph/builder.py` non-lint `build_graph` callers entry + `(fix:` continuation.
   - Delete lines 130–131 (Phase 4.5 MEDIUM): `lint/checks/duplicate_slug.py` `check_duplicate_slugs` allowlist entry + `(fix:` continuation.

3. Run `pytest tests/test_cycle68_backlog_cleanup_lockin.py -x` — both tests must pass post-edit.

**Mutation budget (Step 14 anchor):**
- Re-add `lint/checks/duplicate_slug.py` entry to BACKLOG.md → `test_backlog_does_not_contain_shipped_phase_4_5_high_entries` must FAIL on the new substring.
- Re-add `**AC03** (SHIPPED cycle 68 AC01)` to BACKLOG → `test_backlog_does_not_contain_cycle68_self_reference_section` must FAIL.

**Commit:** `chore(cycle-69): AC01-AC04+AC22 — retire cycle-68 lock-in section + delete 4 verified-stale BACKLOG entries`

---

### TASK 2 — tests/test_cycle69_app_segment_aware_lockin.py (NEW)

**ACs:** AC05

**TDD:** RED — file doesn't exist; first pytest run discovers + passes (production already segment-aware at app.py:291). The test IS the lock-in for the AC03 deletion.

**Edits (NEW file):** parametrized 5-row matrix per design.md Group B + A6 (pass `wiki_dir=tmp_path / "wiki"`).

**Mutation budget (Step 14):** revert `mcp/app.py:291` to `if ".." in page_id:` → rows 1, 3, 5 FAIL.

**Commit:** `test(cycle-69): AC05 — segment-aware page_id lock-in (parametrized matrix)`

---

### TASK 3 — tests/test_cycle69_graph_builder_intentional_bypasses.py (NEW)

**ACs:** AC06

**Edits (NEW file):** AST guard. Parse `query/engine.py` + `evolve/analyzer.py` via `ast.parse`; walk every `Call(func=Name("build_graph"))`; assert `pages` in `kw.arg for kw in call.keywords`. Plus a sanity test that ≥3 build_graph calls exist across the two modules (so a future refactor that removes all calls doesn't leave the AST walk passing vacuously).

**Mutation budget:** add `build_graph(wiki_dir)` (no `pages=`) anywhere in `query/engine.py` → first test FAILs. Remove all build_graph calls → second test FAILs.

**Commit:** `test(cycle-69): AC06 — graph builder FW-7 bypass AST guard`

---

### TASK 4 — tests/test_cycle69_snapshots.py (NEW) + tests/__snapshots__/test_cycle69_snapshots.ambr

**ACs:** AC13 + AC14 + AC15 (3 positive tests + 3 negative-controls = 6 tests)

**TDD:** RED — file doesn't exist; first pytest run with `--snapshot-update` records baseline `.ambr`. Subsequent runs FAIL on drift. Commit BOTH the test file AND the snapshot file.

**Edits (NEW file):**

- AC13 `test_build_extraction_prompt_snapshot` — calls `kb.ingest.extractors.build_extraction_prompt(content, template, purpose)` for fixed inputs (template name=article, content=transformer abstract, purpose=extract_entities). Function verified deterministic (extractors.py:276-333 — zero datetime/env refs). NO defensive monkeypatch.
- AC13 negative-control: change purpose to `extract_concepts`; assert rendered != baseline.
- AC14 `test_contradictions_append_snapshot` — uses `_FakeDate.today() == date(2026, 5, 8)` + `monkeypatch.setattr(kb.ingest.pipeline, "date", _FakeDate)` per A3. Pin contradictions block.
- AC14 negative-control: change second extraction's claim text; persisted block differs.
- AC15 `test_lint_semantic_render_sources_snapshot` — pass list of 3 sources < 100 chars each per A9; pin `lines: list[str]` post-`_render_sources`.
- AC15 negative-control: monkeypatch `kb.config.QUERY_CONTEXT_MAX_CHARS = 50` for a long source; assert lines differ vs default-truncation.

**Mutation budget:**
- AC13 anti-confounder: inject `f"// at {datetime.now().isoformat()}"` into prompt → snapshot diff next run.
- AC14: drop the `monkeypatch.setattr(pipeline, "date", _FakeDate)` line → snapshot drifts day-over-day.
- AC15: drop the negative-control monkeypatch line — snapshot pre-truncation == post-truncation FAILs.

**Commit:** `test(cycle-69): AC13-AC15 — snapshot subjects (build_extraction_prompt, contradictions, _render_sources)`

---

### TASK 5 — tests/test_lint_query_fixes_v092.py (AC07 + AC08 upgrades)

**ACs:** AC07 + AC08

**Edits:**

1. Replace `test_health_lint_uses_logger_error` (lines 277-281) with parametrized augment-aware behavioural test per A7. Spy on `kb.lint.runner.run_all_checks` (default path) AND `kb.lint.augment.orchestrator.run_augment` (augment path); force exception; assert `caplog.records` contains an ERROR-level record with `exc_info is None` (logger.error contract; logger.exception would attach exc_info).

2. Replace `test_health_evolve_uses_logger_error` (lines 284-288) with behavioural test per A8 — spy target is `kb.evolve.analyzer.generate_evolution_report` (verified at `analyzer.py:342`), NOT `analyze_evolution`. Force exception; assert `caplog` has ERROR record without exc_info.

3. Drop now-unused `import inspect` if it's only consumed by these two tests.

**Mutation budget:**
- AC07 default: `logger.error` → `logger.exception` at `mcp/health.py:87` → augment_mode=False FAILs.
- AC07 augment: `logger.error` → `logger.exception` at `mcp/health.py:129` → augment_mode=True FAILs.
- AC08: `logger.error` → `logger.exception` at `mcp/health.py:153` → AC08 FAILs.

**Commit:** `test(cycle-69): AC07/AC08 — C11-L1 upgrade kb_lint/kb_evolve to behavioural log assertions`

---

### TASK 6 — tests/test_v0911_phase392.py (AC09 upgrade)

**ACs:** AC09

**Edits:** Replace `test_trends_module_imports_threshold_from_config` (lines 239-250) with behavioural threshold-divergence test per A1.

Build a verdicts JSON fixture with 2 periods whose pass-rate delta is ~0.2 (in the [0.1, 0.5] range — exceeds default threshold 0.1, below monkeypatched threshold 0.5).
- Default threshold: `compute_verdict_trends(path)["trend"] == "improving"` (delta 0.2 > default 0.1).
- After `monkeypatch.setattr(trends, "VERDICT_TREND_THRESHOLD", 0.5)`: `["trend"] == "stable"` (delta 0.2 < 0.5).

**Mutation budget:** revert `trends.py:118,120` from `previous + VERDICT_TREND_THRESHOLD` to hardcoded `previous + 0.1` → second assertion FAILs (the high-threshold case still returns "improving" because 0.1 is hardcoded).

**Commit:** `test(cycle-69): AC09 — C11-L1 upgrade compute_verdict_trends to threshold-divergence test`

---

### TASK 7 — tests/test_v0915_task01.py (AC10 + AC11 upgrades)

**ACs:** AC10 + AC11

**Edits:**

1. Replace `test_graph_builder_uses_shared_subdirs` (lines 313-322) with behavioural test: build tmp wiki with one file inside a `WIKI_SUBDIR_TO_TYPE` subdir AND one file outside (top-level wiki dir); call `build_graph(wiki_dir=tmp_path / "wiki")`; assert only the inside file appears as a node.

2. Replace `test_evolve_analyzer_uses_shared_subdirs` (lines 324-334) with behavioural test pinning `analyze_coverage`'s `WIKI_SUBDIR_TO_TYPE.values()` enumeration: empty wiki → all expected types appear in `result["under_covered_types"]`.

**Mutation budget:**
- AC10: comment out `WIKI_SUBDIRS` import in `graph/builder.py` → stray top-level file becomes a node → test FAILs.
- AC11: hardcode types literal in `analyze_coverage` → reported set != WIKI_SUBDIR_TO_TYPE.values() → test FAILs.

**Commit:** `test(cycle-69): AC10/AC11 — C11-L1 upgrade graph builder + analyzer to behavioural subdirs tests`

---

### TASK 8 — tests/test_v0915_task08.py (AC12 upgrade)

**ACs:** AC12

**Edits:** Replace `test_regex_has_no_leading_whitespace_prefix` (lines 353-369) with behavioural divergence test per A2. Construct a CRLF + tab-trailing-fence input: `"---\t\r\ntitle: Frontmatter\r\ntype: concept\r\n---\r\n\r\nBody.\r\n"`. Call `parse_frontmatter(page)`; assert `meta["title"] == "Frontmatter"` AND `"Body." in body`.

**Mutation budget:** replace `FRONTMATTER_RE` in `kb.utils.markdown` with `re.compile(r"\A\s*---")` → CRLF + tab-trailing-fence input no longer captures the body group correctly → test FAILs.

**Commit:** `test(cycle-69): AC12 — C11-L1 upgrade analyzer frontmatter to CRLF+tab divergence test`

---

### TASK 9 — Fold AC16 (test_v0917_rewriter.py → test_query.py)

**ACs:** AC16

**Edits:**

1. Append 4 tests from `test_v0917_rewriter.py` to `tests/test_query.py` as bare functions (4-test set fits bare-function shape per Step-5 Q5 host-shape).
2. Revert-check per C40-L3: insert `assert False` at the start of `test_returns_string` in the receiver, run `pytest tests/test_query.py::test_returns_string -x`, confirm FAIL, restore.
3. `git rm tests/test_v0917_rewriter.py`.

**Commit:** `test(cycle-69): AC16 — fold test_v0917_rewriter.py into tests/test_query.py`

---

### TASK 10 — Fold AC17 (test_v0917_raw_fallback.py → test_query.py::TestSearchRawSources)

**ACs:** AC17

**Edits:**

1. Append the 3 `TestSearchRawSources` methods from `test_v0917_raw_fallback.py` to `tests/test_query.py` as a `TestSearchRawSources` class (cohesion around `search_raw_sources`).
2. Revert-check per C40-L3.
3. `git rm tests/test_v0917_raw_fallback.py`.

**Commit:** `test(cycle-69): AC17 — fold test_v0917_raw_fallback.py into tests/test_query.py::TestSearchRawSources`

---

### TASK 11 — Fold AC18 (test_v01002_consolidated_constants.py → test_config.py::TestConfigConstants)

**ACs:** AC18

**Edits:**

1. Append 4 tests from `test_v01002_consolidated_constants.py` to `tests/test_config.py::TestConfigConstants` (created cycle-47 AC1).
2. Revert-check.
3. `git rm tests/test_v01002_consolidated_constants.py`.

**Commit:** `test(cycle-69): AC18 — fold test_v01002_consolidated_constants.py into tests/test_config.py::TestConfigConstants`

---

### TASK 12 — Fold AC19 (test_v0917_hybrid.py → test_query.py::TestHybridQuery)

**ACs:** AC19

**Edits:**

1. Append 6 tests from `test_v0917_hybrid.py::TestRRFFusion` to `tests/test_query.py` as `TestHybridQuery` class.
2. Revert-check.
3. `git rm tests/test_v0917_hybrid.py`.

**Commit:** `test(cycle-69): AC19 — fold test_v0917_hybrid.py into tests/test_query.py::TestHybridQuery`

---

### TASK 13 — CHANGELOG.md / CHANGELOG-history.md / CLAUDE.md / docs/reference/* sync

**ACs:** AC20

**Edits:**

1. `CHANGELOG.md` — prepend cycle-69 entry under `[Unreleased]` Quick Reference, newest first. Compact Items / Tests / Scope / Detail format.
2. `CHANGELOG-history.md` — full per-cycle bullet detail (newest first).
3. `CLAUDE.md` — update Quick Reference: tests count (3274 → ~3290) + files count (~232 → ~228 net after 4 fold deletions + 3 new test files).
4. `docs/reference/implementation-status.md` — append cycle-69 narrative line if exists.
5. **Per `feedback_deepseek_doc_disambiguation`:** when DeepSeek dispatches Step 17 doc-update, ensure the prompt explicitly forbids "migrated to LIB" wording — cycle-69 ships ZERO migrations.

**Commit:** `docs(cycle-69): Step 17 — CHANGELOG / CHANGELOG-history / CLAUDE.md / BACKLOG.md sync`

---

### TASK 14 — Step 24 self-review artifact

**ACs:** AC21

**Edits:** create `docs/superpowers/decisions/2026-05-08-cycle-69-step24-self-review.md` with scorecard.

**Commit:** part of Step 24 (separate commit after merge).

## Summary

- **Tasks:** 14 (1 combined SAME-COMMIT for AC01-AC04+AC22 + 13 atomic per-file)
- **Commits:** 13–14 atomic
- **AC coverage:** 22/22
- **Mutation budget:** 12 mutations (per design.md), each tied to a specific test that must FAIL on revert
- **Order:** lock-in (T1) → snapshots (T4) → C11-L1 (T5–T8) → folds (T9–T12) → docs (T13–T14)

## Step 14 binding mutations summary

| AC | Mutation | Expected FAIL |
|---|---|---|
| AC05 | revert `mcp/app.py:291` to `if ".." in page_id:` | rows 1, 3, 5 of parametrize FAIL |
| AC06 | add bare `build_graph(wiki_dir)` near `query/engine.py:408` | AST guard FAILs |
| AC07 default | `logger.error` → `logger.exception` at `mcp/health.py:87` | augment_mode=False FAILs |
| AC07 augment | `logger.error` → `logger.exception` at `mcp/health.py:129` | augment_mode=True FAILs |
| AC08 | `logger.error` → `logger.exception` at `mcp/health.py:153` | AC08 FAILs |
| AC09 | hardcode `0.1` at `trends.py:118,120` | second assertion FAILs |
| AC10 | comment out `WIKI_SUBDIRS` import in `graph/builder.py` | stray top-level becomes node |
| AC11 | hardcode types literal in `analyze_coverage` | type-set mismatch FAILs |
| AC12 | replace `FRONTMATTER_RE` with inline `re.compile(r"\A\s*---")` | CRLF+tab input FAILs |
| AC13 | inject `f"// at {datetime.now()}"` into prompt | snapshot drifts |
| AC14 | drop `FakeDate` monkeypatch | snapshot drifts daily |
| AC15 | drop truncation negative-control monkeypatch | snapshot a == b FAILs |
| AC22 lock-in | re-add `lint/checks/duplicate_slug.py` to BACKLOG | DELETED_ENTRIES check FAILs |
