REQUEST_CHANGES

MAJOR — `compile_wiki` preserves `in_progress:` only in full-mode tail pruning, but incremental compiles still delete those markers in `find_changed_sources`, breaking the same AC7/CONDITION 13 “operator decides remediation” contract for the default path. See `src/kb/compile/compiler.py:181-187` and the locked re-save at `src/kb/compile/compiler.py:220-227`.
Suggested fix: mirror the new exemption in `find_changed_sources` by excluding manifest entries whose value starts with `in_progress:` from `deleted_keys` / prune persistence, then add an incremental-mode regression test that seeds a missing-source `in_progress:` marker, runs `compile_wiki(incremental=True, ...)`, and asserts the marker still exists after the run.

NIT — The new cycle-25 tests cover the full-mode prune exemption, but they do not pin the incremental path above, which is the more common integration route. `tests/test_cycle25_compile_wiki_in_progress.py`
Suggested fix: extend the same test module with an incremental variant so future refactors cannot reintroduce asymmetric pruning between `find_changed_sources` and the full-mode tail.

No blocker found on the other requested contracts:
- `_dim_mismatches_seen` uses `global` correctly in `VectorIndex.query`.
- `rebuild_indexes` preserves `result["vector"]["cleared"] = True` when main unlink succeeds and only `.tmp` unlink fails.
- `compile_wiki` places the `in_progress` write after `pre_hash` and before the `try`, and it releases `file_lock(manifest_path)` before calling `ingest_source`, so there is no nested-lock self-deadlock on that path.
- CONDITION 13’s full-mode filter does not break the existing `_template/` exemption.
- The cycle-17 double-write regression’s new `1 -> 2` expectation still pins the intended invariant.

Validation: `.venv\Scripts\python.exe -m pytest tests/test_compile.py tests/test_cycle25_compile_wiki_in_progress.py tests/test_cycle25_dim_mismatch.py tests/test_cycle25_rebuild_indexes_tmp.py -q` -> 25 passed.
