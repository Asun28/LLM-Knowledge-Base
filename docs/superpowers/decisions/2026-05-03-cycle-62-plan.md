# Cycle 62 Plan

## Implementation Order
1. Fold config/verdict/lint/compile/CLI/MCP files into their receivers.
2. Fold query/ingest-heavy files into `test_query.py` and `test_ingest.py`.
3. Format and lint modified receivers.
4. Run targeted receiver pytest commands.
5. Run full pytest and docs verification.
6. Update release docs and backlog progress.
7. Run subagent PR review and fix blockers.

## Verification
- Baseline selected sources: `python -m pytest -q <20 source files>`.
- Targeted receivers:
  - `python -m pytest -q tests/test_config.py tests/test_lint_verdicts.py`
  - `python -m pytest -q tests/test_lint.py tests/test_compile.py`
  - `python -m pytest -q tests/test_cli.py tests/test_mcp_core.py`
  - `python -m pytest -q tests/test_ingest.py tests/test_query.py`
- Global:
  - `python -m ruff format --check src/ tests/`
  - `python -m ruff check src/ tests/`
  - `python -m pytest -q`
  - `python scripts/verify_docs.py`
  - `git diff --check`
  - `git status --short`

## Expected Counts
- Test files: `200 -> 180`.
- Tests: `3022 -> 3022`.

Plan status: APPROVED.
