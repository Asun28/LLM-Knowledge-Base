# v0.9.2 Audit Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all critical, high, and medium issues identified in the v0.9.1 comprehensive audit.

**Architecture:** Three independent fix groups targeting different modules — no file conflicts. Each group includes code fixes + new tests. Post-fix: full test suite, CLAUDE.md update, version bump to 0.9.2, commit per group, push.

**Tech Stack:** Python 3.12+, pytest, ruff

---

## Fix Groups

### Group 1: Ingest Pipeline Fixes
**Files:** `src/kb/ingest/pipeline.py`, `src/kb/cli.py`, `src/kb/mcp/app.py`
**Test:** `tests/test_ingest_fixes_v092.py`

Fixes:
1. **Flawed regex** in `_update_existing_page()` (line 188-194) — replace negative lookahead with `finditer` approach
2. **Silent exception** in `_update_existing_page()` (line 181) — add logging with specific exception types
3. **Surface pages_skipped** in CLI `ingest` command and MCP `_format_ingest_result()`

### Group 2: Semantic Lint + Query + MCP Fixes
**Files:** `src/kb/lint/semantic.py`, `src/kb/query/engine.py`, `src/kb/query/bm25.py`, `src/kb/mcp/browse.py`, `src/kb/mcp/health.py`
**Test:** `tests/test_lint_query_fixes_v092.py`

Fixes:
1. **Domain terms in common_words** (semantic.py:100-111) — remove "entity", "concept", "summary", "confidence", "speculative", "inferred"
2. **Consistency group truncation** (semantic.py:160,175) — chunk into sub-groups instead of silent truncation
3. **Context truncation logging** (engine.py:85-87) — add logger.debug when context is truncated
4. **BM25 avgdl guard** (bm25.py:75-76) — add logging when fallback triggers
5. **Case-insensitive page lookup** (browse.py:51-58) — add path validation after fallback resolution
6. **logger.exception** (health.py:19,49) — change to logger.error (don't leak stack traces to MCP client)

### Group 3: Input Validation + Config
**Files:** `src/kb/feedback/store.py`, `src/kb/lint/verdicts.py`, `src/kb/review/refiner.py`, `pyproject.toml`
**Test:** `tests/test_validation_fixes_v092.py`

Fixes:
1. **Feedback input validation** (store.py:38-61) — add length limits for question/notes/cited_pages
2. **Verdict severity validation** (verdicts.py:31-61) — validate issue severity field
3. **Frontmatter validation in refiner** (refiner.py:76) — validate reconstructed page
4. **Dev dependencies** (pyproject.toml) — add pytest to optional-dependencies

### Post-Fix Steps
1. Run full test suite (`python -m pytest`)
2. Update `src/kb/__init__.py` version to "0.9.2"
3. Update `pyproject.toml` version to "0.9.2"
4. Update `CLAUDE.md` with v0.9.2 changes
5. Commit each group separately
6. Push to GitHub
