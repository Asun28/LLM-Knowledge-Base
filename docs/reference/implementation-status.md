# Implementation Status

> **Part of [CLAUDE.md](../../CLAUDE.md)** — detail for the "Implementation Status" section. Shipped phases and per-cycle tallies → `CHANGELOG.md` (compact index, newest first) + `CHANGELOG-history.md` (per-cycle bullet archive). Open work + deferred roadmap → `BACKLOG.md`.

- **Latest full-suite:** 3021 tests / 200 files · 3010 passed + 11 skipped on Windows local (post-cycle-37 split removal — 6 split tests dropped, 2 new regression tests added; **cycle 58 folded 4 small cycle-15/17/18 versioned files (fourth dev-mimo-opus trial cycle, 1 of 5 planned ACs dropped at Step-21 rebase: AC4 was already folded by cycle-54-pickup)** — `test_cycle17_mcp_tool_coverage.py` 110 LOC / 5 classes / 13 tests → `test_mcp_core.py` (cross-module hosting per cycle-50 precedent); `test_cycle18_sanitize.py` 116 LOC / 16 bare functions → `test_utils_text.py` wrapped in new class `TestSanitizePathRedaction` (cycle-50 cross-feature-hosting analogue to disambiguate from yaml_sanitize tests); `test_cycle18_wiki_log.py` 126 LOC / 5 bare functions → `test_utils.py` (module-cohesion with existing append_wiki_log tests); `test_cycle17_capture_two_pass.py` 220 LOC / 2 classes / 10 tests → `test_capture.py` with helper rename `_make_items` → `_make_two_pass_items` per C52-L4 — file count 204 → 200 (-4 sources at branch HEAD; subject to Step 21 rebase if cycle-53 still in flight); test count preserved at 3021 (44 tests folded, 44 land in receivers, net 0). Step-21 rebase note: cycle-58 originally planned 5 ACs but cycle-54-pickup commit `05de0e0` folded the AC4 source (`test_cycle15_lint_status_mature.py`) first; rebase onto origin/main used `git rebase --skip` to drop the redundant commit. **MiMo trial telemetry**: Step-8 plan-gate via `mimocoding-rescue` mimo-v2.5-pro APPROVE no gaps (~222s, ~34k tokens). Steps 1-9 + 12-16 ran primary session per C37-L5 (4 small mechanical folds, primary-holds-context). Step-17 doc-sync via `deepseek-rescue` deepseek-v4-pro produced edits with two factual errors (fabricated "AC4+AC5 batch" line conflating two separate receivers; fabricated "verify-but-trust caught fabrication" self-narrative claiming the doc-sync had failed when in fact it had applied edits) — primary session re-applied per C12-L2 trust-but-verify (candidate **C58-L4 lesson** refining cycle-12 L2 + cycle-54-pickup's C58-L1: subagent doc-sync dispatches must be diff-verified before commit; the cycle-12 L2 + cycle-54-pickup C58-L1 patterns extend to all subagent vendors that produce summary-prose without doc-edit verification, including DeepSeek v4-pro). **cycle 54-pickup folded 4 small cycle-8/cycle-15/cycle-45 versioned files (salvage of abandoned worktree-cycle-54)** — `test_cycle8_health_wiki_dir.py` 4 tests → `test_mcp_browse_health.py` with `_write_health_fold_page` helper; `test_cycle8_models_validation.py` 7 tests → `test_models.py` with `_cycle54`-suffixed import aliases per C52-L4 helper-name uniqueness; `test_cycle15_lint_status_mature.py` 8 tests + 3 classes → `test_lint.py` with `_write_status_mature_page` helper + cycle-prefixed class renames `TestStatusMatureStaleOtherStatusesIgnored` / `TestStatusMatureStaleTodayOverride`; `test_cycle45_package_constants_propagate_to_submodules.py` 5 tests → `test_lint.py` with `_write_pkg_const_fold_page` helper + function-local `_checks_mod` alias — file count 208 → 204 (-4 sources at branch HEAD; subject to Step 21 rebase if cycle-53 still in flight); test count preserved at 3021. Production-code half of the original cycle-54 plan (compile prune-base + Windows PID-liveness) had ALREADY shipped on main via cycle 23 + commit `ea4af42` — cycle-54-pickup deliberately scoped to ONLY the 4 unfinished test folds. **MiMo trial telemetry**: Step-9 ran primary session per C13-L2 + C37-L5 sizing heuristic; Step-9 background reviewer dispatched via `mimocoding-rescue` (mimo-v2.5) AFTER user-flagged contract deviation (254s wall-clock, ~39k tokens, returned CLEAN on all 4 concerns — rename/alias correctness, fixture-discovery preservation, revert-coupling, reload isolation). Step-17 DeepSeek dispatch fabricated success-prose without applying edits — primary session re-applied (candidate C58-L1 lesson refining cycle-12 L2 from Codex to DeepSeek). **cycle 57 folded 5 small Phase 3.96/3.97 + cycle-17 versioned files + DELETED 1 vacuous sentinel** (`test_v0916_task07.py` → `test_mcp_core.py`; `test_v0916_task08.py` → `test_review.py`; `test_v0915_task10.py` → `test_cli.py`; `test_cycle17_lazy_imports.py` → `test_v070.py` with `_cycle17_` prefix renames per C52-L4; `test_v0p5_purpose.py` → `test_utils.py` per Step-5 Q3 single-receiver feature-coherence decision; AC6 DELETE `test_review.py::test_embedding_dim_resolved` per cycle-56+ BACKLOG option (1) since `EMBEDDING_DIM` is fully removed from src/kb), file count 213 → 208 (-5), test count 3022 → 3021 (-1), each fold per-receiver pytest passed; cycle 56 folded 5 small Phase-4 + cycle-15/16 versioned files** (`test_v01012_mcp_validation.py` 2701 B / 7 tests → `tests/test_mcp_core.py` as `TestMcpInputValidation`; `test_v0916_task09.py` 2011 B / 3 tests → `tests/test_v070.py` preserving 3 host classes; `test_v01013_cli_error_truncation.py` 3444 B / 7 tests SPLIT → `tests/test_cli.py` as `TestCliErrorTruncation` (5 tests) + `tests/test_utils_text.py` (2 bare functions) per host-shape preservation C40-L5; `test_v01001_utils_fixes.py` 2754 B / 5 tests → `tests/test_utils.py` as `TestUtilsFixes` with helper renamed `_write_page` → `_write_phase4_concept_page` per C52-L4 (cycle-52 already used `_write_concept_page`); `test_phase4_audit_concurrency.py` 3099 B / 4 tests → `tests/test_utils_io.py` as `TestFileLockConcurrency`) — file count 219 → 214 (-5), test count preserved at 3026, each fold revert-checked per C40-L3, per-fold isolation pytest passed per C51-L1, AC8 picks-marker convention carried forward from cycle 55. Pre-existing Windows pyreadline3 access violation surfaced during pytest collection on test_sanitize_strips_control_chars + test_sweep_orphan_tmp_logs_warning_and_continues_on_unlink_error; reproduces on main, NOT cycle-56-introduced; filed as cycle-57+ BACKLOG MEDIUM. **MiMo trial telemetry**: AC3b/AC4/AC5 dispatched via `mimocoding-rescue` (mimo-v2.5-pro), latencies ~9-14s — first successful sub-agent dispatch series for the trial. AC1/AC2/AC3a were primary-session pre-user-correction (logged as Step-24 lesson candidate). Step-17 doc-sync (this entry) dispatched via `mimochat` (sk- key, mimo-v2-flash) per dev-mimo-opus skill routing — Token-Plan TOS routes docs-for-code work to `mimocoding`, but pure mechanical doc edits use the cheaper `mimochat`. Cycle 55 (first dev-mimo-opus trial cycle, parallel to in-flight cycle-53 + cycle-54 worktrees) folded 4 small Phase-4 fix files (`test_v01003_graph_fixes.py` 53 LoC / 3 tests → `tests/test_graph.py` with C11-L1 vacuous-test upgrade on test 1 (renamed `test_graph_stats_uses_precomputed_out_degrees` → `test_graph_stats_orphans_includes_isolated_node`); `test_v01007_evolve_fixes.py` 78 LoC / 3 tests → `tests/test_evolve.py` verbatim; `test_v01009_ingest_aux_fixes.py` 58 LoC / 3 tests → `tests/test_ingest.py` verbatim; `test_v01011_review_feedback_fixes.py` 63 LoC / 3 tests → `tests/test_review.py` per Q2 host-shape preservation C40-L5) — file count 225 → 221 (-4) **at branch HEAD subject to Step 21 rebase if cycle-53/54 merge first**, test count 3025 → 3026 (+1 from R1-fix behavioral spy `test_graph_stats_avoids_per_node_degree_calls` per Q1 design upgrade — folds initially preserved 3025, R1 review caught design-deviance MAJOR + R1-fix landed the spy), each fold revert-checked per C40-L3, per-fold isolation pytest passed per C51-L1; Step 15 attempted litellm 1.83.0 → 1.83.7 patch reverted on python-dotenv==1.0.1 hard-pin trade-off (would introduce CVE-2026-28684 HIGH symlink — non-impactful in our codebase but Step 14 default REJECTs PR-introduced); first MiMo trial data point captured for 2026-05-31 writeup. Cycle 52 folded 4 small cycle-15/cycle-19 files (`test_cycle19_prune_base_consistency_anchor.py` 2762 B / 2 tests → `tests/test_compile.py` § `# ── Compiler tests ─` as bare functions; `test_cycle19_lint_redundant_patches.py` 2843 B / 1 test → `tests/test_lint.py` under new section `# ── Test-suite lint guards (cycle 52 fold) ─` with R1-AMENDED self-exclusion guard `Path(__file__).resolve()` per Step-5 Q1 decision (b); `test_cycle15_load_all_pages_fields.py` 2975 B / 6 tests → `tests/test_utils.py` § `# ── load_all_pages ─` with helper renamed `_write_page` → `_write_concept_page` per Step-5 Q4; `test_cycle15_query_tier1_wiring.py` 3082 B / 2 tests → `tests/test_query.py` under new section `# ── Tier-1 budget wiring (cycle 52 fold) ─` end-of-file with function-local imports per cycle-19 L2); file count 229 → 225 (-4); test count preserved at 3025; each fold revert-checked per C40-L3 (`assert False` proof showed pytest -x FAIL on the moved method; restored), per-fold isolation pytest passed per C51-L1. Refreshed 4 pip-audit + 2 Dependabot drift + 1 resolver-conflict BACKLOG timestamps cycle-51 → cycle-52 (state unchanged from cycle-51 baseline). Bumped cycle-52+ → cycle-53+ on 3 N/A prerequisite-missing items (windows-latest matrix re-enable, GHA-Windows MP spawn, TestWriteItemFiles POSIX off-by-one); bumped cycle-51+ → cycle-52+ on 2 Dependabot drift entries. Cycle 51 folded 4 small cycle-8/cycle-12/cycle-17 files (`test_cycle12_conftest.py` 1676 B / 2 tests → `tests/test_v070.py` as bare functions under section `# ── 9. tmp_kb_env fixture coverage (cycle 51 fold) ─`; `test_cycle17_capture_prompt.py` 2544 B / 5 tests → `tests/test_capture.py` as `TestCapturePromptFile` class with C41-L1 in-fold accessor upgrade per cycle-19 L2; `test_cycle17_validators.py` 2631 B / 5 methods (23 parametrized cases) → `tests/test_v070.py` as `TestValidateRunId` class; `test_cycle8_package_exports.py` 2791 B / 6 bare functions + helper → `tests/test_v070.py` under section `# ── 10. Package export curation (cycle 51 fold) ─` with helper renamed to `_run_export_import_probe`); file count 233 → 229 (-4); test count preserved at 3025; each fold revert-checked per C40-L3. Refreshed 4 pip-audit + 2 Dependabot drift + 1 resolver-conflict BACKLOG timestamps cycle-50 → cycle-51 (state unchanged from cycle-50 baseline). Bumped cycle-51+ → cycle-52+ on 3 N/A prerequisite-missing items (windows-latest matrix re-enable, GHA-Windows MP spawn, TestWriteItemFiles POSIX off-by-one); bumped cycle-50+ → cycle-51+ on 2 Dependabot drift entries. Cycle 50 folded 4 small cycle-8/cycle-9/cycle-45 files (`test_cycle9_lint_checks.py` 1506 B / 1 test → `tests/test_lint.py` bare function inside `# ── Source coverage checks ─` section per Step-5 Q3; `test_cycle45_lint_runner_order_invariant.py` 1358 B / 1 test → `tests/test_lint.py` bare function inside `# ── Runner tests ─` section with `EXPECTED_CHECK_ORDER` kept function-local per AC5; `test_cycle8_llm_telemetry.py` 2300 B / 2 tests + 2 helpers → `tests/test_llm.py` under new `# ── Telemetry: _make_api_call success path (cycle 50 fold) ─` section, helpers renamed `_FakeMessages`→`_TelemetryFakeMessages` + `_install_client`→`_install_telemetry_client` per Step-5 Q1 R1 amendment; `test_cycle9_mcp_path_validation.py` 2226 B / 9 tests → `tests/test_mcp_core.py` as new `TestMcpWikiDirValidation` class with `@staticmethod _missing_abs_path`); file count 237 → 233 (-4); test count preserved at 3025; each fold revert-checked per C40-L3. Refreshed 4 pip-audit + 2 Dependabot drift + 1 resolver-conflict BACKLOG timestamps cycle-49 → cycle-50 (state unchanged from cycle-49 baseline). Bumped cycle-50+ → cycle-51+ on 3 N/A prerequisite-missing items (windows-latest matrix re-enable, GHA-Windows MP spawn, TestWriteItemFiles POSIX off-by-one); bumped cycle-49+ → cycle-50+ on 2 Dependabot drift entries. Cycle 49 folded 4 small cycle-9/cycle-12 files (`test_cycle12_mcp_console_script.py` 24 LOC → `test_v070.py::TestKbMcpConsoleScript`; `test_cycle9_capture_runtime_guard.py` 33 LOC → `test_capture.py` bare function; `test_cycle9_package_exports.py` 36 LOC → `test_v070.py` bare function per Step-5 host-shape AMEND; `test_cycle9_mcp_app.py` 37 LOC → `test_v070.py::TestMcpAppInstructions` with `@staticmethod _instruction_tool_groups`); file count 241 → 237 (-4); test count preserved at 3025; each fold revert-verified per C40-L3. Refreshed 4 pip-audit + 2 Dependabot drift + 1 resolver-conflict BACKLOG timestamps cycle-48 → cycle-49 (state unchanged from cycle-48 baseline). Bumped cycle-49+ → cycle-50+ on 3 N/A prerequisite-missing items; bumped cycle-47+ → cycle-49+ on 2 Dependabot drift entries. Cycle 48 folded 2 small cycle-9 files (`test_cycle9_evolve.py` → `test_evolve.py`; `test_cycle9_compiler.py` → `test_compile.py`) and closed 2 cycle-48+ test-quality upgrade-candidates from cycle-47 R2 — AC1 `TestKbCreatePageHintErrors` forward-protection on `kb.mcp.ingest.{PROJECT_ROOT,RAW_DIR,SOURCE_TYPE_DIRS}` per cycle-23 L5 + cycle-42 L3; AC2 exact-byte body-region equality on `TestSaveFrontmatterBodyVerbatim` matching python-frontmatter dumps convention + C41-L1 docstring augment to `src/kb/utils/pages.py::save_page_frontmatter`; AC3 atomic_text_write spy on `TestSaveFrontmatterAtomicWrite` revert-verified to FAIL when production switches to direct `path.write_text(...)`. Refreshed 4 pip-audit + 2 Dependabot drift + 1 resolver-conflict BACKLOG timestamps cycle-47 → cycle-48 (state unchanged). Bumped cycle-48+ → cycle-49+ on 3 N/A prerequisite-missing items. File count: actual main was 243 (cycle-47 doc said 241 due to historical drift), post-fold = 241 (cycle 48 brings actual into alignment). Cycle 47 folded 3 small files — `test_cycle16_config_constants.py` → NEW `test_config.py` as `TestConfigConstants`; `test_cycle11_task6_mcp_ingest_type.py` → `test_mcp_core.py` as `TestKbCreatePageHintErrors` with `@staticmethod _assert_create_page_error` per Step-5 Condition 2 + cycle-11 L3 same-class peer rule; `test_cycle14_save_frontmatter.py` → `test_models.py` as 5 classes incl. `TestSaveFrontmatterAtomicWrite` renamed from `TestAtomicWriteProof` per Step-5 N1 — file count 243 → 241 (-2 net), test count preserved at 3025 (19 source tests folded as classes/methods); 7 BACKLOG `cycle-46 re-confirmed` stamps refreshed (5 dep-CVE + 1 resolver-conflict per AC13 scope-expansion + 2 Dependabot drift via cycle-list pattern); AC4 BACKLOG entry re-worded (pip is `.venv` installer, NOT `requirements.txt` pin); AC10 BACKLOG frontier replaced removing false-positive `test_cycle23_workflow_e2e.py` and inserting grep-proven ranked Thread/MP candidates (top-3: cycle-25 dim_mismatch, cycle-23 rebuild_indexes, cycle-24 lock_backoff); 3 cycle-47+ tags bumped to cycle-48+ with "prerequisite missing" notes (no GHA-Windows / POSIX shell). Cycle 46 closed Phase 4.6 LOW shim deletion — 36 test patch sites migrated, _sync_legacy_shim removed from manifest.py + rate.py, _augment_manifest.py + _augment_rate.py DELETED, test_lint_augment_split.py anchor refreshed with pytest.raises(ModuleNotFoundError) forward-protection; net 0 test delta. Cycle 45 closed Phase 4.6 M3 mcp/core.py split (1149 LOC → 447 LOC + mcp/ingest.py 612 LOC + mcp/compile.py 148 LOC); cycle 45 CI hotfix deleted test_cycle45_init_reexports_match_legacy_surface.py (244 → 243 files, 3027 → 3025 tests). Cycle 44 closed Phase 4.6 MEDIUM M1+M2+M4 + LOW L1 + cycle-43 carry-overs — `lint/checks.py` 1046 LOC split into 8 per-rule submodules; `lint/augment.py` 1189 LOC split into 9-file package with `_augment_manifest.py` + `_augment_rate.py` kept as cycle-45-deletion compat shims; `atomic_text_write` gains `exclusive: bool = False` + `capture._exclusive_atomic_write` removed; AC10 fold + 3 vacuous-test upgrades — file count 242 → 241 (−1 + 2), test count 3007 → 3019 (+12). M3 mcp/core.py split DEFERRED to cycle 45 per design Q13 (≥50 patch sites; cycle-22 L4 conservative posture). Earlier: cycle 38 added +2 cycle-38 regression tests + unskipped 12 cycle-36 ubuntu-probe leftovers; cycle 39 folded the cycle-38 cycle-tagged file into `test_capture.py::TestMockScanLlmReloadSafety` per cycle-4 L4 freeze-and-fold — file count 259 → 258, test count unchanged at 3014; cycle 40 folded three more cycle-10/11 files into `test_mcp_browse_health.py` (5-test class), `test_compile.py`+`test_utils_text.py` (split bare functions), `test_query.py` (4-test class) per same rule — file count 258 → 255, test count unchanged at 3014; cycle 41 folded four more cycle-10/11 files — `test_cycle10_validate_wiki_dir.py` → `test_mcp_browse_health.py`, `test_cycle10_capture.py` → `test_capture.py`, `test_cycle10_quality.py` → `test_mcp_quality_new.py`, `test_cycle11_cli_imports.py` → `test_cli.py` — and replaced the C40-L3 docstring-grep test in `test_compile.py` with a behavior-based manifest-immutability regression — file count 255 → 251, test count unchanged at 3014; cycle 43 folded eleven cycle-10/11/12/13 files into their canonical homes — file count 251 → 242, test count 3014 → 3007 (-7 from AC5 dedup of 7 _coerce_str_field bare tests redundant with cycle-43 AC2's parametrized fold; per cycle-17 L3 design narrowing) — plus filed 3 vacuous-test upgrade candidates and 1 deferral entry in BACKLOG.md; ubuntu-latest CI strict-gated since cycle 36; windows-latest CI matrix re-enable deferred to cycle-41+ per cycle-36 L1 CI-cost discipline + cycle-39/40/41/43 carry-over — separate cycle for the new dimension).
- **Latest cycle (57):** Freeze-and-fold continuation (5-fold batch) + sentinel-DELETE for `test_embedding_dim_resolved` — 6 ACs across `tests/` (5 sources removed → 5 receivers; 26 tests folded; 1 sentinel test DELETED). Test count 3022 → 3021 (-1 from AC6 deletion; AC1-AC5 folds preserve count); file count 213 → 208 (-5 sources at branch HEAD, subject to Step 21 rebase if cycles 53/54 merge first). Receivers verified disjoint from in-flight worktree-cycle-53 (test_compile / test_config / test_query) and worktree-cycle-54 (test_compile / test_lint / test_mcp_browse_health / test_models / test_utils_io). AC1 fold `test_v0916_task07.py` (159 LOC / 9 classes) → `tests/test_mcp_core.py` per Step-5 Q1 single-receiver decision matching cycle-50 `TestMcpWikiDirValidation` cross-module precedent. AC2 fold `test_v0916_task08.py` (143 LOC / 4 classes) → `tests/test_review.py` per cycle-55 host-shape precedent (function-local imports per cycle-19 L2). AC3 fold `test_v0915_task10.py` (195 LOC / 3 classes / 8 tests) → `tests/test_cli.py` per cycle-56 receiver precedent (function-local imports). AC4 fold `test_cycle17_lazy_imports.py` (176 LOC / 5 classes / 8 tests + 1 helper + 2 constants) → `tests/test_v070.py` with `_cycle17_module_level_imports` / `_CYCLE17_REPO_ROOT` / `_CYCLE17_SRC_KB_MCP` cycle-prefix renames per C52-L4 helper-name uniqueness + `# noqa: E402` on the 3 appended imports. AC5 fold `test_v0p5_purpose.py` (184 LOC / 7 bare functions) → `tests/test_utils.py` per Step-5 Q3 single-receiver feature-coherence decision (the `wiki/purpose.md` feature touches `utils/pages` + `ingest/extractors` + `query/engine` as a coherent capability). AC6 DELETE `test_review.py::test_embedding_dim_resolved` (cycle-55-folded sentinel) per cycle-56+ BACKLOG option (1) — `grep -rnE "EMBEDDING_DIM" src/kb` returns ZERO hits at cycle 57; sentinel `if not hasattr(config, "EMBEDDING_DIM"): return  # Deleted — PASS` always returned early after the original config removal (CHANGELOG-history.md line 2214); cycle-15 L2 / cycle-44 L4 DROP-with-test-anchor pattern fulfilled. Each fold per-receiver pytest passed; per-fold isolation pytest passed per C51-L1. Dep-CVE re-confirm: same 4 unresolved as cycle 56 (`.data/cycle-57/cve-baseline.json` would show diskcache GHSA-w8v5-vhqr-4h9v, ragas GHSA-95ww-475f-pr4f, litellm GHSA-xqmj-j6mv-4862, pip CVE-2026-3219); Class B PR-introduced empty (cycle 57 changes 0 deps). **MiMo trial telemetry**: All 6 ACs ran in primary session per cycle-13 L2 + cycle-37 L5 sizing heuristic; THIRD `dev-mimo-opus` trial cycle (after 55 + 56). C37-L5 confirmed: small mechanical folds (≤6 ACs, ≤5 src files, primary-holds-context) default to primary-session.

- **Latest cycle (56):** Freeze-and-fold continuation (5-fold batch) + dep-CVE re-confirm + Windows pyreadline3 BACKLOG entry — 5 folds + 1 chore commit + AC8 picks-marker across `tests/` (5 sources removed → 6 receivers; 26 tests folded; test count preserved at 3026 — verified post-fold via `pytest --collect-only -q -p no:capture -p no:debugging` → "3026 tests collected"; file count 219 → 214 (-5 sources, no new test files)) + BACKLOG.md (Phase 4.5 HIGH #4 progress note appended for cycle 56 + new cycle-57+ MEDIUM entry for Windows pyreadline3 access violation observed during Step 12 CI gate — reproduces on main, NOT cycle-56-introduced) + CLAUDE.md (Quick Reference 3026 tests / 214 files; cycle-39..56 carry-over note) + CHANGELOG.md compact entry + CHANGELOG-history.md detail block + docs/reference/{testing,implementation-status}.md narrative. AC1 fold `test_v01012_mcp_validation.py` (2701 B / 7 tests) → `tests/test_mcp_core.py` as `TestMcpInputValidation`. AC2 fold `test_v0916_task09.py` (2011 B / 3 tests) → `tests/test_v070.py` preserving 3 host classes (host-shape preservation per C40-L5). AC3a fold `test_v01013_cli_error_truncation.py` CLI half (5 tests) → `tests/test_cli.py` as `TestCliErrorTruncation`. AC3b fold remaining truncate-helper half (2 bare functions) → `tests/test_utils_text.py` + DELETE source (host-shape split per C40-L5). AC4 fold `test_v01001_utils_fixes.py` (2754 B / 5 tests) → `tests/test_utils.py` as `TestUtilsFixes` with helper renamed `_write_page` → `_write_phase4_concept_page` per C52-L4 (cycle-52 already used `_write_concept_page`). AC5 fold `test_phase4_audit_concurrency.py` (3099 B / 4 tests) → `tests/test_utils_io.py` as `TestFileLockConcurrency`. Each fold revert-verified per C40-L3 (assert False proof → pytest -x FAIL on the moved method; restored). Per-fold isolation pytest passed per C51-L1. AC8 picks-marker convention carried forward from cycle 55. Dep-CVE re-confirm: same 4 unresolved as cycle 55 (`.data/cycle-56/cve-baseline.json`); 2 Dependabot drift unchanged; Class B PR-introduced empty (cycle 56 changes 0 deps). **MiMo trial telemetry**: AC3b/AC4/AC5 dispatched via `mimocoding-rescue` (mimo-v2.5-pro), latencies ~9-14s — first successful sub-agent dispatch series for the trial. AC1/AC2/AC3a were primary-session pre-user-correction (logged as Step-24 lesson candidate: "small mechanical folds default to primary-session per cycle-13 L2; user-direction-to-dispatch must precede the first AC, not arrive mid-batch"). Step-17 doc-sync (this entry) dispatched via `mimochat` (sk- key, mimo-v2-flash) per dev-mimo-opus skill routing — Token-Plan TOS routes docs-for-code work to `mimocoding`, but pure mechanical doc edits use the cheaper `mimochat`.

- **Latest cycle (55):** First MiMo trial cycle: freeze-and-fold continuation + dep-CVE re-confirm — 10 ACs across `tests/` (4 folds: test_v01003_graph_fixes → test_graph.py with C11-L1 vacuous-test upgrade; test_v01007_evolve_fixes → test_evolve.py verbatim; test_v01009_ingest_aux_fixes → test_ingest.py verbatim; test_v01011_review_feedback_fixes → test_review.py per Q2 host-shape preservation C40-L5) + BACKLOG.md (litellm entry cycle-52 → cycle-55 with new python-dotenv pin discovery; HIGH #4 progress note appended; AC4 sentinel test_embedding_dim_resolved cycle-56+ upgrade candidate filed; cycle-56+ getsource batch-filing entry added per R2 Sonnet MINOR-2) + CHANGELOG.md + CHANGELOG-history.md + CLAUDE.md + docs/reference/{testing,implementation-status}.md narrative + 2 cycle-55 decision docs / 4 fold commits + +TBD doc-sync + self-review + R1-fix + R2-fix. Test count 3025 → 3026 (+1 from R1-fix behavioral spy `test_graph_stats_avoids_per_node_degree_calls` upgrade per Q1 design); file count 225 → 221 at branch HEAD (subject to Step 21 rebase if parallel cycle-53/54 merge first). **First real-cycle exercise of project-scoped `dev-mimo-opus` skill**: ran in parallel with worktree-cycle-53 (4/4 folds done, no PR yet) + worktree-cycle-54 (mid-cycle src/kb fixes + 4 different fold picks). Receivers verified disjoint. **First MiMo trial data point**: `mimocoding-rescue` and `mimochat-rescue` not registered in active subagent set; Step 8 plan-gate fell back to `codex:codex-rescue` (REJECT 5 gaps closed inline per C21-L1, ~106s); Steps 7/9/17 ran in primary session per cycle-13 L2 + C37-L5 sizing heuristic. Step 15 litellm patch ATTEMPTED then REVERTED: `pip install --upgrade "litellm==1.83.7"` resolves by silent-downgrading click 8.3.2→8.1.8, importlib-metadata 8.7.1→8.5.0, jsonschema 4.26.0→4.23.0, AND python-dotenv 1.2.2→1.0.1 — the python-dotenv downgrade introduces CVE-2026-28684/GHSA-mf9w-mj56-hr94 (HIGH symlink attack on set_key/unset_key, fix=1.2.2). Explicit `--upgrade "litellm==1.83.7" "python-dotenv==1.2.2"` returns ResolutionImpossible. Project doesn't call set_key/unset_key (`grep -rn "set_key\|unset_key" src/` empty) so the CVE would be theoretical-only, but Step 14 default REJECTs PR-introduced — defer remains correct. BACKLOG litellm entry updated. Cycle-55 worktree at `D:/Projects/llm-wiki-flywheel/.claude/worktrees/cycle-55` per C42-L4 isolation. Step 20 R1+R2 will retain DeepSeek+Codex+Sonnet diversity per dev-mimo-opus skill spec.

- **Latest cycle (52):** Backlog hygiene + freeze-and-fold continuation + dep-CVE re-confirm — 13 ACs across `tests/` (4 folds: cycle19_prune_base_consistency_anchor → test_compile.py bare functions under existing `# ── Compiler tests ─` section; cycle19_lint_redundant_patches → test_lint.py under new section `# ── Test-suite lint guards (cycle 52 fold) ─` with self-exclusion guard upgraded from hardcoded source-filename to `Path(__file__).resolve()` per Step-5 Q1 decision (b) — R1's MAJOR amendment; cycle15_load_all_pages_fields → test_utils.py `# ── load_all_pages ─` section after `test_load_all_pages_normalizes_sources` with helper renamed `_write_page` → `_write_concept_page` per Step-5 Q4 hygiene rule; cycle15_query_tier1_wiring → test_query.py under new section `# ── Tier-1 budget wiring (cycle 52 fold) ─` at end-of-file with function-local imports per cycle-19 L2) + BACKLOG.md (4 dep-CVE timestamps + 2 Dependabot drift + 1 resolver-conflict cycle-51 → cycle-52; 3 cycle-52+ → cycle-53+ tag bumps; 2 cycle-51+ → cycle-52+ Dependabot drift bumps; HIGH #4 progress note appended; new cycle-53+ entry filed for AC1 R1 behavioral-upgrade observation per C40-L3 + Step-5 Q2) + CHANGELOG.md + CHANGELOG-history.md + CLAUDE.md + README.md + docs/reference/{testing,implementation-status}.md narrative + 3 cycle-52 decision docs / 4 implementation commits + +TBD doc-sync + self-review. Test count preserved at 3025; file count 229 → 225. Steps 1-13 + 16 ran primary-session per C37-L5; Step 4 R1 DeepSeek V4 Pro direct CLI returned APPROVE-WITH-AMENDMENTS (1 MAJOR for AC2 self-exclusion guard + 1 NIT for AC1 vacuousness claim, both resolved at Step 5); R2 SKIP per cycle-49 precedent (hygiene-only cycle, no security surface). Step 5 design decision gate primary-session per cycle-21 L1 — promoted DeepSeek's MAJOR amendment + 6 binding decisions including helper-rename + section placement. Step 6 (Context7) + 9.5 (simplify) + 11.5 (existing-CVE patch) skipped per skip-eligibility (no third-party libs; 0 src/ diff; no patchable upstream). Cycle-52 worktree at `D:/Projects/llm-wiki-flywheel-c52` per C42-L4 from-the-start isolation. Zero PR-introduced CVEs (Step 11 baseline-vs-branch diff = empty set; cycle 52 changes 0 dependencies).

- **Latest cycle (51):** Backlog hygiene + freeze-and-fold continuation + dep-CVE re-confirm — 25 ACs across `tests/` (4 folds: cycle12_conftest → test_v070.py bare functions under section 9 with module-level `_is_under` helper; cycle17_capture_prompt → test_capture.py as `TestCapturePromptFile` class (renamed from `TestAC9PromptTemplate`) with C41-L1 in-fold behavioral upgrade switching from `_PROMPT_TEMPLATE` direct read to canonical `_get_prompt_template()` accessor per cycle-19 L2 (3 of 5 tests had latent test-ordering dependency); cycle17_validators → test_v070.py as `TestValidateRunId` class after `TestMcpAppInstructions`; cycle8_package_exports → test_v070.py under section 10 with helper renamed `_run_import_probe` → `_run_export_import_probe`) + BACKLOG.md (4 dep-CVE timestamps + 2 Dependabot drift + 1 resolver-conflict cycle-50 → cycle-51; 3 cycle-51+ → cycle-52+ tag bumps; 2 cycle-50+ → cycle-51+ Dependabot drift bumps; HIGH #4 progress note appended) + CHANGELOG.md + CHANGELOG-history.md + CLAUDE.md + README.md + docs/reference/{testing,implementation-status}.md narrative + 4 cycle-51 decision docs / 4 implementation commits + +TBD doc-sync + self-review. Test count preserved at 3025; file count 233 → 229. Steps 1-13 + 16 ran primary-session per C37-L5; Step 4 R1 DeepSeek V4 Pro direct CLI returned APPROVE-WITH-AMENDMENTS (1 amendment for AC7 TestMcpAppInstructions verification, all addressed at Step 5); R2 SKIP per cycle-49 precedent (hygiene-only cycle, no security surface). Step 5 design decision gate primary-session per cycle-21 L1 — promoted DeepSeek's amendment + added 8 binding decisions including helper-rename + import placement. Step 6 (Context7) + 9.5 (simplify) + 11.5 (existing-CVE patch) skipped per skip-eligibility (no third-party libs; 0 src/ diff; no patchable upstream). Cycle-51 worktree at `D:/Projects/llm-wiki-flywheel-c51` per C42-L4 from-the-start isolation. Zero PR-introduced CVEs (Step 11 baseline-vs-branch diff = empty set; cycle 51 changes 0 dependencies). Operational lesson (preview): C41-L1 fold-with-fix pattern fires in cycle 51 — `test_cycle17_capture_prompt.py` had 3 of 5 tests passing only by test-ordering coincidence (sibling tests in the same file triggered the lazy accessor before the assertion); fold-isolation surfaced the latent bug and the fix routed through canonical accessor in same commit.

- **Latest cycle (50):** Backlog hygiene + freeze-and-fold continuation + dep-CVE re-confirm — 25 ACs across `tests/` (4 folds: cycle9_lint_checks → test_lint.py bare function; cycle45_lint_runner_order_invariant → test_lint.py bare function with function-local EXPECTED_CHECK_ORDER per AC5; cycle8_llm_telemetry → test_llm.py with `_TelemetryFakeMessages`/`_install_telemetry_client` renamed helpers per Step-5 Q1; cycle9_mcp_path_validation → test_mcp_core.py as `TestMcpWikiDirValidation` class with `@staticmethod _missing_abs_path` cross-module hosting kb.mcp.{core,health}) + BACKLOG.md (4 dep-CVE timestamps + 2 Dependabot drift + 1 resolver-conflict cycle-49 → cycle-50; 3 cycle-50+ → cycle-51+ tag bumps; 2 cycle-49+ → cycle-50+ Dependabot drift bumps; HIGH #4 progress note appended) + CHANGELOG.md + CHANGELOG-history.md + CLAUDE.md + README.md + docs/reference/{testing,implementation-status}.md narrative + 3 cycle-50 decision docs / 4 implementation commits + +TBD doc-sync + self-review. Test count preserved at 3025; file count 237 → 233. Steps 1-13 + 16 ran primary-session per C37-L5; Step 4 R1 DeepSeek V4 Pro direct CLI returned APPROVE-WITH-MINOR-AMENDMENTS (3 amendments + 8 CONDITIONS, all addressed at Step 5/Step 9); R2 SKIP per cycle-49 precedent (hygiene-only cycle, no security surface). Step 5 design decision gate primary-session per cycle-21 L1 — promoted DeepSeek's 3 amendments + added 12 binding CONDITIONS + 6 SCOPE-OUT items. Step 6 (Context7) + 9.5 (simplify) + 11.5 (existing-CVE patch) skipped per skip-eligibility (no third-party libs; 0 src/ diff; no patchable upstream). Cycle-50 worktree at `D:/Projects/llm-wiki-flywheel-c50` per C42-L4 from-the-start isolation. Zero PR-introduced CVEs (Step 11 baseline-vs-branch diff = empty set; cycle 50 changes 0 dependencies).

- **Latest cycle (48):** Test-quality upgrades + freeze-and-fold + dep-CVE re-confirm — 16 ACs across `tests/` (AC1 mcp_core forward-protection; AC2+AC3 models contract upgrades; AC4+AC5 cycle-9 fold pair) + 1 src/ docstring augment (`src/kb/utils/pages.py::save_page_frontmatter` per C41-L1) + BACKLOG.md (5 dep-CVE timestamps + 2 Dependabot drift + 1 resolver-conflict cycle-47 → cycle-48 + 3 cycle-48+ → cycle-49+ tag bumps + 2 resolved upgrade-candidates removed + HIGH #4 progress note extended). Test count preserved at 3025; file count 243 → 241 (cycle-47 doc said 241; cycle-48 brings stated into alignment with actual). Steps 1-13 + 16 primary-session per C37-L5; Step 4 R1 DeepSeek direct CLI returned APPROVE-WITH-CONDITIONS (4 conditions, all addressed); Step 5 design gate primary-session per cycle-21 L1 promoted DeepSeek conditions + added C5 self-healing rationale; Step 6 + 9.5 + 11.5 skipped per skip-eligibility (no third-party libs; src diff is 7-line docstring; no patchable upstream). Operational lessons (preview): (a) AC2 first attempt failed — C41-L1 docstring-vs-code mismatch caught at runtime; "verbatim" docstring claim was technically false vs python-frontmatter's actual leading-blank-line + trailing-newline-strip convention. Same-commit fix to docstring AND test rather than pinning false contract. (b) Cycle-47 doc said 241 files but actual main was 243 — historical doc drift; cycle-48 brings stated into alignment. Zero PR-introduced CVEs.

- **Latest cycle (47):** Backlog hygiene + dep-CVE re-confirm + freeze-and-fold continuation — 18 ACs across BACKLOG.md (TASK 1: 7 cycle-46 → cycle-47 stamp refreshes + AC4 pip-locator re-word per Step-5 B1 + AC10 frontier replacement removing false-positive `test_cycle23_workflow_e2e.py` per Step-5 M2 + AC11 Phase 4.5 HIGH #4 progress note appended + 3 cycle-47+ → cycle-48+ tag bumps per AC12) + 3 small folds (TASK 2-4: cycle16_config_constants.py → new test_config.py as TestConfigConstants, cycle11_task6_mcp_ingest_type.py → test_mcp_core.py as TestKbCreatePageHintErrors with @staticmethod helper, cycle14_save_frontmatter.py → test_models.py as 5 classes incl. TestSaveFrontmatterAtomicWrite renamed) + multi-site doc-sync (TASK 5). Zero `src/kb/` changes. File count 243 → 241; test count preserved at 3025 (19 source tests preserved as classes — design said 20 / 9 SaveFrontmatter but actual was 19 / 8; minor miscount, math holds). Steps 1-3 + 7-13 + 16 ran primary-session per C37-L5; Step 4 R1 DeepSeek direct CLI + R2 Codex agent dispatch in parallel — R1 APPROVE clean, R2 APPROVE WITH AMENDMENTS (2 blockers + 3 majors caught real issues: B1 pip pin wording, B2 file-count drift, M3 7th `cycle-46 re-confirmed` stamp at line 158); Step 5 Opus subagent gate resolved Q1-Q5 + B1+B2 + M1+M2+M3 with HIGH confidence + 12 binding CONDITIONS + 10 SCOPE-OUT items. Steps 6+9.5+11.5 skipped per skip-eligibility (no third-party libs; zero src/ diff; no patchable upstream). Operational lessons: (a) `block-no-verify@1.1.2` PreToolUse hook over-matched substrings in `git commit -m "$(heredoc)"` bodies — workaround `git commit -F file` per C35-L4 + C22-L2 used for all 5 commits; (b) R2 agent summary mismatched the source file name (claimed `test_cycle19_create_page_hints.py`, saved review correctly named `test_cycle11_task6_mcp_ingest_type.py`) — don't trust agent summaries; read saved output files; (c) Step 5 design grep over-counted `cycle-46 re-confirmed` literal (Opus claimed 7 hits including lines 170/172, but those use `cycle-37/38/39/40/41/46 re-confirmed drift persists` pattern — different shape — case-insensitive grep for literal returns 5 lines: 126, 129, 132, 135, 158); intent honored at implementation time. Zero PR-introduced CVEs (Step 11 baseline-vs-branch diff = empty set; cycle 47 changes 0 dependencies).
- **Latest cycle (43):** Test-fold continuation (Phase 4.5 HIGH #4) — 12 ACs across `tests/` only; zero `src/kb/` changes. AC1-AC9 + AC11-AC12 folded eleven cycle-10/11/12/13 era versioned test files into their canonical homes per cycle-4 L4 freeze-and-fold rule: AC1 `test_cycle10_browse.py` (1 test) → `test_mcp_browse_health.py`; AC2 `test_cycle10_extraction_validation.py` (4 tests including 10-row parametrized `_coerce_str_field`) → `test_ingest.py`; AC3 `test_cycle10_vector_min_sim.py` (2 tests + helper class) → `test_query.py`; AC4 `test_cycle11_conftest_fixture.py` (1 test) → `test_paths.py::TestTmpProjectFixtureContract`; AC5 `test_cycle11_ingest_coerce.py` (4-of-11 unique tests; 7 _coerce_str_field bare-function duplicates of AC2's parametrized fold dropped per cycle-17 L3 design narrowing — documented in commit + BACKLOG progress note) → `test_ingest.py`; AC6 `test_cycle11_utils_pages.py` (8 tests on page_id/scan_wiki_pages including the cycle-11 AC4 caller-migration runtime probes) → `test_utils.py`; AC7 `test_cycle12_config_project_root.py` (5 tests using `importlib.reload(kb.config)`) → `test_paths.py::TestProjectRootResolution` with autouse `_restore_config_after_test` fixture for cycle-19 L2 / cycle-20 L1 reload-isolation; AC8 `test_cycle12_frontmatter_cache.py` (7 tests; 1 vacuous docstring-introspection test on `gb.__doc__` flagged in BACKLOG per C40-L3) → `test_models.py`; AC9 `test_cycle12_io_sweep.py` (7 tests; 2 vacuous docstring-introspection tests on `load_page_frontmatter.__doc__` + `file_lock`/`atomic_*_write.__doc__` flagged) → `test_utils_io.py`; AC11 `test_cycle13_augment_raw_dir.py` (5-test class) → `test_lint.py::TestRawDirDerivation` preserving class structure (cycle-40 L5 host-shape rule); AC12 `test_cycle13_sweep_wiring.py` (3-test class) → `test_cli.py::TestCliBootSweep` preserving class structure. AC10 (`test_cycle12_sanitize_context.py` → `test_mcp_core.py`) DEFERRED to cycle 44+ due to active cycle-42 Phase 4.6 mcp/* dedup interference. AC13 filed 4 BACKLOG entries: 3 vacuous-test upgrade candidates (with concrete behavior-based replacement plans) + 1 cycle-44 deferral note for AC10. AC14 doc-grep across CLAUDE.md / docs/reference/testing.md / docs/reference/implementation-status.md / README.md per C26-L2 + C39-L3 multi-site rule. Steps 4-5-7-8 ran primary-session+subagent (R1 DeepSeek V4 Pro direct CLI per cycle-39 L1 + R2 Codex agent dispatch in parallel; design-gate Opus subagent resolved 10 questions producing 10 binding CONDITIONS); Steps 6 (Context7) and 9.5 (simplify) skipped per skip-eligibility (no third-party libs, test-only diff). Major operational lesson: parallel cycle-42 session sharing the working tree caused 2 mistaken commits + lost stash mid-cycle; recovered via cherry-pick + git worktree creation at `D:/Projects/llm-wiki-flywheel-c43` for true isolation (skill-patch candidate for Step 16). Test count drift acknowledged: 3014 → 3007 (−7) reflects AC5 dedup, NOT a fold-broke-tests regression — design-amendment per cycle-17 L3 routed through commit+BACKLOG. Zero PR-introduced CVEs (Step 11 baseline-vs-postcheck diff = empty set; same 4 advisories as cycle 41/42 baseline carry over).
- **Latest cycle (41):** Backlog hygiene + freeze-and-fold continuation + C40-L3 docstring-grep upgrade + dep-drift re-verification — 6 ACs across BACKLOG/test/doc surfaces; 1 src/ change (`src/kb/compile/compiler.py` 6-line docstring alignment after discovering it described pre-cycle-4 behaviour). AC1-AC4 folded four more cycle-10/11 era test files per cycle-4 L4 freeze-and-fold rule (test_cycle10_validate_wiki_dir → test_mcp_browse_health.py 8-test cycle-10-AC15 section; test_cycle10_capture → test_capture.py 4 cycle-10 capture regressions including UUID-boundary fence + slow-LLM captured_at + raw_captures docstring invariant; test_cycle10_quality → test_mcp_quality_new.py 2 quality-degradation warning tests preserving the cycle-36 AC5 4-module monkeypatch chain comment; test_cycle11_cli_imports → test_cli.py 6 CLI runner smoke + 2 short-circuit subprocess tests). AC5 closed BACKLOG entry C40-L3 by replacing the vacuous docstring-grep `test_detect_source_drift_docstring_documents_deletion_pruning_persistence` with `test_detect_source_drift_does_not_mutate_manifest_when_sources_deleted` — seeds a manifest with a deleted-source entry, calls `detect_source_drift`, asserts manifest bytes are byte-identical post-call (reverting cycle 4 R1 Codex MAJOR 3 would call `save_manifest` and fail the assertion). Discovered the cycle-10 docstring text was OUTDATED — claimed "deletion-pruning is always persisted even when save_hashes=False" but cycle 4 made the function fully read-only; aligned the docstring to current behaviour as part of the same commit. AC6 re-verified all six cycle-40+ tagged carry-overs against live pip-audit + Dependabot baselines (state matches cycle 40 verbatim — diskcache 5.6.3 / ragas 0.4.3 / litellm 1.83.0 / pip 26.0.1 all still no-upstream-fix; three resolver conflicts persist; two Dependabot drift entries still NOT emitted by pip-audit; litellm 1.83.14 wheel METADATA still pins `Requires-Dist: click==8.1.8`). Steps 1-13 primary-session per C37-L5 (≤15 ACs / 1 src file / primary holds context); Steps 3+4+6+9.5 skipped per skip-eligibility rules (pure hygiene, trivial, no third-party libs, 6-line src delta well under 50 LoC). Zero PR-introduced CVEs (Step 11 baseline-vs-postcheck diff = empty set).
- **Latest cycle (40):** Backlog hygiene + freeze-and-fold continuation + dep-drift re-verification — 11 ACs across BACKLOG/test/doc surfaces; zero `src/kb/` changes. AC1-AC3 folded three more cycle-10/11 era test files per cycle-4 L4 freeze-and-fold rule (test_cycle10_safe_call → test_mcp_browse_health.py::TestSanitizeErrorStrAtMCPBoundary 5-test class; test_cycle10_linker SPLIT to test_compile.py + test_utils_text.py as bare functions; test_cycle11_stale_results → test_query.py::TestFlagStaleResultsEdgeCases 4-test class with cycle-15 AC1 explanatory comment preserved verbatim). AC4-AC10 re-verified seven cycle-39+ tagged carry-overs against live pip-audit + Dependabot baselines (state matches cycle 39); notable cycle-40 finding: pip 26.1 published 2026-04-27 but advisory metadata still shows patched_versions=null + vulnerable_version_range:<=26.0.1, so pip-audit continues to emit empty fix_versions — conservative posture per cycle-22 L4: no pin bump until advisory confirms 26.1 patches the CVE. AC11 marker-appended Phase 4.5 HIGH #4 with cycle-39+40 fold progress (4 files cumulative; tests/ 258 → 255). Steps 1-13 primary-session per C37-L5; Step 5 design gate via Opus subagent (mandatory) → 6 questions resolved with HIGH confidence + 15 binding CONDITIONS satisfied; Steps 3+4+6+9.5 skipped per skip-eligibility rules.
- **Latest cycle (39):** Backlog hygiene + dep-drift re-verification + cycle-38 test fold — 11 ACs across BACKLOG/test/doc surfaces; zero `src/kb/` changes. Re-confirmed seven cycle-39+ tagged carry-over entries (4 no-fix CVEs on diskcache 5.6.3 / ragas 0.4.3 / pip 26.0.1 / litellm 1.83.0 + 3 resolver conflicts from cycle-34 AC52 + 2 Dependabot pip-audit drift entries on litellm `GHSA-r75f-5x8p-qvmc` + `GHSA-v4p8-mg3p-g94g`); pip-audit live env still emits 4 vulns (4 packages, 21089-byte JSON), Dependabot still reports 4 alerts, no upstream relaxation across litellm 1.83.0..1.83.14 (`pip download litellm==1.83.14 --no-deps` zipfile metadata: `Requires-Dist: click==8.1.8` blocks upgrade vs our `click==8.3.2`). Folded `tests/test_cycle38_mock_scan_llm_reload_safe.py` into `tests/test_capture.py::TestMockScanLlmReloadSafety` per cycle-4 L4 freeze-and-fold rule + cycle 38's own self-tagged candidate note: 2 test methods + 4 module-level helpers + 1 autouse `_restore_kb_capture` fixture moved verbatim, defensive `import kb.capture as _kb_capture` line dropped (test_capture.py:20 `from kb.capture import (...)` already loads the module under real PROJECT_ROOT). Re-tagged 3 cycle-39+ items deferred from cycle 39 (windows-latest matrix re-enable, GHA-Windows multiprocessing spawn investigation, TestWriteItemFiles POSIX off-by-one) to cycle-40+ per cycle-23 L3 deferred-promise BACKLOG sync.
- **Latest cycle (38):** POSIX test re-enable: mock_scan_llm dual-site + ruff T20 — closes 2 of 5 cycle-37-deferred candidates (Category A 10-test mock_scan_llm POSIX reload-leak via AC1-AC5 + Category B 2-test atomic_text_write POSIX patch via AC6 strict scope per design Q3). R2 Codex Step-4 BLOCKER refuted the cycle-37 hypothesis: `kb.config` has no `kb.utils.llm` import (verified at `src/kb/config.py:3-9`), so reload(kb.config) cannot cascade. Actual contamination source: `tests/test_capture.py::TestSymlinkGuard::test_symlink_outside_project_root_refuses_import` (lines 700-714) was doing `del sys.modules["kb.capture"]` + reimport in-process; after teardown, sys.modules has NEW kb.capture but test_capture.py's pre-collection bindings still hold OLD module function objects whose `__globals__` is the OLD `__dict__`, so `mock_scan_llm`'s patch on `sys.modules["kb.capture"]` (NEW) doesn't reach the OLD `__dict__` that test functions actually use. AC0 refactors TestSymlinkGuard to subprocess (eliminates contamination source); AC1 widens `mock_scan_llm` to dual-site patch (`kb.utils.llm.call_llm_json` BEFORE `kb.capture.call_llm_json`) as defense-in-depth. AC5 codifies the dual-site contract via order-independent assertion (manually verified non-vacuous: reverting AC1's utils.llm setattr line in conftest's _install fails the test with the contract message). AC6 widens 2 atomic_text_write patches + drops `@_WINDOWS_ONLY`; R2 Codex grep table confirmed strict scope (only test_capture.py:741,754 had cycle-36 risk pattern). Design Q5 amendment: ruff T20 (flake8-print) added to `pyproject.toml [tool.ruff.lint].select` for probe-print defense. AC9 re-confirms Dependabot drift unchanged (litellm GHSA-r75f / GHSA-v4p8 still NOT surfaced by pip-audit live-env). AC7+AC8 (POSIX off-by-one slug + creates_dir) DEFERRED to cycle-39+ per design M1 standing pre-auth — investigation requires deeper POSIX shell access; the `@_WINDOWS_ONLY` skipif on the 2 affected `TestWriteItemFiles` tests stays. ZERO new CI dimensions per cycle-36 L1 (5 cycle-37+38 candidates re-pinned to cycle-39+ in BACKLOG: windows-latest matrix, GHA-Windows spawn investigation, TestWriteItemFiles POSIX off-by-one, fold-into-canonical for the new test file, 2 Dependabot drift entries).
- **Latest cycle (37):** POSIX symlink security fix + requirements split — closes 2 of 7 cycle-37 BACKLOG candidates filed at cycle-36 close. AC1 reorders `is_symlink()` capture in `pair_page_with_sources` to BEFORE `.resolve()` so the existing containment check at `src/kb/review/context.py:86-103` (previously dead code on POSIX because `Path.resolve()` follows symlinks first) actually fires; cycle-36 ubuntu-probe surfaced the real production gap that Windows-only test discovery had hidden. AC2 drops cycle-36 POSIX-only skipif on `test_qb_symlink_outside_raw_rejected`; AC3 adds positive-case `test_qb_symlink_inside_raw_accepted` (intra-`raw/` symlinks where target stays within `raw_dir` are read OK — divergent against AC1 over-restriction). AC4-AC8 split `requirements.txt` into 6 NEW layered files (`requirements-{runtime,hybrid,augment,formats,eval,dev}.txt`) mirroring `pyproject.toml [project.optional-dependencies]`; AC7 amended at design Q3 to keep `requirements.txt` UNCHANGED (frozen 295-line snapshot preserved for reproducibility) — new files are additive. AC6 propagates cycle-35 L8 floor pin `langchain-openai>=1.1.14` to `requirements-eval.txt`. AC9 6-assertion regression test pins file existence + `-r` includes + floor pin + tomllib pyproject cross-check + snapshot preservation. ZERO new CI dimensions per cycle-36 L1 (windows-latest matrix + 4 other cycle-37 BACKLOG candidates remain DEFERRED to cycle 38+). Two-commit sequence (security fix + requirements split) per cycle-36 L1 CI-cost discipline; local-verify-first then push-once.
- **Latest cycle (36):** Test + CI infrastructure hardening — closes 2 of 3 explicit cycle-36 BACKLOG follow-ups (strict pytest CI gate + cross-OS portability matrix; requirements split AC14-AC17 deferred to cycle 37 per Step-5 Q7=B). Three-commit sequence (probe → fix → strict-gate). Probe ubuntu-latest CI surfaced 23 failures across 5 fragility classes; commit 2 applied marker fixes (10 SDK-using tests with `requires_real_api_key()` skipif, 4 Windows-helper tests with anti-POSIX skipif, 2 Windows-only path-semantics tests with anti-POSIX skipif, 5 wiki-content tests fixed via `kb.mcp.quality.WIKI_DIR` mirror-rebind + page seeding, 1 broken test fixture replaced via `tmp_path_factory`, 1 POSIX symlink security test marked anti-POSIX with cycle-37 BACKLOG for the production fix); commit 3 added matrix + dropped continue-on-error. NEW `tests/_helpers/api_key.py::requires_real_api_key()` predicate (gates on `sk-ant-dummy-key-` prefix per AC6); `pytest-timeout >= 2.3` in `[dev]` extras + `[tool.pytest.ini_options] timeout = 120` (catches future hangs at 2-min boundary instead of GHA's 6-hour silent kill). 22 design-gate questions resolved at Step 5 (R1 Opus eval + R2 fallback eval after Codex hang). 6 cycle-37 BACKLOG entries filed: requirements split (deferred), GHA-Windows multiprocessing spawn investigation, mock_scan_llm POSIX reload-leak, POSIX symlink security gap in `pair_page_with_sources`, TestExclusiveAtomicWrite/TestWriteItemFiles POSIX behaviour, Dependabot pip-audit drift on 2 litellm GHSAs.
- **Latest cycle (35):** Pre-Phase-5 BACKLOG batch (M11/M12/M13/M14/M15/M21 closures + cycle-34 AC4e completion). Sanitize regex extended with TWO new alternatives — slash-form Windows UNC long-path `(?://\?/UNC/...)` (T1b) AND URI-guarded ordinary slash UNC `(?<!:)(?://...)` (T1) — plus `(?<![A-Za-z])` lookbehind on the drive-letter alternative to prevent URL overmatch (`https://host/path` no longer collapses to `<path>` via the `s://` collision); `sanitize_error_text` per-path substitution now probes both single-backslash AND OSError-doubled-backslash forms, closing the cycle-33 xfail-strict gap directly. `_update_sources_mapping` and `_update_index_batch` RMW windows wrapped in `file_lock(target_path)` (cycle-19 discipline; NO wrapper-level lock in `_write_index_files` because `file_lock` is `os.O_EXCL` non-reentrant per Step-5 Q7); empty-`wiki_pages` early-return at function entry kills the malformed `→ \n` line + suppresses the `_sources.md not found` warning (T8); membership + per-line scan switched to `escaped_ref` so backtick-bearing source_refs dedup correctly. New shared `_validate_filename_slug(filename) -> tuple[str, str | None]` in `mcp/core.py` (looser sibling of `_validate_save_as_slug` — allows `My Document.md`-style free-form names but rejects NUL / path separators / `..` / trailing dot or space (Windows trim aliasing) / non-ASCII (`[^\x00-\x7F]` blocks homoglyph + RTL-override + zero-width) / over-200-char / Windows-reserved); wired into `_validate_file_inputs` for `kb_ingest_content` + `kb_save_source` parity with `kb_query.save_as`. Architecture diagrams bumped v0.10.0 → v0.11.0 with Playwright PNG re-render (closes deferred cycle-34 AC4e); canonical Playwright snippet codified in `docs/reference/conventions.md` to prevent a third deferral. Step-11b GitPython 3.1.46 → 3.1.47 closes Dependabot GHSA-x2qx-6953-8485 + GHSA-rpm5-65cw-6hj4 (zero `import git` in `src/kb`; transitive tooling dep only). Three pre-existing doc-anchor tests re-anchored to `docs/reference/*.md` after the cycle-34-followup CLAUDE.md split (commit 518db0e).
- **Latest cycle (34):** Release hygiene — packaging metadata (pyproject `readme = "README.md"` + 5 optional-dependency extras `hybrid`/`augment`/`formats`/`eval`/`dev` + `jsonschema` runtime dep + version 0.10.0 → 0.11.0 across `pyproject.toml` + `src/kb/__init__.py`); new `SECURITY.md` documenting four narrow-role CVE acceptance entries (diskcache CVE-2025-69872, litellm GHSA-xqmj-j6mv-4862, pip CVE-2026-3219, ragas CVE-2026-6587); new `.github/workflows/ci.yml` bootstrap (ruff + pytest + pip check + pip-audit + build/twine; `permissions: read-all`, `concurrency: cancel-in-progress`, narrowed `on:` trigger, `actions/{checkout,setup-python}@v6`); README content drift fixes (replaced "No vectors. No chunking." tagline; PDF row removed from supported formats; tests-2850 badge → tests-passing; v0.10.0 → v0.11.0 badge); `.pdf` removed from `SUPPORTED_SOURCE_EXTENSIONS` with clearer ingest-pipeline rejection message; 6 untracked scratch/superseded files deleted (findings.md / progress.md / task_plan.md / claude4.6.md / docs/repo_review.md / docs/repo_review.html); comprehensive review committed at `docs/reviews/2026-04-25-comprehensive-repo-review.{md,html}`. AC49 (boot-lean fix in `kb.lint.augment`) DROPPED at Step 9 after primary-session probe confirmed `kb.cli` already does NOT pull in `kb.lint.fetcher`/`httpx`/`trafilatura` at module-load time; AC56 boot-lean regression test retained as forward protection (cycle-15 L2 DROP-with-test-anchor pattern). AC4e (architecture-diagram version bump + Playwright PNG re-render) DEFERRED to cycle 35 with BACKLOG entry per design-doc fallback (NEW-Q15 option B; practical scope discipline at 57 ACs); shipped in cycle 35.
- **Latest cycle (33):** MCP `Error[partial]:` path-leak redaction at three sites (`kb_ingest_content` + `kb_save_source` post-create-OSError emitters at `mcp/core.py:762,881`; `kb_query.save_as` peer at `core.py:280-281`) — closes BACKLOG mcp/core.py path-leak MEDIUM (cycle-32 threat T11) by pre-computing `sanitized_err = _sanitize_error_str(write_err, file_path)` ONCE per OSError block and reusing for paired log + return so symmetry cannot drift. AC4 fixes the previously-asymmetric `kb_query.save_as` log/return depth. AC6-AC8 add `## Idempotency` docstrings to `_update_sources_mapping` + `_update_index_batch` and pin the serial dedup-on-recall + merge-on-new-pages contracts via `atomic_text_write` spy + `call_count` regressions; AC10 narrows the `ingest/pipeline.py` BACKLOG entry from "duplicate-on-reingest" (closed) to "RMW-concurrency residual" (still open). Spawn cost: NEW BACKLOG MEDIUM for `sanitize.py` ordinary-UNC slash-normalize gap with `pytest.mark.xfail(strict=True)` test marker (Q8 cycle-16 L3 REPL-probed).
- **Latest cycle (32):** CLI ↔ MCP parity category (b) closure + `_is_mcp_error_response` widening + `file_lock` fair-queue stagger mitigation — 2 new CLI subcommands (`compile-scan`, `ingest-content`), `_is_mcp_error_response` tuple widened with `"Error["` prefix (closes silent-exit-0 bug on `kb_ingest_content` `Error[partial]:` post-create-OSError emitter at `mcp/core.py:762`), and `file_lock` gained a `_LOCK_WAITERS` counter + first-sleep position-based stagger (intra-process mitigation only, not cross-process guarantee; `_FAIR_QUEUE_STAGGER_MS=2.0` clamped to `LOCK_POLL_INTERVAL=50ms`).
- **Per-AC rationale, question logs, R1/R2 fix trails** → `CHANGELOG-history.md` + `docs/superpowers/decisions/<date>-cycle<N>-*.md`.
