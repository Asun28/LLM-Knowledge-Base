# Implementation Status

> **Part of [CLAUDE.md](../../CLAUDE.md)** — detail for the "Implementation Status" section. Shipped phases and per-cycle tallies → `CHANGELOG.md` (compact index, newest first) + `CHANGELOG-history.md` (per-cycle bullet archive). Open work + deferred roadmap → `BACKLOG.md`.

- **Latest full-suite:** 3005 tests / 257 files · 2985 passed + 20 skipped on Windows local (cycle 36 added +10 cycle-36 hardening tests + 10 `requires_real_api_key` skipif + 4 anti-POSIX skipif + cycle-23 multiprocessing CI=true skipif; ubuntu-latest CI strict-gated since cycle 36; windows-latest CI matrix re-enable deferred to cycle-37 per commit-4 pivot — second hang at threading.py:355 surfaced in commit-3 strict-gate run).
- **Latest cycle (36):** Test + CI infrastructure hardening — closes 2 of 3 explicit cycle-36 BACKLOG follow-ups (strict pytest CI gate + cross-OS portability matrix; requirements split AC14-AC17 deferred to cycle 37 per Step-5 Q7=B). Three-commit sequence (probe → fix → strict-gate). Probe ubuntu-latest CI surfaced 23 failures across 5 fragility classes; commit 2 applied marker fixes (10 SDK-using tests with `requires_real_api_key()` skipif, 4 Windows-helper tests with anti-POSIX skipif, 2 Windows-only path-semantics tests with anti-POSIX skipif, 5 wiki-content tests fixed via `kb.mcp.quality.WIKI_DIR` mirror-rebind + page seeding, 1 broken test fixture replaced via `tmp_path_factory`, 1 POSIX symlink security test marked anti-POSIX with cycle-37 BACKLOG for the production fix); commit 3 added matrix + dropped continue-on-error. NEW `tests/_helpers/api_key.py::requires_real_api_key()` predicate (gates on `sk-ant-dummy-key-` prefix per AC6); `pytest-timeout >= 2.3` in `[dev]` extras + `[tool.pytest.ini_options] timeout = 120` (catches future hangs at 2-min boundary instead of GHA's 6-hour silent kill). 22 design-gate questions resolved at Step 5 (R1 Opus eval + R2 fallback eval after Codex hang). 6 cycle-37 BACKLOG entries filed: requirements split (deferred), GHA-Windows multiprocessing spawn investigation, mock_scan_llm POSIX reload-leak, POSIX symlink security gap in `pair_page_with_sources`, TestExclusiveAtomicWrite/TestWriteItemFiles POSIX behaviour, Dependabot pip-audit drift on 2 litellm GHSAs.
- **Latest cycle (35):** Pre-Phase-5 BACKLOG batch (M11/M12/M13/M14/M15/M21 closures + cycle-34 AC4e completion). Sanitize regex extended with TWO new alternatives — slash-form Windows UNC long-path `(?://\?/UNC/...)` (T1b) AND URI-guarded ordinary slash UNC `(?<!:)(?://...)` (T1) — plus `(?<![A-Za-z])` lookbehind on the drive-letter alternative to prevent URL overmatch (`https://host/path` no longer collapses to `<path>` via the `s://` collision); `sanitize_error_text` per-path substitution now probes both single-backslash AND OSError-doubled-backslash forms, closing the cycle-33 xfail-strict gap directly. `_update_sources_mapping` and `_update_index_batch` RMW windows wrapped in `file_lock(target_path)` (cycle-19 discipline; NO wrapper-level lock in `_write_index_files` because `file_lock` is `os.O_EXCL` non-reentrant per Step-5 Q7); empty-`wiki_pages` early-return at function entry kills the malformed `→ \n` line + suppresses the `_sources.md not found` warning (T8); membership + per-line scan switched to `escaped_ref` so backtick-bearing source_refs dedup correctly. New shared `_validate_filename_slug(filename) -> tuple[str, str | None]` in `mcp/core.py` (looser sibling of `_validate_save_as_slug` — allows `My Document.md`-style free-form names but rejects NUL / path separators / `..` / trailing dot or space (Windows trim aliasing) / non-ASCII (`[^\x00-\x7F]` blocks homoglyph + RTL-override + zero-width) / over-200-char / Windows-reserved); wired into `_validate_file_inputs` for `kb_ingest_content` + `kb_save_source` parity with `kb_query.save_as`. Architecture diagrams bumped v0.10.0 → v0.11.0 with Playwright PNG re-render (closes deferred cycle-34 AC4e); canonical Playwright snippet codified in `docs/reference/conventions.md` to prevent a third deferral. Step-11b GitPython 3.1.46 → 3.1.47 closes Dependabot GHSA-x2qx-6953-8485 + GHSA-rpm5-65cw-6hj4 (zero `import git` in `src/kb`; transitive tooling dep only). Three pre-existing doc-anchor tests re-anchored to `docs/reference/*.md` after the cycle-34-followup CLAUDE.md split (commit 518db0e).
- **Latest cycle (34):** Release hygiene — packaging metadata (pyproject `readme = "README.md"` + 5 optional-dependency extras `hybrid`/`augment`/`formats`/`eval`/`dev` + `jsonschema` runtime dep + version 0.10.0 → 0.11.0 across `pyproject.toml` + `src/kb/__init__.py`); new `SECURITY.md` documenting four narrow-role CVE acceptance entries (diskcache CVE-2025-69872, litellm GHSA-xqmj-j6mv-4862, pip CVE-2026-3219, ragas CVE-2026-6587); new `.github/workflows/ci.yml` bootstrap (ruff + pytest + pip check + pip-audit + build/twine; `permissions: read-all`, `concurrency: cancel-in-progress`, narrowed `on:` trigger, `actions/{checkout,setup-python}@v6`); README content drift fixes (replaced "No vectors. No chunking." tagline; PDF row removed from supported formats; tests-2850 badge → tests-passing; v0.10.0 → v0.11.0 badge); `.pdf` removed from `SUPPORTED_SOURCE_EXTENSIONS` with clearer ingest-pipeline rejection message; 6 untracked scratch/superseded files deleted (findings.md / progress.md / task_plan.md / claude4.6.md / docs/repo_review.md / docs/repo_review.html); comprehensive review committed at `docs/reviews/2026-04-25-comprehensive-repo-review.{md,html}`. AC49 (boot-lean fix in `kb.lint.augment`) DROPPED at Step 9 after primary-session probe confirmed `kb.cli` already does NOT pull in `kb.lint.fetcher`/`httpx`/`trafilatura` at module-load time; AC56 boot-lean regression test retained as forward protection (cycle-15 L2 DROP-with-test-anchor pattern). AC4e (architecture-diagram version bump + Playwright PNG re-render) DEFERRED to cycle 35 with BACKLOG entry per design-doc fallback (NEW-Q15 option B; practical scope discipline at 57 ACs); shipped in cycle 35.
- **Latest cycle (33):** MCP `Error[partial]:` path-leak redaction at three sites (`kb_ingest_content` + `kb_save_source` post-create-OSError emitters at `mcp/core.py:762,881`; `kb_query.save_as` peer at `core.py:280-281`) — closes BACKLOG mcp/core.py path-leak MEDIUM (cycle-32 threat T11) by pre-computing `sanitized_err = _sanitize_error_str(write_err, file_path)` ONCE per OSError block and reusing for paired log + return so symmetry cannot drift. AC4 fixes the previously-asymmetric `kb_query.save_as` log/return depth. AC6-AC8 add `## Idempotency` docstrings to `_update_sources_mapping` + `_update_index_batch` and pin the serial dedup-on-recall + merge-on-new-pages contracts via `atomic_text_write` spy + `call_count` regressions; AC10 narrows the `ingest/pipeline.py` BACKLOG entry from "duplicate-on-reingest" (closed) to "RMW-concurrency residual" (still open). Spawn cost: NEW BACKLOG MEDIUM for `sanitize.py` ordinary-UNC slash-normalize gap with `pytest.mark.xfail(strict=True)` test marker (Q8 cycle-16 L3 REPL-probed).
- **Latest cycle (32):** CLI ↔ MCP parity category (b) closure + `_is_mcp_error_response` widening + `file_lock` fair-queue stagger mitigation — 2 new CLI subcommands (`compile-scan`, `ingest-content`), `_is_mcp_error_response` tuple widened with `"Error["` prefix (closes silent-exit-0 bug on `kb_ingest_content` `Error[partial]:` post-create-OSError emitter at `mcp/core.py:762`), and `file_lock` gained a `_LOCK_WAITERS` counter + first-sleep position-based stagger (intra-process mitigation only, not cross-process guarantee; `_FAIR_QUEUE_STAGGER_MS=2.0` clamped to `LOCK_POLL_INTERVAL=50ms`).
- **Per-AC rationale, question logs, R1/R2 fix trails** → `CHANGELOG-history.md` + `docs/superpowers/decisions/<date>-cycle<N>-*.md`.
